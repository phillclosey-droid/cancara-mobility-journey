/* @ds-bundle: {"format":4,"namespace":"CancaraDesignSystem_4b0109","components":[{"name":"ActionButton","sourcePath":"src/components/action/Button/ActionButton/ActionButton.jsx"},{"name":"CompactButton","sourcePath":"src/components/action/Button/CompactButton/CompactButton.jsx"},{"name":"IconButton","sourcePath":"src/components/action/Button/IconButton/IconButton.jsx"},{"name":"LinkButton","sourcePath":"src/components/action/Button/LinkButton/LinkButton.jsx"},{"name":"ActionButtonStack","sourcePath":"src/components/action/Button_Stack/ActionButtonStack/ActionButtonStack.jsx"},{"name":"ChatButtonStack","sourcePath":"src/components/action/Button_Stack/ChatButtonStack/ChatButtonStack.jsx"},{"name":"LinkButtonStack","sourcePath":"src/components/action/Button_Stack/LinkButtonStack/LinkButtonStack.jsx"},{"name":"GhostPanel","sourcePath":"src/components/action/Panel/GhostPanel/GhostPanel.jsx"},{"name":"Panel","sourcePath":"src/components/action/Panel/Panel/Panel.jsx"},{"name":"QuickActionButton","sourcePath":"src/components/action/Quick_Action_Button/QuickActionButton/QuickActionButton.jsx"},{"name":"Tile","sourcePath":"src/components/action/Tile/Tile/Tile.jsx"},{"name":"TileGrid","sourcePath":"src/components/action/Tile/TileGrid/TileGrid.jsx"},{"name":"Accordion","sourcePath":"src/components/content/Accordion/Accordion/Accordion.jsx"},{"name":"SelectCategoryGrid","sourcePath":"src/components/content/Category_Selection/SelectCategoryGrid/SelectCategoryGrid.jsx"},{"name":"SelectCategoryItem","sourcePath":"src/components/content/Category_Selection/SelectCategoryItem/SelectCategoryItem.jsx"},{"name":"Divider","sourcePath":"src/components/content/Divider/Divider/Divider.jsx"},{"name":"Icon","sourcePath":"src/components/content/Icon/Icon/Icon.jsx"},{"name":"Pictogram","sourcePath":"src/components/content/Icon/Pictogram/Pictogram.jsx"},{"name":"SpotIllustration","sourcePath":"src/components/content/Icon/SpotIllustration/SpotIllustration.jsx"},{"name":"ListActionGroup","sourcePath":"src/components/content/List/ListActionGroup/ListActionGroup.jsx"},{"name":"ListDataPlaybackGroup","sourcePath":"src/components/content/List/ListDataPlaybackGroup/ListDataPlaybackGroup.jsx"},{"name":"ListItemAction","sourcePath":"src/components/content/List/ListItemAction/ListItemAction.jsx"},{"name":"ListItemDataPlayback","sourcePath":"src/components/content/List/ListItemDataPlayback/ListItemDataPlayback.jsx"},{"name":"Calendar","sourcePath":"src/components/forms/Calendar/Calendar/Calendar.jsx"},{"name":"CalendarDate","sourcePath":"src/components/forms/Calendar/CalendarDate/CalendarDate.jsx"},{"name":"Checkbox","sourcePath":"src/components/forms/Checkbox/Checkbox/Checkbox.jsx"},{"name":"CheckboxGroup","sourcePath":"src/components/forms/Checkbox/CheckboxGroup/CheckboxGroup.jsx"},{"name":"ConfirmationBox","sourcePath":"src/components/forms/Checkbox/ConfirmationBox/ConfirmationBox.jsx"},{"name":"RadioButton","sourcePath":"src/components/forms/RadioButton/RadioButton/RadioButton.jsx"},{"name":"RadioButtonGroup","sourcePath":"src/components/forms/RadioButton/RadioButtonGroup/RadioButtonGroup.jsx"},{"name":"Switch","sourcePath":"src/components/forms/Switch/Switch/Switch.jsx"},{"name":"SwitchConfiguration","sourcePath":"src/components/forms/Switch/SwitchConfiguration/SwitchConfiguration.jsx"},{"name":"CalendarPicker","sourcePath":"src/components/forms/Text_Field/CalendarPicker/CalendarPicker.jsx"},{"name":"CountryPicker","sourcePath":"src/components/forms/Text_Field/CountryPicker/CountryPicker.jsx"},{"name":"DropdownPicker","sourcePath":"src/components/forms/Text_Field/DropdownPicker/DropdownPicker.jsx"},{"name":"GlobalSearch","sourcePath":"src/components/forms/Text_Field/GlobalSearch/GlobalSearch.jsx"},{"name":"MIField","sourcePath":"src/components/forms/Text_Field/MI_Field/MIField.jsx"},{"name":"Password","sourcePath":"src/components/forms/Text_Field/Password/Password.jsx"},{"name":"PhoneNumber","sourcePath":"src/components/forms/Text_Field/PhoneNumber/PhoneNumber.jsx"},{"name":"PhoneNumberExtension","sourcePath":"src/components/forms/Text_Field/PhoneNumberExtension/PhoneNumberExtension.jsx"},{"name":"PhoneNumberField","sourcePath":"src/components/forms/Text_Field/PhoneNumberField/PhoneNumberField.jsx"},{"name":"SortCode","sourcePath":"src/components/forms/Text_Field/SortCode/SortCode.jsx"},{"name":"TextField","sourcePath":"src/components/forms/Text_Field/TextField/TextField.jsx"},{"name":"BottomNav","sourcePath":"src/components/navigation/Bottom_Nav/BottomNav/BottomNav.jsx"},{"name":"CustomHeader","sourcePath":"src/components/navigation/Header/CustomHeader/CustomHeader.jsx"},{"name":"ModalStackHeader","sourcePath":"src/components/navigation/Header/ModalStackHeader/ModalStackHeader.jsx"},{"name":"PresetHeader","sourcePath":"src/components/navigation/Header/PresetHeader/PresetHeader.jsx"},{"name":"Pagination","sourcePath":"src/components/navigation/Pagination/Pagination/Pagination.jsx"},{"name":"PaginationDots","sourcePath":"src/components/navigation/Pagination/PaginationDots/PaginationDots.jsx"},{"name":"TabCollectionScroll","sourcePath":"src/components/navigation/Tabs/TabCollectionScroll/TabCollectionScroll.jsx"},{"name":"ThreeTabsFixed","sourcePath":"src/components/navigation/Tabs/ThreeTabsFixed/ThreeTabsFixed.jsx"},{"name":"TwoTabsFixed","sourcePath":"src/components/navigation/Tabs/TwoTabsFixed/TwoTabsFixed.jsx"},{"name":"Toggle","sourcePath":"src/components/navigation/Toggle/Toggle/Toggle.jsx"},{"name":"Coachmark","sourcePath":"src/components/notification/Coachmark/Coachmark/Coachmark.jsx"},{"name":"CoachmarkPopup","sourcePath":"src/components/notification/Coachmark/CoachmarkPopup/CoachmarkPopup.jsx"},{"name":"ErrorBanner","sourcePath":"src/components/notification/Error_Banner/ErrorBanner/ErrorBanner.jsx"},{"name":"NotificationBadge","sourcePath":"src/components/notification/Notification_Badge/NotificationBadge/NotificationBadge.jsx"},{"name":"NotificationDot","sourcePath":"src/components/notification/Notification_Badge/NotificationDot/NotificationDot.jsx"},{"name":"NotificationHint","sourcePath":"src/components/notification/Notification_Hint/NotificationHint/NotificationHint.jsx"},{"name":"NotificationPanel","sourcePath":"src/components/notification/Notification_Panel/NotificationPanel/NotificationPanel.jsx"},{"name":"NotificationTag","sourcePath":"src/components/notification/Notification_Tag/NotificationTag/NotificationTag.jsx"},{"name":"CustomPopupModal","sourcePath":"src/components/notification/Pop-up_Modal/CustomPopupModal/CustomPopupModal.jsx"},{"name":"PopupModalPresets","sourcePath":"src/components/notification/Pop-up_Modal/PopupModalPresets/PopupModalPresets.jsx"},{"name":"Handle","sourcePath":"src/components/os/Home_indicator/Handle/Handle.jsx"},{"name":"HomeIndicator","sourcePath":"src/components/os/Home_indicator/HomeIndicator/HomeIndicator.jsx"},{"name":"NativeSpinnerConfiguration","sourcePath":"src/components/os/Spinner/NativeSpinnerConfiguration/NativeSpinnerConfiguration.jsx"},{"name":"Spinner","sourcePath":"src/components/os/Spinner/Spinner/Spinner.jsx"},{"name":"SpinnerSet","sourcePath":"src/components/os/Spinner/SpinnerSet/SpinnerSet.jsx"},{"name":"StatusBar","sourcePath":"src/components/os/Status_Bar/StatusBar/StatusBar.jsx"},{"name":"ActionMenuTemplate","sourcePath":"src/components/templates/ActionMenuTemplate/ActionMenuTemplate.jsx"},{"name":"BaseTemplate","sourcePath":"src/components/templates/BaseTemplate/BaseTemplate.jsx"},{"name":"ModalStackTemplate","sourcePath":"src/components/templates/ModalStackTemplate/ModalStackTemplate.jsx"},{"name":"TrayTemplate","sourcePath":"src/components/templates/TrayTemplate/TrayTemplate.jsx"}],"sourceHashes":{"assets/card-theme-toggle.js":"9252644bbdf3","src/components/action/Button/ActionButton/ActionButton.jsx":"4acd29468f6a","src/components/action/Button/CompactButton/CompactButton.jsx":"cac4df827978","src/components/action/Button/IconButton/IconButton.jsx":"9eaae32f73f7","src/components/action/Button/LinkButton/LinkButton.jsx":"b3b17c4efeb3","src/components/action/Button_Stack/ActionButtonStack/ActionButtonStack.jsx":"a095504ae993","src/components/action/Button_Stack/ChatButtonStack/ChatButtonStack.jsx":"39a6f6dc311b","src/components/action/Button_Stack/LinkButtonStack/LinkButtonStack.jsx":"cc5375153978","src/components/action/Panel/GhostPanel/GhostPanel.jsx":"5549db818f00","src/components/action/Panel/Panel/Panel.jsx":"1342f847c9e5","src/components/action/Quick_Action_Button/QuickActionButton/QuickActionButton.jsx":"c2babd9ea72d","src/components/action/Tile/Tile/Tile.jsx":"fbe7e123796a","src/components/action/Tile/TileGrid/TileGrid.jsx":"756f170cd3d4","src/components/content/Accordion/Accordion/Accordion.jsx":"683fa3752c27","src/components/content/Category_Selection/SelectCategoryGrid/SelectCategoryGrid.jsx":"c4f0e573c725","src/components/content/Category_Selection/SelectCategoryItem/SelectCategoryItem.jsx":"9df0fd322fc8","src/components/content/Divider/Divider/Divider.jsx":"f1d5fab86190","src/components/content/Icon/Icon/Icon.jsx":"75fa1d2f4f27","src/components/content/Icon/Pictogram/Pictogram.jsx":"3d18c639d4e1","src/components/content/Icon/SpotIllustration/SpotIllustration.jsx":"c243d3efdc29","src/components/content/List/ListActionGroup/ListActionGroup.jsx":"3779d108338e","src/components/content/List/ListDataPlaybackGroup/ListDataPlaybackGroup.jsx":"9241e958f87f","src/components/content/List/ListItemAction/ListItemAction.jsx":"99ecb3197182","src/components/content/List/ListItemDataPlayback/ListItemDataPlayback.jsx":"9a5c0aa37cd9","src/components/forms/Calendar/Calendar/Calendar.jsx":"e021fabe6b28","src/components/forms/Calendar/CalendarDate/CalendarDate.jsx":"0df40baac849","src/components/forms/Checkbox/Checkbox/Checkbox.jsx":"f6b7d51447b7","src/components/forms/Checkbox/CheckboxGroup/CheckboxGroup.jsx":"16cf528c0b66","src/components/forms/Checkbox/ConfirmationBox/ConfirmationBox.jsx":"c658a8f30fd0","src/components/forms/RadioButton/RadioButton/RadioButton.jsx":"49a7860d5512","src/components/forms/RadioButton/RadioButtonGroup/RadioButtonGroup.jsx":"195c5b56397b","src/components/forms/Switch/Switch/Switch.jsx":"518649abe704","src/components/forms/Switch/SwitchConfiguration/SwitchConfiguration.jsx":"66623ed3da44","src/components/forms/Text_Field/CalendarPicker/CalendarPicker.jsx":"6791e5075b78","src/components/forms/Text_Field/CountryPicker/CountryPicker.jsx":"f43734eb2e9e","src/components/forms/Text_Field/DropdownPicker/DropdownPicker.jsx":"e5e4fd370913","src/components/forms/Text_Field/GlobalSearch/GlobalSearch.jsx":"a337e2c7969a","src/components/forms/Text_Field/MI_Field/MIField.jsx":"65dedec1798e","src/components/forms/Text_Field/Password/Password.jsx":"d26a18f2cb22","src/components/forms/Text_Field/PhoneNumber/PhoneNumber.jsx":"03cdc1dce944","src/components/forms/Text_Field/PhoneNumberExtension/PhoneNumberExtension.jsx":"2f2dc4607abf","src/components/forms/Text_Field/PhoneNumberField/PhoneNumberField.jsx":"df720e0ee70e","src/components/forms/Text_Field/SortCode/SortCode.jsx":"b546708eaf65","src/components/forms/Text_Field/TextField/TextField.jsx":"dc2e683db668","src/components/navigation/Bottom_Nav/BottomNav/BottomNav.jsx":"66d720413aeb","src/components/navigation/Header/CustomHeader/CustomHeader.jsx":"92845c28a41c","src/components/navigation/Header/ModalStackHeader/ModalStackHeader.jsx":"68a2dc94cb1e","src/components/navigation/Header/PresetHeader/PresetHeader.jsx":"28cdd9c158df","src/components/navigation/Pagination/Pagination/Pagination.jsx":"b3f4e568eb8c","src/components/navigation/Pagination/PaginationDots/PaginationDots.jsx":"81d5a684743c","src/components/navigation/Tabs/TabCollectionScroll/TabCollectionScroll.jsx":"8f85703ee43c","src/components/navigation/Tabs/ThreeTabsFixed/ThreeTabsFixed.jsx":"1d75a3bc9b84","src/components/navigation/Tabs/TwoTabsFixed/TwoTabsFixed.jsx":"0711caabf8f4","src/components/navigation/Toggle/Toggle/Toggle.jsx":"ce139084f703","src/components/notification/Coachmark/Coachmark/Coachmark.jsx":"fd0de1612437","src/components/notification/Coachmark/CoachmarkPopup/CoachmarkPopup.jsx":"15b10355c523","src/components/notification/Error_Banner/ErrorBanner/ErrorBanner.jsx":"c58b23c1da3a","src/components/notification/Notification_Badge/NotificationBadge/NotificationBadge.jsx":"f4412997ec96","src/components/notification/Notification_Badge/NotificationDot/NotificationDot.jsx":"16932c7708f2","src/components/notification/Notification_Hint/NotificationHint/NotificationHint.jsx":"eec901327df0","src/components/notification/Notification_Panel/NotificationPanel/NotificationPanel.jsx":"83bc273097da","src/components/notification/Notification_Tag/NotificationTag/NotificationTag.jsx":"c96d67844425","src/components/notification/Pop-up_Modal/CustomPopupModal/CustomPopupModal.jsx":"153766328615","src/components/notification/Pop-up_Modal/PopupModalPresets/PopupModalPresets.jsx":"f0da12268e50","src/components/os/Home_indicator/Handle/Handle.jsx":"668f5ba936ff","src/components/os/Home_indicator/HomeIndicator/HomeIndicator.jsx":"fe1174e4fb33","src/components/os/Spinner/NativeSpinnerConfiguration/NativeSpinnerConfiguration.jsx":"ddde6ce7b891","src/components/os/Spinner/Spinner/Spinner.jsx":"b14779b893ac","src/components/os/Spinner/SpinnerSet/SpinnerSet.jsx":"3239b27fe1fd","src/components/os/Status_Bar/StatusBar/StatusBar.jsx":"653e68680fa7","src/components/templates/ActionMenuTemplate/ActionMenuTemplate.jsx":"f48d31c15f8b","src/components/templates/BaseTemplate/BaseTemplate.jsx":"1f3cdb1ce404","src/components/templates/ModalStackTemplate/ModalStackTemplate.jsx":"d3a95c51e565","src/components/templates/TrayTemplate/TrayTemplate.jsx":"5c15d43298cb"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.CancaraDesignSystem_4b0109 = window.CancaraDesignSystem_4b0109 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// assets/card-theme-toggle.js
try { (() => {
/* ============================================================
   Cancara — specimen-card theme toggle
   Adds a small Light/Dark button to a design-system card so you
   can preview the dark-mode mapping. Presentation aid for the
   cards only — not part of any component.
   ============================================================ */
(function () {
  function make() {
    var root = document.documentElement;
    if (!root.getAttribute("data-theme")) root.setAttribute("data-theme", "light");
    var btn = document.createElement("button");
    btn.type = "button";
    btn.setAttribute("aria-label", "Toggle light or dark mode");
    Object.assign(btn.style, {
      position: "fixed",
      top: "var(--spacing-size12)",
      right: "var(--spacing-size12)",
      zIndex: "9999",
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--spacing-size08)",
      fontFamily: "var(--type-style10-family)",
      fontSize: "var(--type-style10-size)",
      fontWeight: "var(--type-style10-weight)",
      lineHeight: "1",
      padding: "var(--spacing-size08) var(--spacing-size12)",
      borderRadius: "var(--radius-full)",
      border: "var(--border-width-01) solid var(--border-generic-default)",
      background: "var(--background-panel-default)",
      color: "var(--text-generic-default)",
      cursor: "pointer"
    });
    function refreshTokens() {
      // Re-read any token-value captions (cards stamp them with [data-of]).
      var cs = getComputedStyle(document.documentElement);
      document.querySelectorAll("[data-of]").forEach(function (el) {
        el.textContent = cs.getPropertyValue(el.getAttribute("data-of")).trim().toUpperCase();
      });
    }
    function paint() {
      var dark = root.getAttribute("data-theme") === "dark";
      btn.textContent = dark ? "☀  Light" : "☾  Dark";
    }
    btn.addEventListener("click", function () {
      var dark = root.getAttribute("data-theme") === "dark";
      root.setAttribute("data-theme", dark ? "light" : "dark");
      paint();
      refreshTokens();
    });
    paint();
    document.body.appendChild(btn);
  }
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", make);
  } else {
    make();
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "assets/card-theme-toggle.js", error: String((e && e.message) || e) }); }

// src/components/action/Button/LinkButton/LinkButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Link Button
 * The full-width "popup / action-sheet" button used inside a Link Button Stack. Matches the
 * Figma "Link Button" instance (mainComponentName "State=…") exactly:
 *   State: Default | Pressed (:active) | Disabled
 *
 * Geometry (token-driven):
 *   Full-width, min-height --os-hittarget-default (48), label centred, padding 0 --spacing-size16,
 *   square corners (the parent stack clips the rounded popup corners).
 *   Default  → bg --c-temp-popup-button-background,        text --c-temp-popup-button-textdefault
 *   Pressed  → bg --c-temp-popup-button-backgroundpressed, text --c-temp-popup-button-textpressed
 *   Disabled → bg --c-temp-popup-button-backgrounddisabled,text --c-temp-popup-button-textdisabled
 *   Label    → --type-style7-* (15px Bold, line 20) — the Figma "Link" TEXT node, verbatim from
 *              the _Link Button extraction (textStyleName "Style 7"; scaling variant excluded
 *              per the build rules, base Style 7 used).
 */

const STYLE_ID = "cnc-link-button-styles";
const CSS = `
.cnc-linkbtn {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: var(--os-hittarget-default);
  padding: 0 var(--spacing-size16);
  border: none;
  border-radius: var(--radius-none);
  background: var(--c-temp-popup-button-background);
  color: var(--c-temp-popup-button-textdefault);
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  text-align: center;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
}
.cnc-linkbtn:active:not(:disabled), .cnc-linkbtn--pressed {
  background: var(--c-temp-popup-button-backgroundpressed);
  color: var(--c-temp-popup-button-textpressed);
}
.cnc-linkbtn:disabled {
  background: var(--c-temp-popup-button-backgrounddisabled);
  color: var(--c-temp-popup-button-textdisabled);
  cursor: not-allowed;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function LinkButton({
  state = "default",
  disabled = false,
  className = "",
  children = "Link",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-linkbtn"];
  if (state === "pressed") cls.push("cnc-linkbtn--pressed");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    disabled: disabled
  }, rest), children);
}
Object.assign(__ds_scope, { LinkButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button/LinkButton/LinkButton.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Divider/Divider/Divider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Divider
 * A horizontal rule with vertical breathing room. Matches the Figma "Divider" set exactly:
 *   Width: Default (16px side inset) | Full width (edge to edge)
 *   Type : Default (solid) | Dash (dashed)
 *
 * Geometry (token-driven):
 *   Vertical padding --spacing-size24 (Figma 24 top/bottom); Default width insets the rule by
 *   --spacing-size16, Full width spans edge to edge. Rule = a --border-width-01 line in
 *   --border-generic-defaultalt01. Dash renders the EXACT Figma strokeDashes [2,2] @ 1px via a
 *   repeating-linear-gradient (2px dash / 2px gap) — CSS border-style:dashed does not honour the
 *   2/2 array, so the gradient is used; 2px is the un-tokenised Figma dash literal.
 */

const STYLE_ID = "cnc-divider-styles";
const CSS = `
.cnc-divider { display: block; padding: var(--spacing-size24) var(--spacing-size16); }
.cnc-divider--full { padding-left: 0; padding-right: 0; }
.cnc-divider__rule { height: 0; margin: 0; border: none; border-top: var(--border-width-01) solid var(--border-generic-defaultalt01); }
/* Dash = Figma strokeDashes [2,2] @ 1px: exact 2px dash / 2px gap via a repeating gradient
   (CSS border-style:dashed does NOT honour the 2/2 array). 2px = un-tokenised Figma dash value. */
.cnc-divider--dash .cnc-divider__rule {
  border-top: none;
  height: var(--border-width-01);
  background: repeating-linear-gradient(to right, var(--border-generic-defaultalt01) 0 2px, transparent 2px 4px);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function Divider({
  width = "default",
  type = "default",
  className = "",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-divider"];
  if (width === "full") cls.push("cnc-divider--full");
  if (type === "dash") cls.push("cnc-divider--dash");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("hr", {
    className: "cnc-divider__rule"
  }));
}
Object.assign(__ds_scope, { Divider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Divider/Divider/Divider.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button_Stack/LinkButtonStack/LinkButtonStack.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Link Button Stack
 * The action-sheet / popup footer: a rounded-bottom container holding one or two Link Buttons,
 * separated by Dividers. Matches the Figma "Link Button Stack" set exactly:
 *   No. of Links   : 01 | 02
 *   Stack direction: Horizontal | Vertical
 *   State          : Default
 *
 * Geometry (token-driven):
 *   Container → column, bottom corners --radius-default (top square), overflow clipped, so it
 *   seats at the foot of a popup. A full-width Divider runs across the top.
 *   01            → single Link Button.
 *   02 Vertical   → Link Button, full-width Divider, Link Button.
 *   02 Horizontal → equal-width row: Link Button | 1px Separator | Link Button. The Separator is
 *                   --border-width-01 wide in --c-temp-popup-button-borderdefault (Figma "Separator").
 *
 * Composes Link Button + Divider. Pure layout container — no colours of its own beyond the
 * Separator token and the buttons' fills.
 */

const STYLE_ID = "cnc-link-button-stack-styles";
const CSS = `
.cnc-linkstack {
  display: flex;
  flex-direction: column;
  width: 100%;
  border-bottom-left-radius: var(--radius-default);
  border-bottom-right-radius: var(--radius-default);
  overflow: hidden;
}
.cnc-linkstack__row { display: flex; flex-direction: row; }
.cnc-linkstack__row > .cnc-linkbtn { flex: 1 1 0; min-width: 0; }
.cnc-linkstack__sep { flex: 0 0 auto; width: var(--border-width-01); align-self: stretch; background: var(--c-temp-popup-button-borderdefault); }
/* In the stack the divider is JUST the line (Figma instance: height 1, padding Spacing/None) —
   collapse the standalone Divider's --spacing-size24 vertical breathing room. */
.cnc-linkstack .cnc-divider { padding: 0; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function LinkButtonStack({
  direction = "vertical",
  links,
  // array of { label, onClick, disabled }; length 1 or 2
  primaryLabel = "Link",
  secondaryLabel = "Link",
  onPrimary,
  onSecondary,
  count,
  // optional explicit 1 | 2 (else inferred from links)
  className = "",
  ...rest
}) {
  ensureStyles();
  const LB = fromNS("LinkButton", __ds_scope.LinkButton);
  const DV = fromNS("Divider", __ds_scope.Divider);
  if (!LB || !DV) return null;

  // Resolve the 1–2 link definitions.
  const items = links && links.length ? links.slice(0, 2) : [{
    label: primaryLabel,
    onClick: onPrimary
  }].concat((count || 2) >= 2 ? [{
    label: secondaryLabel,
    onClick: onSecondary
  }] : []);
  const n = items.length;
  const cls = ["cnc-linkstack"];
  if (className) cls.push(className);
  const btn = (it, i) => /*#__PURE__*/React.createElement(LB, {
    key: i,
    onClick: it.onClick,
    disabled: it.disabled
  }, it.label);
  let body;
  if (n === 1) {
    body = btn(items[0], 0);
  } else if (direction === "horizontal") {
    body = /*#__PURE__*/React.createElement("div", {
      className: "cnc-linkstack__row"
    }, btn(items[0], 0), /*#__PURE__*/React.createElement("span", {
      className: "cnc-linkstack__sep",
      "aria-hidden": "true"
    }), btn(items[1], 1));
  } else {
    body = /*#__PURE__*/React.createElement(React.Fragment, null, btn(items[0], 0), /*#__PURE__*/React.createElement(DV, {
      width: "full",
      type: "default"
    }), btn(items[1], 1));
  }
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement(DV, {
    width: "full",
    type: "default"
  }), body);
}
Object.assign(__ds_scope, { LinkButtonStack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button_Stack/LinkButtonStack/LinkButtonStack.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Icon/Icon/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Icon
 * Calls a single brand glyph BY NAME from the assets library (assets/icons/<name>.svg).
 * The library stays as files on disk; journey pages just specify which icon they want —
 * nothing is inlined:
 *
 *     <Icon name="Arrows/Arrow Right/Arrow Right" basePath="../.." />
 *
 * COLOUR IS TOKEN-DRIVEN (mask recolour). Monochrome glyphs render as a CSS `mask` of the source
 * file, filled by a colour token — default `currentColor`, so the glyph tracks its container's
 * text token (and therefore maps light/dark and pressed/disabled automatically). Pass `color` to
 * pin a specific token (e.g. color="var(--icon-action-default)"). The SOURCE SVG IS NEVER EDITED —
 * the mask reads its alpha channel.
 *
 *   Multi-colour artwork must NOT be masked (a mask flattens it to one colour). For multi-colour
 *   brand marks pass `asAuthored` to render the file verbatim with <img>. (Illustrations are handled
 *   by SpotIllustration, not Icon.)
 *
 * Figma "Icon" extraction (matched exactly):
 *   - Size variant   : 16 | 24 | 32  (frame width/height in px)
 *   - Padding variant: insets the glyph — 16→2px, 24→3px, 32→4px
 *   - Trim variant   : removes the horizontal padding so the frame hugs
 *                      (16→12, 24→18, 32→24), vertical padding kept
 *
 * Tokens: 4px padding = --spacing-size04. The 16/24 insets are 2px/3px, which are
 * un-tokenised in the Figma source (the padding field is token:null); they are the
 * only literal values and live here alone.
 */

// Internal padding per size. 2px/3px = un-tokenised Figma values (token:null in source).
const ICON_PAD = {
  16: "2px",
  24: "3px",
  32: "var(--spacing-size04)"
};
function Icon({
  name,
  src,
  size = 24,
  padding = false,
  trim = false,
  color,
  // CSS colour or token, e.g. "var(--icon-action-default)". Default currentColor.
  asAuthored = false,
  // render the file verbatim (multi-colour brand marks); no recolour.
  basePath = ".",
  title,
  style,
  ...rest
}) {
  // Call the icon file in the assets library by name (or an explicit src). Encode spaces in paths.
  const url = encodeURI(src || `${basePath}/assets/icons/${name}.svg`);
  const pad = ICON_PAD[size] || "0px";
  const vPad = padding ? pad : "0px";
  const hPad = padding && !trim ? pad : "0px";
  const frameWidth = padding && trim ? `calc(${size}px - 2 * ${pad})` : `${size}px`;
  const glyph = asAuthored ? /*#__PURE__*/React.createElement("img", {
    src: url,
    alt: title || "",
    draggable: "false",
    style: {
      width: "100%",
      height: "100%",
      display: "block"
    }
  }) :
  /*#__PURE__*/
  // Token-driven recolour: the SVG is an alpha mask; background-color (a token) fills the shape.
  React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "block",
      width: "100%",
      height: "100%",
      backgroundColor: color || "currentColor",
      WebkitMaskImage: `url("${url}")`,
      maskImage: `url("${url}")`,
      WebkitMaskRepeat: "no-repeat",
      maskRepeat: "no-repeat",
      WebkitMaskPosition: "center",
      maskPosition: "center",
      WebkitMaskSize: "contain",
      maskSize: "contain"
    }
  });
  return /*#__PURE__*/React.createElement("span", _extends({
    className: "cnc-icon",
    role: title ? "img" : "presentation",
    "aria-label": title || undefined,
    "aria-hidden": title ? undefined : true,
    style: {
      boxSizing: "border-box",
      display: "inline-flex",
      width: frameWidth,
      height: `${size}px`,
      paddingTop: vPad,
      paddingBottom: vPad,
      paddingLeft: hPad,
      paddingRight: hPad,
      flex: "0 0 auto",
      ...style
    }
  }, rest), glyph);
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Icon/Icon/Icon.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button/ActionButton/ActionButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — the Cancara Icon component. Imported for clarity/type intent; resolved at
// render from the compiled DS namespace so it works regardless of cross-directory bundling
// (each component compiles into its own scope; the namespace is the shared, stable handle).

function resolveIcon() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.Icon) return ns.Icon;
  return typeof __ds_scope.Icon !== "undefined" ? __ds_scope.Icon : null;
}

/**
 * Cancara — Action Button
 * Full-width primary action control. Matches the Figma "Action Button" set exactly:
 *   Type        : Primary | Secondary | Tertiary
 *   State       : Default | Pressed (:active) | Disabled
 *   Brand Accent: Primary only — electric-green accent treatment (independent of On Color)
 *   On Color    : variant set for use over coloured / dark surfaces
 *
 * Nested ref — the right-side "Icon" instance from Figma is the Cancara `Icon`
 * component (Size=24, Padding=True, Trim=False). It is present on Default/Pressed and
 * dropped on Disabled, exactly as in the extraction. The icon is a swappable instance
 * in Figma, so it is opt-in here via `icon` (or `iconSrc`) — no glyph is guessed.
 * The glyph is a mask filled by currentColor, and the button sets color to its --text-action-*
 * token per variant/state, so the icon matches the label and maps light/dark automatically.
 *
 * OWNER-DIRECTED (icon position): in Figma the Label and the Right-icon-container share the
 * centred H-stack, so a present icon nudges the centred label ~half-an-icon to the left. Per
 * owner direction the label is centred across the FULL button and the icon is absolutely pinned
 * to the trailing edge (right inset = --spacing-size48, the Figma H-stack horizontal padding) —
 * the icon's position is unchanged, but it no longer affects the label's centring — the label is
 * now centred across the FULL button. Documented, owner-confirmed deviation (token-driven; no new values).
 *
 * Every value is token-driven (no hardcoded colours/sizes):
 *   height  → --os-hittarget-default      radius → --c-actionbutton-defaultcornerradius
 *   border  → --c-actionbutton-borderwidth padding → --spacing-size12 / --spacing-size48
 *   label   → --type-style4-*             fills/text/stroke → --background-action-* /
 *             --text-action-* / --border-action-* / --c-brandaccentbutton-* (Figma bindings)
 * Pressed is the Figma "Pressed" variant via :active; Disabled via the native disabled attr.
 * No hover state is defined (none exists in the Figma extraction).
 * Tertiary label is underlined (link treatment) per project-owner direction — the extraction's
 * TEXT node carries no textDecoration field (the Figma export tool doesn't capture it), so this
 * is the one documented, owner-confirmed addition; text-decoration is a type property, no token.
 */

const STYLE_ID = "cnc-action-button-styles";
const CSS = `
.cnc-abtn {
  box-sizing: border-box;
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-none);
  width: 100%;
  min-height: var(--os-hittarget-default);
  padding: var(--spacing-size12) var(--spacing-size48);
  border: none;
  border-radius: var(--c-actionbutton-defaultcornerradius);
  font-family: var(--type-style4-family);
  font-size: var(--type-style4-size);
  line-height: var(--type-style4-line);
  font-weight: var(--type-style4-weight);
  letter-spacing: var(--type-style4-tracking);
  text-align: center;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
}
.cnc-abtn--auto { width: auto; }
/* Figma: the trailing icon sits centred within a 48px-square hit-target region
   (--os-hittarget-default) flush to the button's right edge. The 24px glyph is
   pinned so its centre lands at the centre of that region: right inset =
   --spacing-size12 ((48 - 24) / 2), vertically centred via top:50% +
   translateY(-50%). The label FILLs and is centred across the FULL button, so
   the absolutely-positioned glyph never shifts it off-centre. */
.cnc-abtn__label { flex: 1 1 auto; text-align: center; }
.cnc-abtn__icon {
  position: absolute;
  right: var(--spacing-size12);
  top: 50%;
  transform: translateY(-50%);
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* Primary */
.cnc-abtn--primary { background: var(--background-action-default); color: var(--text-action-inversealt01); }
.cnc-abtn--primary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Secondary (bordered) */
.cnc-abtn--secondary { background: var(--background-action-defaultalt01); color: var(--text-action-default); border: var(--c-actionbutton-borderwidth) solid var(--border-action-default); }
.cnc-abtn--secondary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); border: none; }

/* Tertiary — text-style (link) treatment: label is underlined. */
.cnc-abtn--tertiary { background: var(--background-action-defaultalt01); color: var(--text-action-default); }
.cnc-abtn--tertiary .cnc-abtn__label { text-decoration: underline; text-underline-offset: 0.15em; }
.cnc-abtn--tertiary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Brand Accent (Primary only — independent of On Color) */
.cnc-abtn--accent { background: var(--c-brandaccentbutton-primary-background-default); color: var(--c-brandaccentbutton-primary-text-default); }
.cnc-abtn--accent:active:not(:disabled) { background: var(--c-brandaccentbutton-primary-background-pressed); color: var(--c-brandaccentbutton-primary-text-pressed); }

/* On Color */
.cnc-abtn--oncolor.cnc-abtn--primary { background: var(--background-action-oncolor-default); color: var(--text-action-inverse); }
.cnc-abtn--oncolor.cnc-abtn--primary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }
.cnc-abtn--oncolor.cnc-abtn--secondary { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); border: var(--c-actionbutton-borderwidth) solid var(--border-action-oncolor-default); }
.cnc-abtn--oncolor.cnc-abtn--secondary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); border: none; }
.cnc-abtn--oncolor.cnc-abtn--tertiary { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); }
.cnc-abtn--oncolor.cnc-abtn--tertiary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }

/* Disabled (all types) */
.cnc-abtn:disabled { background: var(--background-action-disabled); color: var(--text-action-disabled); border: none; cursor: not-allowed; }
.cnc-abtn--oncolor:disabled { background: var(--background-action-oncolor-disabled); color: var(--text-action-disabled); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function ActionButton({
  variant = "primary",
  brandAccent = false,
  onColor = false,
  disabled = false,
  fullWidth = true,
  icon,
  iconSrc,
  iconTitle,
  basePath = ".",
  className = "",
  children = "Button",
  ...rest
}) {
  ensureStyles();
  // Brand Accent applies to Primary and is independent of On Color (matches Figma).
  const accent = brandAccent && variant === "primary";
  const cls = ["cnc-abtn", `cnc-abtn--${variant}`];
  if (accent) cls.push("cnc-abtn--accent");
  if (onColor && !accent) cls.push("cnc-abtn--oncolor");
  if (!fullWidth) cls.push("cnc-abtn--auto");
  if (className) cls.push(className);
  // Figma: right-side icon present on Default/Pressed, dropped on Disabled.
  const IconCmp = resolveIcon();
  const showIcon = (icon || iconSrc) && !disabled && IconCmp;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    disabled: disabled
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-abtn__label"
  }, children), showIcon ? /*#__PURE__*/React.createElement(IconCmp, {
    className: "cnc-abtn__icon",
    name: icon,
    src: iconSrc,
    size: 24,
    padding: true,
    basePath: basePath,
    title: iconTitle
  }) : null);
}
Object.assign(__ds_scope, { ActionButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button/ActionButton/ActionButton.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button/CompactButton/CompactButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — the Cancara Icon component (resolved from the DS namespace at render so it
// works regardless of cross-directory bundling). Same pattern as ActionButton.

/**
 * Cancara — Compact Button
 * Hugs-content action control (toolbars, inline actions, chips of intent). Matches the Figma
 * "Compact Button" set exactly:
 *   Size         : Default | Small
 *   Type         : Primary | Secondary | Tertiary
 *   State        : Default | Pressed (:active) | Disabled
 *   Icon Position: None | Left | Right
 *   Brand Accent : Primary only — electric-green (independent of On Color)
 *   On Color     : variant set for use over coloured / dark surfaces
 *
 * Geometry (token-driven; no hardcoded values):
 *   Default → min-height --os-hittarget-default, radius --c-compactbutton-defaultcornerradius,
 *             padding --spacing-size12 / --spacing-size16
 *   Small   → height --c-compactbutton-smallminheight, radius --c-compactbutton-smallcornerradius,
 *             padding 0 / --spacing-size16
 *   Label   → --type-style7-* inside an 8px (--spacing-size08) wrapper — the wrapper's inline
 *             padding is the Figma icon↔label gap (Figma itemSpacing is 0).
 *   Icon    → Default 24px, Small 16px, Padding=True (3px / 2px inset, the standard Figma
 *             instance). Kept on Disabled (Figma retains the icon on disabled compact
 *             variants). The glyph is a mask filled by currentColor, so it inherits the button's
 *             --text-action-* token per variant/state and maps light/dark automatically.
 *   Border  → --c-compactbutton-borderwidth (secondary). Fills/text/stroke reuse the same
 *             --background-action-* / --text-action-* / --border-action-* / --c-brandaccentbutton-*
 *             bindings as Action Button.
 * No hover state (none in the Figma extraction). min/max-width constraints are omitted — they
 * are un-tokenised in the source (token:null); the control hugs its content.
 */

const STYLE_ID = "cnc-compact-button-styles";
const CSS = `
.cnc-cbtn {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-none);
  border: none;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  text-align: center;
}
.cnc-cbtn__label { padding: 0 var(--spacing-size08); }
.cnc-cbtn__icon { flex: 0 0 auto; }

/* Size */
.cnc-cbtn--default { min-height: var(--os-hittarget-default); padding: var(--spacing-size12) var(--spacing-size16); border-radius: var(--c-compactbutton-defaultcornerradius); }
.cnc-cbtn--small { min-height: var(--c-compactbutton-smallminheight); height: var(--c-compactbutton-smallminheight); padding: 0 var(--spacing-size16); border-radius: var(--c-compactbutton-smallcornerradius); }

/* Primary */
.cnc-cbtn--primary { background: var(--background-action-default); color: var(--text-action-inversealt01); }
.cnc-cbtn--primary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Secondary (bordered) */
.cnc-cbtn--secondary { background: var(--background-action-defaultalt01); color: var(--text-action-default); border: var(--c-compactbutton-borderwidth) solid var(--border-action-default); }
.cnc-cbtn--secondary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); border: none; }

/* Tertiary */
.cnc-cbtn--tertiary { background: var(--background-action-defaultalt01); color: var(--text-action-default); }
.cnc-cbtn--tertiary:active:not(:disabled) { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Brand Accent (Primary only — independent of On Color) */
.cnc-cbtn--accent { background: var(--c-brandaccentbutton-primary-background-default); color: var(--c-brandaccentbutton-primary-text-default); }
.cnc-cbtn--accent:active:not(:disabled) { background: var(--c-brandaccentbutton-primary-background-pressed); color: var(--c-brandaccentbutton-primary-text-pressed); }

/* On Color */
.cnc-cbtn--oncolor.cnc-cbtn--primary { background: var(--background-action-oncolor-default); color: var(--text-action-inverse); }
.cnc-cbtn--oncolor.cnc-cbtn--primary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }
.cnc-cbtn--oncolor.cnc-cbtn--secondary { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); border: var(--c-compactbutton-borderwidth) solid var(--border-action-oncolor-default); }
.cnc-cbtn--oncolor.cnc-cbtn--secondary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); border: none; }
.cnc-cbtn--oncolor.cnc-cbtn--tertiary { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); }
.cnc-cbtn--oncolor.cnc-cbtn--tertiary:active:not(:disabled) { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }

/* Disabled (all types) */
.cnc-cbtn:disabled { background: var(--background-action-disabled); color: var(--text-action-disabled); border: none; cursor: not-allowed; }
.cnc-cbtn--oncolor:disabled { background: var(--background-action-oncolor-disabled); color: var(--text-action-disabled); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function resolveIcon() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.Icon) return ns.Icon;
  return typeof __ds_scope.Icon !== "undefined" ? __ds_scope.Icon : null;
}
function CompactButton({
  variant = "primary",
  size = "default",
  iconPosition = "none",
  icon,
  iconSrc,
  iconTitle,
  brandAccent = false,
  onColor = false,
  disabled = false,
  basePath = ".",
  className = "",
  children = "Button",
  ...rest
}) {
  ensureStyles();
  const accent = brandAccent && variant === "primary";
  const cls = ["cnc-cbtn", `cnc-cbtn--${variant}`, `cnc-cbtn--${size}`];
  if (accent) cls.push("cnc-cbtn--accent");
  if (onColor && !accent) cls.push("cnc-cbtn--oncolor");
  if (className) cls.push(className);
  const IconCmp = resolveIcon();
  const hasIcon = iconPosition !== "none" && (icon || iconSrc) && IconCmp;
  const iconSize = size === "small" ? 16 : 24;
  const glyph = hasIcon ? /*#__PURE__*/React.createElement(IconCmp, {
    className: "cnc-cbtn__icon",
    name: icon,
    src: iconSrc,
    size: iconSize,
    padding: true,
    basePath: basePath,
    title: iconTitle
  }) : null;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    disabled: disabled
  }, rest), iconPosition === "left" ? glyph : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cbtn__label"
  }, children), iconPosition === "right" ? glyph : null);
}
Object.assign(__ds_scope, { CompactButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button/CompactButton/CompactButton.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button/IconButton/IconButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — the Cancara Icon component (resolved from the DS namespace at render).

/**
 * Cancara — Icon Button
 * A circular, icon-only action control. Matches the Figma "Icon Button" set exactly:
 *   Size        : Default | Small
 *   Type        : Primary | Secondary | Tertiary
 *   State       : Default | Pressed (:active) | Disabled
 *   Brand Accent: Primary only — electric-green (independent of On Color)
 *   On Color    : variant set for use over coloured / dark surfaces
 *
 * Geometry (token-driven; no hardcoded values):
 *   Both sizes occupy a --os-hittarget-default (48px) tap target.
 *   Default → the 48px target IS the filled circle; icon 24px (Padding=True, 3px inset).
 *   Small   → 48px transparent target containing a --c-iconbutton-smallminsize (32px) filled
 *             circle; icon 16px (bare).
 *   Radius  → --c-iconbutton-defaultcornerradius (circle). Border → --c-iconbutton-borderwidth
 *             (secondary). Fills/stroke reuse --background-action-* / --border-action-* /
 *             --c-brandaccentbutton-* — the same bindings as Action / Compact Button.
 *
 * Icon-only: an accessible `label` is required (mapped to aria-label); the glyph is decorative.
 * Backgrounds/borders are token-driven (--background-action-* / --border-action-* / brand-accent).
 * Icon COLOUR is token-driven too: the glyph is a mask filled by `currentColor`, and each circle
 * variant/state sets `color` to its matching text token (brand-accent uses its dedicated
 * --c-brandaccentbutton-primary-icon-* token), so the glyph tracks the variant and maps light/dark
 * automatically — no colour is left to the journey page. No hover state (none in the extraction).
 */

const STYLE_ID = "cnc-icon-button-styles";
const CSS = `
.cnc-ibtn {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: var(--os-hittarget-default);
  height: var(--os-hittarget-default);
  padding: 0;
  border: none;
  background: transparent;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
}
.cnc-ibtn:disabled { cursor: not-allowed; }
.cnc-ibtn__circle {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--c-iconbutton-defaultcornerradius);
}
.cnc-ibtn--default .cnc-ibtn__circle { width: var(--os-hittarget-default); height: var(--os-hittarget-default); }
.cnc-ibtn--small .cnc-ibtn__circle { width: var(--c-iconbutton-smallminsize); height: var(--c-iconbutton-smallminsize); }

/* Variant fills + matching icon colour (glyph = mask filled by currentColor). */
/* Primary */
.cnc-ibtn--primary .cnc-ibtn__circle { background: var(--background-action-default); color: var(--text-action-inversealt01); }
.cnc-ibtn--primary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Secondary (bordered) */
.cnc-ibtn--secondary .cnc-ibtn__circle { background: var(--background-action-defaultalt01); color: var(--text-action-default); border: var(--c-iconbutton-borderwidth) solid var(--border-action-default); }
.cnc-ibtn--secondary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); border: none; }

/* Tertiary */
.cnc-ibtn--tertiary .cnc-ibtn__circle { background: var(--background-action-defaultalt01); color: var(--text-action-default); }
.cnc-ibtn--tertiary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-pressed); color: var(--text-action-pressedalt01); }

/* Brand Accent (Primary only — independent of On Color); glyph uses its dedicated icon token. */
.cnc-ibtn--accent .cnc-ibtn__circle { background: var(--c-brandaccentbutton-primary-background-default); color: var(--c-brandaccentbutton-primary-icon-default); }
.cnc-ibtn--accent:active:not(:disabled) .cnc-ibtn__circle { background: var(--c-brandaccentbutton-primary-background-pressed); color: var(--c-brandaccentbutton-primary-icon-pressed); }

/* On Color */
.cnc-ibtn--oncolor.cnc-ibtn--primary .cnc-ibtn__circle { background: var(--background-action-oncolor-default); color: var(--text-action-inverse); }
.cnc-ibtn--oncolor.cnc-ibtn--primary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }
.cnc-ibtn--oncolor.cnc-ibtn--secondary .cnc-ibtn__circle { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); border: var(--c-iconbutton-borderwidth) solid var(--border-action-oncolor-default); }
.cnc-ibtn--oncolor.cnc-ibtn--secondary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); border: none; }
.cnc-ibtn--oncolor.cnc-ibtn--tertiary .cnc-ibtn__circle { background: var(--background-action-oncolor-defaultalt01); color: var(--text-action-defaultalt01); }
.cnc-ibtn--oncolor.cnc-ibtn--tertiary:active:not(:disabled) .cnc-ibtn__circle { background: var(--background-action-oncolor-pressed); color: var(--text-action-pressedalt01); }

/* Disabled (all types) */
.cnc-ibtn:disabled .cnc-ibtn__circle { background: var(--background-action-disabled); color: var(--text-action-disabled); border: none; }
.cnc-ibtn--oncolor:disabled .cnc-ibtn__circle { background: var(--background-action-oncolor-disabled); color: var(--text-action-disabled); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function resolveIcon() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.Icon) return ns.Icon;
  return typeof __ds_scope.Icon !== "undefined" ? __ds_scope.Icon : null;
}
function IconButton({
  variant = "primary",
  size = "default",
  icon,
  iconSrc,
  label,
  brandAccent = false,
  onColor = false,
  disabled = false,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const accent = brandAccent && variant === "primary";
  const cls = ["cnc-ibtn", `cnc-ibtn--${variant}`, `cnc-ibtn--${size}`];
  if (accent) cls.push("cnc-ibtn--accent");
  if (onColor && !accent) cls.push("cnc-ibtn--oncolor");
  if (className) cls.push(className);
  const IconCmp = resolveIcon();
  const iconSize = size === "small" ? 16 : 24;
  const padded = size !== "small"; // Default icon is Padding=True; Small is bare.

  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    disabled: disabled,
    "aria-label": label
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-ibtn__circle"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    src: iconSrc,
    size: iconSize,
    padding: padded,
    basePath: basePath
  }) : null));
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button/IconButton/IconButton.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button_Stack/ActionButtonStack/ActionButtonStack.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — the approved Action Button. Resolved from the DS namespace at render so it
// works regardless of cross-directory bundling.

/**
 * Cancara — Action Button Stack
 * A layout container that arranges Action Buttons. Matches the Figma "Action Button Stack" set:
 *   Layout         : Default (Primary + Secondary + Tertiary) | Primary + Secondary | Primary + Tertiary
 *   Stack Direction: Vertical | Horizontal
 *
 * Geometry (token-driven):
 *   Gap between buttons → --spacing-size16 (Figma itemSpacing 16).
 *   Vertical   → full-width column, no padding. Order: Primary, Secondary, Tertiary.
 *   Horizontal → --spacing-size16 side padding; the secondary/tertiary sits LEFT of the primary
 *                in an equal-width row. In the Default layout the Tertiary spans full width
 *                beneath that row (exactly the Figma composition).
 * Composes Action Button only — no fills/colours of its own (it is a pure layout primitive).
 */

const STYLE_ID = "cnc-action-button-stack-styles";
const CSS = `
.cnc-abtnstack { display: flex; flex-direction: column; gap: var(--spacing-size16); }
.cnc-abtnstack--horizontal { padding: 0 var(--spacing-size16); }
.cnc-abtnstack__row { display: flex; flex-direction: row; gap: var(--spacing-size16); }
.cnc-abtnstack__row > * { flex: 1 1 0; min-width: 0; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function resolveActionButton() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.ActionButton) return ns.ActionButton;
  return typeof __ds_scope.ActionButton !== "undefined" ? __ds_scope.ActionButton : null;
}
function ActionButtonStack({
  direction = "vertical",
  layout = "default",
  primaryLabel = "Continue",
  secondaryLabel = "Back",
  tertiaryLabel = "Not now",
  onPrimary,
  onSecondary,
  onTertiary,
  className = "",
  ...rest
}) {
  ensureStyles();
  const AB = resolveActionButton();
  if (!AB) return null;
  const showSecondary = layout === "default" || layout === "primary-secondary";
  const showTertiary = layout === "default" || layout === "primary-tertiary";
  const primary = /*#__PURE__*/React.createElement(AB, {
    variant: "primary",
    onClick: onPrimary
  }, primaryLabel);
  const secondary = showSecondary ? /*#__PURE__*/React.createElement(AB, {
    variant: "secondary",
    onClick: onSecondary
  }, secondaryLabel) : null;
  const tertiary = showTertiary ? /*#__PURE__*/React.createElement(AB, {
    variant: "tertiary",
    onClick: onTertiary
  }, tertiaryLabel) : null;
  const cls = ["cnc-abtnstack", `cnc-abtnstack--${direction}`];
  if (className) cls.push(className);
  if (direction === "vertical") {
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cls.join(" ")
    }, rest), primary, secondary, tertiary);
  }

  // Horizontal: secondary/tertiary sits left of primary in an equal-width row.
  const sideButton = showSecondary ? secondary : tertiary;
  const row = /*#__PURE__*/React.createElement("div", {
    className: "cnc-abtnstack__row"
  }, sideButton, primary);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), row, layout === "default" ? tertiary : null);
}
Object.assign(__ds_scope, { ActionButtonStack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button_Stack/ActionButtonStack/ActionButtonStack.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Button_Stack/ChatButtonStack/ChatButtonStack.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — the approved Action Button and Icon Button. Resolved from the DS namespace
// at render so they work regardless of cross-directory bundling.

/**
 * Cancara — Chat Button Stack
 * The "message us" composer row from Figma "↳ Button Stack" (Chat Button Stack): a full-width
 * primary Action Button beside a circular Icon Button.
 *
 * Geometry (token-driven):
 *   Row, vertically centred, gap --spacing-size16, side padding --spacing-size16 (Figma).
 *   Action Button → Primary, fills the remaining width (label e.g. "Message us 24/7", no icon).
 *   Icon Button   → Default size, Tertiary (transparent), fixed 48px.
 *
 * FLAG — the Figma source also contains a Notification Badge instance, but it is hidden
 * (visible:false) by default, and the Notification Badge component is not built yet. It is
 * therefore exposed here only as an optional `badge` slot (render a Notification Badge into it
 * once that component exists); nothing is fabricated.
 */

const STYLE_ID = "cnc-chat-button-stack-styles";
const CSS = `
.cnc-chatstack { display: flex; flex-direction: row; align-items: center; gap: var(--spacing-size16); padding: 0 var(--spacing-size16); }
.cnc-chatstack__action { flex: 1 1 auto; min-width: 0; }
.cnc-chatstack__end { flex: 0 0 auto; position: relative; display: inline-flex; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ChatButtonStack({
  label = "Message us 24/7",
  onAction,
  icon,
  iconLabel = "Open chat",
  onIcon,
  badge = null,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const AB = fromNS("ActionButton", __ds_scope.ActionButton);
  const IB = fromNS("IconButton", __ds_scope.IconButton);
  const cls = ["cnc-chatstack"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), AB ? /*#__PURE__*/React.createElement(AB, {
    className: "cnc-chatstack__action",
    variant: "primary",
    onClick: onAction
  }, label) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-chatstack__end"
  }, IB ? /*#__PURE__*/React.createElement(IB, {
    variant: "tertiary",
    size: "default",
    icon: icon,
    label: iconLabel,
    onClick: onIcon,
    basePath: basePath
  }) : null, badge));
}
Object.assign(__ds_scope, { ChatButtonStack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Button_Stack/ChatButtonStack/ChatButtonStack.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Accordion/Accordion/Accordion.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Accordion
 * A collapsible panel row. Matches the Figma "Accordion" set exactly:
 *   Open    : False (header only) | True (header + expanded content)
 *   Width   : Compact (16px screen gutter, rounded panel) | Full Width (edge to edge, square)
 *   Usage   : Group (one item in a stack) | Single (standalone)
 *   Position: Top | Middle | Bottom (where the item sits in a Group)
 *   Content : None (closed) | Content slot (children) | Text only (body copy)  — Open=True
 *   State   : Default | Pressed (:active)
 *
 * Geometry (token-driven):
 *   Outer inset — Compact pads L/R --spacing-size16; Full Width is edge to edge.
 *   Group "Heading" (Position=Top only) — --type-style7-* / --text-generic-default, bold, with
 *     --spacing-size16 below; in Full Width it keeps its own --spacing-size16 L/R inset.
 *   Header (H Stack) — --background-panel-default fill (Pressed → --background-panel-pressed),
 *     --spacing-size16 padding, --spacing-size12 gap, items centred. Optional 24px leading Icon
 *     (Padding=False, hidden unless `icon` given). V-stack: Label --type-style6-* /
 *     --text-action-default + optional Subtitle --type-style8-* / --text-generic-subdued
 *     (--spacing-size04 gap). Trailing 16px chevron Icon (Padding=True) — Chevron Down closed,
 *     Chevron Up open. The header sets color: --text-action-default, so the leading glyph and the
 *     chevron (masks filled by currentColor) track that token and map light/dark automatically.
 *   Corner radius (--radius-default = Figma "Corner Radius/Default", 12) — Compact only:
 *     header rounds its TOP corners when it is the panel's top edge (Single or Position=Top),
 *     and its BOTTOM corners only when closed AND it is the bottom edge (Single or Bottom);
 *     when open, the bottom corners move to the expanded Wrapper. Full Width is always square.
 *   Expanded Wrapper — --c-temp-accordion-expandedpanelbackground fill, --spacing-size16 padding;
 *     Text only body = --type-style8-* / --text-generic-default; Content slot = children.
 *   Separators are Divider instances (Width=Full width, Type=Default). A header/content separator
 *     shows whenever open; an item separator shows below the item for every Group row except the
 *     last (Bottom) — never for Single.
 *
 *   NOTE (flagged): in Figma the Accordion's Divider instances override the standalone Divider's
 *   vertical padding to 0 (Spacing/None). The approved Divider component bakes in --spacing-size24
 *   top/bottom with no prop to remove it, so this composes <Divider> and zeroes that padding via a
 *   passed class (`.cnc-accordion__rule`) — representing the Figma instance override without
 *   editing the approved component. Pending owner direction on a permanent `flush` prop.
 *
 *   NOTE (flagged): Single-usage Figma radii carry value 12 with token:null (Group rows carry
 *   token "Corner Radius/Default"). Treated as the same --radius-default to honour "no hardcoded
 *   values"; flagged as a likely Figma detach.
 */

const STYLE_ID = "cnc-accordion-styles";
const CSS = `
.cnc-accordion { box-sizing: border-box; width: 100%; font-family: var(--type-style6-family); }
.cnc-accordion--compact { padding-left: var(--spacing-size16); padding-right: var(--spacing-size16); }

.cnc-accordion__heading {
  padding-bottom: var(--spacing-size16);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--text-generic-default);
}
.cnc-accordion--full .cnc-accordion__heading { padding-left: var(--spacing-size16); padding-right: var(--spacing-size16); }

.cnc-accordion__header {
  box-sizing: border-box;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size12);
  width: 100%;
  padding: var(--spacing-size16);
  background: var(--background-panel-default);
  border: none;
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none; appearance: none;
  font-family: var(--type-style6-family);
  color: var(--text-action-default);
}
.cnc-accordion__header:active:not(:disabled), .cnc-accordion__header--pressed { background: var(--background-panel-pressed); }
.cnc-accordion__header--rt { border-top-left-radius: var(--radius-default); border-top-right-radius: var(--radius-default); }
.cnc-accordion__header--rb { border-bottom-left-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }

.cnc-accordion__lead, .cnc-accordion__chevron { flex: 0 0 auto; display: inline-flex; }
.cnc-accordion__text { display: flex; flex-direction: column; gap: var(--spacing-size04); flex: 1 1 auto; min-width: 0; }
.cnc-accordion__label {
  font-family: var(--type-style6-family); font-size: var(--type-style6-size);
  line-height: var(--type-style6-line); font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking); color: var(--text-action-default);
}
.cnc-accordion__subtitle {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-subdued);
}

.cnc-accordion__wrap {
  box-sizing: border-box;
  padding: var(--spacing-size16);
  background: var(--c-temp-accordion-expandedpanelbackground);
}
.cnc-accordion__wrap--rb { border-bottom-left-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }
.cnc-accordion__body {
  margin: 0;
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-default);
}

/* Figma instance override: the in-accordion Divider has its vertical padding set to 0. */
.cnc-accordion .cnc-accordion__rule { padding-top: 0; padding-bottom: 0; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Accordion({
  open = false,
  width = "compact",
  // "compact" | "full"
  usage = "group",
  // "group" | "single"
  position = "top",
  // "top" | "middle" | "bottom"
  content = "none",
  // "none" | "content-slot" | "text-only"
  state = "default",
  // "default" | "pressed"
  heading = "Heading",
  // Group section heading (Position=Top only)
  label = "Accordion Label",
  subtitle,
  // optional second line
  icon,
  // optional 24px leading glyph name
  bodyText = "Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
  children,
  // content="content-slot"
  onToggle,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const DividerCmp = fromNS("Divider", __ds_scope.Divider);
  const isFull = width === "full";
  const isSingle = usage === "single";
  const isTop = position === "top";
  const isBottom = position === "bottom";
  const isOpen = !!open;

  // Corner radius (Compact only; Full Width is always square).
  const headerTop = !isFull && (isSingle || isTop);
  const headerBottom = !isFull && !isOpen && (isSingle || isBottom);
  const wrapperBottom = !isFull && isOpen && (isSingle || isBottom);

  // Separators. Item separator sits below every Group row except the last (Bottom).
  const itemSeparator = !isSingle && !isBottom;
  const outerCls = ["cnc-accordion", isFull ? "cnc-accordion--full" : "cnc-accordion--compact"];
  if (className) outerCls.push(className);
  const headerCls = ["cnc-accordion__header"];
  if (state === "pressed") headerCls.push("cnc-accordion__header--pressed");
  if (headerTop) headerCls.push("cnc-accordion__header--rt");
  if (headerBottom) headerCls.push("cnc-accordion__header--rb");
  const wrapCls = ["cnc-accordion__wrap"];
  if (wrapperBottom) wrapCls.push("cnc-accordion__wrap--rb");
  const rule = DividerCmp ? /*#__PURE__*/React.createElement(DividerCmp, {
    width: "full",
    type: "default",
    className: "cnc-accordion__rule"
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: outerCls.join(" ")
  }, rest), isTop ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-accordion__heading"
  }, heading) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: headerCls.join(" "),
    onClick: onToggle
  }, icon && IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-accordion__lead"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 24,
    basePath: basePath
  })) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-accordion__text"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-accordion__label"
  }, label), subtitle ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-accordion__subtitle"
  }, subtitle) : null), IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-accordion__chevron"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: isOpen ? "Arrows/Chevron Up/Chevron Up" : "Arrows/Chevron Down/Chevron Down",
    size: 16,
    padding: true,
    basePath: basePath
  })) : null), isOpen ? rule : null, isOpen ? /*#__PURE__*/React.createElement("div", {
    className: wrapCls.join(" ")
  }, content === "text-only" ? /*#__PURE__*/React.createElement("p", {
    className: "cnc-accordion__body"
  }, bodyText) : children) : null, itemSeparator ? rule : null);
}
Object.assign(__ds_scope, { Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Accordion/Accordion/Accordion.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Category_Selection/SelectCategoryItem/SelectCategoryItem.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _Select Category Item  (nested child of Select Category Grid)
 * A circular icon button with a label below. Matches the Figma "_Select Category Item" set exactly:
 *   State: Default | Active
 *
 * Geometry (token-driven):
 *   Column — 80px item frame (Figma fixed width, token:null), centred, --spacing-size04 gap.
 *   Circle ("Button"/"Shape") — 48px frame (Figma fixed, token:null), --radius-circle. 24px Icon
 *     (Padding=False), masked + filled by the Figma icon-colour token for the state.
 *     Default → --c-selectcategoryitem-backgrounddefault fill, --border-width-01
 *       --c-selectcategoryitem-borderdefault border, glyph --c-selectcategoryitem-icondefault.
 *     Active  → --c-selectcategoryitem-backgroundactive fill, --border-width-02
 *       --c-selectcategoryitem-borderactive border, glyph --c-selectcategoryitem-iconactive.
 *   Label — --type-style9-* / --text-generic-default, centred; wraps to two lines within 80px.
 *
 *   48 and 80 are the only literals: Figma frame sizes carrying token:null (cf. Quick Action 80px).
 *
 *   The glyph colour is set on the circle via the Figma --c-selectcategoryitem-icon* tokens (the
 *   Icon's currentColor fills the mask), so it tracks state and maps light/dark automatically —
 *   resolving the earlier dark-mode contrast flag (the default circle's dark fill now pairs with
 *   the theme-mapped icon token instead of a baked-in dark glyph).
 */

const STYLE_ID = "cnc-sci-styles";
const CSS = `
.cnc-sci {
  display: inline-flex; flex-direction: column; align-items: center;
  gap: var(--spacing-size04);
  width: 80px;                 /* Figma item frame size (token:null) */
  padding: 0; border: none; background: none;
  cursor: pointer; -webkit-appearance: none; appearance: none;
  font-family: var(--type-style9-family);
}
.cnc-sci__circle {
  box-sizing: border-box;
  width: 48px; height: 48px;   /* Figma "Button" frame size (token:null) */
  display: flex; align-items: center; justify-content: center;
  border-radius: var(--radius-circle);
  background: var(--c-selectcategoryitem-backgrounddefault);
  border: var(--border-width-01) solid var(--c-selectcategoryitem-borderdefault);
  color: var(--c-selectcategoryitem-icondefault);
}
.cnc-sci--active .cnc-sci__circle {
  background: var(--c-selectcategoryitem-backgroundactive);
  border: var(--border-width-02) solid var(--c-selectcategoryitem-borderactive);
  color: var(--c-selectcategoryitem-iconactive);
}
.cnc-sci__label {
  font-family: var(--type-style9-family); font-size: var(--type-style9-size);
  line-height: var(--type-style9-line); font-weight: var(--type-style9-weight);
  letter-spacing: var(--type-style9-tracking); color: var(--text-generic-default);
  text-align: center; white-space: pre-line;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function SelectCategoryItem({
  state = "default",
  // "default" | "active"
  icon,
  // 24px glyph name
  label = "Category label",
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const cls = ["cnc-sci"];
  if (state === "active") cls.push("cnc-sci--active");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    "aria-pressed": state === "active"
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sci__circle"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 24,
    basePath: basePath
  }) : null), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sci__label"
  }, label));
}
Object.assign(__ds_scope, { SelectCategoryItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Category_Selection/SelectCategoryItem/SelectCategoryItem.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Category_Selection/SelectCategoryGrid/SelectCategoryGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Select Category Grid  (composes _Select Category Item)
 * A single horizontal row of up to three Select Category Items. Matches the Figma
 * "_Select Category Grid" set exactly:
 *   Row Items: 01 | 02 | 03   (how many of the three slots are shown)
 *
 * Geometry (token-driven):
 *   Row — HORIZONTAL auto-layout, SPACE_BETWEEN, items centred, L/R padding --spacing-size16,
 *     top/bottom 0. Always lays out THREE 80px item slots; SPACE_BETWEEN distributes them across
 *     the row. In Figma the surplus slots beyond "Row Items" are real Item instances kept at
 *     opacity 0 (so the visible items keep their SPACE_BETWEEN positions) — here those slots are
 *     equivalent 80px spacers (aria-hidden), preserving identical spacing without an instance.
 *   Width — 375 (Figma FIXED frame width; min 343, max 375), height HUG. The 375/343, the 80px
 *     slot width and the 88px slot height are Figma frame sizes carrying token:null — documented
 *     literals, exactly as Icon/Pictogram/Select Category Item carry their un-tokenised sizes.
 *   itemSpacing 22 (token:null) is the Figma recorded gap; SPACE_BETWEEN supersedes it, so it is
 *     not applied (documented, not used).
 *
 * The Item children carry the only colour/state; this component adds layout only — no fills,
 * strokes, radii, behaviour, motion or props beyond the extraction.
 */

const STYLE_ID = "cnc-scg-styles";
const CSS = `
.cnc-scg {
  box-sizing: border-box;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  max-width: 375px;            /* Figma FIXED frame width (token:null) */
  min-width: 343px;            /* Figma minWidth (token:null) */
  padding: 0 var(--spacing-size16);
}
.cnc-scg__slot { flex: 0 0 auto; }
/* Surplus slot beyond Row Items — equivalent of Figma's opacity:0 Item instance. */
.cnc-scg__spacer { width: 80px; flex: 0 0 auto; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function SelectCategoryGrid({
  rowItems = 3,
  // 1 | 2 | 3 — how many of the three slots are shown
  items,
  // array of { icon, label, state, onClick } for the visible slots
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const ItemCmp = fromNS("SelectCategoryItem", __ds_scope.SelectCategoryItem);
  const count = Math.max(1, Math.min(3, rowItems));
  const data = Array.isArray(items) ? items : [];
  const cls = ["cnc-scg"];
  if (className) cls.push(className);

  // Always three physical slots (Figma keeps surplus slots present, at opacity 0).
  const slots = [];
  for (let i = 0; i < 3; i += 1) {
    if (i < count && ItemCmp) {
      const it = data[i] || {};
      slots.push(/*#__PURE__*/React.createElement("span", {
        className: "cnc-scg__slot",
        key: i
      }, /*#__PURE__*/React.createElement(ItemCmp, {
        icon: it.icon,
        label: it.label != null ? it.label : "Category label",
        state: it.state || "default",
        onClick: it.onClick,
        basePath: basePath
      })));
    } else {
      slots.push(/*#__PURE__*/React.createElement("span", {
        className: "cnc-scg__spacer",
        "aria-hidden": "true",
        key: i
      }));
    }
  }
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), slots);
}
Object.assign(__ds_scope, { SelectCategoryGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Category_Selection/SelectCategoryGrid/SelectCategoryGrid.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Icon/SpotIllustration/SpotIllustration.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Spot Illustration
 * Renders a full-colour brand illustration at a fixed spot size. Matches the Figma
 * "Spot Illustration" set exactly:
 *   Size   : 64 | 80 | 100 | 128 | 160  (square frame, px)
 *   Padding: False (artwork fills the frame) | True (artwork inset to 75%, centred)
 *
 * Geometry:
 *   Square frame at the chosen Size (Figma FIXED frame dims, token:null in source — documented
 *   literals, exactly as Icon/Pictogram carry their un-tokenised sizes). Padding=True insets the
 *   artwork to 75% of the frame (the Figma ratio: 48/64, 60/80, 75/100, 96/128, 120/160 — all 0.75),
 *   centred.
 *
 * Illustrations ship as PAIRED light/dark artwork ("Savings.svg" + "Savings - Dark Mode.svg").
 * Both are rendered and swapped on [data-theme="dark"] — an artwork swap (separate source files),
 * not a recolour. Artwork is rendered AS-AUTHORED (its own colours, which come from the brand's
 * colourway artwork in assets/illustrations/<Colourway>/). Pass the illustration path under
 * assets/illustrations without extension, e.g. "Default/Savings/Savings" (the instance the Figma
 * swap points at); the " - Dark Mode" sibling is derived (override via *Src). Nothing is inlined.
 */

const STYLE_ID = "cnc-spot-illustration-styles";
const CSS = `
.cnc-spotill { box-sizing: border-box; display: inline-flex; align-items: center; justify-content: center; flex: 0 0 auto; }
.cnc-spotill__art { display: block; width: 100%; height: 100%; }
.cnc-spotill--padded .cnc-spotill__art { width: 75%; height: 75%; }
.cnc-spotill__art--dark { display: none; }
[data-theme="dark"] .cnc-spotill__art--light { display: none; }
[data-theme="dark"] .cnc-spotill__art--dark { display: block; }
[data-theme="dark"] .cnc-spotill--padded .cnc-spotill__art--dark { display: block; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function SpotIllustration({
  size = 100,
  padding = false,
  illustration,
  illustrationSrc,
  illustrationDarkSrc,
  basePath = ".",
  title,
  className = "",
  ...rest
}) {
  ensureStyles();
  // Paired light/dark artwork: "<path>.svg" and "<path> - Dark Mode.svg".
  const lightUrl = illustrationSrc || (illustration ? `${basePath}/assets/illustrations/${illustration}.svg` : null);
  const darkUrl = illustrationDarkSrc || (illustration ? `${basePath}/assets/illustrations/${illustration} - Dark Mode.svg` : lightUrl);
  const cls = ["cnc-spotill"];
  if (padding) cls.push("cnc-spotill--padded");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" "),
    role: title ? "img" : "presentation",
    "aria-label": title || undefined,
    "aria-hidden": title ? undefined : true,
    style: {
      width: `${size}px`,
      height: `${size}px`
    }
  }, rest), /*#__PURE__*/React.createElement("img", {
    className: "cnc-spotill__art cnc-spotill__art--light",
    src: lightUrl,
    alt: title || "",
    draggable: "false"
  }), /*#__PURE__*/React.createElement("img", {
    className: "cnc-spotill__art cnc-spotill__art--dark",
    src: darkUrl,
    alt: title || "",
    draggable: "false"
  }));
}
Object.assign(__ds_scope, { SpotIllustration });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Icon/SpotIllustration/SpotIllustration.jsx", error: String((e && e.message) || e) }); }

// src/components/content/Icon/Pictogram/Pictogram.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Pictogram
 * A tinted "canvas" holding a brand asset. Matches the Figma "Pictogram" set exactly:
 *   Asset: Pictogram (40×40 circle, holds a 24px Icon glyph)
 *        | Illustration (64×64, radius 12, holds a 64px illustration)
 *   Style: Default | Default Alt 01 | Default Alt 02
 *        | Primary | Primary Alt 01 | Primary Alt 02 | Primary Alt 03
 *        | Secondary | Secondary Alt 01 | Secondary Alt 02 | Secondary Alt 03 | Secondary Alt 04
 *
 * Geometry & colour (token-driven):
 *   Pictogram    → 40×40, --radius-circle, canvas --c-pictogram-icon-canvas-<style>.
 *   Illustration → 64×64, --radius-default (12), canvas --c-pictogram-illustration-canvas-<style>.
 *   Only the CANVAS background is set here (the component's own fill, from the extraction).
 *   The 40px / 64px canvas sizes are the Figma FIXED frame dimensions (sizing token:null in the
 *   source — no size token exists), so they are documented literals, exactly as the Icon
 *   component carries its un-tokenised 16/24/32 sizes.
 *
 * The held GLYPH is colour-driven by the Figma --c-pictogram-icon-<style> token: it is rendered
 * through the nested Icon as a mask filled by that token (e.g. white on the dark-green canvases,
 * black on the light ones), so the glyph reads correctly on every canvas and maps light/dark
 * automatically. Pictogram glyphs come from the Icon library by name (assets/icons/<name>.svg);
 * illustrations are rendered through the nested Spot Illustration component (64px) inside the
 * tinted canvas. Nothing is inlined.
 *
 * Illustrations ship as PAIRED light/dark artwork (e.g. "Savings.svg" + "Savings - Dark Mode.svg").
 * Both are rendered and swapped on [data-theme="dark"] via CSS, so the correct artwork shows in
 * each theme — this is an artwork swap (separate source files), not a recolour. Pass the
 * illustration path (the part under assets/illustrations/ without extension), e.g.
 * illustration="Default/Savings/Savings"; the " - Dark Mode" sibling is derived automatically
 * (override either file with illustrationSrc / illustrationDarkSrc).
 */

const STYLES = ["default", "defaultalt01", "defaultalt02", "primary", "primaryalt01", "primaryalt02", "primaryalt03", "secondary", "secondaryalt01", "secondaryalt02", "secondaryalt03", "secondaryalt04"];
const STYLE_ID = "cnc-pictogram-styles";
const CSS = `
.cnc-pictogram {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  overflow: hidden;
}
.cnc-pictogram__asset { display: block; }
/* 40 / 64 = Figma FIXED frame sizes (sizing token:null in source); documented literals, as in Icon. */
.cnc-pictogram--pictogram { width: 40px; height: 40px; border-radius: var(--radius-circle); }
.cnc-pictogram--illustration { width: 64px; height: 64px; border-radius: var(--radius-default); }
${STYLES.map(s => `.cnc-pictogram--pictogram.cnc-pictogram--${s} { background: var(--c-pictogram-icon-canvas-${s}); }`).join("\n")}
${STYLES.map(s => `.cnc-pictogram--illustration.cnc-pictogram--${s} { background: var(--c-pictogram-illustration-canvas-${s}); }`).join("\n")}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function resolveIcon() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.Icon) return ns.Icon;
  return typeof __ds_scope.Icon !== "undefined" ? __ds_scope.Icon : null;
}
function resolveSpotIllustration() {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns.SpotIllustration) return ns.SpotIllustration;
  return typeof __ds_scope.SpotIllustration !== "undefined" ? __ds_scope.SpotIllustration : null;
}
function Pictogram({
  asset = "pictogram",
  style = "default",
  // Pictogram (icon) mode — glyph from the Icon library, masked + filled by --c-pictogram-icon-<style>.
  icon,
  iconSrc,
  // Illustration mode — referenced by path (under assets/illustrations, no extension) or src.
  illustration,
  illustrationSrc,
  illustrationDarkSrc,
  basePath = ".",
  title,
  className = "",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-pictogram", `cnc-pictogram--${asset}`, `cnc-pictogram--${style}`];
  if (className) cls.push(className);
  let inner = null;
  if (asset === "illustration") {
    // Nested Spot Illustration at 64px with Padding=True — the artwork insets to 0.75 (≈8px each
    // side in the 64px canvas) so the tinted token canvas shows around it and the artwork never
    // touches the rounded edge. Spot Illustration handles the paired light/dark artwork swap.
    const SpotCmp = resolveSpotIllustration();
    inner = SpotCmp ? /*#__PURE__*/React.createElement(SpotCmp, {
      className: "cnc-pictogram__asset",
      size: 64,
      padding: true,
      illustration: illustration,
      illustrationSrc: illustrationSrc,
      illustrationDarkSrc: illustrationDarkSrc,
      basePath: basePath,
      title: title
    }) : null;
  } else {
    const IconCmp = resolveIcon();
    inner = IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
      name: icon,
      src: iconSrc,
      size: 24,
      color: `var(--c-pictogram-icon-${style})`,
      basePath: basePath,
      title: title
    }) : null;
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" "),
    role: title ? "img" : "presentation",
    "aria-label": title || undefined,
    "aria-hidden": title ? undefined : true
  }, rest), inner);
}
Object.assign(__ds_scope, { Pictogram });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/Icon/Pictogram/Pictogram.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Panel/GhostPanel/GhostPanel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Ghost Panel
 * A dashed-border "add / placeholder" panel. Matches the Figma "Ghost Panel" set exactly:
 *   State: Default | Pressed (:active)
 *
 * Geometry (token-driven):
 *   Wrapper insets the card by --spacing-size16 (screen gutter, Figma 0/16 padding).
 *   Card → --background-panel-default fill; DASHED stroke --border-generic-defaultalt01,
 *          --border-width-02 weight, EXACT dash pattern 6/9 with ROUND dash caps (inline SVG rect —
 *          CSS dashed cannot honour the 6/9 array or round caps); --radius-default (12); --spacing-size16
 *          padding; --spacing-size16 row gap; items centred.
 *   Leading → 64px Illustration Pictogram (paired light/dark artwork, themed automatically).
 *   Text V-stack → --spacing-size04 gap; Heading --type-style4-* (Bold) / --text-generic-default,
 *          Description --type-style8-* / --text-generic-subdued.
 *   Trailing → Icon Button (Small · Primary · Brand Accent) — the electric-green add control.
 *   Pressed → --background-panel-pressed via :active and the `state="pressed"` prop.
 *
 * Composes Pictogram + Icon Button. Outer is a <div> (it contains an interactive Icon Button,
 * so it must not itself be a button). The dash pattern 6/9 and 2px weight are the Figma stroke
 * values (strokeDashes:[6,9], strokeWeight:2) — un-tokenised in source, mapped to --border-width-02.
 */

const STYLE_ID = "cnc-ghost-panel-styles";
const CSS = `
.cnc-ghostpanel-wrap { box-sizing: border-box; width: 100%; padding: 0 var(--spacing-size16); }
.cnc-ghostpanel {
  box-sizing: border-box;
  position: relative;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: var(--spacing-size16);
  width: 100%;
  padding: var(--spacing-size16);
  background: var(--background-panel-default);
  border-radius: var(--radius-default);
}
/* EXACT Figma dashed stroke: strokeDashes [6,9] @ --border-width-02, inside-aligned, on a
   --radius-default rect. CSS border-style:dashed cannot honour the 6/9 array, so an inline SVG
   rect carries the precise dash (stroke = token, so it themes light/dark). 6/9 are the
   un-tokenised Figma dash literals. */
.cnc-ghostpanel__border { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; }
.cnc-ghostpanel__border rect {
  x: calc(var(--border-width-02) / 2);
  y: calc(var(--border-width-02) / 2);
  width: calc(100% - var(--border-width-02));
  height: calc(100% - var(--border-width-02));
  rx: var(--radius-default);
  ry: var(--radius-default);
  fill: none;
  stroke: var(--border-generic-defaultalt01);
  stroke-width: var(--border-width-02);
  stroke-dasharray: 6 9;
  stroke-linecap: round;
}
.cnc-ghostpanel--pressed { background: var(--background-panel-pressed); }
.cnc-ghostpanel__text { display: flex; flex-direction: column; gap: var(--spacing-size04); flex: 1 1 auto; min-width: 0; }
.cnc-ghostpanel__heading {
  font-family: var(--type-style4-family); font-size: var(--type-style4-size);
  line-height: var(--type-style4-line); font-weight: var(--type-style4-weight);
  letter-spacing: var(--type-style4-tracking); color: var(--text-generic-default);
}
.cnc-ghostpanel__desc {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-subdued);
}
.cnc-ghostpanel__lead, .cnc-ghostpanel__trail { flex: 0 0 auto; display: inline-flex; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function GhostPanel({
  state = "default",
  heading = "Heading",
  description = "Description",
  // Leading 64px Illustration Pictogram
  illustration,
  illustrationSrc,
  illustrationDarkSrc,
  pictogramStyle = "default",
  // Trailing Icon Button (Small · Primary · Brand Accent)
  actionIcon = "Action/Plus/Plus",
  actionLabel = "Add",
  onAction,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const PictogramCmp = fromNS("Pictogram", __ds_scope.Pictogram);
  const IconButtonCmp = fromNS("IconButton", __ds_scope.IconButton);
  const cls = ["cnc-ghostpanel"];
  if (state === "pressed") cls.push("cnc-ghostpanel--pressed");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", {
    className: "cnc-ghostpanel-wrap"
  }, /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("svg", {
    className: "cnc-ghostpanel__border",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("rect", null)), PictogramCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-ghostpanel__lead"
  }, /*#__PURE__*/React.createElement(PictogramCmp, {
    asset: "illustration",
    style: pictogramStyle,
    illustration: illustration,
    illustrationSrc: illustrationSrc,
    illustrationDarkSrc: illustrationDarkSrc,
    basePath: basePath
  })) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-ghostpanel__text"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-ghostpanel__heading"
  }, heading), /*#__PURE__*/React.createElement("span", {
    className: "cnc-ghostpanel__desc"
  }, description)), IconButtonCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-ghostpanel__trail"
  }, /*#__PURE__*/React.createElement(IconButtonCmp, {
    variant: "primary",
    size: "small",
    brandAccent: true,
    icon: actionIcon,
    label: actionLabel,
    onClick: onAction,
    basePath: basePath
  })) : null));
}
Object.assign(__ds_scope, { GhostPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Panel/GhostPanel/GhostPanel.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Panel/Panel/Panel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Panel
 * A tappable list row. Matches the Figma "Panel" set exactly:
 *   Asset: None | Icon (24px leading glyph) | Pictogram (40px leading pictogram)
 *   State: Default | Pressed (:active)
 *
 * Geometry (token-driven):
 *   Wrapper insets the card by --spacing-size16 (the screen gutter, Figma 0/16 padding).
 *   Card → --background-panel-default fill, --border-panel-default stroke, --radius-default (12),
 *          --spacing-size16 padding, --spacing-size16 row gap, items centred.
 *   Text V-stack → --spacing-size04 gap; Heading --type-style5-* / --text-generic-default,
 *          Description --type-style8-* / --text-generic-subdued.
 *   Trailing chevron → 24px Icon (Padding=True); the card sets color: --text-generic-default so the
 *          chevron and any leading Icon glyph (masks filled by currentColor) track that token and
 *          map light/dark automatically.
 *   Pressed → --background-panel-pressed (Figma "Pressed" variant) via :active and the
 *             `state="pressed"` prop.
 *
 * Composes Icon and Pictogram; the Pictogram glyph is coloured by its own --c-pictogram-icon token.
 * The card is a <button> so it is keyboard/press accessible and carries no nested interactive controls.
 */

const STYLE_ID = "cnc-panel-styles";
const CSS = `
.cnc-panel-wrap { box-sizing: border-box; width: 100%; padding: 0 var(--spacing-size16); }
.cnc-panel {
  box-sizing: border-box;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: var(--spacing-size16);
  width: 100%;
  padding: var(--spacing-size16);
  background: var(--background-panel-default);
  border: var(--border-width-01) solid var(--border-panel-default);
  border-radius: var(--radius-default);
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  font-family: var(--type-style5-family);
  color: var(--text-generic-default);
}
.cnc-panel:active:not(:disabled), .cnc-panel--pressed { background: var(--background-panel-pressed); }
.cnc-panel__text { display: flex; flex-direction: column; gap: var(--spacing-size04); flex: 1 1 auto; min-width: 0; }
.cnc-panel__heading {
  font-family: var(--type-style5-family); font-size: var(--type-style5-size);
  line-height: var(--type-style5-line); font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking); color: var(--text-generic-default);
}
.cnc-panel__desc {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-subdued);
}
.cnc-panel__lead, .cnc-panel__trail { flex: 0 0 auto; display: inline-flex; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Panel({
  asset = "none",
  state = "default",
  heading = "Heading",
  description = "Description",
  // Leading asset wiring
  icon,
  // asset="icon": Icon-library glyph name
  pictogramIcon,
  // asset="pictogram": glyph for the 40px Pictogram
  pictogramStyle = "default",
  // asset="pictogram": canvas style
  // Trailing chevron (masked glyph, filled by the card's currentColor)
  trailingIcon = "Arrows/Chevron Right/Chevron Right",
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const PictogramCmp = fromNS("Pictogram", __ds_scope.Pictogram);
  const cls = ["cnc-panel"];
  if (state === "pressed") cls.push("cnc-panel--pressed");
  if (className) cls.push(className);
  let lead = null;
  if (asset === "icon" && IconCmp) {
    lead = /*#__PURE__*/React.createElement("span", {
      className: "cnc-panel__lead"
    }, /*#__PURE__*/React.createElement(IconCmp, {
      name: icon,
      size: 24,
      padding: true,
      basePath: basePath
    }));
  } else if (asset === "pictogram" && PictogramCmp) {
    lead = /*#__PURE__*/React.createElement("span", {
      className: "cnc-panel__lead"
    }, /*#__PURE__*/React.createElement(PictogramCmp, {
      asset: "pictogram",
      style: pictogramStyle,
      icon: pictogramIcon,
      basePath: basePath
    }));
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "cnc-panel-wrap"
  }, /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" ")
  }, rest), lead, /*#__PURE__*/React.createElement("span", {
    className: "cnc-panel__text"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-panel__heading"
  }, heading), /*#__PURE__*/React.createElement("span", {
    className: "cnc-panel__desc"
  }, description)), IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-panel__trail"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: trailingIcon,
    size: 24,
    padding: true,
    basePath: basePath
  })) : null));
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Panel/Panel/Panel.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Tile/Tile/Tile.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Tile
 * A tappable tile group. Matches the Figma "Tile" set exactly:
 *   Layout : Single (one full-width tile, leading pictogram + trailing chevron)
 *          | Double (two equal side-by-side tiles, no chevron)
 *   Preset : Link Only            — pictogram + Link
 *          | Link + Description   — pictogram + Description (subdued) over Link (bold)
 *          | Heading + Description — inline 24px Icon + Heading, then Description; no pictogram
 *   State  : Default | Tile 01 Pressed | Tile 02 Pressed  (per-tile :active + `pressed` flag)
 *
 * Geometry (token-driven; no hardcoded values):
 *   Wrapper insets the tiles by --spacing-size16 (the Figma 0/16 screen gutter).
 *   Tile card → --background-panel-default fill, --border-width-01 --border-panel-default stroke,
 *               --radius-default (12) radius, --spacing-size16 padding, --spacing-size16 gap.
 *   Text V-stack gap --spacing-size04; Heading H-stack gap --spacing-size08.
 *   Pressed → --background-panel-pressed (Figma "Tile NN Pressed" variant), via :active and
 *             the per-cell `pressed` flag.
 *
 * Type (Figma styles): Link → --type-style7-* / --text-action-default;
 *   Description (Link+Desc) → --type-style6-* / --text-generic-subdued;
 *   Heading → --type-style8-* / --text-generic-default;
 *   Description (Heading+Desc) → --type-style9-* / --text-generic-subdued.
 *
 * Composes Pictogram (40px leading; coloured by its own --c-pictogram-icon token) and Icon (16px
 * trailing chevron; 24px inline leading icon in the Heading preset). The cell sets
 * color: --text-generic-default (→ --c-temp-tile-textpressed when pressed), so the masked chevron
 * and inline icon (filled by currentColor) track state and map light/dark automatically.
 * Each tile is a <button> so it is keyboard/press accessible.
 */

const DEFAULT_GLYPH = "Finance/Piggy Bank/Piggy Bank";
const DEFAULT_CHEVRON = "Arrows/Chevron Right/Chevron Right";
const STYLE_ID = "cnc-tile-styles";
const CSS = `
.cnc-tile { box-sizing: border-box; width: 100%; padding: 0 var(--spacing-size16); font-family: var(--type-style7-family); }
.cnc-tile__cells { display: flex; }
.cnc-tile--single .cnc-tile__cells { flex-direction: column; }
.cnc-tile--double .cnc-tile__cells { flex-direction: row; gap: var(--spacing-size16); align-items: stretch; }

.cnc-tile__cell {
  box-sizing: border-box;
  display: flex;
  width: 100%;
  margin: 0;
  background: var(--background-panel-default);
  border: var(--border-width-01) solid var(--border-panel-default);
  border-radius: var(--radius-default);
  padding: var(--spacing-size16);
  gap: var(--spacing-size16);
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  font: inherit;
  color: var(--text-generic-default);
}
.cnc-tile__cell:active, .cnc-tile__cell--pressed { background: var(--background-panel-pressed); color: var(--c-temp-tile-textpressed); }
.cnc-tile--double .cnc-tile__cell { flex: 1 1 0; min-width: 0; }

/* Single = horizontal row, centred */
.cnc-tile__cell--single { flex-direction: row; align-items: center; }
/* Double = vertical column */
.cnc-tile__cell--double { flex-direction: column; }
.cnc-tile__cell--double.cnc-tile__cell--link { align-items: center; text-align: center; }
.cnc-tile__cell--double.cnc-tile__cell--heading-description { justify-content: center; }
/* Double Link + Description: pictogram centred, text block full-width left */
.cnc-tile__cell--double.cnc-tile__cell--link-description > .cnc-tile__lead { align-self: center; }

.cnc-tile__lead, .cnc-tile__trail { flex: 0 0 auto; display: inline-flex; }
.cnc-tile__body { display: flex; flex-direction: column; gap: var(--spacing-size04); flex: 1 1 auto; min-width: 0; }
.cnc-tile__cell--double.cnc-tile__cell--link .cnc-tile__body { align-items: center; }
.cnc-tile__hstack { display: flex; flex-direction: row; align-items: center; gap: var(--spacing-size08); }

.cnc-tile__link { font-family: var(--type-style7-family); font-size: var(--type-style7-size); line-height: var(--type-style7-line); font-weight: var(--type-style7-weight); letter-spacing: var(--type-style7-tracking); color: var(--text-action-default); }
.cnc-tile__desc { font-family: var(--type-style6-family); font-size: var(--type-style6-size); line-height: var(--type-style6-line); font-weight: var(--type-style6-weight); letter-spacing: var(--type-style6-tracking); color: var(--text-generic-subdued); }
.cnc-tile__heading { font-family: var(--type-style8-family); font-size: var(--type-style8-size); line-height: var(--type-style8-line); font-weight: var(--type-style8-weight); letter-spacing: var(--type-style8-tracking); color: var(--text-generic-default); }
.cnc-tile__subdesc { font-family: var(--type-style9-family); font-size: var(--type-style9-size); line-height: var(--type-style9-line); font-weight: var(--type-style9-weight); letter-spacing: var(--type-style9-tracking); color: var(--text-generic-subdued); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function renderCell(cell, idx, layout, preset, basePath, IconCmp, PictogramCmp) {
  const isDouble = layout === "double";
  const cls = ["cnc-tile__cell", `cnc-tile__cell--${isDouble ? "double" : "single"}`, `cnc-tile__cell--${preset}`];
  if (cell.pressed) cls.push("cnc-tile__cell--pressed");
  const showPictogram = preset === "link" || preset === "link-description";
  const lead = showPictogram && PictogramCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tile__lead"
  }, /*#__PURE__*/React.createElement(PictogramCmp, {
    asset: "pictogram",
    style: cell.pictogramStyle || "default",
    icon: cell.pictogram || DEFAULT_GLYPH,
    basePath: basePath
  })) : null;
  let body;
  if (preset === "link") {
    body = /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__body"
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__link"
    }, cell.link != null ? cell.link : "Link"));
  } else if (preset === "link-description") {
    body = /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__body"
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__desc"
    }, cell.description != null ? cell.description : "Description"), /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__link"
    }, cell.link != null ? cell.link : "Link"));
  } else {
    // heading-description: inline 24px leading Icon (Padding=True, Trim=True) + Heading, then Description
    body = /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__body"
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__hstack"
    }, IconCmp ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__lead"
    }, /*#__PURE__*/React.createElement(IconCmp, {
      name: cell.icon || DEFAULT_GLYPH,
      size: 24,
      padding: true,
      trim: true,
      basePath: basePath
    })) : null, /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__heading"
    }, cell.heading != null ? cell.heading : "Heading")), /*#__PURE__*/React.createElement("span", {
      className: "cnc-tile__subdesc"
    }, cell.description != null ? cell.description : "Description"));
  }
  const trail = !isDouble && IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tile__trail"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: cell.trailingIcon || DEFAULT_CHEVRON,
    size: 16,
    padding: true,
    basePath: basePath
  })) : null;
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    key: idx,
    className: cls.join(" "),
    onClick: cell.onClick,
    disabled: cell.disabled
  }, lead, body, trail);
}
function Tile({
  layout = "single",
  preset = "link",
  tiles,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const PictogramCmp = fromNS("Pictogram", __ds_scope.Pictogram);
  const count = layout === "double" ? 2 : 1;
  const src = Array.isArray(tiles) && tiles.length ? tiles : [];
  const cells = [];
  for (let i = 0; i < count; i += 1) cells.push(src[i] || {});
  const cls = ["cnc-tile", `cnc-tile--${layout}`, `cnc-tile--${preset}`];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tile__cells"
  }, cells.map((cell, i) => renderCell(cell, i, layout, preset, basePath, IconCmp, PictogramCmp))));
}
Object.assign(__ds_scope, { Tile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Tile/Tile/Tile.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Tile/TileGrid/TileGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Tile Grid
 * A titled group of Double tiles with a trailing "View All" action. Matches the Figma
 * "Tile Grid" set exactly:
 *   Tile Preset : Link Only | Link + Description  (forwarded to each Double Tile)
 *
 * Structure (token-driven):
 *   Header (Container) → Heading (--type-style3-* / --text-generic-default) on a
 *     --spacing-size16 left gutter, bottom-aligned with a Tertiary Compact Button ("View All").
 *     The heading's --spacing-size16 padding-bottom matches the Figma Container baseline.
 *   List (V Stack) → --spacing-size16 gap between Tile rows; each row is a Double-layout Tile.
 *
 * Composes the Tile (Layout=Double) and Compact Button (Tertiary) primitives — neither is
 * re-implemented here. The grid renders two default rows when no `rows` are supplied.
 */

const STYLE_ID = "cnc-tilegrid-styles";
const CSS = `
.cnc-tilegrid { box-sizing: border-box; width: 100%; display: flex; flex-direction: column; font-family: var(--type-style3-family); }
.cnc-tilegrid__header { display: flex; flex-direction: row; align-items: flex-end; padding-left: var(--spacing-size16); }
.cnc-tilegrid__heading-wrap { flex: 1 1 auto; min-width: 0; padding-bottom: var(--spacing-size16); }
.cnc-tilegrid__heading { font-family: var(--type-style3-family); font-size: var(--type-style3-size); line-height: var(--type-style3-line); font-weight: var(--type-style3-weight); letter-spacing: var(--type-style3-tracking); color: var(--text-generic-default); margin: 0; }
.cnc-tilegrid__action { flex: 0 0 auto; }
.cnc-tilegrid__list { display: flex; flex-direction: column; gap: var(--spacing-size16); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function TileGrid({
  tilePreset = "link",
  heading = "Heading",
  actionLabel = "View All",
  onAction,
  rows,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const TileCmp = fromNS("Tile", __ds_scope.Tile);
  const CompactButtonCmp = fromNS("CompactButton", __ds_scope.CompactButton);

  // Each row is a Double tile → an array of 2 cells. Default to two placeholder rows.
  const list = Array.isArray(rows) && rows.length ? rows : [[{}, {}], [{}, {}]];
  const cls = ["cnc-tilegrid"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tilegrid__header"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tilegrid__heading-wrap"
  }, /*#__PURE__*/React.createElement("h3", {
    className: "cnc-tilegrid__heading"
  }, heading)), CompactButtonCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tilegrid__action"
  }, /*#__PURE__*/React.createElement(CompactButtonCmp, {
    variant: "tertiary",
    onClick: onAction,
    basePath: basePath
  }, actionLabel)) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tilegrid__list"
  }, list.map((row, i) => TileCmp ? /*#__PURE__*/React.createElement(TileCmp, {
    key: i,
    layout: "double",
    preset: tilePreset,
    tiles: row,
    basePath: basePath
  }) : null)));
}
Object.assign(__ds_scope, { TileGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Tile/TileGrid/TileGrid.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Calendar/CalendarDate/CalendarDate.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara _Date (exported as CalendarDate) — INTERNAL (private) component. Figma marks it `_`
 * (hidden from the Assets panel); it is composed by Calendar to fill each cell of the month grid,
 * and is NOT placed directly. Exported only so Calendar can resolve it from the namespace.
 *
 * NAME: the Figma source is `_Date`. The public export drops the `_`, which would be `Date` — but
 * `Date` is a JS global (the Date constructor). To avoid clobbering it on the DS namespace this
 * component is exported as **CalendarDate** (its only consumer, Calendar, resolves it by that name).
 *
 * Matches the extraction's single axis:
 *   State : Empty | Disabled | Today | Default | Pressed | Selected   (the `✏️ Date` text is the cell number)
 *
 * Anatomy (token-driven):
 *   Cell — 48×48 (Figma fixed, token:null → literal), --radius-full (Figma 200, token:null →
 *     the exact-value primitive), VERTICAL centre/centre, holds the date number.
 *       Background:
 *         Default / Disabled / Empty → --c-temp-calendar-datebackground
 *         Today                      → --c-temp-calendar-todaybackgroundindicator
 *         Selected                   → --c-temp-calendar-dateselectedbackground
 *       Number text (Style 5, see flag):
 *         Default                    → --c-temp-calendar-datetextdefault
 *         Pressed                    → --c-temp-calendar-datetextpressed (also on :active of an interactive cell)
 *         Disabled                   → --c-temp-calendar-datetextdisabled
 *         Today                      → --c-temp-calendar-todaytextindicator
 *         Selected                   → --c-temp-calendar-dateselectedtext
 *         Empty                      → no number rendered (leading/trailing blank cell)
 *   Light/dark maps automatically from the token pairs.
 *
 * INTERACTIVE (additive, opt-in): pass `onClick` to make a cell a real <button> (Empty/Disabled
 * never fire). Off by default so the primitive stays prop-driven and Figma-faithful.
 *
 * FLAGS:
 *   • Internal — composed by Calendar, not for direct use.
 *   • The `_Date` extraction (uploads/components/FORMS/Calendar/_Date.json) CONFIRMS the number is
 *     Style 5 and the per-state token mapping above. State=Pressed is now built too (was previously
 *     omitted) — text --c-temp-calendar-datetextpressed, also applied on :active of an interactive cell.
 *   • ⚠️ WRONG-BRAND CONTENT in _Date.json (CLAUDE.md brand-scope flag): the source cells use the
 *     font "Lloyds Bank Jack" (NOT GT Ultra) and heritage-Lloyds GREEN literals (Date Text Default
 *     #006a4d, Today Text Indicator #006a4d, Date Text Pressed #99c3b7, Date Selected Background
 *     #024731) — i.e. the OLD Lloyds brand, not Reimagined. This component deliberately does NOT use
 *     them: it keeps GT Ultra Style 5 and the Reimagined --c-temp-calendar-* tokens (which resolve to
 *     the brand's black/grey in light, green accents in dark from lloyds_reimagined_tokens.json). The
 *     token NAMES in _Date.json map 1:1 to ours; only the wrong-brand VALUES/font are rejected. Owner
 *     to confirm the _Date master should be re-pointed to Reimagined.
 *   • 48 cell size + radius 200 are Figma literals (token:null) → 48px / --radius-full.
 *   • `onClick` interactive mode is ADDITIVE (not in the extraction); off by default.
 */

const STYLE_ID = "cnc-calendardate-styles";
const CSS = `
.cnc-cdate {
  box-sizing: border-box;
  flex: 0 0 auto;
  width: 48px; height: 48px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: var(--radius-full);          /* Figma 200 (token:null) → exact-value primitive */
  padding: 0; border: 0;
  font-family: var(--type-style5-family);
  font-size: var(--type-style5-size);
  line-height: var(--type-style5-line);
  font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking);
  text-align: center;
}
.cnc-cdate--button { cursor: pointer; -webkit-appearance: none; appearance: none; }
.cnc-cdate--button:disabled { cursor: default; }

/* Backgrounds + number colours per state */
.cnc-cdate--default  { background: var(--c-temp-calendar-datebackground); color: var(--c-temp-calendar-datetextdefault); }
.cnc-cdate--pressed  { background: var(--c-temp-calendar-datebackground); color: var(--c-temp-calendar-datetextpressed); }
.cnc-cdate--button.cnc-cdate--default:active { color: var(--c-temp-calendar-datetextpressed); }
.cnc-cdate--disabled { background: var(--c-temp-calendar-datebackground); color: var(--c-temp-calendar-datetextdisabled); }
.cnc-cdate--empty    { background: var(--c-temp-calendar-datebackground); color: transparent; }
.cnc-cdate--today    { background: var(--c-temp-calendar-todaybackgroundindicator); color: var(--c-temp-calendar-todaytextindicator); }
.cnc-cdate--selected { background: var(--c-temp-calendar-dateselectedbackground); color: var(--c-temp-calendar-dateselectedtext); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function CalendarDate({
  state = "default",
  // "empty" | "disabled" | "today" | "default" | "pressed" | "selected"
  date = "",
  // the cell number (string/number)
  onClick,
  // (date)=>void — ADDITIVE: makes the cell an interactive button
  className = "",
  ...rest
}) {
  ensureStyles();
  const isEmpty = state === "empty";
  const isDisabled = state === "disabled";
  const interactive = typeof onClick === "function" && !isEmpty;
  const cls = ["cnc-cdate", `cnc-cdate--${state}`];
  if (interactive) cls.push("cnc-cdate--button");
  if (className) cls.push(className);
  const label = isEmpty ? "" : date;
  if (interactive) {
    return /*#__PURE__*/React.createElement("button", _extends({
      type: "button",
      className: cls.join(" "),
      disabled: isDisabled,
      "aria-pressed": state === "selected",
      onClick: () => {
        if (!isDisabled) onClick(date);
      }
    }, rest), label);
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), label);
}
Object.assign(__ds_scope, { CalendarDate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Calendar/CalendarDate/CalendarDate.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Calendar/Calendar/Calendar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Calendar
 * The custom month-grid date picker (Figma "Calendar", Preset=Custom). A panel that stacks, flush
 * with no padding (totals 424px tall at 343 wide, exactly as the extraction):
 *   Header (40)  ·  Month stepper (48)  ·  Day-names row (48)  ·  5 date rows (48 each)  ·  Cancel (48)
 *
 * Anatomy (token-driven, faithful to the extraction):
 *   Root — VERTICAL, --background-panel-default, --radius-medium (Figma 16), overflow hidden so the
 *     header crown and the bottom edge clip to the radius. Width 343 (Figma fixed; `width` overrides).
 *   Header — FILL, 40h, --c-temp-calendar-headerbackground, top corners --radius-medium, 1px INSIDE
 *     --c-temp-calendar-headerborder (transparent in light, blue in dark — both from the token),
 *     10px padding (Figma literal). "Instructions" text — Style 6 / --c-temp-calendar-headertext.
 *   Month stepper — FILL H Stack, 48h:
 *     • Prev / Next — 48×48 --c-temp-calendar-monthstepperbackground squares holding a 24px chevron
 *       Icon (Arrows/Chevron Left|Right). Icon colour anchored to --text-action-inversealt01 (see flag).
 *     • Quick selection — FILL, --c-temp-calendar-quickselectionbackground, centred; the month label
 *       (Style 5 / --c-temp-calendar-quickselectiontext). The hidden 16px chevron-down affordance
 *       (visible:false in Figma) is omitted.
 *   Day names — FILL H Stack, 48h, --c-temp-calendar-daynamesbackground, SPACE_BETWEEN, 4px L/R pad;
 *     seven 48×48 cells, label VERTICALLY CENTRED (see flag), Style 6 / --c-temp-calendar-daynamestext.
 *   Date rows — FILL H Stacks, 48h, SPACE_BETWEEN, 4px L/R pad; each holds seven _Date (CalendarDate)
 *     cells driven by the `weeks` data (state + number).
 *   Cancel — the Cancara Action Button (Type=Tertiary, "Cancel"), full width, flush at the bottom.
 *
 * Every colour/spacing resolves to a token; light/dark maps automatically.
 *
 * INTERACTIVE (additive, opt-in): `onSelectDate(date)` makes each non-empty cell tappable;
 * `onPrevMonth` / `onNextMonth` make the steppers buttons; `onCancel` wires the Cancel button.
 * All off by default so the primitive stays prop-driven and Figma-faithful.
 *
 * FLAGS:
 *   • Day-name vertical alignment — the extraction top-aligns each label (48px cell, SPACE_BETWEEN,
 *     single child → top, textAlignVertical TOP), which reads unbalanced next to the centred date
 *     numbers. Per owner direction the labels are VERTICALLY CENTRED instead — a small, deliberate
 *     deviation from the literal extraction so they line up with the date cells.
 *   • Composes the private _Date (CalendarDate), Icon, and Action Button (resolved from the namespace).
 *   • Preset=NATIVE NOT BUILT — the Native variant is a single `_Calendar Configuration` (OS=iOS)
 *     instance (the iOS wheel date picker) that the extraction does NOT expand, and there is no
 *     `_Calendar Configuration.json`. Per flag-don't-guess it is deferred pending that extraction;
 *     `preset="native"` logs a one-time console flag and falls back to the Custom layout so nothing
 *     breaks. (Token --c-temp-calendar-wheelpickertext exists but no geometry does.)
 *   • Stepper chevron colour — the stepper Icon is an INSTANCE_SWAP whose fill the extraction doesn't
 *     capture, and there is no dedicated stepper-icon token. Anchored to --text-action-inversealt01
 *     (the on-action text token) because the stepper background equals --background-action-default
 *     (black in light / green in dark), so the glyph contrasts and maps light/dark. Documented choice.
 *   • Default `weeks` reproduce the Figma sample grid verbatim — including its quirk (the last row is
 *     24·25·26·27·28·28·30, i.e. a duplicated 28 and a skipped 29). Kept faithful; pass `weeks` to
 *     supply a real month.
 *   • Header/stepper/quick paddings (10) and the 48 cell size are Figma literals (token:null); the
 *     month-grid cell internals (type style) are inferred — see _Date.
 *   • Width FIXED 343 in Figma kept as the default (`width` overrides).
 */

const STYLE_ID = "cnc-calendar-styles";
const CSS = `
.cnc-cal {
  box-sizing: border-box;
  display: inline-flex; flex-direction: column; align-items: stretch;
  background: var(--background-panel-default);
  border-radius: var(--radius-medium);
  overflow: hidden;
}
.cnc-cal *, .cnc-cal *::before, .cnc-cal *::after { box-sizing: border-box; }

/* Header */
.cnc-cal__header {
  display: flex; align-items: center; justify-content: center;
  min-height: 40px; padding: 10px;                 /* Figma literals (token:null) */
  gap: 10px;
  background: var(--c-temp-calendar-headerbackground);
  border: var(--border-width-01) solid var(--c-temp-calendar-headerborder);
  border-top-left-radius: var(--radius-medium);
  border-top-right-radius: var(--radius-medium);
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--c-temp-calendar-headertext);
}

/* Month stepper */
.cnc-cal__stepper { display: flex; align-items: center; min-height: 48px; }
.cnc-cal__step {
  flex: 0 0 auto;
  width: 48px; height: 48px;
  display: inline-flex; align-items: center; justify-content: center;
  padding: 0; border: 0;
  background: var(--c-temp-calendar-monthstepperbackground);
  color: var(--text-action-inversealt01);          /* anchors the chevron (see flag) */
}
button.cnc-cal__step { cursor: pointer; -webkit-appearance: none; appearance: none; }
.cnc-cal__quick {
  flex: 1 1 auto; align-self: stretch;
  display: flex; align-items: center; justify-content: center; gap: var(--spacing-size12);
  background: var(--c-temp-calendar-quickselectionbackground);
  font-family: var(--type-style5-family);
  font-size: var(--type-style5-size);
  line-height: var(--type-style5-line);
  font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking);
  color: var(--c-temp-calendar-quickselectiontext);
}

/* Day names */
.cnc-cal__daynames {
  display: flex; align-items: flex-start; justify-content: space-between;
  min-height: 48px; padding: 0 var(--spacing-size04);
  background: var(--c-temp-calendar-daynamesbackground);
}
.cnc-cal__dayname {
  flex: 0 0 auto;
  width: 48px; height: 48px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--c-temp-calendar-daynamestext);
}

/* Date rows */
.cnc-cal__week {
  display: flex; align-items: flex-start; justify-content: space-between;
  min-height: 48px; padding: 0 var(--spacing-size04);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
const DEFAULT_DAY_NAMES = ["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"];

// Default grid — reproduces the Figma sample verbatim (incl. the 28-dup / skipped-29 quirk; flagged).
const E = {
  state: "empty"
};
const DEFAULT_WEEKS = [[E, E, E, E, E, {
  state: "disabled",
  date: 1
}, {
  state: "disabled",
  date: 2
}], [{
  state: "disabled",
  date: 3
}, {
  state: "disabled",
  date: 4
}, {
  state: "today",
  date: 5
}, {
  state: "default",
  date: 6
}, {
  state: "default",
  date: 7
}, {
  state: "default",
  date: 8
}, {
  state: "default",
  date: 9
}], [10, 11, 12, 13, 14, 15, 16].map(d => ({
  state: "default",
  date: d
})), [{
  state: "default",
  date: 17
}, {
  state: "default",
  date: 18
}, {
  state: "default",
  date: 19
}, {
  state: "default",
  date: 20
}, {
  state: "default",
  date: 21
}, {
  state: "default",
  date: 22
}, {
  state: "selected",
  date: 23
}], [24, 25, 26, 27, 28, 28, 30].map(d => ({
  state: "default",
  date: d
}))];
let _nativeFlagged = false;
function Calendar({
  preset = "custom",
  // "custom" (built) | "native" (deferred — see flag)
  instructions = "Instructions",
  month = "June 2024",
  dayNames = DEFAULT_DAY_NAMES,
  weeks = DEFAULT_WEEKS,
  cancelLabel = "Cancel",
  width = 343,
  // Figma fixed width (token:null)
  basePath = ".",
  onSelectDate,
  // (date)=>void — ADDITIVE: tappable cells
  onPrevMonth,
  // ()=>void — ADDITIVE: stepper button
  onNextMonth,
  // ()=>void — ADDITIVE: stepper button
  onCancel,
  // ()=>void — ADDITIVE: Cancel button
  className = "",
  style,
  ...rest
}) {
  ensureStyles();
  if (preset === "native" && !_nativeFlagged && typeof console !== "undefined") {
    _nativeFlagged = true;
    console.warn("[Cancara Calendar] preset=\"native\" is not built — the iOS wheel picker (_Calendar Configuration) " + "was not included in the Figma extraction. Falling back to the Custom layout.");
  }
  const DateCell = fromNS("CalendarDate", __ds_scope.CalendarDate);
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const Action = fromNS("ActionButton", __ds_scope.ActionButton);
  const Prev = typeof onPrevMonth === "function" ? "button" : "span";
  const Next = typeof onNextMonth === "function" ? "button" : "span";
  return /*#__PURE__*/React.createElement("div", _extends({
    className: ["cnc-cal", className].filter(Boolean).join(" "),
    style: {
      width: typeof width === "number" ? `${width}px` : width,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__header"
  }, instructions), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__stepper"
  }, /*#__PURE__*/React.createElement(Prev, {
    className: "cnc-cal__step",
    type: Prev === "button" ? "button" : undefined,
    "aria-label": Prev === "button" ? "Previous month" : undefined,
    onClick: onPrevMonth
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: "Arrows/Chevron Left/Chevron Left",
    size: 24,
    padding: true,
    basePath: basePath
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__quick"
  }, month), /*#__PURE__*/React.createElement(Next, {
    className: "cnc-cal__step",
    type: Next === "button" ? "button" : undefined,
    "aria-label": Next === "button" ? "Next month" : undefined,
    onClick: onNextMonth
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: "Arrows/Chevron Right/Chevron Right",
    size: 24,
    padding: true,
    basePath: basePath
  }) : null)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__daynames"
  }, dayNames.map((d, i) => /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__dayname",
    key: i
  }, d))), weeks.map((row, ri) => /*#__PURE__*/React.createElement("div", {
    className: "cnc-cal__week",
    key: ri
  }, row.map((cell, ci) => DateCell ? /*#__PURE__*/React.createElement(DateCell, {
    key: ci,
    state: cell.state || "default",
    date: cell.date,
    onClick: onSelectDate
  }) : null))), Action ? /*#__PURE__*/React.createElement(Action, {
    variant: "tertiary",
    basePath: basePath,
    onClick: onCancel
  }, cancelLabel) : null);
}
Object.assign(__ds_scope, { Calendar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Calendar/Calendar/Calendar.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Checkbox/Checkbox/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Checkbox
 * The base checkbox box (Figma "Checkbox"). Composed by Checkbox Group and Confirmation Box.
 * Matches the extraction's three axes:
 *   State           : Default | Disabled
 *   Selected        : False | True
 *   Hit target area : True (48×48 centred touch frame) | False (hugs the 32×32 box)
 *   → 2 × 2 × 2 = 8 variants.
 *
 * Anatomy (token-driven):
 *   Root — when hitTargetArea=True a FIXED 48×48 frame, content CENTER/CENTER (the 44px+ touch
 *     target); when False the root hugs the box (32×32). No fill / no stroke / no padding.
 *     • Checkbox box — 32×32, radius --c-temp-input-checkbox-cornerradius (8),
 *       border --c-temp-input-checkbox-borderwidth (1) solid:
 *         - Default · unselected : bg --background-input-default, border --border-input-default
 *         - Default · selected   : bg --c-temp-input-checkbox-backgroundactive,
 *                                  border --c-temp-input-checkbox-borderactive
 *         - Disabled (either)    : bg --background-input-disabled, border --border-input-disabled
 *         · Tick Icon (24, padding ⇒ 3px inset) — VISIBLE only when Selected=True.
 *
 * ICON COLOUR (token-driven, pinned): the tick is masked and its colour resolves to the Figma
 * checkbox tick token — Default+selected → --c-temp-input-checkbox-tickdefault; Disabled+selected
 * → --c-temp-input-checkbox-tickactivedisabled. Both map light/dark automatically.
 *
 * INTERACTIVE (additive, opt-in): pass `onChange` to make the box a real toggle button — click /
 * Space / Enter flips selection and calls onChange(next). Off by default so the DS primitive stays
 * spec-faithful (purely prop-driven `selected`). Disabled never fires.
 *
 * FLAGS:
 *   • Tick glyph — Figma supplies it via INSTANCE_SWAP (node id, no name in the extraction).
 *     Default chosen: "Action/Tick/Tick", prop-driven via `tickIcon`. Inferred — flagged.
 *   • Box radius 8 / box 32 / hit frame 48 carry token:null in Figma sizing → 32/48 are literals;
 *     the radius uses the matching --c-temp-input-checkbox-cornerradius token (8).
 *   • `onChange` interactive toggle is ADDITIVE (not in the extraction); off by default.
 */

const STYLE_ID = "cnc-checkbox-styles";
const CSS = `
.cnc-cb { box-sizing: border-box; }
.cnc-cb *, .cnc-cb *::before, .cnc-cb *::after { box-sizing: border-box; }

/* Root — hit target frame (48×48) or hug (32×32). */
.cnc-cb {
  display: inline-flex; align-items: center; justify-content: center;
  flex: 0 0 auto;
  padding: 0; border: 0; background: transparent;
}
.cnc-cb--hit { width: 48px; height: 48px; }

/* Interactive root is a real button. */
.cnc-cb--button { cursor: pointer; }
.cnc-cb--button:disabled { cursor: default; }

/* The 32×32 box. */
.cnc-cb__box {
  width: 32px; height: 32px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: var(--c-temp-input-checkbox-cornerradius);
  border-style: solid;
  border-width: var(--c-temp-input-checkbox-borderwidth);
  background: var(--background-input-default);
  border-color: var(--border-input-default);
}
.cnc-cb__box--selected {
  background: var(--c-temp-input-checkbox-backgroundactive);
  border-color: var(--c-temp-input-checkbox-borderactive);
}
.cnc-cb__box--disabled {
  background: var(--background-input-disabled);
  border-color: var(--border-input-disabled);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Checkbox({
  state = "default",
  // "default" | "disabled"
  selected = false,
  // True ⇒ tick visible + active fill/border
  hitTargetArea = true,
  // True ⇒ 48×48 touch frame; False ⇒ hug the 32×32 box
  tickIcon = "Action/Tick/Tick",
  onChange,
  // (next:boolean)=>void — ADDITIVE: makes the box an interactive toggle
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const isDisabled = state === "disabled";
  const interactive = typeof onChange === "function";

  // Pinned tick token per state.
  const tickColor = isDisabled ? "var(--c-temp-input-checkbox-tickactivedisabled)" : "var(--c-temp-input-checkbox-tickdefault)";
  const boxCls = ["cnc-cb__box"];
  if (selected) boxCls.push("cnc-cb__box--selected");
  if (isDisabled) boxCls.push("cnc-cb__box--disabled");
  const box = /*#__PURE__*/React.createElement("span", {
    className: boxCls.join(" ")
  }, selected && IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: tickIcon,
    size: 24,
    padding: true,
    color: tickColor,
    basePath: basePath
  }) : null);
  const rootCls = ["cnc-cb"];
  if (hitTargetArea) rootCls.push("cnc-cb--hit");
  if (interactive) rootCls.push("cnc-cb--button");
  if (className) rootCls.push(className);
  if (interactive) {
    return /*#__PURE__*/React.createElement("button", _extends({
      type: "button",
      className: rootCls.join(" "),
      role: "checkbox",
      "aria-checked": selected,
      "aria-disabled": isDisabled || undefined,
      disabled: isDisabled,
      onClick: () => {
        if (!isDisabled) onChange(!selected);
      }
    }, rest), box);
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: rootCls.join(" ")
  }, rest), box);
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Checkbox/Checkbox/Checkbox.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Checkbox/CheckboxGroup/CheckboxGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Checkbox Group
 * A vertical list of labelled checkbox rows under one bold group header (Figma "Checkbox Group").
 * Matches the extraction's two axes:
 *   No. of Items : 1 | 2 | 3 | 4 | 5 | 6   → derives from `items.length` (1–6 in Figma).
 *   State        : Default | Disabled       → forwarded to every nested Checkbox.
 *   → 6 × 2 = 12 variants.
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, itemSpacing --spacing-size08, left-aligned:
 *     • Label (TEXT, type Style 7 Bold, --text-input-label) — the single group header, FILL width.
 *     • Item NN (per item) — HORIZONTAL, min-height 48, itemSpacing --spacing-size12, top-aligned:
 *         · Checkbox (nested instance, Hit target area=False ⇒ hugs 32×32) wrapped with a
 *           paddingTop --spacing-size08 (the Figma instance override that drops the box to align
 *           with the text). State follows the group; per-item `selected` from the items data.
 *         · Container (FILL, paddingTop --spacing-size12) → Item text (Style 5 Regular,
 *           --text-input-default).
 *
 * COLOUR: every colour resolves to a token; light/dark maps automatically. NOTE — faithful to the
 * extraction, the header + item text keep --text-input-label / --text-input-default in BOTH states
 * (Figma does NOT swap them to a disabled token when State=Disabled — only the nested checkbox
 * boxes change). The tick colour is pinned inside Checkbox.
 *
 * INTERACTIVE (additive, opt-in): pass `onItemChange(index, next)` to make each row a real toggle
 * (forwards to the nested Checkbox's additive `onChange`). Off by default so the primitive stays
 * spec-faithful (purely prop-driven `selected`). Disabled rows never fire.
 *
 * FLAGS:
 *   • `items` accepts strings or { label, selected } — count (1–6) derives from its length, matching
 *     the Figma "No. of Items" axis. `onItemChange` interactive toggle is ADDITIVE (not in the
 *     extraction); off by default.
 *   • Item-row min-height 48 / checkbox paddingTop 8 / text paddingTop 12 carry token:null sizing in
 *     Figma → 48 is a literal; the two paddings use the matching --spacing-size08 / --spacing-size12.
 *   • Width FIXED 375 in Figma; kept as the `width` default (minWidth 343) for reuse.
 */

const STYLE_ID = "cnc-checkboxgroup-styles";
const CSS = `
.cnc-cbg { box-sizing: border-box; }
.cnc-cbg *, .cnc-cbg *::before, .cnc-cbg *::after { box-sizing: border-box; }

/* Root — vertical stack, left-aligned, 16px side padding, 8px gap. */
.cnc-cbg {
  display: flex; flex-direction: column; align-items: stretch;
  gap: var(--spacing-size08);
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}

/* Group header — Style 7 Bold. */
.cnc-cbg__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

/* Item row — horizontal, top-aligned, 12px gap, 48px min touch height. */
.cnc-cbg__item {
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size12);
  min-height: 48px;
}

/* Checkbox wrapper — Figma instance override drops the box by 8px. */
.cnc-cbg__box { flex: 0 0 auto; padding-top: var(--spacing-size08); }

/* Text container — fills, dropped 12px to sit beside the box. */
.cnc-cbg__text-wrap { flex: 1 1 auto; min-width: 0; padding-top: var(--spacing-size12); }
.cnc-cbg__text {
  margin: 0;
  font-family: var(--type-style5-family);
  font-size: var(--type-style5-size);
  line-height: var(--type-style5-line);
  font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking);
  color: var(--text-input-default);
  text-wrap: pretty;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function CheckboxGroup({
  label = "Label",
  // bold group header (Style 7)
  items = ["Item", "Item", "Item"],
  // string | { label, selected } — length = No. of Items (1–6)
  state = "default",
  // "default" | "disabled" — forwarded to every Checkbox
  tickIcon = "Action/Tick/Tick",
  onItemChange,
  // (index:number, next:boolean)=>void — ADDITIVE interactive toggle
  width = 375,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const CheckboxCmp = fromNS("Checkbox", __ds_scope.Checkbox);
  const interactive = typeof onItemChange === "function";
  const rows = (items || []).map(it => typeof it === "string" ? {
    label: it,
    selected: false
  } : {
    label: it.label,
    selected: !!it.selected
  });
  const cls = ["cnc-cbg"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cbg__label"
  }, label), rows.map((row, i) => /*#__PURE__*/React.createElement("div", {
    className: "cnc-cbg__item",
    key: i
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cbg__box"
  }, CheckboxCmp ? /*#__PURE__*/React.createElement(CheckboxCmp, {
    state: state,
    selected: row.selected,
    hitTargetArea: false,
    tickIcon: tickIcon,
    basePath: basePath,
    onChange: interactive ? next => onItemChange(i, next) : undefined
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cbg__text-wrap"
  }, /*#__PURE__*/React.createElement("p", {
    className: "cnc-cbg__text"
  }, row.label)))));
}
Object.assign(__ds_scope, { CheckboxGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Checkbox/CheckboxGroup/CheckboxGroup.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Checkbox/ConfirmationBox/ConfirmationBox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Confirmation Box
 * A single confirmation statement with a leading checkbox, wrapped in a bordered box
 * (Figma "Confirmation Box"). Composes the base Checkbox (Hit target area=False).
 * Matches the extraction's single axis:
 *   State : Default | Selected | Disabled   → 3 variants.
 * The State axis maps onto the nested Checkbox:
 *   Default  → Checkbox state=default,  selected=false
 *   Selected → Checkbox state=default,  selected=true
 *   Disabled → Checkbox state=disabled, selected=false
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — HORIZONTAL bordered box. 1px solid --border-generic-defaultalt01, cornerRadius 0
 *     (--radius-none), padding T --spacing-size04 / R --spacing-size16 / B --spacing-size24 /
 *     L --spacing-size16, itemSpacing --spacing-size12. No fill.
 *     • H Stack (FILL) — HORIZONTAL, itemSpacing --spacing-size12, top-aligned:
 *         · V Stack — paddingTop --spacing-size08 (drops the box to align with the text);
 *           Checkbox (Hit target area=False ⇒ hugs 32×32).
 *         · Wrapper (FILL) — paddingTop --spacing-size12; statement text (Style 5 Regular,
 *           --text-input-default).
 *
 * COLOUR: every colour resolves to a token; light/dark maps automatically. Faithful to the
 * extraction, the statement text keeps --text-input-default in ALL three states (Figma does NOT
 * swap it to a disabled token — only the nested checkbox box changes). The tick colour is pinned
 * inside Checkbox.
 *
 * INTERACTIVE (additive, opt-in): pass `onChange(next)` to make the box a real toggle (forwards to
 * the nested Checkbox's additive `onChange`; the whole row becomes clickable). Off by default so the
 * primitive stays spec-faithful (purely prop-driven via `state`). Disabled never fires.
 *
 * FLAGS:
 *   • Root stroke weight + cornerRadius carry token:null in Figma → mapped to the matching
 *     primitives --border-width-01 (1) / --radius-none (0).
 *   • V Stack paddingTop 8 / Wrapper paddingTop 12 carry token:null sizing in Figma → use the
 *     matching --spacing-size08 / --spacing-size12 (same pattern as Checkbox Group rows).
 *   • Width FIXED 375 in Figma; kept as the `width` default (minWidth 343) for reuse.
 *   • `onChange` interactive toggle is ADDITIVE (not in the extraction); off by default.
 */

const STYLE_ID = "cnc-confirmationbox-styles";
const CSS = `
.cnc-confbox { box-sizing: border-box; }
.cnc-confbox *, .cnc-confbox *::before, .cnc-confbox *::after { box-sizing: border-box; }

/* Root — bordered box, top-aligned row. */
.cnc-confbox {
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size12);
  border-style: solid;
  border-width: var(--border-width-01);
  border-color: var(--border-generic-defaultalt01);
  border-radius: var(--radius-none);
  padding-top: var(--spacing-size04);
  padding-right: var(--spacing-size16);
  padding-bottom: var(--spacing-size24);
  padding-left: var(--spacing-size16);
}

/* Interactive root is a real button (whole row clickable). */
.cnc-confbox--button {
  width: 100%; text-align: left; background: transparent; cursor: pointer; font: inherit;
}
.cnc-confbox--button:disabled { cursor: default; }

/* Checkbox wrapper — Figma V Stack drops the box by 8px. */
.cnc-confbox__box { flex: 0 0 auto; padding-top: var(--spacing-size08); }

/* Statement text container — fills, dropped 12px to sit beside the box. */
.cnc-confbox__text-wrap { flex: 1 1 auto; min-width: 0; padding-top: var(--spacing-size12); }
.cnc-confbox__text {
  margin: 0;
  font-family: var(--type-style5-family);
  font-size: var(--type-style5-size);
  line-height: var(--type-style5-line);
  font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking);
  color: var(--text-input-default);
  text-wrap: pretty;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ConfirmationBox({
  state = "default",
  // "default" | "selected" | "disabled"
  text = "I confirm that: Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
  tickIcon = "Action/Tick/Tick",
  onChange,
  // (next:boolean)=>void — ADDITIVE: makes the whole row an interactive toggle
  width = 375,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const CheckboxCmp = fromNS("Checkbox", __ds_scope.Checkbox);
  const isDisabled = state === "disabled";
  const isSelected = state === "selected";
  const cbState = isDisabled ? "disabled" : "default";
  const interactive = typeof onChange === "function";
  const cls = ["cnc-confbox"];
  if (interactive) cls.push("cnc-confbox--button");
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const body = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-confbox__box"
  }, CheckboxCmp ? /*#__PURE__*/React.createElement(CheckboxCmp, {
    state: cbState,
    selected: isSelected,
    hitTargetArea: false,
    tickIcon: tickIcon,
    basePath: basePath
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-confbox__text-wrap"
  }, /*#__PURE__*/React.createElement("p", {
    className: "cnc-confbox__text"
  }, text)));
  if (interactive) {
    return /*#__PURE__*/React.createElement("button", _extends({
      type: "button",
      className: cls.join(" "),
      style: rootStyle,
      role: "checkbox",
      "aria-checked": isSelected,
      "aria-disabled": isDisabled || undefined,
      disabled: isDisabled,
      onClick: () => {
        if (!isDisabled) onChange(!isSelected);
      }
    }, rest), body);
  }
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), body);
}
Object.assign(__ds_scope, { ConfirmationBox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Checkbox/ConfirmationBox/ConfirmationBox.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/RadioButton/RadioButton/RadioButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Radio Button
 * The base radio control (Figma "Radio Button"). Composed by Radio Button Group.
 * Matches the extraction's three axes:
 *   State           : Default | Disabled
 *   Selected        : False | True
 *   Hit target area : True (48×48 centred touch frame) | False (hugs the 32×32 circle)
 *   → 2 × 2 × 2 = 8 variants.
 *
 * Anatomy (token-driven):
 *   Root — when hitTargetArea=True a FIXED 48×48 frame, content CENTER/CENTER (the 44px+ touch
 *     target); when False the root hugs the circle (32×32). No fill / no stroke / no padding.
 *     • Radio circle — 32×32, fully round (--radius-circle = 9999, the exact Figma value),
 *       border --c-temp-input-radiobutton-borderwidth (1) solid:
 *         - Default · unselected : bg --background-input-default, border --border-input-default
 *         - Default · selected   : bg --c-temp-input-radiobutton-backgroundactive,
 *                                  border --c-temp-input-radiobutton-borderactive
 *         - Disabled (either)    : bg --background-input-disabled, border --border-input-disabled
 *         · Dot (12×12 filled circle) — VISIBLE only when Selected=True:
 *             - Default + selected  → --c-temp-input-radiobutton-dotdefault
 *             - Disabled + selected → --c-temp-input-radiobutton-dotactivedisabled
 *           (Both map light/dark automatically. The dot is a SHAPE, not a glyph — no Icon/mask.)
 *
 * INTERACTIVE (additive, opt-in): pass `onChange` to make the control a real radio button — click /
 * Space / Enter selects it and calls onChange(true). Off by default so the DS primitive stays
 * spec-faithful (purely prop-driven `selected`). Disabled never fires. Radio Button Group drives
 * mutual exclusion; a lone interactive Radio only ever turns itself on.
 *
 * FLAGS:
 *   • Circle 32 / hit frame 48 / dot 12 carry token:null in Figma sizing → written as literals
 *     (same convention as Checkbox). The circle's 9999 corner-radius is also token:null in Figma;
 *     rendered with the exact-value primitive --radius-circle (9999px) to stay 100% token-driven.
 *   • Border width: strokeWeightToken null in Figma; uses --c-temp-input-radiobutton-borderwidth
 *     (1px), the matching component token (same pattern as Checkbox).
 *   • `onChange` interactive select is ADDITIVE (not in the extraction); off by default.
 */

const STYLE_ID = "cnc-radio-styles";
const CSS = `
.cnc-rb { box-sizing: border-box; }
.cnc-rb *, .cnc-rb *::before, .cnc-rb *::after { box-sizing: border-box; }

/* Root — hit target frame (48×48) or hug (32×32). */
.cnc-rb {
  display: inline-flex; align-items: center; justify-content: center;
  flex: 0 0 auto;
  padding: 0; border: 0; background: transparent;
}
.cnc-rb--hit { width: 48px; height: 48px; }

/* Interactive root is a real button. */
.cnc-rb--button { cursor: pointer; }
.cnc-rb--button:disabled { cursor: default; }

/* The 32×32 circle. */
.cnc-rb__circle {
  width: 32px; height: 32px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: var(--radius-circle);
  border-style: solid;
  border-width: var(--c-temp-input-radiobutton-borderwidth);
  background: var(--background-input-default);
  border-color: var(--border-input-default);
}
.cnc-rb__circle--selected {
  background: var(--c-temp-input-radiobutton-backgroundactive);
  border-color: var(--c-temp-input-radiobutton-borderactive);
}
.cnc-rb__circle--disabled {
  background: var(--background-input-disabled);
  border-color: var(--border-input-disabled);
}

/* The 12×12 dot (shape, not a glyph). */
.cnc-rb__dot {
  width: 12px; height: 12px;
  border-radius: var(--radius-circle);
  background: var(--c-temp-input-radiobutton-dotdefault);
}
.cnc-rb__dot--disabled {
  background: var(--c-temp-input-radiobutton-dotactivedisabled);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function RadioButton({
  state = "default",
  // "default" | "disabled"
  selected = false,
  // True ⇒ dot visible + active fill/border
  hitTargetArea = true,
  // True ⇒ 48×48 touch frame; False ⇒ hug the 32×32 circle
  onChange,
  // (next:true)=>void — ADDITIVE: makes the control an interactive radio
  className = "",
  ...rest
}) {
  ensureStyles();
  const isDisabled = state === "disabled";
  const interactive = typeof onChange === "function";
  const circleCls = ["cnc-rb__circle"];
  if (selected) circleCls.push("cnc-rb__circle--selected");
  if (isDisabled) circleCls.push("cnc-rb__circle--disabled");
  const dotCls = ["cnc-rb__dot"];
  if (isDisabled) dotCls.push("cnc-rb__dot--disabled");
  const circle = /*#__PURE__*/React.createElement("span", {
    className: circleCls.join(" ")
  }, selected ? /*#__PURE__*/React.createElement("span", {
    className: dotCls.join(" ")
  }) : null);
  const rootCls = ["cnc-rb"];
  if (hitTargetArea) rootCls.push("cnc-rb--hit");
  if (interactive) rootCls.push("cnc-rb--button");
  if (className) rootCls.push(className);
  if (interactive) {
    return /*#__PURE__*/React.createElement("button", _extends({
      type: "button",
      className: rootCls.join(" "),
      role: "radio",
      "aria-checked": selected,
      "aria-disabled": isDisabled || undefined,
      disabled: isDisabled,
      onClick: () => {
        if (!isDisabled && !selected) onChange(true);
      }
    }, rest), circle);
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: rootCls.join(" ")
  }, rest), circle);
}
Object.assign(__ds_scope, { RadioButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/RadioButton/RadioButton/RadioButton.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/RadioButton/RadioButtonGroup/RadioButtonGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Radio Button Group
 * A vertical list of labelled radio rows under one bold group header (Figma "Radio Button Group").
 * Matches the extraction's two axes:
 *   No. of Items : 1 | 2 | 3 | 4 | 5 | 6   → derives from `items.length` (1–6 in Figma).
 *   State        : Default | Disabled       → forwarded to every nested Radio Button.
 *   → 6 × 2 = 12 variants.
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, itemSpacing --spacing-size08, left-aligned:
 *     • H Stack (header row, HORIZONTAL) → Label (TEXT, type Style 7 Bold, --text-input-label, FILL).
 *     • Item NN (per item) — HORIZONTAL, min-height 48, itemSpacing --spacing-size12, top-aligned:
 *         · Radio Button (nested instance, Hit target area=False ⇒ hugs the 32×32 circle) wrapped
 *           with a paddingTop --spacing-size08 (the Figma instance override that drops the control to
 *           align with the text). State follows the group; per-item `selected` from the items data.
 *         · Container (FILL, paddingTop --spacing-size12) → Item text (Style 5 Regular,
 *           --text-input-default).
 *
 * COLOUR: every colour resolves to a token; light/dark maps automatically. NOTE — faithful to the
 * extraction, the header + item text keep --text-input-label / --text-input-default in BOTH states
 * (Figma does NOT swap them to a disabled token when State=Disabled — only the nested radio control
 * changes). The dot colour is pinned inside Radio Button.
 *
 * INTERACTIVE (additive, opt-in): pass `onChange(index)` to make the rows mutually-exclusive radios
 * (selecting a row forwards to the nested Radio Button's additive `onChange`). Off by default so the
 * primitive stays spec-faithful (purely prop-driven per-item `selected`). Disabled rows never fire.
 *
 * FLAGS:
 *   • `items` accepts strings or { label, selected } — count (1–6) derives from its length, matching
 *     the Figma "No. of Items" axis. `onChange` interactive single-select is ADDITIVE (not in the
 *     extraction); off by default. Radios are mutually exclusive — only one row may be `selected`.
 *   • Item-row min-height 48 / radio paddingTop 8 / text paddingTop 12 carry token:null sizing in
 *     Figma → 48 is a literal; the two paddings use the matching --spacing-size08 / --spacing-size12.
 *   • Width FIXED 375 in Figma; kept as the `width` default (minWidth 343) for reuse.
 */

const STYLE_ID = "cnc-radiogroup-styles";
const CSS = `
.cnc-rbg { box-sizing: border-box; }
.cnc-rbg *, .cnc-rbg *::before, .cnc-rbg *::after { box-sizing: border-box; }

/* Root — vertical stack, left-aligned, 16px side padding, 8px gap. */
.cnc-rbg {
  display: flex; flex-direction: column; align-items: stretch;
  gap: var(--spacing-size08);
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}

/* Header row (H Stack) — left-aligned. */
.cnc-rbg__header { display: flex; flex-direction: row; align-items: center; }

/* Group header — Style 7 Bold. */
.cnc-rbg__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
  flex: 1 1 auto;
}

/* Item row — horizontal, top-aligned, 12px gap, 48px min touch height. */
.cnc-rbg__item {
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size12);
  min-height: 48px;
}

/* Radio wrapper — Figma instance override drops the control by 8px. */
.cnc-rbg__control { flex: 0 0 auto; padding-top: var(--spacing-size08); }

/* Text container — fills, dropped 12px to sit beside the control. */
.cnc-rbg__text-wrap { flex: 1 1 auto; min-width: 0; padding-top: var(--spacing-size12); }
.cnc-rbg__text {
  margin: 0;
  font-family: var(--type-style5-family);
  font-size: var(--type-style5-size);
  line-height: var(--type-style5-line);
  font-weight: var(--type-style5-weight);
  letter-spacing: var(--type-style5-tracking);
  color: var(--text-input-default);
  text-wrap: pretty;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function RadioButtonGroup({
  label = "Label",
  // bold group header (Style 7)
  items = ["Item", "Item", "Item"],
  // string | { label, selected } — length = No. of Items (1–6)
  state = "default",
  // "default" | "disabled" — forwarded to every Radio Button
  onChange,
  // (index:number)=>void — ADDITIVE: interactive mutually-exclusive select
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const RadioCmp = fromNS("RadioButton", __ds_scope.RadioButton);
  const interactive = typeof onChange === "function";
  const rows = (items || []).map(it => typeof it === "string" ? {
    label: it,
    selected: false
  } : {
    label: it.label,
    selected: !!it.selected
  });
  const cls = ["cnc-rbg"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle,
    role: "radiogroup",
    "aria-label": label
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-rbg__header"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-rbg__label"
  }, label)), rows.map((row, i) => /*#__PURE__*/React.createElement("div", {
    className: "cnc-rbg__item",
    key: i
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-rbg__control"
  }, RadioCmp ? /*#__PURE__*/React.createElement(RadioCmp, {
    state: state,
    selected: row.selected,
    hitTargetArea: false,
    onChange: interactive ? () => onChange(i) : undefined
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-rbg__text-wrap"
  }, /*#__PURE__*/React.createElement("p", {
    className: "cnc-rbg__text"
  }, row.label)))));
}
Object.assign(__ds_scope, { RadioButtonGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/RadioButton/RadioButtonGroup/RadioButtonGroup.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Switch/SwitchConfiguration/SwitchConfiguration.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara _Switch Configuration — INTERNAL (private) component. Figma marks it `_` (hidden from the
 * Assets panel); it is composed by Switch, not placed directly, and stays exported only so Switch can
 * resolve it from the namespace. Renders the platform switch chrome (track + handle) for one variant.
 *
 * Matches the extraction's three axes:
 *   OS       : iOS | iPadOS (identical geometry) | Android (Material thumb)
 *   Selected : Off | On
 *   State    : Default | Disabled
 *   → 3 × 2 × 2 = 12 variants.
 *
 * Anatomy (token-driven):
 *   iOS / iPadOS (51×48 hug):
 *     • Track  — 51×31 pill (radius 100, Figma literal), 1px INSIDE border (--border-width-01).
 *     • Handle — 27×27 round, inset 2px; sits left (Off) / right (On, +20px travel).
 *       Off·Default  : track --c-temp-switch-trackbackgrounddefault / border --…-trackborderdefault,
 *                      handle --…-handledefault
 *       On·Default   : track --…-trackbackgroundselecteddefault / border --…-trackborderselected,
 *                      handle --…-handleselected
 *       Off·Disabled : track --…-trackbackgrounddisabled / border --…-trackborderdisabled,
 *                      handle --…-handledisabled
 *       On·Disabled  : track --…-trackbackgroundselecteddisabled / border --…-trackborderselecteddisabled,
 *                      handle --…-handleselecteddisabled
 *   Android (48×48 hug):
 *     • Wrapper 34×20 · Track 34×14 (radius 7, Figma literal, no border) · Handle 20×20 round
 *       (--radius-circle), left (Off) / right (On, +14px travel).
 *       Off·Default  : track --c-temp-switch-trackbackgrounddefaultalt01, handle --…-handledefault
 *       On·Default   : track --…-trackbackgroundselecteddefault,         handle --…-handleselected
 *       Off·Disabled : track --…-trackbackgrounddisabled,                handle --…-handledisabled
 *       On·Disabled  : track --…-trackbackgroundselecteddisabled,        handle --…-handleselecteddisabled
 *
 * COLOUR: every fill/border resolves to a --c-temp-switch-* token; light/dark maps automatically.
 *
 * FLAGS:
 *   • Internal — composed by Switch, not for direct use.
 *   • ⚠️ AUDIT — NON-TOKEN HARDCODED HEX (authorised exception, Switch only, 2026-06-24).
 *     The Figma handle elevation (3 DROP_SHADOW effects per handle, + the Android "Elevation" VECTOR)
 *     has NO design token — the colours are raw hex-with-alpha and the token set contains no
 *     --c-temp-switch-*shadow token. Per explicit owner instruction these literal shadow values are
 *     reproduced VERBATIM from the extraction for THIS component only; this is a deliberate, flagged
 *     deviation from the project's no-hardcoded-hex rule (the rule itself is unchanged). The shadows
 *     are faithful to the extraction: they render ONLY in State=Default — in Disabled the iOS effects
 *     are visible:false and the Android Elevation vector is absent, so no shadow is drawn there.
 *       iOS / iPadOS handle: 0 3px 1px #0000000f, 0 3px 8px #00000026, 0 0 0 1px #0000000a
 *       Android handle:      0 1px 3px #00000033, 0 2px 1px #0000001f, 0 1px 1px #00000024
 *   • The Android "Pulse" ripple (40×40) is invisible (empty fill) in every static state → omitted.
 *   • Sizing is token:null in Figma → literals: iOS track 51×31 / handle 27 / travel 20 / inset 2;
 *     Android wrapper 34×20 / track 34×14 / handle 20 / travel 14. Track radii 100 (iOS) & 7 (Android)
 *     are Figma literals (token:null); the Android thumb uses the exact --radius-circle primitive.
 *   • The toggle TRANSITION is ADDITIVE (the extraction is static) — eased so the parent Switch slides
 *     smoothly; disabled under prefers-reduced-motion.
 */

const STYLE_ID = "cnc-switchconfig-styles";
const CSS = `
.cnc-sw { box-sizing: border-box; flex: 0 0 auto; }
.cnc-sw *, .cnc-sw *::before, .cnc-sw *::after { box-sizing: border-box; }

/* ---- iOS / iPadOS (identical) ---- */
.cnc-sw--ios {
  position: relative; width: 51px; height: 48px;
  display: inline-flex; align-items: center; justify-content: center;
}
.cnc-sw__track-ios {
  position: relative; width: 51px; height: 31px;
  border-radius: 100px;                       /* Figma literal (token:null) */
  border: var(--border-width-01) solid transparent;
}
.cnc-sw__handle-ios {
  position: absolute; top: 50%; left: 2px;
  width: 27px; height: 27px; border-radius: 100px;
  transform: translateY(-50%);
}
.cnc-sw--ios.cnc-sw--on .cnc-sw__handle-ios { transform: translate(20px, -50%); }

/* ---- Android (Material) ---- */
.cnc-sw--android {
  position: relative; width: 48px; height: 48px;
  display: inline-flex; align-items: center; justify-content: center;
}
.cnc-sw__wrap { position: relative; width: 34px; height: 20px; }
.cnc-sw__track-android {
  position: absolute; top: 3px; left: 0;
  width: 34px; height: 14px; border-radius: 7px;   /* Figma literal (token:null) */
}
.cnc-sw__handle-android {
  position: absolute; top: 0; left: 0;
  width: 20px; height: 20px; border-radius: var(--radius-circle);
}
.cnc-sw--android.cnc-sw--on .cnc-sw__handle-android { transform: translateX(14px); }

/* ---- Additive transition (extraction is static) — off under reduced-motion ---- */
@media (prefers-reduced-motion: no-preference) {
  .cnc-sw__handle-ios, .cnc-sw__handle-android,
  .cnc-sw__track-ios, .cnc-sw__track-android {
    transition: transform .18s ease, background-color .18s ease, border-color .18s ease;
  }
}

/* ===== iOS / iPadOS — track ===== */
.cnc-sw--ios.cnc-sw--off.cnc-sw--default  .cnc-sw__track-ios { background: var(--c-temp-switch-trackbackgrounddefault); border-color: var(--c-temp-switch-trackborderdefault); }
.cnc-sw--ios.cnc-sw--on.cnc-sw--default   .cnc-sw__track-ios { background: var(--c-temp-switch-trackbackgroundselecteddefault); border-color: var(--c-temp-switch-trackborderselected); }
.cnc-sw--ios.cnc-sw--off.cnc-sw--disabled .cnc-sw__track-ios { background: var(--c-temp-switch-trackbackgrounddisabled); border-color: var(--c-temp-switch-trackborderdisabled); }
.cnc-sw--ios.cnc-sw--on.cnc-sw--disabled  .cnc-sw__track-ios { background: var(--c-temp-switch-trackbackgroundselecteddisabled); border-color: var(--c-temp-switch-trackborderselecteddisabled); }

/* ===== iOS / iPadOS — handle ===== */
.cnc-sw--ios.cnc-sw--off.cnc-sw--default  .cnc-sw__handle-ios { background: var(--c-temp-switch-handledefault); }
.cnc-sw--ios.cnc-sw--on.cnc-sw--default   .cnc-sw__handle-ios { background: var(--c-temp-switch-handleselected); }
.cnc-sw--ios.cnc-sw--off.cnc-sw--disabled .cnc-sw__handle-ios { background: var(--c-temp-switch-handledisabled); }
.cnc-sw--ios.cnc-sw--on.cnc-sw--disabled  .cnc-sw__handle-ios { background: var(--c-temp-switch-handleselecteddisabled); }

/* ===== Android — track ===== */
.cnc-sw--android.cnc-sw--off.cnc-sw--default  .cnc-sw__track-android { background: var(--c-temp-switch-trackbackgrounddefaultalt01); }
.cnc-sw--android.cnc-sw--on.cnc-sw--default   .cnc-sw__track-android { background: var(--c-temp-switch-trackbackgroundselecteddefault); }
.cnc-sw--android.cnc-sw--off.cnc-sw--disabled .cnc-sw__track-android { background: var(--c-temp-switch-trackbackgrounddisabled); }
.cnc-sw--android.cnc-sw--on.cnc-sw--disabled  .cnc-sw__track-android { background: var(--c-temp-switch-trackbackgroundselecteddisabled); }

/* ===== Android — handle ===== */
.cnc-sw--android.cnc-sw--off.cnc-sw--default  .cnc-sw__handle-android { background: var(--c-temp-switch-handledefault); }
.cnc-sw--android.cnc-sw--on.cnc-sw--default   .cnc-sw__handle-android { background: var(--c-temp-switch-handleselected); }
.cnc-sw--android.cnc-sw--off.cnc-sw--disabled .cnc-sw__handle-android { background: var(--c-temp-switch-handledisabled); }
.cnc-sw--android.cnc-sw--on.cnc-sw--disabled  .cnc-sw__handle-android { background: var(--c-temp-switch-handleselecteddisabled); }

/* =============================================================================
   ⚠️ AUDIT FLAG — NON-TOKEN HARDCODED HEX (authorised exception, Switch ONLY)
   The handle elevation has no design token (raw hex-with-alpha; no switch-shadow
   token exists). Per owner instruction (2026-06-24) the Figma DROP_SHADOW values
   are reproduced verbatim for this component only — a deliberate, flagged break
   from the no-hardcoded-hex rule, which is otherwise unchanged. Faithful to the
   extraction: shadows render ONLY in State=Default (in Disabled the iOS effects
   are visible:false and the Android Elevation vector is absent → no shadow).
   These literals do not theme (no light/dark token); same in both themes per Figma.
   ============================================================================= */
.cnc-sw--ios.cnc-sw--default .cnc-sw__handle-ios {
  box-shadow: 0 3px 1px #0000000f, 0 3px 8px #00000026, 0 0 0 1px #0000000a;
}
.cnc-sw--android.cnc-sw--default .cnc-sw__handle-android {
  box-shadow: 0 1px 3px #00000033, 0 2px 1px #0000001f, 0 1px 1px #00000024;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function SwitchConfiguration({
  os = "ios",
  // "ios" | "ipados" | "android"  (ios & ipados render identically)
  selected = false,
  // Off | On
  state = "default",
  // "default" | "disabled"
  className = "",
  ...rest
}) {
  ensureStyles();
  const isAndroid = os === "android";
  const cls = ["cnc-sw", isAndroid ? "cnc-sw--android" : "cnc-sw--ios", selected ? "cnc-sw--on" : "cnc-sw--off", state === "disabled" ? "cnc-sw--disabled" : "cnc-sw--default"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), isAndroid ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-sw__wrap"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-sw__track-android"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sw__handle-android"
  })) : /*#__PURE__*/React.createElement("span", {
    className: "cnc-sw__track-ios"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-sw__handle-ios"
  })));
}
Object.assign(__ds_scope, { SwitchConfiguration });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Switch/SwitchConfiguration/SwitchConfiguration.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Switch/Switch/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Switch
 * The public switch (Figma "↳ Switch"): a single COMPONENT that composes one _Switch Configuration
 * instance (default OS=iOS, Selected=Off, State=Default) inside a hug VERTICAL frame. Forwards the
 * OS / Selected / State props to the nested configuration.
 *
 * Anatomy (token-driven, faithful to the extraction):
 *   Root — VERTICAL, primary/counter axis MIN (top-left), HUG, no padding / no border / no fill.
 *     • Configuration (nested _Switch Configuration) — renders the platform chrome.
 *
 * INTERACTIVE (additive, opt-in): pass `onChange` to make it a real switch — click / Space / Enter
 * toggles and calls onChange(next:boolean). Off by default so the primitive stays spec-faithful
 * (purely prop-driven `selected`). Disabled never fires.
 *
 * FLAGS:
 *   • Composes the private _Switch Configuration (resolved from the namespace).
 *   • `onChange` interactive toggle is ADDITIVE (not in the extraction); off by default.
 *   • Elevation / ripple omission, sizing literals, and the additive transition are documented on
 *     _Switch Configuration.
 */

const STYLE_ID = "cnc-switch-styles";
const CSS = `
.cnc-switch {
  box-sizing: border-box;
  display: inline-flex; flex-direction: column; align-items: flex-start;
  padding: 0; border: 0; background: transparent;
}
.cnc-switch--button { cursor: pointer; }
.cnc-switch--button:disabled { cursor: default; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Switch({
  os = "ios",
  // "ios" | "ipados" | "android" — forwarded to the configuration
  selected = false,
  // Off | On
  state = "default",
  // "default" | "disabled"
  onChange,
  // (next:boolean)=>void — ADDITIVE: makes it an interactive switch
  className = "",
  ...rest
}) {
  ensureStyles();
  const Config = fromNS("SwitchConfiguration", __ds_scope.SwitchConfiguration);
  const isDisabled = state === "disabled";
  const interactive = typeof onChange === "function";
  const inner = Config ? /*#__PURE__*/React.createElement(Config, {
    os: os,
    selected: selected,
    state: state
  }) : null;
  const cls = ["cnc-switch"];
  if (interactive) cls.push("cnc-switch--button");
  if (className) cls.push(className);
  if (interactive) {
    return /*#__PURE__*/React.createElement("button", _extends({
      type: "button",
      className: cls.join(" "),
      role: "switch",
      "aria-checked": selected,
      "aria-disabled": isDisabled || undefined,
      disabled: isDisabled,
      onClick: () => {
        if (!isDisabled) onChange(!selected);
      }
    }, rest), inner);
  }
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), inner);
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Switch/Switch/Switch.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/CalendarPicker/CalendarPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Calendar Picker
 * A date-entry trigger (Figma "Calendar Picker"): a bordered box (radius 12, min-height 56) holding
 * a date value + a calendar glyph, with a hint line beneath. The extraction defines only the closed
 * TRIGGER — there is NO embedded Calendar / option surface in the Figma set, so none is invented here
 * (an optional `calendar` slot is exposed for consumers who want to drop the real Calendar in). Axes:
 *   Layout : Default | Inline
 *     • Default — top "When" label (Style 7 Bold) above a full-width box; the calendar Icon (24) sits
 *       INSIDE the box at the trailing edge; a Hint line sits beneath the box.
 *     • Inline — no top label; a left "When :" prefix + masked date inside the box, with a SEPARATE
 *       56×56 icon button (holding the calendar Icon) to the right; the Hint sits in a second row,
 *       reserving a 56px column under the icon button.
 *   State  : Default | Active | Disabled   (6 variants total — NO Error in this set).
 *
 * Per state (faithful to the extraction):
 *   • Default  — 1px --border-input-default; calendar icon shown; hint shown.
 *   • Active   — 2px --border-input-active; calendar icon shown; hint shown.
 *   • Disabled — bg --background-input-disabled, 1px --border-input-disabled, text
 *                --text-input-disabled; CALENDAR ICON REMOVED (no Icon child in either Disabled
 *                variant); HINT REMOVED (no hint row when disabled).
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, gap --spacing-size08.
 *   DEFAULT layout:
 *     • "When" label (Style 7 Bold, --text-input-label — NOT dimmed when disabled, faithful).
 *     • V Stack → Input box (border per state, radius --c-input-cornerradius, padding
 *       --spacing-size16, gap --spacing-size08, min-height --c-input-minheight):
 *         - date value (Style 6, FILL, --text-input-default / disabled --text-input-disabled)
 *         - Calendar Icon (24) — pinned --c-temp-input-iconaction; hidden when disabled.
 *         - Border underline (RECTANGLE h2) — --c-temp-input-borderunderline / …active (transparent).
 *       → Hint line beneath (shown unless disabled).
 *   INLINE layout (no top label):
 *     • H Stack (HORIZONTAL):
 *         - Input box (FILL): inner H Stack (gap --spacing-size04) → [ "When" + ":" group
 *           (--c-temp-input-leftlabel / disabled --text-input-disabled) , masked date
 *           (--c-temp-input-textmasked / disabled --text-input-disabled) ] + Border underline.
 *         - Icon button (56×56, bg --c-temp-input-iconactionbackground transparent) → calendar Icon
 *           (24, --c-temp-input-iconaction). Hidden when disabled (no icon button child).
 *     • Hint row (H Stack): hint line (fills) + 56px empty column under the icon button.
 *
 * INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: the trigger is a real <button>
 * that toggles Default⇄Active on click (the only two non-disabled states the extraction defines) and
 * fires `onToggle(open)` / `onClick`. If a `calendar` slot is supplied, it is revealed beneath the
 * field while open. Off by default; disabled triggers never toggle.
 *
 * ICON COLOUR (token-driven mask recolour): the calendar glyph is pinned to the dedicated input
 * action-icon token --c-temp-input-iconaction (light/dark maps automatically).
 *
 * FLAGS:
 *   • NOT composed of Text Field / Calendar — the build plan said "composes Calendar + Text Field",
 *     but the extraction builds the Input chrome INLINE (FRAME, not a Text Field INSTANCE) and embeds
 *     NO Calendar surface → built self-contained, chrome mirrors the shared Input tokens for family
 *     consistency (same pattern as Sort Code / Password / MI Field / Global Search / Dropdown / Country
 *     pickers). An optional `calendar` ReactNode slot honours the "composes Calendar" intent without
 *     inventing UI (renders nothing by default; shown only when supplied + interactive + open).
 *   • Calendar glyph supplied via INSTANCE_SWAP node ID (no captured fill in the extraction) → name
 *     defaulted to "Date & time/Calendar/Calendar", colour pinned --c-temp-input-iconaction. Inferred.
 *   • Calendar icon REMOVED in Disabled (both layouts); Hint REMOVED in Disabled — faithful.
 *   • Hint sub-component — the Figma "Hint message" instance (Type=Hint) was NOT expanded → internals
 *     inferred: a single Style 8 line, --text-generic-subdued, with a --spacing-size04 top gap (the
 *     V Stack itemSpacing is 0; gap inferred, matching Text Field). The Figma sample copy is lorem
 *     placeholder → NOT shipped; the hint renders only when `hint` is supplied (Text Field precedent).
 *   • Input radius 12 / min-height 56 carry token:null in Figma → bound to --c-input-cornerradius /
 *     --c-input-minheight (the shared Input values).
 *   • Inline second-row hint H Stack is FIXED 343 + a 56px icon button in Figma (overflows) → rendered
 *     as a filling hint + a 56px reserved column, faithful to the intent.
 *   • Width — Figma frame FIXED 375; kept as the `width` prop default for reuse.
 *   • Interactive / onToggle / onClick / calendar slot — ADDITIVE behaviour, not in the extraction.
 */

const STYLE_ID = "cnc-calendarpicker-styles";
const CSS = `
.cnc-cp { box-sizing: border-box; }
.cnc-cp *, .cnc-cp *::before, .cnc-cp *::after { box-sizing: border-box; }

.cnc-cp {
  display: flex; flex-direction: column; align-items: stretch;
  gap: var(--spacing-size08);
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}

/* Top label (Default layout) — Style 7 Bold. */
.cnc-cp__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

/* V Stack (Default) — input box stacked above the hint, no gap (Figma itemSpacing 0). */
.cnc-cp__vstack { display: flex; flex-direction: column; align-items: stretch; }

/* Row that holds the input box (+ icon button in Inline). */
.cnc-cp__row { display: flex; flex-direction: row; align-items: flex-start; }

/* Input box. */
.cnc-cp__input {
  position: relative;
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
  text-align: left;
  font: inherit;
}
button.cnc-cp__input { cursor: pointer; }
button.cnc-cp__input:disabled { cursor: default; }
.cnc-cp__input--active { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-cp__input--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Date value — Style 6. */
.cnc-cp__value {
  flex: 1 1 auto; min-width: 0;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
}
.cnc-cp__value--masked   { color: var(--c-temp-input-textmasked); }
.cnc-cp__value--disabled { color: var(--text-input-disabled); }

/* Inline: left "When :" group + masked date, gap --spacing-size04. */
.cnc-cp__inline {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size04);
}
.cnc-cp__leftlabel {
  flex: 0 0 auto;
  display: inline-flex; flex-direction: row; align-items: center;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--c-temp-input-leftlabel);
  white-space: nowrap;
}
.cnc-cp__leftlabel--disabled { color: var(--text-input-disabled); }

/* Calendar glyph — Default layout sits inline at the trailing edge. */
.cnc-cp__cal { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Inline icon button — 56×56 transparent frame holding the calendar glyph. */
.cnc-cp__iconbtn {
  flex: 0 0 auto;
  width: 56px; height: 56px;
  display: inline-flex; align-items: center; justify-content: center;
  padding: 0; margin: 0; border: 0;
  background: var(--c-temp-input-iconactionbackground);
}
button.cnc-cp__iconbtn { cursor: pointer; font: inherit; }
button.cnc-cp__iconbtn:disabled { cursor: default; }

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-cp__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-cp__underline--active { background: var(--c-temp-input-borderunderlineactive); }

/* Hint line (sub-component internals inferred — see FLAGS): Style 8, top gap --spacing-size04. */
.cnc-cp__hint {
  margin-top: var(--spacing-size04);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-generic-subdued);
  text-wrap: pretty;
}

/* Inline hint row — hint fills, a 56px column reserves the icon-button slot. */
.cnc-cp__hintrow { display: flex; flex-direction: row; align-items: flex-start; }
.cnc-cp__hintrow .cnc-cp__hint { flex: 1 1 auto; min-width: 0; }
.cnc-cp__hintspacer { flex: 0 0 auto; width: 56px; }

/* Optional calendar slot (additive). */
.cnc-cp__calendarslot { margin-top: var(--spacing-size08); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function CalendarPicker({
  layout = "default",
  // "default" | "inline"
  state = "default",
  // "default" | "active" | "disabled"
  label = "When",
  // top label (Default layout)
  inlineLabel = "When",
  // left label (Inline layout)
  value = "17 Sept 2021",
  // displayed date text
  hint = "",
  // hint line (Figma shows lorem placeholder; rendered only when supplied)
  icon = "Date & time/Calendar/Calendar",
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  calendar = null,
  // optional ReactNode dropped beneath when interactive + open
  onToggle,
  // (open: boolean) => void
  onClick,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const {
    useState
  } = React;
  const [open, setOpen] = useState(false);
  const isInline = layout === "inline";
  const isDisabled = state === "disabled";

  // Interactive derives Active from the open flag; presentational uses `state`.
  let effState = state;
  if (interactive && !isDisabled) effState = open ? "active" : "default";
  const isActive = effState === "active";
  const inputCls = ["cnc-cp__input"];
  if (isActive) inputCls.push("cnc-cp__input--active");
  if (isDisabled) inputCls.push("cnc-cp__input--disabled");
  const handleToggle = () => {
    if (isDisabled) return;
    const next = !open;
    setOpen(next);
    if (typeof onToggle === "function") onToggle(next);
    if (typeof onClick === "function") onClick();
  };

  // Calendar glyph — hidden when disabled (faithful to the extraction).
  const calIcon = !isDisabled && IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 24,
    color: "var(--c-temp-input-iconaction)",
    basePath: basePath
  }) : null;

  // Value text colour: Default → --text-input-default; Inline → --c-temp-input-textmasked; either →
  // --text-input-disabled when disabled.
  const valueCls = ["cnc-cp__value"];
  if (isDisabled) valueCls.push("cnc-cp__value--disabled");else if (isInline) valueCls.push("cnc-cp__value--masked");
  const underlineCls = ["cnc-cp__underline"];
  if (isActive) underlineCls.push("cnc-cp__underline--active");
  const InputTag = interactive ? "button" : "div";
  const inputProps = interactive ? {
    type: "button",
    onClick: handleToggle,
    disabled: isDisabled,
    "aria-haspopup": "dialog",
    "aria-expanded": open
  } : {};
  const llCls = ["cnc-cp__leftlabel"];
  if (isDisabled) llCls.push("cnc-cp__leftlabel--disabled");
  const showHint = !isDisabled && typeof hint === "string" && hint.length > 0;

  // ---- Input box contents ----
  let inputInner;
  if (isInline) {
    inputInner = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cnc-cp__inline"
    }, /*#__PURE__*/React.createElement("span", {
      className: llCls.join(" ")
    }, /*#__PURE__*/React.createElement("span", null, inlineLabel), /*#__PURE__*/React.createElement("span", null, ":")), /*#__PURE__*/React.createElement("span", {
      className: valueCls.join(" ")
    }, value)), /*#__PURE__*/React.createElement("span", {
      className: underlineCls.join(" "),
      "aria-hidden": "true"
    }));
  } else {
    inputInner = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: valueCls.join(" ")
    }, value), calIcon ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-cp__cal"
    }, calIcon) : null, /*#__PURE__*/React.createElement("span", {
      className: underlineCls.join(" "),
      "aria-hidden": "true"
    }));
  }

  // ---- Inline icon button (separate 56×56 frame) ----
  let iconButton = null;
  if (isInline && !isDisabled) {
    if (interactive) {
      iconButton = /*#__PURE__*/React.createElement("button", {
        type: "button",
        className: "cnc-cp__iconbtn",
        onClick: handleToggle,
        "aria-label": "Open calendar"
      }, calIcon);
    } else {
      iconButton = /*#__PURE__*/React.createElement("span", {
        className: "cnc-cp__iconbtn"
      }, calIcon);
    }
  }
  const cls = ["cnc-cp"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const calendarSlot = interactive && open && calendar ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__calendarslot"
  }, calendar) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), !isInline ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__label"
  }, label) : null, isInline ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__row"
  }, /*#__PURE__*/React.createElement(InputTag, _extends({
    className: inputCls.join(" ")
  }, inputProps), inputInner), iconButton), showHint ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__hintrow"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__hint"
  }, hint), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__hintspacer",
    "aria-hidden": "true"
  })) : null) : /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__row"
  }, /*#__PURE__*/React.createElement(InputTag, _extends({
    className: inputCls.join(" ")
  }, inputProps), inputInner)), showHint ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__hint"
  }, hint) : null), calendarSlot);
}
Object.assign(__ds_scope, { CalendarPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/CalendarPicker/CalendarPicker.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/CountryPicker/CountryPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Country Picker
 * A compact dial-code / country selector (Figma "Country Picker"): a labelled, HUG-width bordered box
 * (radius 12, min-height 56) showing a country flag + dial code (e.g. "+44") and a trailing chevron.
 * The closed trigger is what the extraction defines — there is NO menu/option-list in the Figma set,
 * so none is invented here. One axis:
 *   State : Default | Active | Disabled   (3 variants — NO Error in this set).
 *
 * Per state (faithful to the extraction):
 *   • Default  — 1px --border-input-default; chevron shown.
 *   • Active   — 2px --border-input-active; chevron shown.
 *   • Disabled — bg --background-input-disabled, 1px --border-input-disabled, dial-code
 *                --text-input-disabled; CHEVRON REMOVED (no Icon child in the Disabled variant —
 *                the H Stack narrows 109→93, i.e. drops the 16px icon).
 *
 * Anatomy (HUG width — Figma is HUG ~109; `width` defaults to fit-content, overridable; token-driven):
 *   Root — VERTICAL, gap --spacing-size08, no padding.
 *     • Label "Select country" (Style 7 Bold, --text-input-label — NOT dimmed when disabled, faithful).
 *     • H Stack → Input box (border per state, radius --c-input-cornerradius, min-height
 *       --c-input-minheight, padding --spacing-size16 / --spacing-size12, itemSpacing 0, align center):
 *         - Flag (24×24) — country flag glyph (see FLAGS: emoji in the extraction).
 *         - Container (padding-left/right --spacing-size08) → dial code "+44" (Style 6,
 *           --text-input-default / disabled --text-input-disabled).
 *         - Chevron Icon (16) — pinned --c-temp-input-iconaction; hidden when disabled.
 *         - Border underline (RECTANGLE h2, absolute bottom) — --c-temp-input-borderunderline /
 *           ...active / ...disabled (transparent in every state per the token set → invisible).
 *
 * INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: the trigger is a real <button>
 * that toggles Default⇄Active on click (the only two non-disabled states the extraction defines —
 * no menu is rendered, matching the spec) and fires `onToggle(open)` / `onClick`. Off by default;
 * disabled triggers never toggle.
 *
 * ICON COLOUR (token-driven mask recolour): the chevron is pinned to the dedicated input
 * action-icon token --c-temp-input-iconaction (light/dark maps automatically).
 *
 * FLAGS:
 *   • FLAG GLYPH IS AN EMOJI in the extraction (🇬🇧, GT Ultra VF) — there is no flag-SVG asset in the
 *     library and Figma renders flags as emoji. Reproduced as an emoji by default (faithful), but the
 *     brand rule is "no emoji": exposed as the `flag` prop (accepts any node, e.g. an <img> flag asset)
 *     so journeys can swap in a real flag. OWNER DECISION NEEDED — keep emoji or source flag SVGs.
 *   • Dial code "+44" is sample content → `dialCode` prop (default "+44").
 *   • Chevron glyph is supplied via INSTANCE_SWAP node ID (no captured fill in the extraction) →
 *     name defaulted (Arrows/Chevron Down) and colour pinned to --c-temp-input-iconaction. Inferred.
 *   • Chevron REMOVED in Disabled — faithful to the extraction (no Icon child there).
 *   • Input radius 12 / min-height 56 carry token:null in Figma → bound to --c-input-cornerradius /
 *     --c-input-minheight (the shared Input values, as Text Field / Dropdown Picker do).
 *   • NOT composed of Text Field — the extraction builds the Input chrome INLINE (FRAME, not a Text
 *     Field INSTANCE) → built self-contained, chrome mirrors the shared Input tokens for family
 *     consistency (same pattern as Sort Code / Password / MI Field / Global Search / Dropdown Picker).
 *   • No Error state in this set — rendered faithfully (Default/Active/Disabled only).
 *   • Width — Figma is HUG; kept HUG (fit-content) as the `width` default for reuse.
 *   • Interactive / onToggle / onClick — ADDITIVE behaviour, not in the extraction.
 */

const STYLE_ID = "cnc-countrypicker-styles";
const CSS = `
.cnc-cp { box-sizing: border-box; }
.cnc-cp *, .cnc-cp *::before, .cnc-cp *::after { box-sizing: border-box; }

.cnc-cp {
  display: inline-flex; flex-direction: column; align-items: stretch;
  gap: var(--spacing-size08);
}

/* Top label — Style 7 Bold. */
.cnc-cp__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

/* H Stack row holding the input box. */
.cnc-cp__row { display: flex; flex-direction: row; align-items: flex-start; }

/* Input box. */
.cnc-cp__input {
  position: relative;
  display: inline-flex; flex-direction: row; align-items: center;
  min-height: var(--c-input-minheight);
  padding-top: var(--spacing-size16);
  padding-bottom: var(--spacing-size16);
  padding-left: var(--spacing-size12);
  padding-right: var(--spacing-size12);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
  text-align: left;
  font: inherit;
}
button.cnc-cp__input { cursor: pointer; }
button.cnc-cp__input:disabled { cursor: default; }
.cnc-cp__input--active { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-cp__input--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Flag — 24×24 box; the glyph (emoji by default, see FLAGS) is sized to fill it. */
.cnc-cp__flag {
  flex: 0 0 auto;
  width: 24px; height: 24px;
  display: inline-flex; align-items: center; justify-content: center;
  overflow: hidden;
  font-size: 22px; line-height: 24px;
}
.cnc-cp__flag img, .cnc-cp__flag svg { width: 100%; height: 100%; object-fit: cover; display: block; }

/* Container — dial code with --spacing-size08 left/right padding. */
.cnc-cp__code {
  flex: 0 0 auto;
  display: inline-flex; align-items: center; justify-content: center;
  padding-left: var(--spacing-size08);
  padding-right: var(--spacing-size08);
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  white-space: nowrap;
}
.cnc-cp__code--disabled { color: var(--text-input-disabled); }

/* Chevron. */
.cnc-cp__chev { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-cp__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-cp__underline--active   { background: var(--c-temp-input-borderunderlineactive); }
.cnc-cp__underline--disabled { background: var(--c-temp-input-borderunderlinedisabled); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function CountryPicker({
  state = "default",
  // "default" | "active" | "disabled"
  showLabel = true,
  // Figma "Show Label" boolean — hide the top label when composed inside Phone Number
  label = "Select country",
  // top label (Style 7 Bold)
  flag = "🇬🇧",
  // flag glyph — emoji by default (see FLAGS); accepts any node
  dialCode = "+44",
  // dial code shown in the Container
  icon = "Arrows/Chevron Down/Chevron Down",
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  onToggle,
  // (open: boolean) => void
  onClick,
  basePath = ".",
  width,
  // default HUG (fit-content); override for a fixed width
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const {
    useState
  } = React;
  const [open, setOpen] = useState(false);
  const isDisabled = state === "disabled";

  // Interactive overrides Active off the open flag; presentational uses `state`.
  let effState = state;
  if (interactive && !isDisabled) effState = open ? "active" : "default";
  const isActive = effState === "active";
  const inputCls = ["cnc-cp__input"];
  if (isActive) inputCls.push("cnc-cp__input--active");
  if (isDisabled) inputCls.push("cnc-cp__input--disabled");
  const handleToggle = () => {
    if (isDisabled) return;
    const next = !open;
    setOpen(next);
    if (typeof onToggle === "function") onToggle(next);
    if (typeof onClick === "function") onClick();
  };

  // Chevron — hidden when disabled (faithful to the extraction).
  const chevron = !isDisabled && IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 16,
    color: "var(--c-temp-input-iconaction)",
    basePath: basePath
  }) : null;
  const codeCls = ["cnc-cp__code"];
  if (isDisabled) codeCls.push("cnc-cp__code--disabled");
  const underlineCls = ["cnc-cp__underline"];
  if (isActive) underlineCls.push("cnc-cp__underline--active");
  if (isDisabled) underlineCls.push("cnc-cp__underline--disabled");
  const InputTag = interactive ? "button" : "div";
  const inputProps = interactive ? {
    type: "button",
    onClick: handleToggle,
    disabled: isDisabled,
    "aria-haspopup": "listbox",
    "aria-expanded": open
  } : {};
  const cls = ["cnc-cp"];
  if (className) cls.push(className);
  const rootStyle = {};
  if (typeof width !== "undefined") rootStyle.width = typeof width === "number" ? `${width}px` : width;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), showLabel ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__label"
  }, label) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cp__row"
  }, /*#__PURE__*/React.createElement(InputTag, _extends({
    className: inputCls.join(" ")
  }, inputProps), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__flag",
    "aria-hidden": "true"
  }, flag), /*#__PURE__*/React.createElement("span", {
    className: codeCls.join(" ")
  }, dialCode), chevron ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-cp__chev"
  }, chevron) : null, /*#__PURE__*/React.createElement("span", {
    className: underlineCls.join(" "),
    "aria-hidden": "true"
  }))));
}
Object.assign(__ds_scope, { CountryPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/CountryPicker/CountryPicker.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/DropdownPicker/DropdownPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Dropdown Picker
 * A select trigger (Figma "Dropdown Picker"): a bordered box (radius 12, min-height 56) that opens
 * a picker. The closed trigger is what the extraction defines — there is NO menu/option-list in the
 * Figma set, so none is invented here. Two axes:
 *   Layout : Default | Inline   — Default = top label above a full-width box with a trailing chevron;
 *                                  Inline = no top label, a left "Label :" prefix inside the box and
 *                                  a separate 56×56 icon button holding the chevron to the right.
 *   State  : Default | Active | Disabled   (6 variants total — NO Error in this set).
 *
 * Per state (faithful to the extraction):
 *   • Default  — 1px --border-input-default; chevron shown.
 *   • Active   — 2px --border-input-active; chevron shown.
 *   • Disabled — bg --background-input-disabled, 1px --border-input-disabled, text
 *                --text-input-disabled; CHEVRON REMOVED (no Icon child in either Disabled variant).
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, gap --spacing-size08.
 *   DEFAULT layout:
 *     • Label (Style 7 Bold, --text-input-label — NOT dimmed when disabled, faithful).
 *     • H Stack → Input box (border per state, radius --c-input-cornerradius, padding
 *       --spacing-size16, gap --spacing-size08, min-height --c-input-minheight):
 *         - "Select one option" (Style 6, FILL, --text-input-default / disabled --text-input-disabled)
 *         - Chevron Icon (16) — pinned --c-temp-input-iconaction; hidden when disabled.
 *         - Border underline (RECTANGLE h2) — --c-temp-input-borderunderline (transparent → invisible).
 *   INLINE layout (no top label):
 *     • H Stack (HORIZONTAL):
 *         - Input box (FILL): inner H Stack (gap --spacing-size04) → [ "Label" + ":" group
 *           (--c-temp-input-leftlabel / disabled --text-input-disabled) , "Select one option"
 *           (--c-temp-input-textmasked / disabled --text-input-disabled) ] + Border underline.
 *         - Icon button (56×56, bg --c-temp-input-iconactionbackground transparent) → chevron Icon
 *           (16, --c-temp-input-iconaction). Hidden when disabled (no icon button child).
 *
 * INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: the trigger is a real <button>
 * that toggles Default⇄Active on click (the only two non-disabled states the extraction defines —
 * no menu is rendered, matching the spec) and fires `onToggle(open)` / `onClick`. Off by default;
 * disabled triggers never toggle.
 *
 * ICON COLOUR (token-driven mask recolour): the chevron is pinned to the dedicated input
 * action-icon token --c-temp-input-iconaction (light/dark maps automatically).
 *
 * FLAGS:
 *   • Chevron glyph is supplied via INSTANCE_SWAP node ID (no captured fill in the extraction) →
 *     name defaulted (Arrows/Chevron Down) and colour pinned to --c-temp-input-iconaction. Inferred.
 *   • Chevron REMOVED in Disabled (both layouts) — faithful to the extraction (no Icon child there).
 *   • Input radius 12 / min-height 56 carry token:null in Figma → bound to --c-input-cornerradius /
 *     --c-input-minheight (the shared Input values, as Text Field / Global Search do).
 *   • NOT composed of Text Field — the build plan said "composes Text Field" but the extraction builds
 *     the Input chrome INLINE (FRAME, not a Text Field INSTANCE) → built self-contained, chrome
 *     mirrors the shared Input tokens for family consistency (same pattern as Sort Code / Password /
 *     MI Field / Global Search).
 *   • Inline itemSpacing 8 carries token:null on the Inline root (Default root tokenises it as
 *     Spacing/Size 08) → both rendered with --spacing-size08 (same 8px value).
 *   • No Error state in this set — rendered faithfully (Default/Active/Disabled only).
 *   • Width — Figma frame FIXED 375; kept as the `width` prop default for reuse.
 *   • Interactive / onToggle / onClick — ADDITIVE behaviour, not in the extraction.
 */

const STYLE_ID = "cnc-dropdownpicker-styles";
const CSS = `
.cnc-dp { box-sizing: border-box; }
.cnc-dp *, .cnc-dp *::before, .cnc-dp *::after { box-sizing: border-box; }

.cnc-dp {
  display: flex; flex-direction: column; align-items: stretch;
  gap: var(--spacing-size08);
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}

/* Top label (Default layout) — Style 7 Bold. */
.cnc-dp__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

/* Row that holds the input box (+ icon button in Inline). */
.cnc-dp__row { display: flex; flex-direction: row; align-items: flex-start; }

/* Input box. */
.cnc-dp__input {
  position: relative;
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
  text-align: left;
  font: inherit;
}
button.cnc-dp__input { cursor: pointer; }
button.cnc-dp__input:disabled { cursor: default; }
.cnc-dp__input--active { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-dp__input--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Value text — Style 6. */
.cnc-dp__value {
  flex: 1 1 auto; min-width: 0;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
}
.cnc-dp__value--masked   { color: var(--c-temp-input-textmasked); }
.cnc-dp__value--disabled { color: var(--text-input-disabled); }

/* Inline: left "Label :" group + masked value, gap --spacing-size04. */
.cnc-dp__inline {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size04);
}
.cnc-dp__leftlabel {
  flex: 0 0 auto;
  display: inline-flex; flex-direction: row; align-items: center;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--c-temp-input-leftlabel);
  white-space: nowrap;
}
.cnc-dp__leftlabel--disabled { color: var(--text-input-disabled); }

/* Chevron — Default layout sits inline at the trailing edge. */
.cnc-dp__chev { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Inline icon button — 56×56 transparent frame holding the chevron. */
.cnc-dp__iconbtn {
  flex: 0 0 auto;
  width: 56px; height: 56px;
  display: inline-flex; align-items: center; justify-content: center;
  padding: 0; margin: 0; border: 0;
  background: var(--c-temp-input-iconactionbackground);
}
button.cnc-dp__iconbtn { cursor: pointer; font: inherit; }
button.cnc-dp__iconbtn:disabled { cursor: default; }

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-dp__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function DropdownPicker({
  layout = "default",
  // "default" | "inline"
  state = "default",
  // "default" | "active" | "disabled"
  label = "Label",
  // top label (Default layout)
  inlineLabel = "Label",
  // left label (Inline layout)
  value = "Select one option",
  // displayed trigger text
  icon = "Arrows/Chevron Down/Chevron Down",
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  onToggle,
  // (open: boolean) => void
  onClick,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const {
    useState
  } = React;
  const [open, setOpen] = useState(false);
  const isInline = layout === "inline";
  const isDisabled = state === "disabled";

  // Interactive overrides Active off the open flag; presentational uses `state`.
  let effState = state;
  if (interactive && !isDisabled) effState = open ? "active" : "default";
  const isActive = effState === "active";
  const inputCls = ["cnc-dp__input"];
  if (isActive) inputCls.push("cnc-dp__input--active");
  if (isDisabled) inputCls.push("cnc-dp__input--disabled");
  const handleToggle = () => {
    if (isDisabled) return;
    const next = !open;
    setOpen(next);
    if (typeof onToggle === "function") onToggle(next);
    if (typeof onClick === "function") onClick();
  };

  // Chevron — hidden when disabled (faithful to the extraction).
  const chevron = !isDisabled && IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 16,
    color: "var(--c-temp-input-iconaction)",
    basePath: basePath
  }) : null;

  // Value text colour: Default layout → --text-input-default; Inline → --c-temp-input-textmasked;
  // either → --text-input-disabled when disabled.
  const valueCls = ["cnc-dp__value"];
  if (isDisabled) valueCls.push("cnc-dp__value--disabled");else if (isInline) valueCls.push("cnc-dp__value--masked");
  const InputTag = interactive ? "button" : "div";
  const inputProps = interactive ? {
    type: "button",
    onClick: handleToggle,
    disabled: isDisabled,
    "aria-haspopup": "listbox",
    "aria-expanded": open
  } : {};
  const llCls = ["cnc-dp__leftlabel"];
  if (isDisabled) llCls.push("cnc-dp__leftlabel--disabled");

  // ---- Input box contents ----
  let inputInner;
  if (isInline) {
    inputInner = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cnc-dp__inline"
    }, /*#__PURE__*/React.createElement("span", {
      className: llCls.join(" ")
    }, /*#__PURE__*/React.createElement("span", null, inlineLabel), /*#__PURE__*/React.createElement("span", null, ":")), /*#__PURE__*/React.createElement("span", {
      className: valueCls.join(" ")
    }, value)), /*#__PURE__*/React.createElement("span", {
      className: "cnc-dp__underline",
      "aria-hidden": "true"
    }));
  } else {
    inputInner = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: valueCls.join(" ")
    }, value), chevron ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-dp__chev"
    }, chevron) : null, /*#__PURE__*/React.createElement("span", {
      className: "cnc-dp__underline",
      "aria-hidden": "true"
    }));
  }

  // ---- Inline icon button (separate 56×56 frame) ----
  let iconButton = null;
  if (isInline && !isDisabled) {
    if (interactive) {
      iconButton = /*#__PURE__*/React.createElement("button", {
        type: "button",
        className: "cnc-dp__iconbtn",
        onClick: handleToggle,
        "aria-label": "Open picker"
      }, chevron);
    } else {
      iconButton = /*#__PURE__*/React.createElement("span", {
        className: "cnc-dp__iconbtn"
      }, chevron);
    }
  }
  const cls = ["cnc-dp"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), !isInline ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-dp__label"
  }, label) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-dp__row"
  }, /*#__PURE__*/React.createElement(InputTag, _extends({
    className: inputCls.join(" ")
  }, inputProps), inputInner), iconButton));
}
Object.assign(__ds_scope, { DropdownPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/DropdownPicker/DropdownPicker.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/GlobalSearch/GlobalSearch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Global Search
 * The app-wide search input (Figma "Global Search"). A single bordered box (radius 12, min-height
 * 56) with a leading Search icon, a value/placeholder area, and a trailing affordance that swaps
 * with state. One axis only:
 *   State : Default | Active | Edit | Submitted  (4 variants — NO Disabled in this set).
 *
 * Per state (faithful to the extraction):
 *   • Default   — 1px --border-input-default; placeholder "What can we help you with?"
 *                 (--text-input-placeholder); trailing Voice icon; no caret.
 *   • Active    — 2px --border-input-active; placeholder + caret (--c-temp-input-cursor);
 *                 trailing Voice icon.
 *   • Edit      — 2px --border-input-active; entered value (--text-input-default) + caret;
 *                 trailing CLEAR button (24×24 frame, 16px Cross-Filled glyph,
 *                 --c-temp-input-clearicon); underline token --c-temp-input-borderunderlineactive.
 *   • Submitted — 1px --border-input-default; entered value, no caret; trailing Voice icon.
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16 (no label in this component).
 *     • V Stack — VERTICAL:
 *         · Input — HORIZONTAL box, bg --background-input-default, border per State,
 *           radius --c-input-cornerradius (12), padding --spacing-size16, gap --spacing-size08,
 *           min-height --c-input-minheight (56). Contains:
 *             - Search icon (24, padding) — leading. Pinned --c-temp-input-iconaction.
 *             - H Stack (FILL) — value area: text then (Active/Edit) a 2×21 caret.
 *             - Trailing — Voice icon (Default/Active/Submitted, 24, no padding,
 *               --c-temp-input-iconaction) OR a Clear button (Edit).
 *             - Border underline (RECTANGLE, h2) — fill token per State (transparent in the token
 *               set, so invisible — rendered faithfully).
 *
 * Two modes:
 *   • PRESENTATIONAL (default) — faithful to the extraction; `state`/`value`/`placeholder` drive it.
 *   • INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: a real <input> that focuses
 *     (→ Active border), types (→ value + Clear button; Voice hides), clears (Clear button empties
 *     and refocuses), and submits on Enter. Layered on top of the spec; off by default.
 *
 * ICON COLOUR (token-driven mask recolour): Search + Voice glyphs pinned to the dedicated input
 * action-icon token --c-temp-input-iconaction; the Clear glyph pinned to --c-temp-input-clearicon.
 *
 * FLAGS:
 *   • Search / Voice glyphs are supplied via INSTANCE_SWAP node IDs (no captured fill in the
 *     extraction) → names defaulted (Navigation/Search, Navigation/Microphone) and colour pinned
 *     to the dedicated --c-temp-input-iconaction token. Inferred — flagged.
 *   • Clear glyph is an SF-Pro private-use vector (􀁡, a filled X-circle) → reproduced with the
 *     library's Sentiment system/Cross Filled glyph, recoloured by --c-temp-input-clearicon.
 *   • Caret 2×21 r10 and Clear frame 24 / glyph 16 carry token:null sizing in Figma → literals.
 *   • No Disabled state in this set — rendered faithfully (4 states only).
 *   • Interactive / onSubmit / onClear / onVoice — ADDITIVE behaviour, not in the extraction.
 *   • Width — Figma frame FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-globalsearch-styles";
const CSS = `
.cnc-gs { box-sizing: border-box; }
.cnc-gs *, .cnc-gs *::before, .cnc-gs *::after { box-sizing: border-box; }

.cnc-gs {
  display: flex; flex-direction: column; align-items: stretch;
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}

.cnc-gs__vstack { display: flex; flex-direction: column; align-items: stretch; gap: 0; }

/* Input box. */
.cnc-gs__input {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-gs__input--active { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }

/* Value area. */
.cnc-gs__hstack {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
}

.cnc-gs__value {
  flex: 0 1 auto; min-width: 0; max-width: 100%;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
}
.cnc-gs__value--placeholder { color: var(--text-input-placeholder); }
.cnc-gs__value--edit        { color: var(--text-input-default); }

/* Real input — INTERACTIVE mode (additive). Inherits the Style-6 value type; native caret. */
.cnc-gs__field {
  flex: 1 1 auto; min-width: 0;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-gs__field::placeholder { color: var(--text-input-placeholder); opacity: 1; }

/* Caret — presentational Active / Edit only. */
.cnc-gs__cursor {
  flex: 0 0 auto;
  width: 2px; height: 21px;
  border-radius: 10px;
  background: var(--c-temp-input-cursor);
}

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-gs__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-gs__underline--active { background: var(--c-temp-input-borderunderlineactive); }

.cnc-gs__trailing { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Clear button — 24×24 frame around a 16px glyph (Figma sizing literals). */
.cnc-gs__clear {
  flex: 0 0 auto;
  width: 24px; height: 24px;
  display: inline-flex; align-items: center; justify-content: center;
  padding: 0; margin: 0; border: 0; background: transparent;
}
button.cnc-gs__clear { cursor: pointer; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function GlobalSearch({
  state = "default",
  // "default" | "active" | "edit" | "submitted"
  placeholder = "What can we help you with?",
  value = "Data input",
  // shown in edit / submitted (presentational)
  searchIcon = "Navigation/Search/Search",
  voiceIcon = "Navigation/Microphone/Microphone",
  clearIcon = "Sentiment system/Cross Filled/Cross Filled",
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  onChange,
  // (value) => void
  onSubmit,
  // (value) => void  — Enter
  onClear,
  // () => void
  onVoice,
  // () => void  — Voice affordance tapped
  onFocus,
  onBlur,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const {
    useState,
    useRef,
    useCallback
  } = React;
  const [val, setVal] = useState("");
  const [focused, setFocused] = useState(false);
  const inputRef = useRef(null);

  // ---- Resolve effective state ----
  // Interactive: focus → active; a non-empty value while focused reads as Edit, while blurred as
  // Submitted. Presentational: the `state` prop drives everything.
  let effState = state;
  if (interactive) {
    if (focused) effState = val ? "edit" : "active";else effState = val ? "submitted" : "default";
  }
  const isActive = effState === "active";
  const isEdit = effState === "edit";
  const isSubmitted = effState === "submitted";
  const hasValue = isEdit || isSubmitted;
  const inputCls = ["cnc-gs__input"];
  if (isActive || isEdit) inputCls.push("cnc-gs__input--active");
  const underlineCls = ["cnc-gs__underline"];
  if (isEdit) underlineCls.push("cnc-gs__underline--active");
  const handleClear = useCallback(() => {
    setVal("");
    if (typeof onChange === "function") onChange("");
    if (typeof onClear === "function") onClear();
    if (inputRef.current) inputRef.current.focus();
  }, [onChange, onClear]);

  // ---- Value area ----
  let valueArea;
  if (interactive) {
    valueArea = /*#__PURE__*/React.createElement("input", {
      ref: inputRef,
      className: "cnc-gs__field",
      type: "search",
      value: val,
      placeholder: placeholder,
      onChange: e => {
        setVal(e.target.value);
        if (typeof onChange === "function") onChange(e.target.value);
      },
      onFocus: e => {
        setFocused(true);
        if (typeof onFocus === "function") onFocus(e);
      },
      onBlur: e => {
        setFocused(false);
        if (typeof onBlur === "function") onBlur(e);
      },
      onKeyDown: e => {
        if (e.key === "Enter" && typeof onSubmit === "function") onSubmit(val);
      }
    });
  } else {
    // Presentational: text then a caret (Active / Edit) — faithful to the extraction order.
    const caret = isActive || isEdit ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-gs__cursor"
    }) : null;
    if (hasValue) {
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
        className: "cnc-gs__value cnc-gs__value--edit"
      }, value), caret);
    } else {
      // Default / Active — placeholder.
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
        className: "cnc-gs__value cnc-gs__value--placeholder"
      }, placeholder), caret);
    }
  }

  // ---- Trailing affordance ----
  // Edit → Clear button. Default / Active / Submitted → Voice icon.
  // Interactive: a non-empty value → Clear; empty → Voice.
  const showClear = interactive ? !!val : isEdit;
  let trailing = null;
  if (IconCmp) {
    if (showClear) {
      const ClearTag = interactive ? "button" : "span";
      const clearProps = interactive ? {
        type: "button",
        onClick: handleClear,
        "aria-label": "Clear search"
      } : {
        "aria-hidden": "true"
      };
      trailing = /*#__PURE__*/React.createElement(ClearTag, _extends({
        className: "cnc-gs__clear"
      }, clearProps), /*#__PURE__*/React.createElement(IconCmp, {
        name: clearIcon,
        size: 16,
        color: "var(--c-temp-input-clearicon)",
        basePath: basePath
      }));
    } else {
      const VoiceTag = interactive ? "button" : "span";
      const voiceProps = interactive ? {
        type: "button",
        onClick: () => {
          if (typeof onVoice === "function") onVoice();
        },
        "aria-label": "Search by voice"
      } : {
        "aria-hidden": "true"
      };
      trailing = /*#__PURE__*/React.createElement("span", {
        className: "cnc-gs__trailing"
      }, /*#__PURE__*/React.createElement(VoiceTag, _extends({
        className: "cnc-gs__clear",
        style: interactive ? undefined : {
          width: "24px",
          height: "24px"
        }
      }, voiceProps), /*#__PURE__*/React.createElement(IconCmp, {
        name: voiceIcon,
        size: 24,
        color: "var(--c-temp-input-iconaction)",
        basePath: basePath
      })));
    }
  }
  const cls = ["cnc-gs"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-gs__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: inputCls.join(" ")
  }, IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-gs__trailing"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: searchIcon,
    size: 24,
    padding: true,
    color: "var(--c-temp-input-iconaction)",
    basePath: basePath
  })) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-gs__hstack"
  }, valueArea), trailing, /*#__PURE__*/React.createElement("span", {
    className: underlineCls.join(" "),
    "aria-hidden": "true"
  }))));
}
Object.assign(__ds_scope, { GlobalSearch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/GlobalSearch/GlobalSearch.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/MI_Field/MIField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — MI Field (Memorable Information)
 * The memorable-info entry field (Figma "MI Field"): a horizontal row of THREE labelled 56×56
 * input cells, each capturing one masked character. Above each cell sits its own small label
 * ("1st" / "3rd" / "5th") naming which character of the memorable word is asked for. One axis —
 * State (7 variants):
 *   Default | Input 01 Active | Input 02 Active | Input 03 Active | All filled | Error | Disabled
 *
 * Anatomy (root FIXED 375, min 343 — the cluster is centred):
 *   Root — HORIZONTAL, align center, justify center, gap --spacing-size16, padding L/R --spacing-size16:
 *     • Memorable info ×3 — VERTICAL, gap --spacing-size08:
 *         · Cell label (TEXT, Style 8 Regular, --text-input-label, centred, 56 wide).
 *         · Input box — 56×56, bg --background-input-default (disabled --background-input-disabled),
 *           radius --c-temp-input-mifield-shape01cornerradius (8), content centred:
 *             - Password bullet (8px circle, --c-temp-input-mifield-passwordinput) — filled cells only.
 *             - Border underline (h2 rect, transparent token per state) — faithful, invisible.
 *           Border: default 1px --border-input-default · active 2px --border-input-active ·
 *           error 1px --border-input-critical · disabled 1px --border-input-disabled.
 *
 * Per-state cell treatment (cells 0..2 = 1st / 3rd / 5th):
 *   Default          — three empty default cells.
 *   Input 01 Active  — cell 1 ACTIVE (2px border); none filled yet.
 *   Input 02 Active  — cell 1 filled (bullet), cell 2 ACTIVE, cell 3 empty.
 *   Input 03 Active  — cells 1–2 filled, cell 3 ACTIVE.
 *   All filled       — all three filled (bullets), default border.
 *   Error            — three empty cells with the critical border (no hint in the extraction).
 *   Disabled         — three empty disabled cells.
 *
 * Two modes:
 *   • PRESENTATIONAL (default) — faithful to the extraction; prop-driven via `state` (+ `labels`).
 *   • INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: three real single-character
 *     masked inputs that focus, type, auto-advance on fill and backspace to the previous cell; the
 *     focused cell gets the active border. `onChange` fires the joined value. Off by default so the
 *     primitive stays Figma-faithful.
 *
 * 100% token-driven; light/dark maps automatically. No icons, no nested DS components.
 *
 * FLAGS:
 *   • Composition — the extraction builds each of the three cells INLINE (nodeType FRAME, not an
 *     INSTANCE). Built self-contained to match the extraction exactly; it does NOT instance Text
 *     Field (chrome mirrors the Input tokens for family consistency).
 *   • Cell count is FIXED at three (the extraction shows exactly three cells). The labels
 *     ("1st"/"3rd"/"5th") are the Figma sample values, exposed as the `labels` prop (length 3).
 *   • Hidden "Shape 02" POLYGON (the alt squircle, visible:false in every variant) is omitted, as
 *     in Sort Code / Quick Action.
 *   • Shape 01 cornerRadius / cell size 56 / bullet 8 carry token:null sizing in the node; the
 *     radius is bound to the dedicated --c-temp-input-mifield-shape01cornerradius token (exact 8),
 *     the bullet fill to --c-temp-input-mifield-passwordinput; 56 is a sizing literal.
 *   • Width — FIXED 375 in Figma; kept as the default `width` prop. `interactive`/`onChange` are additive.
 */

const STYLE_ID = "cnc-mifield-styles";
const CSS = `
.cnc-mi, .cnc-mi * , .cnc-mi *::before, .cnc-mi *::after { box-sizing: border-box; }

.cnc-mi {
  display: flex; flex-direction: row; align-items: center; justify-content: center;
  gap: var(--spacing-size16);
  padding: 0 var(--spacing-size16);
}

/* One memorable-info column: small label above the input box. */
.cnc-mi__col { display: flex; flex-direction: column; align-items: center; gap: var(--spacing-size08); }

.cnc-mi__label {
  width: 56px;
  text-align: center;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-input-label);
}

/* 56×56 input box. */
.cnc-mi__box {
  position: relative;
  flex: 0 0 auto;
  width: 56px; height: 56px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  border-radius: var(--c-temp-input-mifield-shape01cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-mi__box--active   { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-mi__box--error    { border-color: var(--border-input-critical); }
.cnc-mi__box--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Masked-character bullet — presentational filled cells. */
.cnc-mi__bullet {
  flex: 0 0 auto;
  width: 8px; height: 8px;
  border-radius: 50%;
  background: var(--c-temp-input-mifield-passwordinput);
}

/* Real per-cell input — INTERACTIVE mode (additive). Centred, masked, native caret. */
.cnc-mi__field {
  width: 100%; height: 100%;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  text-align: center;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-mi__field:disabled { color: var(--text-input-disabled); -webkit-text-fill-color: var(--text-input-disabled); }

/* Border underline inside each cell — token-driven (transparent in every state per the token set). */
.cnc-mi__underline {
  position: absolute; left: 0; right: 0; bottom: 0; height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-mi__underline--active { background: var(--c-temp-input-borderunderlineactive); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}

// Per-state cell config — which cells are filled (bullet), which one is active, and the box variant.
const STATE_CONFIG = {
  default: {
    filled: [false, false, false],
    active: -1,
    variant: "default"
  },
  input01: {
    filled: [false, false, false],
    active: 0,
    variant: "default"
  },
  input02: {
    filled: [true, false, false],
    active: 1,
    variant: "default"
  },
  input03: {
    filled: [true, true, false],
    active: 2,
    variant: "default"
  },
  allFilled: {
    filled: [true, true, true],
    active: -1,
    variant: "default"
  },
  error: {
    filled: [false, false, false],
    active: -1,
    variant: "error"
  },
  disabled: {
    filled: [false, false, false],
    active: -1,
    variant: "disabled"
  }
};
function MIField({
  state = "default",
  // default | input01 | input02 | input03 | allFilled | error | disabled
  labels = ["1st", "3rd", "5th"],
  // the per-cell character labels (Figma sample; length 3)
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  onChange,
  // (joined string) => void
  onComplete,
  // (joined) => void when all three characters entered
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const {
    useState,
    useRef,
    useCallback
  } = React;
  const cfg = STATE_CONFIG[state] || STATE_CONFIG.default;
  const cls = ["cnc-mi"];
  if (className) cls.push(className);
  const lbls = Array.isArray(labels) && labels.length === 3 ? labels : ["1st", "3rd", "5th"];

  // ---------- INTERACTIVE (additive) ----------
  const [chars, setChars] = useState(["", "", ""]);
  const [focusIdx, setFocusIdx] = useState(-1);
  const refs = [useRef(null), useRef(null), useRef(null)];
  const isDisabled = state === "disabled";
  const handleType = useCallback((i, raw) => {
    const ch = (raw || "").slice(-1);
    setChars(prev => {
      const next = prev.slice();
      next[i] = ch;
      const joined = next.join("");
      if (typeof onChange === "function") onChange(joined);
      if (next.every(c => c.length === 1) && typeof onComplete === "function") onComplete(joined);
      return next;
    });
    if (ch && i < 2 && refs[i + 1].current) refs[i + 1].current.focus();
  }, [onChange, onComplete]);
  const handleKey = useCallback((i, e) => {
    if (e.key === "Backspace" && !e.currentTarget.value && i > 0 && refs[i - 1].current) {
      refs[i - 1].current.focus();
    }
  }, []);
  const boxVariant = i => {
    if (interactive) {
      if (isDisabled) return "disabled";
      return focusIdx === i ? "active" : "default";
    }
    if (cfg.variant === "error") return "error";
    if (cfg.variant === "disabled") return "disabled";
    return i === cfg.active ? "active" : "default";
  };
  const renderCol = i => {
    const variant = boxVariant(i);
    const boxCls = ["cnc-mi__box"];
    if (variant !== "default") boxCls.push("cnc-mi__box--" + variant);
    const underlineCls = ["cnc-mi__underline"];
    if (variant === "active") underlineCls.push("cnc-mi__underline--active");
    let content;
    if (interactive) {
      content = /*#__PURE__*/React.createElement("input", {
        ref: refs[i],
        className: "cnc-mi__field",
        type: "password",
        inputMode: "text",
        maxLength: 1,
        value: chars[i],
        disabled: isDisabled,
        "aria-label": `Memorable information — ${lbls[i]} character`,
        onChange: e => handleType(i, e.target.value),
        onKeyDown: e => handleKey(i, e),
        onFocus: () => setFocusIdx(i),
        onBlur: () => setFocusIdx(-1)
      });
    } else {
      content = cfg.filled[i] ? /*#__PURE__*/React.createElement("span", {
        className: "cnc-mi__bullet",
        "aria-hidden": "true"
      }) : null;
    }
    return /*#__PURE__*/React.createElement("div", {
      className: "cnc-mi__col",
      key: i
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-mi__label"
    }, lbls[i]), /*#__PURE__*/React.createElement("div", {
      className: boxCls.join(" ")
    }, content, /*#__PURE__*/React.createElement("span", {
      className: underlineCls.join(" "),
      "aria-hidden": "true"
    })));
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: {
      width: typeof width === "number" ? width + "px" : width
    }
  }, rest), renderCol(0), renderCol(1), renderCol(2));
}
Object.assign(__ds_scope, { MIField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/MI_Field/MIField.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/Password/Password.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — the "Show / Hide" button is a Tertiary Compact Button. Imported for standalone use;
// resolved from the DS namespace at render via fromNS() (cross-directory safe).

/**
 * Cancara — Password
 * The password entry field (Figma "Password"): the Text-Field chrome (label + bordered box + value
 * area + transparent underline + optional hint) with a trailing "Show / Hide" Compact Button and a
 * masked value. Three axes (18 variants):
 *   State        : Default | Active | Error            (NOTE: no Disabled in this set)
 *   Data         : None | Placeholder | Edit
 *   Mask password: True (value hidden → dots, button "Show") | False (value shown, button "Hide")
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, itemSpacing --spacing-size08:
 *     • Label (TEXT, Style 7 Bold, --text-input-label).
 *     • V Stack — VERTICAL, gap 0:
 *         · Input — HORIZONTAL box, bg --background-input-default, border per State,
 *           radius --c-input-cornerradius (12), paddingLeft --spacing-size16 (no right padding —
 *           the Show button hugs the right edge), gap --spacing-size08, min-height --c-input-minheight
 *           (56). Contains:
 *             - H Stack (FILL) — the value area:
 *                 · content — None: empty · Placeholder: "Password" (Style 6, --text-input-placeholder)
 *                   · Edit + masked: dots (--c-temp-input-textmasked) · Edit + unmasked: the value
 *                     (Style 6, --text-input-default) · Interactive: a real <input>.
 *                 · Cursor — 2×21 rect, --c-temp-input-cursor, radius 10, at the insertion point;
 *                   shown in State=Active (all data) AND State=Error+Data=Edit (per the extraction).
 *             - Border underline (RECTANGLE, h2) — token-driven (--c-temp-input-borderunderline /
 *               -active / -error). All TRANSPARENT in the token set → invisible in every state
 *               (rendered faithfully, absolutely positioned at the box bottom).
 *             - Show button — Compact Button, Size=Default, Type=Tertiary, Icon Position=None.
 *               Label "Show" when masked, "Hide" when unmasked.
 *         · Hint message — visible in Figma ONLY in Error (Type=Error). Rendered when in Error (or
 *           when `hint` is supplied): Style 8, --text-input-error (Error) / --text-generic-subdued.
 *
 * Border width: --c-temp-input-borderwidthdefault (1) for Default/Error,
 * --c-temp-input-borderwidthactive (2) for Active.
 *
 * Two modes:
 *   • PRESENTATIONAL (default) — faithful to the extraction. State/data/mask are prop-driven
 *     (`state`, `dataFormat`, `masked`, `value`); the caret is a static rectangle; the Show button
 *     is non-interactive chrome.
 *   • INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: a real <input> (focus, type,
 *     native caret, blur-validation via `validate`/`required`) whose masking is toggled live by the
 *     Show / Hide button (input type flips password↔text). Off by default so the DS primitive stays
 *     Figma-faithful.
 *
 * 100% token-driven; light/dark maps automatically.
 *
 * FLAGS:
 *   • Composition — the build plan listed Password as "composes Text Field", but the extraction
 *     builds the field chrome INLINE (the Input is a FRAME, not a Text Field INSTANCE) and composes
 *     only the Show button (a Tertiary Compact Button INSTANCE). Built self-contained to match the
 *     extraction exactly — it does NOT instance Text Field; it composes Compact Button. (Same
 *     pattern as Sort Code.) The chrome deliberately mirrors Text Field's tokens so the two read as
 *     one family.
 *   • No Disabled — the set has only Default / Active / Error (Text Field additionally has Disabled).
 *     Kept faithful: no Disabled state.
 *   • Masked glyph — Figma renders the masked value with an SF-Pro private-use dot glyph (fontSize 8,
 *     fillToken Component/Temp/Input/Text Masked). Reproduced as bullet characters (•) coloured
 *     --c-temp-input-textmasked; default 8 dots (the Figma sample length), or `value.length` when a
 *     `value` is supplied.
 *   • Caret in Error+Edit — the extraction shows the Cursor in State=Active (all data) AND in
 *     State=Error+Data=Edit; reproduced exactly.
 *   • Hint message sub-component — same unexpanded instance as Text Field; internals inferred (a
 *     single Style 8 line, error colour --text-input-error, --spacing-size04 top gap; margin token 0
 *     honoured). Matches Text Field for family consistency. (Dedicated --c-temp-input-texthint /
 *     -texthinterror tokens exist but Text Field set the precedent of --text-input-error — followed
 *     here.)
 *   • Input cornerRadius / min-height — token:null in this extraction (literal 12 / 56); bound to
 *     --c-input-cornerradius / --c-input-minheight (the values Text Field uses) for family
 *     consistency.
 *   • Show-button label "Show"/"Hide" and the sample value "Password" are the Figma strings, exposed
 *     as `showLabel` / `hideLabel` / `value`. Width FIXED 375 kept as the `width` default.
 *   • interactive / validate / required — ADDITIVE behaviour for test journeys, NOT in the extraction;
 *     off by default.
 */

const STYLE_ID = "cnc-password-styles";
const CSS = `
.cnc-pw { box-sizing: border-box; }
.cnc-pw *, .cnc-pw *::before, .cnc-pw *::after { box-sizing: border-box; }

.cnc-pw {
  display: flex; flex-direction: column; align-items: stretch;
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
  gap: var(--spacing-size08);
}

/* Top label. */
.cnc-pw__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

.cnc-pw__vstack { display: flex; flex-direction: column; align-items: stretch; gap: 0; }

/* Input box — paddingLeft only (the Show button hugs the right edge). */
.cnc-pw__input {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding-left: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-pw__input--active { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-pw__input--error  { border-color: var(--border-input-critical); }

/* Value area. */
.cnc-pw__hstack {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
}

/* Static value text — hugs its content so the caret can sit right after it. */
.cnc-pw__value {
  flex: 0 1 auto; min-width: 0; max-width: 100%;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
}
.cnc-pw__value--placeholder { color: var(--text-input-placeholder); }
.cnc-pw__value--edit        { color: var(--text-input-default); }
/* Masked dots — slightly wider tracking so the bullets read as the SF-Pro masked glyph. */
.cnc-pw__value--masked { color: var(--c-temp-input-textmasked); letter-spacing: 2px; }

/* Real input — INTERACTIVE mode (additive). Inherits the Style-6 value type; native caret. */
.cnc-pw__field {
  flex: 1 1 auto; min-width: 0;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-pw__field::placeholder { color: var(--text-input-placeholder); opacity: 1; }

/* Caret — presentational only. */
.cnc-pw__cursor {
  flex: 0 0 auto;
  width: 2px; height: 21px;
  border-radius: 10px;
  background: var(--c-temp-input-cursor);
}

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-pw__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-pw__underline--active { background: var(--c-temp-input-borderunderlineactive); }
.cnc-pw__underline--error  { background: var(--c-temp-input-borderunderlineerror); }

/* Show / Hide button wrapper — hugs the right edge of the box. */
.cnc-pw__show { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Hint / error message (sub-component internals inferred — see FLAGS). */
.cnc-pw__hint {
  margin-top: var(--spacing-size04);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-generic-subdued);
  text-wrap: pretty;
}
.cnc-pw__hint--error { color: var(--text-input-error); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// Named validation presets for INTERACTIVE journeys (additive — not part of the Figma spec).
const VALIDATORS = {
  required: v => v && String(v).trim() ? "" : "Please enter your password."
};
function Password({
  state = "default",
  // "default" | "active" | "error"  (no disabled in this set)
  dataFormat = "none",
  // "none" | "placeholder" | "edit"
  masked = true,
  // Mask password: true → dots + "Show"; false → revealed + "Hide"
  label = "Label",
  showFieldLabel = true,
  // ADDITIVE: hide the top label (composers like the Pop-up Modal Presets)
  showCursor,
  // ADDITIVE: force caret on/off independent of state (presentational only)
  value,
  // shown when dataFormat="edit" (presentational, default "Password"); seeds interactive
  placeholder = "Password",
  hint = "",
  // hint / error message (visible in Figma only in Error)
  showLabel = "Show",
  hideLabel = "Hide",
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  validate = null,
  // fn(value)=>msg("" if valid) | preset string | array of those
  required = false,
  onChange,
  // (value) => void
  onToggleMask,
  // (revealed) => void
  onFocus,
  onBlur,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const CompactBtn = fromNS("CompactButton", __ds_scope.CompactButton);
  const {
    useState,
    useCallback
  } = React;
  const [val, setVal] = useState(typeof value === "string" ? value : "");
  const [focused, setFocused] = useState(false);
  const [touched, setTouched] = useState(false);
  const [revealed, setRevealed] = useState(!masked); // interactive masking state

  const runValidate = useCallback(v => {
    const rules = [];
    if (required) rules.push("required");
    if (Array.isArray(validate)) rules.push(...validate);else if (validate) rules.push(validate);
    for (const r of rules) {
      const fn = typeof r === "function" ? r : VALIDATORS[r];
      if (!fn) continue;
      const msg = fn(v) || "";
      if (msg) return msg;
    }
    return "";
  }, [validate, required]);

  // Resolve effective state/data/mask/hint.
  let effState = state;
  let effDataFormat = dataFormat;
  let effMasked = masked;
  let effHint = hint;
  if (interactive) {
    const validationMsg = touched ? runValidate(val) : "";
    effDataFormat = val ? "edit" : "placeholder";
    effMasked = !revealed;
    if (validationMsg || state === "error") {
      effState = "error";
      effHint = validationMsg || hint;
    } else if (focused) {
      effState = "active";
    } else {
      effState = "default";
    }
  }
  const isActive = effState === "active";
  const isError = effState === "error";
  const inputCls = ["cnc-pw__input"];
  if (isActive) inputCls.push("cnc-pw__input--active");
  if (isError) inputCls.push("cnc-pw__input--error");
  const underlineCls = ["cnc-pw__underline"];
  if (isActive) underlineCls.push("cnc-pw__underline--active");
  if (isError) underlineCls.push("cnc-pw__underline--error");

  // Caret shows in Active (all data) AND in Error + Edit (per the extraction). An explicit
  // `showCursor` overrides this in presentational mode (Figma "Show cursor" boolean — used by
  // composers that show a caret while State=Default, e.g. the Pop-up Modal Presets).
  const derivedCaret = isActive || isError && effDataFormat === "edit";
  const showCaret = !interactive && showCursor != null ? showCursor : derivedCaret;

  // ---- Value area content ----
  let valueArea = null;
  if (interactive) {
    valueArea = /*#__PURE__*/React.createElement("input", {
      className: "cnc-pw__field",
      type: revealed ? "text" : "password",
      value: val,
      placeholder: placeholder,
      "aria-invalid": isError || undefined,
      onChange: e => {
        setVal(e.target.value);
        if (typeof onChange === "function") onChange(e.target.value);
      },
      onFocus: e => {
        setFocused(true);
        if (typeof onFocus === "function") onFocus(e);
      },
      onBlur: e => {
        setFocused(false);
        setTouched(true);
        if (typeof onBlur === "function") onBlur(e);
      }
    });
  } else {
    const caret = showCaret ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-pw__cursor",
      "aria-hidden": "true"
    }) : null;
    if (effDataFormat === "placeholder") {
      // Empty field → caret sits BEFORE the placeholder.
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, caret, /*#__PURE__*/React.createElement("span", {
        className: "cnc-pw__value cnc-pw__value--placeholder"
      }, placeholder));
    } else if (effDataFormat === "edit") {
      // Field with a value → caret sits AFTER the text. Masked → dots, unmasked → the value.
      const text = value == null ? "Password" : value;
      const display = effMasked ? "•".repeat(text.length || 8) : text;
      const vCls = ["cnc-pw__value", effMasked ? "cnc-pw__value--masked" : "cnc-pw__value--edit"];
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
        className: vCls.join(" ")
      }, display), caret);
    } else {
      // None → caret only (at the start).
      valueArea = caret;
    }
  }
  const cls = ["cnc-pw"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const showHint = isError || typeof effHint === "string" && effHint.length > 0;
  const toggleMask = () => {
    setRevealed(r => {
      const next = !r;
      if (typeof onToggleMask === "function") onToggleMask(next);
      return next;
    });
  };
  const btnLabel = effMasked ? showLabel : hideLabel;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), showFieldLabel ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-pw__label"
  }, label) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-pw__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: inputCls.join(" ")
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-pw__hstack"
  }, valueArea), /*#__PURE__*/React.createElement("span", {
    className: underlineCls.join(" "),
    "aria-hidden": "true"
  }), CompactBtn ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-pw__show"
  }, /*#__PURE__*/React.createElement(CompactBtn, {
    variant: "tertiary",
    size: "default",
    iconPosition: "none",
    basePath: basePath,
    onClick: interactive ? toggleMask : undefined,
    "aria-pressed": interactive ? !effMasked : undefined
  }, btnLabel)) : null), showHint ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-pw__hint" + (isError ? " cnc-pw__hint--error" : "")
  }, effHint) : null));
}
Object.assign(__ds_scope, { Password });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/Password/Password.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/PhoneNumberField/PhoneNumberField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _Phone number  (INTERNAL — composed by Phone Number / Phone Number + Extension, not for direct use)
 *
 * The phone-number field workhorse (Figma "_Phone number" COMPONENT_SET). A single shared label sits
 * above a horizontal row of: a label-less Country Picker (dial code, e.g. "+44") + a phone-number input
 * box. This file is also the implementation behind the extension variant — pass `showExtension` to add a
 * fixed 56px extension box (field #3); the public Phone Number / Phone Number + Extension Number entries
 * are thin composers over this component.
 *
 * Axes (faithful to "_Phone number.json"): State × Data → 10 variants.
 *   State : Default | Error | Active | Error Active | Disabled
 *   Data  : None (empty box) | Start Edit (caret, empty) | Edit (value + caret) | Completed (value, no caret)
 * (`_Phone number` has NO extension; `showExtension` defaults false so this base matches its JSON exactly.
 *  The extension axes — Field #2 / Field #3 — come from "_Phone number + Extension Number.json".)
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, gap --spacing-size08:
 *     • Label (TEXT, Style 7 Bold, --text-input-label).
 *     • V Stack — VERTICAL, gap 0:
 *         · H Stack — HORIZONTAL, gap --spacing-size08, align bottom:
 *             - Country Picker (label-less; state mirrors disabled, else Default).
 *             - Phone box — FILL: bordered Input (bg --background-input-default / disabled
 *               --background-input-disabled, radius --c-input-cornerradius, min-height --c-input-minheight,
 *               padding --spacing-size16). Border per state. Value area (Style 6, --text-input-default /
 *               disabled --text-input-disabled) + caret (2×21, --c-temp-input-cursor, radius 10) when
 *               Active/Error-Active. Border underline (h2) token-driven (transparent every state).
 *             - [showExtension] Extension box — FIXED 56px, same chrome, its own state/data.
 *         · Hint message — visible only in Error / Error Active: Style 8, --text-input-error.
 *
 * Border width: --c-temp-input-borderwidthdefault (1) for Default/Error/Completed/Disabled;
 *   --c-temp-input-borderwidthactive (2) for Active / Error Active. Border colour: Error/Error Active
 *   --border-input-critical · Active --border-input-active · Disabled --border-input-disabled · else
 *   --border-input-default.
 *
 * INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: the phone (and extension) boxes
 * render real <input type="tel"> elements that focus (→ Active), type (→ Edit + onChange), and blur
 * (→ Default). Off by default so the DS primitive stays spec-faithful.
 *
 * FLAGS:
 *   • NOT composed of Text Field — the "_Phone number" extraction builds the phone Input chrome INLINE
 *     (a FRAME, not a Text Field INSTANCE), so it is built self-contained here, mirroring the shared
 *     Input tokens for family consistency (same pattern as Sort Code / Password / MI Field / Global
 *     Search / Dropdown Picker / Country Picker). The PUBLIC "Phone number" and the "+ Extension"
 *     extractions instead place Text Field INSTANCEs for the phone/extension fields — a documented
 *     deviation; the rendered result is identical (label-less Text-Field chrome).
 *   • Phone/extension boxes have NO label of their own — the single shared label sits at the top of the
 *     composite (Figma sets the nested Text Field instances label-less; reproduced inline).
 *   • Country Picker stays Default while the phone field is Active/Error (the picker is not part of the
 *     active/error styling — faithful to the extraction); it mirrors Disabled. Overridable via
 *     `countryState`.
 *   • Hint sub-component internals inferred (single Style 8 line, --text-input-error, --spacing-size04
 *     top gap) — same as Text Field; its margin token is 0, honoured.
 *   • Data axis has NO "Placeholder" (phone fields never show placeholder text — None = empty box).
 *   • Caret 2×21 r10, box min-h 56 / radius 12 carry token:null in Figma → bound to --c-temp-input-cursor
 *     dims (literal) / --c-input-minheight / --c-input-cornerradius. Width FIXED 375 kept.
 *   • showExtension / interactive / onChange — ADDITIVE (showExtension surfaces the "+ Extension" form;
 *     not present in the bare "_Phone number" JSON).
 */

const STYLE_ID = "cnc-phonenumber-styles";
const CSS = `
.cnc-pn { box-sizing: border-box; }
.cnc-pn *, .cnc-pn *::before, .cnc-pn *::after { box-sizing: border-box; }

.cnc-pn {
  display: flex; flex-direction: column; align-items: stretch;
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
  gap: var(--spacing-size08);
}

/* Top label — Style 7 Bold. */
.cnc-pn__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

.cnc-pn__vstack { display: flex; flex-direction: column; align-items: stretch; gap: 0; }

/* H Stack — country picker + phone box (+ extension). Bottom-aligned (counterAxis MAX). */
.cnc-pn__row { display: flex; flex-direction: row; align-items: flex-end; gap: var(--spacing-size08); }

/* Phone / extension input box. */
.cnc-pn__box {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-pn__box--phone { flex: 1 1 auto; min-width: 0; }
.cnc-pn__box--ext   { flex: 0 0 56px; width: 56px; }
.cnc-pn__box--active   { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-pn__box--error    { border-color: var(--border-input-critical); }
.cnc-pn__box--erroractive { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-critical); }
.cnc-pn__box--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Value area. */
.cnc-pn__hstack { flex: 1 1 auto; min-width: 0; display: flex; flex-direction: row; align-items: center; }

.cnc-pn__value {
  flex: 0 1 auto; min-width: 0; max-width: 100%;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
}
.cnc-pn__value--disabled { color: var(--text-input-disabled); }

/* Real input — INTERACTIVE mode (additive). */
.cnc-pn__field {
  flex: 1 1 auto; min-width: 0;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-pn__field:disabled { color: var(--text-input-disabled); -webkit-text-fill-color: var(--text-input-disabled); }

/* Caret — presentational Active / Error-Active. */
.cnc-pn__cursor { flex: 0 0 auto; width: 2px; height: 21px; border-radius: 10px; background: var(--c-temp-input-cursor); }

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-pn__underline { position: absolute; left: 0; right: 0; bottom: 0; height: 2px; background: var(--c-temp-input-borderunderline); }
.cnc-pn__underline--active { background: var(--c-temp-input-borderunderlineactive); }
.cnc-pn__underline--error  { background: var(--c-temp-input-borderunderlineerror); }

/* Hint / error message (internals inferred — see FLAGS). */
.cnc-pn__hint {
  margin-top: var(--spacing-size04);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-input-error);
  text-wrap: pretty;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// One phone/extension box. `variant` = "phone" | "ext".
function PhoneBox({
  variant,
  state,
  data,
  value,
  interactive,
  inputType,
  onChange,
  ariaLabel
}) {
  const {
    useState
  } = React;
  const [val, setVal] = useState(typeof value === "string" ? value : "");
  const [focused, setFocused] = useState(false);

  // Effective state/data.
  let effState = state;
  let effData = data;
  if (interactive) {
    if (state === "disabled") {
      effState = "disabled";
    } else if (state === "error" || state === "errorActive") {
      effState = focused ? "errorActive" : "error";
    } else {
      effState = focused ? "active" : "default";
    }
    effData = val ? "edit" : focused ? "startEdit" : "none";
  }
  const isDisabled = effState === "disabled";
  const isActive = effState === "active";
  const isError = effState === "error";
  const isErrorActive = effState === "errorActive";
  const showCaret = (isActive || isErrorActive) && !interactive;
  const showValue = effData === "edit" || effData === "completed";
  const boxCls = ["cnc-pn__box", variant === "ext" ? "cnc-pn__box--ext" : "cnc-pn__box--phone"];
  if (isActive) boxCls.push("cnc-pn__box--active");
  if (isError) boxCls.push("cnc-pn__box--error");
  if (isErrorActive) boxCls.push("cnc-pn__box--erroractive");
  if (isDisabled) boxCls.push("cnc-pn__box--disabled");
  const underlineCls = ["cnc-pn__underline"];
  if (isActive) underlineCls.push("cnc-pn__underline--active");
  if (isError || isErrorActive) underlineCls.push("cnc-pn__underline--error");
  let valueArea;
  if (interactive) {
    valueArea = /*#__PURE__*/React.createElement("input", {
      className: "cnc-pn__field",
      type: inputType || "tel",
      value: val,
      disabled: isDisabled,
      "aria-label": ariaLabel,
      "aria-invalid": isError || isErrorActive || undefined,
      onChange: e => {
        setVal(e.target.value);
        if (typeof onChange === "function") onChange(e.target.value);
      },
      onFocus: () => setFocused(true),
      onBlur: () => setFocused(false)
    });
  } else {
    const caret = showCaret ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-pn__cursor"
    }) : null;
    if (showValue) {
      const vCls = ["cnc-pn__value", isDisabled ? "cnc-pn__value--disabled" : ""];
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
        className: vCls.join(" ")
      }, value == null ? "Data input" : value), caret);
    } else {
      // None / Start Edit → caret only (at the start) or empty.
      valueArea = caret;
    }
  }
  return /*#__PURE__*/React.createElement("div", {
    className: boxCls.join(" ")
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-pn__hstack"
  }, valueArea), /*#__PURE__*/React.createElement("span", {
    className: underlineCls.join(" "),
    "aria-hidden": "true"
  }));
}
function PhoneNumberField({
  label = "Phone number",
  // Country picker
  flag = "🇬🇧",
  dialCode = "+44",
  countryState,
  // override; defaults to disabled-mirror, else "default"
  // Phone field
  state = "default",
  // "default" | "error" | "active" | "errorActive" | "disabled"
  data = "none",
  // "none" | "startEdit" | "edit" | "completed"
  value,
  hint = "",
  // shown only in Error / Error Active
  // Extension field (additive — surfaces the "+ Extension" form)
  showExtension = false,
  extState = "default",
  extData = "none",
  extValue,
  // — Interactive (additive, opt-in) —
  interactive = false,
  onChange,
  // (phoneValue) => void
  onExtChange,
  // (extValue) => void
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const CountryPickerCmp = fromNS("CountryPicker", __ds_scope.CountryPicker);
  const cpState = countryState || (state === "disabled" ? "disabled" : "default");
  const isError = state === "error" || state === "errorActive";
  const showHint = isError && typeof hint === "string" && hint.length > 0;
  const cls = ["cnc-pn"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-pn__label"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "cnc-pn__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-pn__row"
  }, CountryPickerCmp ? /*#__PURE__*/React.createElement(CountryPickerCmp, {
    showLabel: false,
    state: cpState,
    flag: flag,
    dialCode: dialCode,
    basePath: basePath
  }) : null, /*#__PURE__*/React.createElement(PhoneBox, {
    variant: "phone",
    state: state,
    data: data,
    value: value,
    interactive: interactive,
    onChange: onChange,
    ariaLabel: label
  }), showExtension ? /*#__PURE__*/React.createElement(PhoneBox, {
    variant: "ext",
    state: extState,
    data: extData,
    value: extValue,
    interactive: interactive,
    onChange: onExtChange,
    ariaLabel: "Extension"
  }) : null), showHint ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-pn__hint"
  }, hint) : null));
}
Object.assign(__ds_scope, { PhoneNumberField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/PhoneNumberField/PhoneNumberField.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/PhoneNumberExtension/PhoneNumberExtension.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _Phone number + Extension Number  (INTERNAL — composed by Phone Number, not for direct use)
 *
 * The phone-number-with-extension variant (Figma "_Phone number + Extension Number" COMPONENT_SET):
 * a label-less Country Picker + a FILL phone box (field #2) + a FIXED 56px extension box (field #3),
 * under one shared label. A thin composer over `PhoneNumberField` (showExtension=true) — all chrome and
 * tokens live there; this maps the extraction's two state axes onto the workhorse.
 *
 * Axes (faithful to the extraction): Field #2 State/Data × Field #3 State/Data (15 variants).
 *   Field #2 → the phone box;  Field #3 → the extension box.  Each: State (Default/Error/Active/
 *   Error Active/Disabled) × Data (None/Start Edit/Edit/Completed).
 *
 * FLAGS:
 *   • Composes `PhoneNumberField` (showExtension) — same self-contained chrome; the per-field state
 *     axes are forwarded. (See PhoneNumberField for the full anatomy / token notes.)
 *   • The extraction places the phone/extension fields as Text Field INSTANCEs; reproduced as the
 *     label-less inline boxes the workhorse renders (identical result) — documented deviation.
 *   • Width FIXED 375 kept; interactive / onChange additive.
 */

function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function PhoneNumberExtension({
  label = "Phone number",
  flag = "🇬🇧",
  dialCode = "+44",
  countryState,
  // Field #2 — phone
  field2State = "default",
  field2Data = "none",
  phoneValue,
  hint = "",
  // Field #3 — extension
  field3State = "default",
  field3Data = "none",
  extValue,
  // — Interactive (additive) —
  interactive = false,
  onChange,
  onExtChange,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  const PhoneNumberFieldCmp = fromNS("PhoneNumberField", __ds_scope.PhoneNumberField);
  if (!PhoneNumberFieldCmp) return null;
  return /*#__PURE__*/React.createElement(PhoneNumberFieldCmp, _extends({
    label: label,
    flag: flag,
    dialCode: dialCode,
    countryState: countryState,
    showExtension: true,
    state: field2State,
    data: field2Data,
    value: phoneValue,
    hint: hint,
    extState: field3State,
    extData: field3Data,
    extValue: extValue,
    interactive: interactive,
    onChange: onChange,
    onExtChange: onExtChange,
    basePath: basePath,
    width: width,
    className: className
  }, rest));
}
Object.assign(__ds_scope, { PhoneNumberExtension });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/PhoneNumberExtension/PhoneNumberExtension.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/PhoneNumber/PhoneNumber.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Phone number
 * The placed phone-number field (Figma "↳ Text Field / Phone number"): a label-less Country Picker
 * (dial code, e.g. "+44") + a phone-number input box, with an optional fixed 56px extension box, under
 * one shared label. This is the public entry users place — it composes the phone children
 * (`PhoneNumberField` / `PhoneNumberExtension`), which carry all the chrome and tokens.
 *
 * `showExtension` defaults TRUE — the public extraction includes the Extension Number field. Turn it
 * off for a phone-only field.
 *
 * Anatomy / tokens — see `PhoneNumberField`. State × Data drive the phone field; ext* drive the
 * extension box.
 *
 * FLAGS:
 *   • Composes `PhoneNumberExtension` (when showExtension) or `PhoneNumberField` (phone only) — the
 *     extraction places a Country Picker + two Text Field INSTANCEs; reproduced via the self-contained
 *     workhorse (identical render) — documented deviation, consistent with the Text-Field family.
 *   • The public extraction's shared label reads "Select country" (an odd sample for a phone field) →
 *     defaulted to the sensible "Phone number"; override via `label`. (Flagged.)
 *   • showExtension defaults TRUE (faithful — the extraction includes the extension box).
 *   • interactive / onChange / onExtChange — ADDITIVE. Width FIXED 375 kept.
 */

function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function PhoneNumber({
  label = "Phone number",
  flag = "🇬🇧",
  dialCode = "+44",
  countryState,
  // Phone field
  state = "default",
  // "default" | "error" | "active" | "errorActive" | "disabled"
  data = "none",
  // "none" | "startEdit" | "edit" | "completed"
  value,
  hint = "",
  // Extension (on by default — faithful to the public extraction)
  showExtension = true,
  extState = "default",
  extData = "none",
  extValue,
  // — Interactive (additive) —
  interactive = false,
  onChange,
  onExtChange,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  const Field = fromNS("PhoneNumberField", __ds_scope.PhoneNumberField);
  const Ext = fromNS("PhoneNumberExtension", __ds_scope.PhoneNumberExtension);
  if (showExtension && Ext) {
    return /*#__PURE__*/React.createElement(Ext, _extends({
      label: label,
      flag: flag,
      dialCode: dialCode,
      countryState: countryState,
      field2State: state,
      field2Data: data,
      phoneValue: value,
      hint: hint,
      field3State: extState,
      field3Data: extData,
      extValue: extValue,
      interactive: interactive,
      onChange: onChange,
      onExtChange: onExtChange,
      basePath: basePath,
      width: width,
      className: className
    }, rest));
  }
  if (!Field) return null;
  return /*#__PURE__*/React.createElement(Field, _extends({
    label: label,
    flag: flag,
    dialCode: dialCode,
    countryState: countryState,
    state: state,
    data: data,
    value: value,
    hint: hint,
    interactive: interactive,
    onChange: onChange,
    basePath: basePath,
    width: width,
    className: className
  }, rest));
}
Object.assign(__ds_scope, { PhoneNumber });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/PhoneNumber/PhoneNumber.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/SortCode/SortCode.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Sort Code
 * The UK bank sort-code entry field (Figma "Sort Code"): a label above a horizontal row of THREE
 * 56×56 two-digit input cells joined by short dash separators. State axis (8 variants):
 *   Default | Input 01 | Input 02 | Input 03 | Valid Sort Code | Error | Disabled | Disabled Input
 *
 * Anatomy (HUG width ≈ 216 — token-driven):
 *   Root — VERTICAL, gap --spacing-size08, align-items start:
 *     • Label (TEXT, Style 7 Bold, --text-input-label).
 *     • Column (gap 0):
 *         · Row (H Stack) — HORIZONTAL, align center, gap --spacing-size08:
 *             - Cell ×3 — 56×56 box, bg --background-input-default (disabled --background-input-disabled),
 *               radius --c-temp-input-cornerradius (12), padding --spacing-size16, content centred:
 *                 · digits (Style 6 Regular, --text-input-default; disabled --text-input-disabled)
 *                 · Cursor (2×21 rect, --c-temp-input-cursor, radius 10) — active cell only.
 *                 · Border underline (h2 rect, transparent token per state) — faithful, invisible.
 *               Border: default 1px --border-input-default · active 2px --border-input-active ·
 *               error 1px --border-input-critical · disabled 1px --border-input-disabled.
 *             - Separator (8×2 rect, --border-generic-defaultalt02) between cells.
 *         · Hint message — Valid state only: the resolved bank name (Style 8, --text-input-validated).
 *
 * Per-state cell treatment:
 *   Default       — three empty default cells.
 *   Input 01/02/03 — cells 1..N filled "11"/"22"/"33"; the current (Nth) cell is ACTIVE (2px border
 *                   + caret); already-filled cells use the default border; later cells are empty.
 *   Valid Sort Code — all three filled (default border) + the validated bank-name hint.
 *   Error         — three empty cells with the critical border (no hint in the extraction).
 *   Disabled      — three empty disabled cells.  Disabled Input — three filled disabled cells.
 *
 * Two modes:
 *   • PRESENTATIONAL (default) — faithful to the extraction; everything is prop-driven via `state`
 *     (+ optional `values` / `validHint`).
 *   • INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: three real numeric <input>s
 *     that focus, type, auto-advance on fill and backspace to the previous cell; the focused cell
 *     gets the active border. `onChange` fires the joined "dd-dd-dd" value. Off by default so the
 *     primitive stays Figma-faithful.
 *
 * 100% token-driven; light/dark maps automatically. No icons, no nested DS components.
 *
 * FLAGS:
 *   • Composition — the build plan listed Sort Code as "composes Text Field", but the extraction
 *     builds each of the three cells INLINE (nodeType FRAME, not a Text Field INSTANCE). Built
 *     self-contained to match the extraction exactly; it does NOT instance Text Field.
 *   • Hint message sub-component — the Valid hint ("Type=Sort Code Valid", text "Lloyds Bank PLC")
 *     is an INSTANCE that was NOT expanded; its internals are inferred (a single Style 8 line in
 *     --text-input-validated, with a --spacing-size04 top gap; its margin token is 0, honoured).
 *   • Cell digits "11"/"22"/"33" and the bank-name string are the Figma sample values, exposed as
 *     `values` / `validHint` props.
 *   • Width — HUG in Figma (≈216); kept HUG (no width prop). `interactive` / `onChange` are additive.
 */

const STYLE_ID = "cnc-sortcode-styles";
const CSS = `
.cnc-sc, .cnc-sc * , .cnc-sc *::before, .cnc-sc *::after { box-sizing: border-box; }

.cnc-sc {
  display: inline-flex; flex-direction: column; align-items: flex-start;
  gap: var(--spacing-size08);
}

.cnc-sc__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

.cnc-sc__col { display: flex; flex-direction: column; align-items: flex-start; gap: 0; }

.cnc-sc__row { display: flex; flex-direction: row; align-items: center; gap: var(--spacing-size08); }

/* Two-digit cell. */
.cnc-sc__cell {
  position: relative;
  flex: 0 0 auto;
  width: 56px; height: 56px;
  display: flex; flex-direction: row; align-items: center; justify-content: center;
  padding: var(--spacing-size16);
  border-radius: var(--c-temp-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-sc__cell--active   { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-sc__cell--error    { border-color: var(--border-input-critical); }
.cnc-sc__cell--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

.cnc-sc__digits {
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
}
.cnc-sc__digits--disabled { color: var(--text-input-disabled); }

/* Caret — presentational active cell only. */
.cnc-sc__cursor {
  flex: 0 0 auto;
  width: 2px; height: 21px;
  border-radius: 10px;
  background: var(--c-temp-input-cursor);
}

/* Real per-cell input — INTERACTIVE mode (additive). Centred, Style-6, native caret. */
.cnc-sc__field {
  width: 100%; height: 100%;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  text-align: center;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-sc__field:disabled { color: var(--text-input-disabled); -webkit-text-fill-color: var(--text-input-disabled); }

/* Border underline inside each cell — token-driven (transparent in every state per the token set). */
.cnc-sc__underline {
  position: absolute; left: 0; right: 0; bottom: 0; height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-sc__underline--active   { background: var(--c-temp-input-borderunderlineactive); }
.cnc-sc__underline--error    { background: var(--c-temp-input-borderunderlineerror); }
.cnc-sc__underline--disabled { background: var(--c-temp-input-borderunderlinedisabled); }

/* Dash separator between cells. */
.cnc-sc__sep {
  flex: 0 0 auto;
  width: 8px; height: 2px;
  background: var(--border-generic-defaultalt02);
}

/* Valid hint (sub-component internals inferred — see FLAGS). */
.cnc-sc__hint {
  margin-top: var(--spacing-size04);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-input-validated);
  text-wrap: pretty;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}

// Figma sample digit values per state, and which cell is the active (current) one.
const STATE_CONFIG = {
  default: {
    digits: ["", "", ""],
    active: -1,
    variant: "default",
    hint: false
  },
  input01: {
    digits: ["11", "", ""],
    active: 0,
    variant: "default",
    hint: false
  },
  input02: {
    digits: ["11", "22", ""],
    active: 1,
    variant: "default",
    hint: false
  },
  input03: {
    digits: ["11", "22", "33"],
    active: 2,
    variant: "default",
    hint: false
  },
  valid: {
    digits: ["11", "22", "33"],
    active: -1,
    variant: "default",
    hint: true
  },
  error: {
    digits: ["", "", ""],
    active: -1,
    variant: "error",
    hint: false
  },
  disabled: {
    digits: ["", "", ""],
    active: -1,
    variant: "disabled",
    hint: false
  },
  disabledInput: {
    digits: ["11", "22", "33"],
    active: -1,
    variant: "disabled",
    hint: false
  }
};
function SortCode({
  state = "default",
  // default | input01 | input02 | input03 | valid | error | disabled | disabledInput
  label = "Label",
  values,
  // optional [a,b,c] override of the cell digits (presentational)
  validHint = "Lloyds Bank PLC",
  // resolved bank name shown in the Valid state
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  onChange,
  // (joined "dd-dd-dd") => void
  onComplete,
  // (joined) => void when all 6 digits entered
  className = "",
  ...rest
}) {
  ensureStyles();
  const {
    useState,
    useRef,
    useCallback
  } = React;
  const cfg = STATE_CONFIG[state] || STATE_CONFIG.default;
  const cls = ["cnc-sc"];
  if (className) cls.push(className);

  // ---------- INTERACTIVE (additive) ----------
  const [parts, setParts] = useState(["", "", ""]);
  const [focusIdx, setFocusIdx] = useState(-1);
  const refs = [useRef(null), useRef(null), useRef(null)];
  const isDisabled = state === "disabled" || state === "disabledInput";
  const join = arr => arr.map(p => (p || "").padEnd(0)).join("-");
  const handleType = useCallback((i, raw) => {
    const digits = raw.replace(/\D/g, "").slice(0, 2);
    setParts(prev => {
      const next = prev.slice();
      next[i] = digits;
      const joined = next.join("-");
      if (typeof onChange === "function") onChange(joined);
      if (next.every(p => p.length === 2) && typeof onComplete === "function") onComplete(joined);
      return next;
    });
    if (digits.length === 2 && i < 2 && refs[i + 1].current) refs[i + 1].current.focus();
  }, [onChange, onComplete]);
  const handleKey = useCallback((i, e) => {
    if (e.key === "Backspace" && !e.currentTarget.value && i > 0 && refs[i - 1].current) {
      refs[i - 1].current.focus();
    }
  }, []);
  const cellVariant = i => {
    if (interactive) {
      if (isDisabled) return "disabled";
      return focusIdx === i ? "active" : "default";
    }
    if (cfg.variant === "error") return "error";
    if (cfg.variant === "disabled") return "disabled";
    return i === cfg.active ? "active" : "default";
  };
  const renderCell = i => {
    const variant = cellVariant(i);
    const cellCls = ["cnc-sc__cell"];
    if (variant !== "default") cellCls.push("cnc-sc__cell--" + variant);
    const underlineCls = ["cnc-sc__underline"];
    if (variant !== "default") underlineCls.push("cnc-sc__underline--" + variant);
    let content;
    if (interactive) {
      content = /*#__PURE__*/React.createElement("input", {
        ref: refs[i],
        className: "cnc-sc__field",
        type: "text",
        inputMode: "numeric",
        maxLength: 2,
        value: parts[i],
        disabled: isDisabled,
        "aria-label": `${label} digits ${i * 2 + 1} to ${i * 2 + 2}`,
        onChange: e => handleType(i, e.target.value),
        onKeyDown: e => handleKey(i, e),
        onFocus: () => setFocusIdx(i),
        onBlur: () => setFocusIdx(-1)
      });
    } else {
      const digits = (values && values[i] != null ? values[i] : cfg.digits[i]) || "";
      const isCellDisabled = cfg.variant === "disabled";
      const showCaret = variant === "active";
      content = /*#__PURE__*/React.createElement(React.Fragment, null, digits ? /*#__PURE__*/React.createElement("span", {
        className: "cnc-sc__digits" + (isCellDisabled ? " cnc-sc__digits--disabled" : "")
      }, digits) : null, showCaret ? /*#__PURE__*/React.createElement("span", {
        className: "cnc-sc__cursor",
        "aria-hidden": "true"
      }) : null);
    }
    return /*#__PURE__*/React.createElement("div", {
      className: cellCls.join(" "),
      key: i
    }, content, /*#__PURE__*/React.createElement("span", {
      className: underlineCls.join(" "),
      "aria-hidden": "true"
    }));
  };
  const showHint = !interactive && cfg.hint && validHint;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sc__label"
  }, label), /*#__PURE__*/React.createElement("div", {
    className: "cnc-sc__col"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-sc__row"
  }, renderCell(0), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sc__sep",
    "aria-hidden": "true"
  }), renderCell(1), /*#__PURE__*/React.createElement("span", {
    className: "cnc-sc__sep",
    "aria-hidden": "true"
  }), renderCell(2)), showHint ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-sc__hint"
  }, validHint) : null));
}
Object.assign(__ds_scope, { SortCode });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/SortCode/SortCode.jsx", error: String((e && e.message) || e) }); }

// src/components/forms/Text_Field/TextField/TextField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Text Field
 * The base form input (Figma "Text Field"). Every picker (Sort Code, Password, Dropdown,
 * Country/Calendar pickers, etc.) composes this field. Matches the extraction's three axes:
 *   Layout      : Default (label above) | Inline (left "Label :" inside the box)
 *   State       : Default | Active | Error | Disabled
 *   Data format : None | Placeholder | Edit
 *   → 2 × 4 × 3 = 24 variants.
 *
 * Two modes:
 *   • PRESENTATIONAL (default) — faithful to the Figma extraction. State/data are prop-driven
 *     (`state`, `dataFormat`, `value`); the caret is a static rectangle shown only in Active.
 *   • INTERACTIVE (`interactive`) — ADDITIVE, opt-in for test journeys: renders a real <input>,
 *     so the field focuses, types, shows the native caret, and (with `validate`/`required`) flips
 *     to the Error state + hint on blur. This is behaviour LAYERED ON TOP of the spec — the DS
 *     primitive still matches Figma exactly by default. (See FLAGS.)
 *
 * Anatomy (375 wide — Figma FIXED, exposed as `width`; token-driven):
 *   Root — VERTICAL, paddingLeft/Right --spacing-size16, itemSpacing --spacing-size08:
 *     • Label (TEXT, type Style 7 Bold, --text-input-label) — DEFAULT layout only (omitted Inline).
 *     • V Stack — VERTICAL, gap 0:
 *         · Input — HORIZONTAL box, bg --background-input-default (disabled --background-input-disabled),
 *           border per State, radius --c-input-cornerradius (12), padding --spacing-size16,
 *           gap --spacing-size08, min-height --c-input-minheight (56). Contains:
 *             - H Stack (FILL) — the value area:
 *                 · [Inline] left "Label" + ":" (Style 6, --c-temp-input-leftlabel;
 *                   disabled --text-input-disabled), paddingRight 12 on the label group.
 *                 · content — None: empty · Placeholder: "Placeholder text"
 *                   (Style 6, --text-input-placeholder) · Edit: value (Style 6, --text-input-default;
 *                   disabled --text-input-disabled) · Interactive: a real <input>.
 *                 · [Active, presentational] Cursor — 2×21 rect, --c-temp-input-cursor, radius 10,
 *                   positioned at the insertion point (before an empty/placeholder field, after a value).
 *             - Border underline (RECTANGLE, h2) — fill token per State
 *               (--c-temp-input-borderunderline / -active / -error). All four are TRANSPARENT in
 *               the token set, so the underline is invisible in every state — rendered faithfully.
 *             - [Error] trailing alert Icon (24, padding). - [Edit] optional Custom icon
 *               (24, no padding) — hidden in Figma; rendered only when `customIcon` is supplied.
 *         · Hint message — visible in Figma ONLY in Error. Rendered when `hint` is supplied (or
 *           always in Error): type Style 8, --text-input-error (Error) / --text-generic-subdued.
 *
 * Border width: --c-temp-input-borderwidthdefault (1) for Default/Error/Disabled,
 * --c-temp-input-borderwidthactive (2) for Active.
 *
 * ICON COLOUR (token-driven): error alert pinned to --text-input-error; edit custom icon pinned
 * to --c-temp-input-clearicon (the Figma clear/action-icon token).
 *
 * FLAGS:
 *   • Hint message sub-component — the Figma "Hint message" instance (Type=Hint / Type=Error) was
 *     NOT expanded in the extraction, so its internals (any leading icon, exact type style, top
 *     spacing) are INFERRED: a single Style 8 line, coloured by type, with a --spacing-size04 top
 *     gap. Its margin spacing token is 0, honoured. Flagged for proofing.
 *   • Error alert / Edit custom icon — Figma supplies these via INSTANCE_SWAP node IDs, not
 *     resolvable to names from the extraction. Sensible defaults chosen (Sentiment system/Critical
 *     for the alert), both prop-driven. Inferred — flagged.
 *   • Interactive / validate / required — ADDITIVE behaviour for test journeys, NOT in the Figma
 *     extraction; off by default so the DS primitive stays spec-faithful.
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-textfield-styles";
const CSS = `
.cnc-tf { box-sizing: border-box; }
.cnc-tf *, .cnc-tf *::before, .cnc-tf *::after { box-sizing: border-box; }

.cnc-tf {
  display: flex; flex-direction: column; align-items: stretch;
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
  gap: var(--spacing-size08);
}

/* Top label — Default layout only. */
.cnc-tf__label {
  font-family: var(--type-style7-family);
  font-size: var(--type-style7-size);
  line-height: var(--type-style7-line);
  font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking);
  color: var(--text-input-label);
}

.cnc-tf__vstack { display: flex; flex-direction: column; align-items: stretch; gap: 0; }

/* Input box. */
.cnc-tf__input {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
  min-height: var(--c-input-minheight);
  padding: var(--spacing-size16);
  border-radius: var(--c-input-cornerradius);
  background: var(--background-input-default);
  border-style: solid;
  border-width: var(--c-temp-input-borderwidthdefault);
  border-color: var(--border-input-default);
}
.cnc-tf__input--active   { border-width: var(--c-temp-input-borderwidthactive); border-color: var(--border-input-active); }
.cnc-tf__input--error    { border-color: var(--border-input-critical); }
.cnc-tf__input--disabled { background: var(--background-input-disabled); border-color: var(--border-input-disabled); }

/* Value area. */
.cnc-tf__hstack {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center;
}
.cnc-tf__hstack--inline { gap: 4px; }

.cnc-tf__leftlabel {
  display: flex; flex-direction: row; align-items: center;
  padding-right: 12px;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--c-temp-input-leftlabel);
  white-space: nowrap;
}
.cnc-tf__leftlabel--disabled { color: var(--text-input-disabled); }

/* Static value text — hugs its content so the caret can sit right after it. */
.cnc-tf__value {
  flex: 0 1 auto; min-width: 0; max-width: 100%;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
}
.cnc-tf__value--placeholder { color: var(--text-input-placeholder); }
.cnc-tf__value--edit        { color: var(--text-input-default); }
.cnc-tf__value--disabled    { color: var(--text-input-disabled); }

/* Real input — INTERACTIVE mode (additive). Inherits the Style-6 value type; native caret. */
.cnc-tf__field {
  flex: 1 1 auto; min-width: 0;
  border: 0; outline: 0; background: transparent; padding: 0; margin: 0;
  font-family: var(--type-style6-family);
  font-size: var(--type-style6-size);
  line-height: var(--type-style6-line);
  font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking);
  color: var(--text-input-default);
  caret-color: var(--c-temp-input-cursor);
}
.cnc-tf__field::placeholder { color: var(--text-input-placeholder); opacity: 1; }
.cnc-tf__field:disabled { color: var(--text-input-disabled); -webkit-text-fill-color: var(--text-input-disabled); }

/* Caret — presentational Active only. */
.cnc-tf__cursor {
  flex: 0 0 auto;
  width: 2px; height: 21px;
  border-radius: 10px;
  background: var(--c-temp-input-cursor);
}

/* Border underline — token-driven (transparent in every state per the token set). */
.cnc-tf__underline {
  position: absolute; left: 0; right: 0; bottom: 0;
  height: 2px;
  background: var(--c-temp-input-borderunderline);
}
.cnc-tf__underline--active { background: var(--c-temp-input-borderunderlineactive); }
.cnc-tf__underline--error  { background: var(--c-temp-input-borderunderlineerror); }

.cnc-tf__trailing { flex: 0 0 auto; display: inline-flex; align-items: center; }

/* Hint / error message (sub-component internals inferred — see FLAGS). */
.cnc-tf__hint {
  margin-top: var(--spacing-size04);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
  color: var(--text-generic-subdued);
  text-wrap: pretty;
}
.cnc-tf__hint--error { color: var(--text-input-error); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// Named validation presets for INTERACTIVE journeys (additive — not part of the Figma spec).
const VALIDATORS = {
  required: v => v && String(v).trim() ? "" : "Please complete this field.",
  email: v => !v ? "" : /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(v) ? "" : "Enter a valid email address, including an @.",
  sortcode: v => !v ? "" : /^\d{2}-?\d{2}-?\d{2}$/.test(String(v).replace(/\s/g, "")) ? "" : "Enter a 6-digit sort code."
};
function TextField({
  layout = "default",
  // "default" | "inline"
  state = "default",
  // "default" | "active" | "error" | "disabled"
  dataFormat = "none",
  // "none" | "placeholder" | "edit"
  label = "Label",
  value,
  // shown when dataFormat="edit" (presentational, defaults to "Data input"); seeds the interactive value when supplied
  placeholder = "Placeholder text",
  hint = "",
  // hint / error message (visible in Figma only in Error)
  errorIcon = "Sentiment system/Critical/Critical",
  customIcon = null,
  // optional trailing icon for dataFormat="edit" (hidden in Figma)
  // — Interactive mode (additive, opt-in) —
  interactive = false,
  // render a real <input>: focus, type, native caret, blur-validation
  type = "text",
  // input type when interactive: text | email | tel | password | number …
  validate = null,
  // fn(value)=>msg("" if valid) | preset string | array of those
  required = false,
  // convenience: prepend the "required" rule
  onChange,
  // (value) => void
  onFocus,
  onBlur,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const {
    useState,
    useCallback
  } = React;
  const [val, setVal] = useState(typeof value === "string" ? value : "");
  const [focused, setFocused] = useState(false);
  const [touched, setTouched] = useState(false);
  const runValidate = useCallback(v => {
    const rules = [];
    if (required) rules.push("required");
    if (Array.isArray(validate)) rules.push(...validate);else if (validate) rules.push(validate);
    for (const r of rules) {
      const fn = typeof r === "function" ? r : VALIDATORS[r];
      if (!fn) continue;
      const msg = fn(v) || "";
      if (msg) return msg;
    }
    return "";
  }, [validate, required]);
  const isInline = layout === "inline";

  // Resolve effective state/data/hint. Interactive mode derives them from focus + value + validation;
  // a forced state="disabled" always wins, and an explicit state="error" still shows.
  let effState = state;
  let effDataFormat = dataFormat;
  let effHint = hint;
  if (interactive) {
    const validationMsg = touched ? runValidate(val) : "";
    effDataFormat = val ? "edit" : "placeholder";
    if (state === "disabled") {
      effState = "disabled";
    } else if (validationMsg || state === "error") {
      effState = "error";
      effHint = validationMsg || hint;
    } else if (focused) {
      effState = "active";
    } else {
      effState = "default";
    }
  }
  const isDisabled = effState === "disabled";
  const isActive = effState === "active";
  const isError = effState === "error";
  const inputCls = ["cnc-tf__input"];
  if (isActive) inputCls.push("cnc-tf__input--active");
  if (isError) inputCls.push("cnc-tf__input--error");
  if (isDisabled) inputCls.push("cnc-tf__input--disabled");
  const underlineCls = ["cnc-tf__underline"];
  if (isActive) underlineCls.push("cnc-tf__underline--active");
  if (isError) underlineCls.push("cnc-tf__underline--error");

  // ---- Value area content ----
  let valueArea = null;
  if (interactive) {
    valueArea = /*#__PURE__*/React.createElement("input", {
      className: "cnc-tf__field",
      type: type,
      value: val,
      placeholder: placeholder,
      disabled: isDisabled,
      "aria-invalid": isError || undefined,
      onChange: e => {
        setVal(e.target.value);
        if (typeof onChange === "function") onChange(e.target.value);
      },
      onFocus: e => {
        setFocused(true);
        if (typeof onFocus === "function") onFocus(e);
      },
      onBlur: e => {
        setFocused(false);
        setTouched(true);
        if (typeof onBlur === "function") onBlur(e);
      }
    });
  } else {
    // Presentational: static text + a caret at the insertion point when Active.
    const caret = isActive ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-tf__cursor"
    }) : null;
    if (effDataFormat === "placeholder") {
      // Empty field → caret sits BEFORE the placeholder.
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, caret, /*#__PURE__*/React.createElement("span", {
        className: "cnc-tf__value cnc-tf__value--placeholder"
      }, placeholder));
    } else if (effDataFormat === "edit") {
      // Field with a value → caret sits AFTER the text.
      const vCls = ["cnc-tf__value", isDisabled ? "cnc-tf__value--disabled" : "cnc-tf__value--edit"];
      valueArea = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
        className: vCls.join(" ")
      }, value == null ? "Data input" : value), caret);
    } else {
      // None → caret only (at the start).
      valueArea = caret;
    }
  }
  const cls = ["cnc-tf"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const showHint = isError || typeof effHint === "string" && effHint.length > 0;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), !isInline ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tf__label"
  }, label) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tf__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: inputCls.join(" ")
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tf__hstack" + (isInline ? " cnc-tf__hstack--inline" : "")
  }, isInline ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tf__leftlabel" + (isDisabled ? " cnc-tf__leftlabel--disabled" : "")
  }, label, ":") : null, valueArea), /*#__PURE__*/React.createElement("span", {
    className: underlineCls.join(" "),
    "aria-hidden": "true"
  }), isError && IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tf__trailing"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: errorIcon,
    size: 24,
    padding: true,
    color: "var(--text-input-error)",
    basePath: basePath
  })) : null, effDataFormat === "edit" && customIcon && IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tf__trailing"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: customIcon,
    size: 24,
    color: "var(--c-temp-input-clearicon)",
    basePath: basePath
  })) : null), showHint ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-tf__hint" + (isError ? " cnc-tf__hint--error" : "")
  }, effHint) : null));
}
Object.assign(__ds_scope, { TextField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/forms/Text_Field/TextField/TextField.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Pagination/PaginationDots/PaginationDots.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — _Pagination Dots  (INTERNAL — composed by Pagination, not for direct use)
 *
 * The dot strip inside a Pagination control. Mirrors the Figma "_Pagination Dots" set
 * (Total No. of Items × Active Item). A horizontal row, 8px gap (Spacing/Size 08), centred.
 *
 * Dot model (faithful to the variant set):
 *   • Active dot   — 24 × 6 pill, fill Icon/Generic/Default.
 *   • Other dots   — circle, fill Icon/Generic/Subdued, sized by distance from the active dot:
 *       – total ≤ 4 : every non-active dot is 6 × 6 (no shrink, none hidden).
 *       – total ≥ 5 : distance 1 → 6 × 6 · distance 2 → 4 × 4 · distance ≥ 3 → hidden.
 *   This reproduces all 11 Figma variants (2–5 items) exactly and extends the same windowing
 *   rule to larger totals.
 *
 * FLAG: the dot dimensions (24 / 6 / 4) carry token:null in the Figma sizing — they are literal
 * per the extraction (no spacing/size token was bound to them), so they are written as literals.
 * The only spacing token in this component is the 8px gap (var(--spacing-size08)). No value invented.
 *
 * Colour is token-driven (Icon/Generic/*), so light/dark map automatically — no [data-theme] block.
 */

const STYLE_ID = "cnc-pagdots-styles";
const CSS = `
.cnc-pagdots {
  display: inline-flex; align-items: center;
  gap: var(--spacing-size08);
  height: 6px;
}
.cnc-pagdots__dot { flex: 0 0 auto; border-radius: 9999px; }
.cnc-pagdots__dot--active { width: 24px; height: 6px; background: var(--icon-generic-default); }
.cnc-pagdots__dot--medium { width: 6px;  height: 6px; background: var(--icon-generic-subdued); }
.cnc-pagdots__dot--small  { width: 4px;  height: 4px; background: var(--icon-generic-subdued); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function dotModel(total, active) {
  const out = [];
  for (let i = 1; i <= total; i++) {
    const d = Math.abs(i - active);
    let kind;
    if (d === 0) kind = "active";else if (total <= 4) kind = "medium";else if (d === 1) kind = "medium";else if (d === 2) kind = "small";else kind = null; // hidden
    if (kind) out.push({
      key: i,
      kind
    });
  }
  return out;
}
function PaginationDots({
  total = 3,
  active = 1,
  className = "",
  ...rest
}) {
  ensureStyles();
  const n = Math.max(1, Math.round(total));
  const a = Math.min(n, Math.max(1, Math.round(active)));
  const dots = dotModel(n, a);
  const cls = ["cnc-pagdots"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    role: "group",
    "aria-label": `Page ${a} of ${n}`
  }, rest), dots.map(dot => /*#__PURE__*/React.createElement("span", {
    key: dot.key,
    className: `cnc-pagdots__dot cnc-pagdots__dot--${dot.kind}`,
    "aria-current": dot.kind === "active" ? "true" : undefined
  })));
}
Object.assign(__ds_scope, { PaginationDots });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Pagination/PaginationDots/PaginationDots.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Pagination/Pagination/Pagination.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Pagination
 * A centred paging control: a previous affordance · the _Pagination Dots strip · a next affordance.
 * Matches the Figma "Pagination" set: Position (Start | Middle | End) × Layout (+ Arrows | + Buttons).
 *
 * Faithful mapping of the Figma axes onto data props:
 *   • Position is derived from `active` / `total` — Start = first item (previous hidden),
 *     End = last item (next hidden), Middle = both shown. In Figma the hidden chevron is set to
 *     opacity 0 (space retained so the dots stay centred); reproduced here with opacity:0 +
 *     pointer-events:none + aria-hidden.
 *   • Layout:
 *       – "arrows"  → previous/next are Icon Buttons (Size Small, Tertiary) with Chevron Left/Right.
 *       – "buttons" → previous/next are Compact Buttons (Size Small, Tertiary, Icon Position None)
 *                     labelled "Back" / "Next".
 *
 * Anatomy (token-driven):
 *   Bar — fixed 48px tall (Figma 375×48 frame), horizontal --spacing-size24 L/R padding, the three
 *   items centred as a cluster with --spacing-size16 gap (primaryAxisAlignItems CENTER in Figma).
 *   Dots — _Pagination Dots, total = `total`, active = `active`.
 *
 * FLAG: the Figma frame is a FIXED 375×48 mobile width with the cluster centred; this build keeps the
 * 48px height + centred cluster but lets the bar take its container width (`width` prop, default
 * "375px") so it is reusable. The Figma preview fixes 3 dots; any `total` is supported via the dots.
 * No properties, states or motion added beyond the extraction.
 */

const STYLE_ID = "cnc-pagination-styles";
const CSS = `
.cnc-pagination {
  box-sizing: border-box;
  display: flex; flex-direction: row;
  align-items: center; justify-content: center;
  gap: var(--spacing-size16);
  height: 48px;
  padding-left: var(--spacing-size24); padding-right: var(--spacing-size24);
}
.cnc-pagination__ctrl { flex: 0 0 auto; display: inline-flex; align-items: center; }
.cnc-pagination__ctrl--hidden { opacity: 0; pointer-events: none; }
.cnc-pagination__dots { flex: 0 0 auto; display: inline-flex; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Pagination({
  total = 3,
  active = 1,
  layout = "arrows",
  // "arrows" | "buttons"
  onPrev,
  onNext,
  backLabel = "Back",
  nextLabel = "Next",
  basePath = ".",
  width = "375px",
  className = "",
  ...rest
}) {
  ensureStyles();
  const Dots = fromNS("PaginationDots", __ds_scope.PaginationDots);
  const IconBtn = fromNS("IconButton", __ds_scope.IconButton);
  const CompactBtn = fromNS("CompactButton", __ds_scope.CompactButton);
  const n = Math.max(1, Math.round(total));
  const a = Math.min(n, Math.max(1, Math.round(active)));
  const showPrev = a > 1; // Position !== Start
  const showNext = a < n; // Position !== End

  const cls = ["cnc-pagination"];
  if (className) cls.push(className);
  const ctrlCls = shown => "cnc-pagination__ctrl" + (shown ? "" : " cnc-pagination__ctrl--hidden");
  const renderCtrl = (dir, shown) => {
    const isPrev = dir === "prev";
    const handler = isPrev ? onPrev : onNext;
    if (layout === "buttons") {
      return /*#__PURE__*/React.createElement("span", {
        className: ctrlCls(shown),
        "aria-hidden": shown ? undefined : "true"
      }, CompactBtn ? /*#__PURE__*/React.createElement(CompactBtn, {
        size: "small",
        variant: "tertiary",
        iconPosition: "none",
        onClick: shown ? handler : undefined,
        tabIndex: shown ? undefined : -1
      }, isPrev ? backLabel : nextLabel) : null);
    }
    return /*#__PURE__*/React.createElement("span", {
      className: ctrlCls(shown),
      "aria-hidden": shown ? undefined : "true"
    }, IconBtn ? /*#__PURE__*/React.createElement(IconBtn, {
      size: "small",
      variant: "tertiary",
      icon: isPrev ? "Arrows/Chevron Left/Chevron Left" : "Arrows/Chevron Right/Chevron Right",
      label: isPrev ? "Previous" : "Next",
      basePath: basePath,
      onClick: shown ? handler : undefined,
      tabIndex: shown ? undefined : -1
    }) : null);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: {
      width
    },
    role: "navigation",
    "aria-label": "Pagination"
  }, rest), renderCtrl("prev", showPrev), /*#__PURE__*/React.createElement("span", {
    className: "cnc-pagination__dots"
  }, Dots ? /*#__PURE__*/React.createElement(Dots, {
    total: n,
    active: a
  }) : null), renderCtrl("next", showNext));
}
Object.assign(__ds_scope, { Pagination });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Pagination/Pagination/Pagination.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Tabs/TabCollectionScroll/TabCollectionScroll.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Tab Collection Scroll
 * A horizontally-scrolling tab collection. Matches the Figma "Tab Collection Scroll" set:
 *   Layout      : Default (scrolling pill row) | Alt 01 (underline)
 *   Active Tab  : Tab 01 … Tab 06  (driven by `value` = active index; any tab count via `tabs`)
 *
 * Anatomy (token-driven):
 *   Default — 48px row, padding-left 16 (right 0 / Spacing-None), overflow-x scroll. An H-stack of
 *     pill tabs (gap --spacing-size12, each min-width 134, 36px tall, padding 8 24, Style 8, radius
 *     --radius-default). ACTIVE pill: solid --c-temp-toggle-backgroundactive fill,
 *     --c-temp-toggle-textdefault label, no border. INACTIVE pills: transparent with a
 *     --border-action-default hairline and --text-action-default label. The row overflows the 375
 *     frame and scrolls. (The source reuses the Toggle fill/text tokens for this layout.)
 *   Alt 01 — 52px SCROLLING tab row, padding 0 16. Tabs size to their content (padding 16) and the
 *     row scrolls horizontally (overflow-x). Labels use --c-temp-tabs-textdefaultalt01 / -textactivealt01.
 *     The underline
 *     is rendered per-tab as a BOTTOM BORDER so it scrolls in sync: INACTIVE tabs take a
 *     --border-generic-default 1px baseline; the ACTIVE tab a 2px --c-temp-tabs-borderactivealt01.
 *     (Figma draws the inactive stroke as a full box + a separate 2px strip — rendered here as a
 *     bottom baseline so the scrolling row reads as underline tabs, not boxed cells. Flagged.)
 *
 * Width: the Figma frame is FIXED 375. Build keeps that as the default viewport (`width` prop); both
 * the Default pill row and the Alt 01 underline row scroll horizontally inside it.
 */

const STYLE_ID = "cnc-tabscroll-styles";
const CSS = `
.cnc-tabscroll { box-sizing: border-box; }
.cnc-tabscroll *, .cnc-tabscroll *::before, .cnc-tabscroll *::after { box-sizing: border-box; }

/* ---- Default: scrolling pill row ---- */
.cnc-tabscroll--default {
  display: flex; align-items: center; height: 48px;
  padding-left: var(--spacing-size16);
  overflow-x: auto; overflow-y: hidden;
  scrollbar-width: none; -ms-overflow-style: none;
}
.cnc-tabscroll--default::-webkit-scrollbar { display: none; }
.cnc-tabscroll__row { display: flex; align-items: center; gap: var(--spacing-size12); height: 36px; }
.cnc-tabscroll__pill {
  flex: 0 0 auto; min-width: 134px;
  display: inline-flex; align-items: center; justify-content: center;
  height: 36px; padding: var(--spacing-size08) var(--spacing-size24);
  border: var(--border-width-01) solid var(--border-action-default);
  border-radius: var(--radius-default);
  background: transparent; cursor: pointer;
  color: var(--text-action-default);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabscroll__pill.is-active {
  border-color: transparent;
  background: var(--c-temp-toggle-backgroundactive);
  color: var(--c-temp-toggle-textdefault);
}

/* ---- Alt 01: underline (scrolls; baseline + active underline as per-tab bottom borders) ---- */
.cnc-tabscroll--alt01 {
  display: flex; align-items: stretch; height: 52px;
  padding: 0 var(--spacing-size16);
  overflow-x: auto; overflow-y: hidden;
  scrollbar-width: none; -ms-overflow-style: none;
}
.cnc-tabscroll--alt01::-webkit-scrollbar { display: none; }
.cnc-tabscroll__utab {
  flex: 0 0 auto;
  display: flex; align-items: center; justify-content: center;
  height: 52px; padding: var(--spacing-size16);
  border: 0;
  border-bottom: var(--border-width-01) solid var(--border-generic-default);
  background: transparent; cursor: pointer;
  color: var(--c-temp-tabs-textdefaultalt01);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabscroll__utab.is-active {
  color: var(--c-temp-tabs-textactivealt01);
  border-bottom: 2px solid var(--c-temp-tabs-borderactivealt01);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function TabCollectionScroll({
  tabs = ["Tab 01", "Tab 02", "Tab 03", "Tab 04", "Tab 05", "Tab 06"],
  layout = "default",
  value = 0,
  onChange,
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const active = Number(value) || 0;
  const select = i => {
    if (typeof onChange === "function") onChange(i);
  };
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  if (layout === "alt01") {
    const cls = ["cnc-tabscroll", "cnc-tabscroll--alt01"];
    if (className) cls.push(className);
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cls.join(" "),
      role: "tablist",
      style: rootStyle
    }, rest), tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "tab",
      "aria-selected": i === active,
      className: "cnc-tabscroll__utab" + (i === active ? " is-active" : ""),
      onClick: () => select(i)
    }, label)));
  }
  const cls = ["cnc-tabscroll", "cnc-tabscroll--default"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    role: "tablist",
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tabscroll__row"
  }, tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    role: "tab",
    "aria-selected": i === active,
    className: "cnc-tabscroll__pill" + (i === active ? " is-active" : ""),
    onClick: () => select(i)
  }, label))));
}
Object.assign(__ds_scope, { TabCollectionScroll });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Tabs/TabCollectionScroll/TabCollectionScroll.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Tabs/ThreeTabsFixed/ThreeTabsFixed.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — 3 Tabs Fixed
 * A fixed (equal-width) three-tab control. Matches the Figma "3 Tabs Fixed" set exactly:
 *   Layout      : Default (joined segmented pill) | Alt 01 (underline)
 *   Active Tab  : Tab 01 | Tab 02 | Tab 03  (driven by `value` = active index 0 | 1 | 2)
 *
 * Identical anatomy to 2 Tabs Fixed — only the cell count differs (three equal-width segments):
 *   Default — 48px row, padding 0 16. Equal-width segments (36px tall, padding 8 0, Style 8). ACTIVE
 *     segment solid (--c-temp-toggle-backgroundactive / --c-temp-toggle-textdefault, no border);
 *     INACTIVE segments transparent with --border-action-default hairline and --text-action-default.
 *     First/last segments carry the outer --radius-default corners; joined cells collapse the shared
 *     1px edge. (Faithful to the source, which reuses the Toggle fill/text tokens here.)
 *   Alt 01 — 52px tab row, padding 0 16, equal-width cells (padding 16). INACTIVE cells take a
 *     --border-generic-default hairline; the active cell has none. Labels use
 *     --c-temp-tabs-textdefaultalt01 / --c-temp-tabs-textactivealt01. A 2px underline strip
 *     (padding 0 16) sits beneath: one segment per tab, active filled --c-temp-tabs-borderactivealt01.
 *
 * Width: the Figma frame is FIXED 375×(48|52). Build keeps the heights + centred cluster but exposes
 * a `width` prop (default 375) for reuse. No additions beyond the extraction.
 */

const STYLE_ID = "cnc-tabsfixed-styles";
const CSS = `
.cnc-tabs { box-sizing: border-box; }
.cnc-tabs *, .cnc-tabs *::before, .cnc-tabs *::after { box-sizing: border-box; }

/* ---- Default: joined segmented pill ---- */
.cnc-tabs--default { display: flex; align-items: center; height: 48px; padding: 0 var(--spacing-size16); }
.cnc-tabs__hstack { display: flex; align-items: stretch; width: 100%; height: 36px; }
.cnc-tabs__seg {
  flex: 1 1 0; min-width: 0;
  display: flex; align-items: center; justify-content: center;
  padding: var(--spacing-size08) 0;
  border: var(--border-width-01) solid var(--border-action-default);
  background: transparent; cursor: pointer;
  color: var(--text-action-default);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabs__seg + .cnc-tabs__seg { margin-left: calc(-1 * var(--border-width-01)); }
.cnc-tabs__seg:first-child { border-top-left-radius: var(--radius-default); border-bottom-left-radius: var(--radius-default); }
.cnc-tabs__seg:last-child { border-top-right-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }
.cnc-tabs__seg.is-active {
  position: relative; z-index: 1;
  border-color: transparent;
  background: var(--c-temp-toggle-backgroundactive);
  color: var(--c-temp-toggle-textdefault);
}

/* ---- Alt 01: underline ---- */
.cnc-tabs--alt01 { display: flex; flex-direction: column; }
.cnc-tabs__row { display: flex; align-items: stretch; padding: 0 var(--spacing-size16); }
.cnc-tabs__tab {
  flex: 1 1 0; min-width: 0;
  display: flex; align-items: center; justify-content: center;
  height: 52px; padding: var(--spacing-size16);
  border: var(--border-width-01) solid transparent;
  background: transparent; cursor: pointer;
  color: var(--c-temp-tabs-textdefaultalt01);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabs__tab + .cnc-tabs__tab { margin-left: calc(-1 * var(--border-width-01)); }
.cnc-tabs__tab:not(.is-active) { border-color: var(--border-generic-default); }
.cnc-tabs__tab.is-active { position: relative; z-index: 1; color: var(--c-temp-tabs-textactivealt01); }
.cnc-tabs__underline { display: flex; height: 2px; padding: 0 var(--spacing-size16); }
.cnc-tabs__ind { flex: 1 1 0; height: 2px; background: transparent; }
.cnc-tabs__ind.is-active { background: var(--c-temp-tabs-borderactivealt01); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}

/** Shared fixed-tabs renderer (count is fixed by the wrapper). */
function FixedTabs({
  tabs,
  layout,
  value,
  onChange,
  width,
  className,
  ...rest
}) {
  ensureStyles();
  const active = Number(value) || 0;
  const select = i => {
    if (typeof onChange === "function") onChange(i);
  };
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  if (layout === "alt01") {
    const cls = ["cnc-tabs", "cnc-tabs--alt01"];
    if (className) cls.push(className);
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cls.join(" "),
      role: "tablist",
      style: rootStyle
    }, rest), /*#__PURE__*/React.createElement("div", {
      className: "cnc-tabs__row"
    }, tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "tab",
      "aria-selected": i === active,
      className: "cnc-tabs__tab" + (i === active ? " is-active" : ""),
      onClick: () => select(i)
    }, label))), /*#__PURE__*/React.createElement("div", {
      className: "cnc-tabs__underline",
      "aria-hidden": "true"
    }, tabs.map((_, i) => /*#__PURE__*/React.createElement("span", {
      key: i,
      className: "cnc-tabs__ind" + (i === active ? " is-active" : "")
    }))));
  }
  const cls = ["cnc-tabs", "cnc-tabs--default"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    role: "tablist",
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tabs__hstack"
  }, tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    role: "tab",
    "aria-selected": i === active,
    className: "cnc-tabs__seg" + (i === active ? " is-active" : ""),
    onClick: () => select(i)
  }, label))));
}
function ThreeTabsFixed({
  tab1Label = "Tab 01",
  tab2Label = "Tab 02",
  tab3Label = "Tab 03",
  layout = "default",
  value = 0,
  onChange,
  width = 375,
  className = "",
  ...rest
}) {
  return /*#__PURE__*/React.createElement(FixedTabs, _extends({
    tabs: [tab1Label, tab2Label, tab3Label],
    layout: layout,
    value: value,
    onChange: onChange,
    width: width,
    className: className
  }, rest));
}
Object.assign(__ds_scope, { ThreeTabsFixed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Tabs/ThreeTabsFixed/ThreeTabsFixed.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Tabs/TwoTabsFixed/TwoTabsFixed.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — 2 Tabs Fixed
 * A fixed (equal-width) two-tab control. Matches the Figma "2 Tabs Fixed" set exactly:
 *   Layout      : Default (joined segmented pill) | Alt 01 (underline)
 *   Active Tab  : Tab 01 | Tab 02  (driven by `value` = active index 0 | 1)
 *
 * Anatomy (token-driven):
 *   Default — 48px row, padding 0 16. An H-stack of equal-width segments (36px tall, padding 8 0,
 *     Style 8 label). The ACTIVE segment is a solid fill (--c-temp-toggle-backgroundactive) with
 *     --c-temp-toggle-textdefault text and no border; INACTIVE segments are transparent with a
 *     --border-action-default hairline and --text-action-default text. The first/last segments carry
 *     the outer --radius-default corners; inner edges are square and the joined cells collapse their
 *     shared 1px edge. (Faithful to the source, which reuses the Toggle fill/text tokens here.)
 *   Alt 01 — 52px tab row, padding 0 16, equal-width cells (padding 16). INACTIVE cells take a
 *     --border-generic-default hairline; the active cell has none. Labels use
 *     --c-temp-tabs-textdefaultalt01 / --c-temp-tabs-textactivealt01. A 2px underline strip
 *     (padding 0 16) sits beneath: one segment per tab, the active one filled
 *     --c-temp-tabs-borderactivealt01, the rest transparent.
 *
 * Width: the Figma frame is FIXED 375×(48|52). Build keeps the heights + centred cluster but exposes
 * a `width` prop (default 375) for reuse. No additions beyond the extraction — no motion/elevation,
 * and the unused Tabs icon/border tokens are not applied (the source carries no icons here).
 */

const STYLE_ID = "cnc-tabsfixed-styles";
const CSS = `
.cnc-tabs { box-sizing: border-box; }
.cnc-tabs *, .cnc-tabs *::before, .cnc-tabs *::after { box-sizing: border-box; }

/* ---- Default: joined segmented pill ---- */
.cnc-tabs--default { display: flex; align-items: center; height: 48px; padding: 0 var(--spacing-size16); }
.cnc-tabs__hstack { display: flex; align-items: stretch; width: 100%; height: 36px; }
.cnc-tabs__seg {
  flex: 1 1 0; min-width: 0;
  display: flex; align-items: center; justify-content: center;
  padding: var(--spacing-size08) 0;
  border: var(--border-width-01) solid var(--border-action-default);
  background: transparent; cursor: pointer;
  color: var(--text-action-default);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabs__seg + .cnc-tabs__seg { margin-left: calc(-1 * var(--border-width-01)); }
.cnc-tabs__seg:first-child { border-top-left-radius: var(--radius-default); border-bottom-left-radius: var(--radius-default); }
.cnc-tabs__seg:last-child { border-top-right-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }
.cnc-tabs__seg.is-active {
  position: relative; z-index: 1;
  border-color: transparent;
  background: var(--c-temp-toggle-backgroundactive);
  color: var(--c-temp-toggle-textdefault);
}

/* ---- Alt 01: underline ---- */
.cnc-tabs--alt01 { display: flex; flex-direction: column; }
.cnc-tabs__row { display: flex; align-items: stretch; padding: 0 var(--spacing-size16); }
.cnc-tabs__tab {
  flex: 1 1 0; min-width: 0;
  display: flex; align-items: center; justify-content: center;
  height: 52px; padding: var(--spacing-size16);
  border: var(--border-width-01) solid transparent;
  background: transparent; cursor: pointer;
  color: var(--c-temp-tabs-textdefaultalt01);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-tabs__tab + .cnc-tabs__tab { margin-left: calc(-1 * var(--border-width-01)); }
.cnc-tabs__tab:not(.is-active) { border-color: var(--border-generic-default); }
.cnc-tabs__tab.is-active { position: relative; z-index: 1; color: var(--c-temp-tabs-textactivealt01); }
.cnc-tabs__underline { display: flex; height: 2px; padding: 0 var(--spacing-size16); }
.cnc-tabs__ind { flex: 1 1 0; height: 2px; background: transparent; }
.cnc-tabs__ind.is-active { background: var(--c-temp-tabs-borderactivealt01); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}

/** Shared fixed-tabs renderer (count is fixed by the wrapper). */
function FixedTabs({
  tabs,
  layout,
  value,
  onChange,
  width,
  className,
  ...rest
}) {
  ensureStyles();
  const active = Number(value) || 0;
  const select = i => {
    if (typeof onChange === "function") onChange(i);
  };
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  if (layout === "alt01") {
    const cls = ["cnc-tabs", "cnc-tabs--alt01"];
    if (className) cls.push(className);
    return /*#__PURE__*/React.createElement("div", _extends({
      className: cls.join(" "),
      role: "tablist",
      style: rootStyle
    }, rest), /*#__PURE__*/React.createElement("div", {
      className: "cnc-tabs__row"
    }, tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "tab",
      "aria-selected": i === active,
      className: "cnc-tabs__tab" + (i === active ? " is-active" : ""),
      onClick: () => select(i)
    }, label))), /*#__PURE__*/React.createElement("div", {
      className: "cnc-tabs__underline",
      "aria-hidden": "true"
    }, tabs.map((_, i) => /*#__PURE__*/React.createElement("span", {
      key: i,
      className: "cnc-tabs__ind" + (i === active ? " is-active" : "")
    }))));
  }
  const cls = ["cnc-tabs", "cnc-tabs--default"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    role: "tablist",
    style: rootStyle
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tabs__hstack"
  }, tabs.map((label, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    role: "tab",
    "aria-selected": i === active,
    className: "cnc-tabs__seg" + (i === active ? " is-active" : ""),
    onClick: () => select(i)
  }, label))));
}
function TwoTabsFixed({
  tab1Label = "Tab 01",
  tab2Label = "Tab 02",
  layout = "default",
  value = 0,
  onChange,
  width = 375,
  className = "",
  ...rest
}) {
  return /*#__PURE__*/React.createElement(FixedTabs, _extends({
    tabs: [tab1Label, tab2Label],
    layout: layout,
    value: value,
    onChange: onChange,
    width: width,
    className: className
  }, rest));
}
Object.assign(__ds_scope, { TwoTabsFixed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Tabs/TwoTabsFixed/TwoTabsFixed.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Toggle/Toggle/Toggle.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Toggle
 * A two-tab segmented switch (e.g. Yes / No). Matches the Figma "Toggle" set exactly:
 *   State        : Default | Disabled
 *   Selection    : All Inactive (neither) | Tab 1 Active | Tab 2 Active
 *
 * Anatomy (token-driven):
 *   Track  — pill, --c-temp-toggle-backgrounddefault, padding 2, radius --c-temp-toggle-cornerradiuslarge.
 *   Tab    — equal-width label cell (Style 8). The active tab gains the sliding pill fill
 *            (--c-temp-toggle-backgroundactive, or --c-temp-toggle-backgroundactivedisabled when disabled)
 *            at radius --c-temp-toggle-cornerradiusdefault; its label flips to --c-temp-toggle-textdefault.
 *            Inactive labels use --text-action-default; every label in the Disabled state uses
 *            --text-action-disabled (active included).
 *   Separator — 1px hairline (--border-generic-defaultalt01), shown ONLY when neither tab is active.
 *
 * FLAG: the Figma outer-pill corner radius is a literal 10px (token: null). The toggle's two radius
 * tokens are 8 (default, used by the active pill) and 12 (large). The pill is rendered with the large
 * token (--c-temp-toggle-cornerradiuslarge = 12) — the closest semantic match — pending owner confirm
 * of the 10-vs-12 discrepancy. No new token was invented.
 *
 * No additions beyond the extraction — the unused Toggle border tokens are not applied (the source
 * strokes are empty), and no motion/elevation is introduced.
 */

const STYLE_ID = "cnc-toggle-styles";
const CSS = `
.cnc-toggle {
  box-sizing: border-box;
  display: inline-flex; align-items: center; justify-content: center;
  min-width: 118px; height: 48px;
}
.cnc-toggle__track {
  box-sizing: border-box;
  display: flex; align-items: center;
  width: 100%; height: 36px;
  padding: 2px;
  background: var(--c-temp-toggle-backgrounddefault);
  border-radius: var(--c-temp-toggle-cornerradiuslarge);
}
.cnc-toggle__tab {
  flex: 1 1 0; min-width: 0;
  box-sizing: border-box;
  display: flex; align-items: center; justify-content: center;
  height: 32px; padding: 0 var(--spacing-size12);
  border: none; background: transparent; cursor: pointer;
  border-radius: var(--c-temp-toggle-cornerradiusdefault);
  color: var(--text-action-default);
  white-space: nowrap;
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
.cnc-toggle__tab.is-active {
  background: var(--c-temp-toggle-backgroundactive);
  color: var(--c-temp-toggle-textdefault);
}
.cnc-toggle.is-disabled .cnc-toggle__tab { cursor: default; color: var(--text-action-disabled); }
.cnc-toggle.is-disabled .cnc-toggle__tab.is-active {
  background: var(--c-temp-toggle-backgroundactivedisabled);
  color: var(--text-action-disabled);
}
.cnc-toggle__sep {
  flex: 0 0 auto; align-self: center;
  width: var(--border-width-01); height: 12px;
  background: var(--border-generic-defaultalt01);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function Toggle({
  tab1Label = "Yes",
  tab2Label = "No",
  value = null,
  // null (all inactive) | 1 | 2
  onChange,
  disabled = false,
  className = "",
  ...rest
}) {
  ensureStyles();
  const active1 = value === 1;
  const active2 = value === 2;
  const noneActive = !active1 && !active2;
  const cls = ["cnc-toggle"];
  if (disabled) cls.push("is-disabled");
  if (className) cls.push(className);
  const select = v => {
    if (disabled || typeof onChange !== "function") return;
    onChange(v);
  };
  const tabCls = on => "cnc-toggle__tab" + (on ? " is-active" : "");
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    role: "group"
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-toggle__track"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: tabCls(active1),
    "aria-pressed": active1,
    disabled: disabled,
    onClick: () => select(1)
  }, tab1Label), noneActive ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-toggle__sep",
    "aria-hidden": "true"
  }) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: tabCls(active2),
    "aria-pressed": active2,
    disabled: disabled,
    onClick: () => select(2)
  }, tab2Label)));
}
Object.assign(__ds_scope, { Toggle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Toggle/Toggle/Toggle.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Coachmark/Coachmark/Coachmark.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _Coachmark  (INTERNAL — composed by Coachmark Pop-up, not for direct use)
 *
 * The coachmark bubble: an arrow pointer joined to a white panel holding a leading sentiment Icon,
 * Title (Style 7) + Message (Style 8), a trailing close Icon, and — for the Multiple display — a
 * "n / total" progress counter and a Next / Done text button. Matches the Figma "_Coachmark" set
 * exactly. Axes:
 *   Display:        Multiple | Single        (Multiple adds the counter + Next/Done row)
 *   Order:          Start / Middle | End     (Multiple button label: Next vs Done; no effect on Single)
 *   Arrow Position: Top | Bottom             (pointer above the panel vs below it)
 *
 * Anatomy (token-driven):
 *   Root (V) — width ≤375 with --spacing-size16 L/R padding (panel reference 343), arrow and panel
 *     overlapped by 1px (Figma "Panel/Arrow/Neg 01 Spacing", value -1) so the pointer fuses to the panel.
 *   Arrow — 12px-tall triangle filled --background-panel-default, sat --spacing-size16 from the left
 *     edge (Figma arrow frame padding-left 16; the Arrow sub-component's exact base width / horizontal
 *     placement is not expanded in the extraction — see prompt flag).
 *   Panel (Container) — bg --background-panel-default, radius --radius-default (Figma 12, token:null).
 *     Single: padding 12/16/12/16. Multiple: padding 12/16/0/16 (the counter row's 48px height supplies
 *     the bottom space). Holds an H Stack: leading 16px sentiment Icon (top-aligned) + a FILL V Stack.
 *   V Stack — title row (Title Style 7 + Message Style 8, with the trailing close Hit Target/Icon) and,
 *     for Multiple, the counter row: "n" "/" "total" (Style 11, --text-generic-default) pushed left with
 *     the Next / Done Hit Target ("--text-action-default") on the right.
 *
 *   Glyph colour: both glyphs are masked Icons anchored to a --text-* token via `color` on their
 *   container (leading → --text-generic-default; close → --text-generic-default) so they track
 *   pressed/disabled/dark automatically. No hand-written [data-theme] block; theming is automatic.
 *
 * Internal — composed by Coachmark Pop-up. No additions beyond the extraction (no extra variants,
 * states, motion, or elevation).
 */

const STYLE_ID = "cnc-coachmark-styles";
const CSS = `
.cnc-cm {
  box-sizing: border-box;
  display: flex; flex-direction: column; align-items: stretch;
  width: 100%; max-width: 375px;
  padding-left: var(--spacing-size16); padding-right: var(--spacing-size16);
}
/* Arrow — 12px triangle filled with the panel colour, offset from the left edge */
.cnc-cm__arrow {
  width: 0; height: 0; flex: 0 0 auto;
  margin-left: var(--cm-arrow-offset, var(--spacing-size16));
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
}
.cnc-cm--top    .cnc-cm__arrow { border-bottom: 12px solid var(--background-panel-default); margin-bottom: -1px; }
.cnc-cm--bottom .cnc-cm__arrow { border-top: 12px solid var(--background-panel-default); margin-top: -1px; }
.cnc-cm--bottom { flex-direction: column-reverse; }

.cnc-cm__panel {
  box-sizing: border-box;
  background: var(--background-panel-default);
  border-radius: var(--radius-default);
  display: flex; flex-direction: column;
  padding: var(--spacing-size12) var(--spacing-size16);
}
.cnc-cm--multiple .cnc-cm__panel { padding-bottom: 0; }

.cnc-cm__hstack {
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size12);
}
.cnc-cm__iconwrap {
  flex: 0 0 auto;
  display: flex; align-items: flex-start; justify-content: center;
  color: var(--text-generic-default);   /* anchors the masked leading glyph */
}
.cnc-cm__vstack { display: flex; flex-direction: column; flex: 1 1 auto; min-width: 0; }

.cnc-cm__titlerow {
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size12); min-height: 40px;
}
.cnc-cm__text { display: flex; flex-direction: column; flex: 1 1 auto; min-width: 0; }
.cnc-cm__title {
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--text-generic-default);
}
.cnc-cm__message {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-default);
}
.cnc-cm__close {
  flex: 0 0 auto; align-self: center;
  display: inline-flex; align-items: center; justify-content: center;
  width: 48px; height: 48px; margin: -8px -8px -8px 0;    /* 48 hit target; glyph centred in the Figma 32px slot (centre 32px from panel edge) */
  padding: 0; border: none; background: transparent;
  -webkit-appearance: none; appearance: none;
  color: var(--text-generic-default);   /* anchors the masked close glyph */
}
.cnc-cm__close--btn { cursor: pointer; }

.cnc-cm__counterrow {
  display: flex; flex-direction: row; align-items: center;
  min-height: 48px;
}
.cnc-cm__counter {
  flex: 1 1 auto;
  font-family: var(--type-style11-family); font-size: var(--type-style11-size);
  line-height: var(--type-style11-line); font-weight: var(--type-style11-weight);
  letter-spacing: var(--type-style11-tracking); color: var(--text-generic-default);
}
.cnc-cm__counter .cnc-cm__sep { padding: 0 2px; }
.cnc-cm__advance {
  flex: 0 0 auto;
  display: inline-flex; align-items: center; min-height: 48px;
  padding: 0; border: none; background: transparent;
  -webkit-appearance: none; appearance: none;
  font-family: var(--type-style11-family); font-size: var(--type-style11-size);
  line-height: var(--type-style11-line); font-weight: var(--type-style11-weight);
  letter-spacing: var(--type-style11-tracking); color: var(--text-action-default);
}
.cnc-cm__advance--btn { cursor: pointer; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Coachmark({
  display = "multiple",
  // "multiple" | "single"
  order = "start-middle",
  // "start-middle" | "end"  (Multiple button: Next vs Done)
  arrowPosition = "top",
  // "top" | "bottom"
  title = "Title",
  message = "Message",
  leadingNo = "2",
  // counter — current step
  trailingNo = "5",
  // counter — total steps
  icon = "Sentiment system/Info/Info",
  // leading glyph (instance swap in source)
  closeIcon = "Navigation/Close/Close",
  arrowOffset,
  // override the arrow's distance from the left edge
  onClose,
  // close handler (omit → static affordance)
  onAdvance,
  // Next/Done handler (omit → static affordance)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const cls = ["cnc-cm", `cnc-cm--${display}`, `cnc-cm--${arrowPosition}`];
  if (className) cls.push(className);
  const style = arrowOffset != null ? {
    "--cm-arrow-offset": arrowOffset
  } : undefined;
  const advanceLabel = order === "end" ? "Done" : "Next";
  const closeGlyph = IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: closeIcon,
    size: 16,
    padding: true,
    basePath: basePath
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: style
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__arrow",
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__panel"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__hstack"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__iconwrap"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 16,
    basePath: basePath
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__vstack"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__titlerow"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__text"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__title"
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__message"
  }, message)), onClose ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-cm__close cnc-cm__close--btn",
    "aria-label": "Close",
    onClick: onClose
  }, closeGlyph) : /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__close",
    "aria-hidden": "true"
  }, closeGlyph)), display === "multiple" ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-cm__counterrow"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__counter"
  }, leadingNo, /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__sep"
  }, "/"), trailingNo), onAdvance ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-cm__advance cnc-cm__advance--btn",
    onClick: onAdvance
  }, advanceLabel) : /*#__PURE__*/React.createElement("span", {
    className: "cnc-cm__advance"
  }, advanceLabel)) : null))));
}
Object.assign(__ds_scope, { Coachmark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Coachmark/Coachmark/Coachmark.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Coachmark/CoachmarkPopup/CoachmarkPopup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Coachmark Pop-up
 * The full-screen onboarding overlay: a dimmed scrim (Background/Overlay/Default) with a circular
 * "spotlight" cutout revealing the target element, plus a positioned _Coachmark bubble pointing at it.
 * Matches the Figma "Coachmark pop-up" component (375×812). Figma note: "Detach component to move the
 * position of the coachmark and cut out" — so the cutout and bubble are positioned manually here via
 * props (cutout: top/left/size; bubble: top/left), defaulting to a sensible upper-left placement.
 *
 * Anatomy (token-driven):
 *   Scrim — fills the frame with --background-overlay-default. The cutout is a transparent circle
 *     (Figma "Subtract": Overlay rect minus a 44px radius-9999 rect) made with a huge box-shadow of the
 *     overlay colour around a transparent --radius-circle disc, so the target shows through the hole.
 *   Bubble — the composed _Coachmark, absolutely positioned, forwarding all of its props.
 *
 * No additions beyond the extraction — no extra variants, states, motion, or elevation.
 */

const STYLE_ID = "cnc-cmpopup-styles";
const CSS = `
.cnc-cmpopup {
  box-sizing: border-box;
  position: relative; overflow: hidden;
  width: 375px; height: 812px;
}
.cnc-cmpopup__cutout {
  position: absolute; box-sizing: border-box;
  width: var(--cmp-cut-size, 44px); height: var(--cmp-cut-size, 44px);
  top: var(--cmp-cut-top, 64px); left: var(--cmp-cut-left, 16px);
  border-radius: var(--radius-circle);
  /* the surrounding scrim is painted by the spread shadow; the disc itself stays transparent */
  box-shadow: 0 0 0 9999px var(--background-overlay-default);
}
.cnc-cmpopup__bubble {
  position: absolute;
  top: var(--cmp-bubble-top, 116px); left: var(--cmp-bubble-left, 0px);
  width: 375px; max-width: 375px;
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function CoachmarkPopup({
  // Spotlight cutout (px or any CSS length)
  cutoutTop = "64px",
  cutoutLeft = "16px",
  cutoutSize = "44px",
  // Bubble placement
  bubbleTop = "116px",
  bubbleLeft = "0px",
  // Forwarded _Coachmark props
  display = "single",
  order = "start-middle",
  arrowPosition = "top",
  title = "Title",
  message = "Message",
  leadingNo = "2",
  trailingNo = "5",
  icon,
  closeIcon,
  arrowOffset,
  onClose,
  onAdvance,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const CoachmarkCmp = fromNS("Coachmark", __ds_scope.Coachmark);
  const cls = ["cnc-cmpopup"];
  if (className) cls.push(className);
  const style = {
    "--cmp-cut-top": cutoutTop,
    "--cmp-cut-left": cutoutLeft,
    "--cmp-cut-size": cutoutSize,
    "--cmp-bubble-top": bubbleTop,
    "--cmp-bubble-left": bubbleLeft
  };
  const coachProps = {
    display,
    order,
    arrowPosition,
    title,
    message,
    leadingNo,
    trailingNo,
    icon,
    closeIcon,
    arrowOffset,
    onClose,
    onAdvance,
    basePath
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: style
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-cmpopup__cutout",
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cmpopup__bubble"
  }, CoachmarkCmp ? /*#__PURE__*/React.createElement(CoachmarkCmp, coachProps) : null));
}
Object.assign(__ds_scope, { CoachmarkPopup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Coachmark/CoachmarkPopup/CoachmarkPopup.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Error_Banner/ErrorBanner/ErrorBanner.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Error Banner
 * A full-width critical banner: a leading alert Icon + Title, with a trailing close hit target.
 * Single component (no variants). Matches the Figma "↳ Error Banner" extraction exactly.
 *
 * Anatomy (token-driven):
 *   Root (H row) — fill --background-sentiment-criticalalt02 (the fixed critical red), 375 FIXED
 *     width / HUG height (48), square (no radius), padding-left --spacing-size16, gap --spacing-size24,
 *     items top-aligned (Figma counterAxis=MIN). No stroke.
 *   H Stack — flex:1, gap --spacing-size08, padding --spacing-size12 top/bottom; holds the 24px Icon
 *     (Padding=True) and the Title (Style 6 / --text-sentiment-criticalalt01).
 *   Hit Target Area — 48×48, centred, holds the 16px close Icon (Padding=True). Clickable when onClose.
 *
 *   Glyph colour is token-driven (CSS mask) and PINNED to the dedicated Figma icon token
 *   --icon-sentiment-criticalalt01 (white) on both glyphs — the banner red and white content are
 *   fixed (single :root values, no dark override), so this is constant across themes by design.
 *
 * No additions beyond the extraction — no extra variants, states, motion, or elevation.
 */

const STYLE_ID = "cnc-errbanner-styles";
const CSS = `
.cnc-errbanner {
  box-sizing: border-box;
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size24);
  width: 100%; max-width: 375px;     /* Figma FIXED frame width (token:null) */
  padding-left: var(--spacing-size16);
  background: var(--background-sentiment-criticalalt02);
}
.cnc-errbanner__stack {
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size08);
  flex: 1 1 auto; min-width: 0;
  padding-top: var(--spacing-size12);
  padding-bottom: var(--spacing-size12);
}
.cnc-errbanner__title {
  flex: 1 1 auto; min-width: 0;
  font-family: var(--type-style6-family); font-size: var(--type-style6-size);
  line-height: var(--type-style6-line); font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking); color: var(--text-sentiment-criticalalt01);
}
.cnc-errbanner__close {
  flex: 0 0 auto;
  display: inline-flex; align-items: center; justify-content: center;
  width: 48px; height: 48px;          /* Figma hit-target frame (token:null) */
  padding: 0; border: none; background: transparent;
  -webkit-appearance: none; appearance: none;
}
.cnc-errbanner__close--btn { cursor: pointer; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ErrorBanner({
  title = "Title",
  icon = "Sentiment system/Critical/Critical",
  // leading alert glyph
  closeIcon = "Navigation/Close/Close",
  // trailing close glyph
  onClose,
  // close handler (omit → static close affordance)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const ICON_COLOR = "var(--icon-sentiment-criticalalt01)";
  const cls = ["cnc-errbanner"];
  if (className) cls.push(className);
  const closeCls = ["cnc-errbanner__close"];
  if (onClose) closeCls.push("cnc-errbanner__close--btn");
  const closeGlyph = IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: closeIcon,
    size: 16,
    padding: true,
    color: ICON_COLOR,
    basePath: basePath
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-errbanner__stack"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 24,
    padding: true,
    color: ICON_COLOR,
    basePath: basePath
  }) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-errbanner__title"
  }, title)), onClose ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: closeCls.join(" "),
    "aria-label": "Dismiss",
    onClick: onClose
  }, closeGlyph) : /*#__PURE__*/React.createElement("span", {
    className: closeCls.join(" "),
    "aria-hidden": "true"
  }, closeGlyph));
}
Object.assign(__ds_scope, { ErrorBanner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Error_Banner/ErrorBanner/ErrorBanner.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Notification_Badge/NotificationBadge/NotificationBadge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Notification Badge
 * A count bubble. Matches the Figma "Notification Badge" set exactly:
 *   Type: Default (critical red) | Offers (brand green)
 *
 * Geometry (token-driven):
 *   Height --spacing-size24 (Figma 24), min-width --spacing-size24 (circle when single digit),
 *   horizontal padding --spacing-size04 (Figma 4, un-tokenised → matched to the size-04 primitive),
 *   radius --radius-circle, label --type-style12-* centred.
 *   Default → background --c-temp-notification-badge-background, text --text-generic-defaultalt01.
 *   Offers  → background --c-temp-notification-tag-new-backgrounddefault,
 *             text --c-temp-notification-tag-new-textdefault.
 */

const STYLE_ID = "cnc-notification-badge-styles";
const CSS = `
.cnc-notifbadge {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: var(--spacing-size24);
  height: var(--spacing-size24);
  padding: 0 var(--spacing-size04);
  border-radius: var(--radius-circle);
  font-family: var(--type-style12-family);
  font-size: var(--type-style12-size);
  line-height: var(--type-style12-line);
  font-weight: var(--type-style12-weight);
  letter-spacing: var(--type-style12-tracking);
  text-align: center;
}
.cnc-notifbadge--default { background: var(--c-temp-notification-badge-background); color: var(--text-generic-defaultalt01); }
.cnc-notifbadge--offers { background: var(--c-temp-notification-tag-new-backgrounddefault); color: var(--c-temp-notification-tag-new-textdefault); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function NotificationBadge({
  type = "default",
  className = "",
  children = "12",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-notifbadge", `cnc-notifbadge--${type}`];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), children);
}
Object.assign(__ds_scope, { NotificationBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Notification_Badge/NotificationBadge/NotificationBadge.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Notification_Badge/NotificationDot/NotificationDot.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Notification Dot
 * The Figma "↳ Notification Badge" / Notification Dot: a small status dot. An 8px circle in the
 * notification badge background colour. Token-driven; no text. Decorative by default — pass
 * `label` to expose an accessible name.
 */

const STYLE_ID = "cnc-notification-dot-styles";
const CSS = `
.cnc-notifdot {
  box-sizing: border-box;
  display: inline-block;
  width: var(--spacing-size08);
  height: var(--spacing-size08);
  border-radius: var(--radius-circle);
  background: var(--c-temp-notification-badge-background);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function NotificationDot({
  label,
  className = "",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-notifdot"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" "),
    role: label ? "img" : "presentation",
    "aria-label": label || undefined,
    "aria-hidden": label ? undefined : true
  }, rest));
}
Object.assign(__ds_scope, { NotificationDot });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Notification_Badge/NotificationDot/NotificationDot.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Bottom_Nav/BottomNav/BottomNav.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Bottom Nav
 * The app's fixed bottom tab bar. Matches the Figma "Bottom Nav" set:
 *   Active : None | Tab 1 … Tab 5  (driven by `active` = active index; -1/null = None)
 *
 * Anatomy (token-driven, faithful to the extraction):
 *   Bar — width 375 (Figma FIXED), VERTICAL stack: H Stack (tabs) · Home indicator slot · Active
 *     border (underline) strip. Background --c-temp-bottomnav-background; a 1px upward drop shadow
 *     (x --c-temp-bottomnav-dropshadow-xaxis 0, y -1, blur --c-temp-bottomnav-dropshadow-blur 0,
 *     colour --c-temp-bottomnav-dropshadow-default).
 *   H Stack — horizontal, each tab FILLs equally. A tab is a VERTICAL stack: Icon (Size 24, Padding)
 *     + label (type Style 13, 11px GT Ultra), padding-top/bottom --spacing-size08, 4px icon→label gap.
 *     Icon colour is PINNED to --c-temp-bottomnav-icon{default|active}; label to
 *     --c-temp-bottomnav-text{default|active}. Both map light/dark via the token.
 *   Active indicator — a single 4px bar pinned to the TOP edge of the bar, filled
 *     --c-temp-bottomnav-underline, that SLIDES horizontally to sit over the active tab (width =
 *     one tab). Hidden when Active=None. (Owner-directed: the raw extraction stacked the underline
 *     strip below the row; corrected to a top-edge sliding indicator. Slide respects
 *     prefers-reduced-motion.)
 *   Notification Dot — optional per tab (`dot:true`); the 8px Notification Dot, positioned at the
 *     icon's top-right. Hidden by default (Figma ships it visible:false on Tab 5).
 *
 * FLAGS:
 *   • Home indicator — the Figma composes the OS "Home indicator" component, which is NOT yet built
 *     (OS category). Exposed here as a `homeIndicator` slot (ReactNode) so a template can drop the
 *     real component in; renders nothing by default. No placeholder invented.
 *   • Notification Dot position — the extraction lists the dot as a flow child of the tab with no
 *     explicit absolute-positioning data; placed at the icon top-right (standard badge placement),
 *     inferred. Flagged.
 *   • Active indicator — top-edge sliding bar is owner-directed; the raw Figma extraction placed the
 *     underline strip as the last vertical child (below the row). Corrected per owner. Flagged.
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-bottomnav-styles";
const CSS = `
.cnc-bottomnav { box-sizing: border-box; }
.cnc-bottomnav *, .cnc-bottomnav *::before, .cnc-bottomnav *::after { box-sizing: border-box; }

.cnc-bottomnav {
  position: relative;
  display: flex; flex-direction: column; align-items: stretch;
  background: var(--c-temp-bottomnav-background);
  box-shadow:
    var(--c-temp-bottomnav-dropshadow-xaxis)
    var(--c-temp-bottomnav-dropshadow-yaxis)
    var(--c-temp-bottomnav-dropshadow-blur)
    var(--c-temp-bottomnav-dropshadow-default);
}

.cnc-bottomnav__row { display: flex; flex-direction: row; align-items: flex-start; }

.cnc-bottomnav__tab {
  flex: 1 1 0; min-width: 0;
  display: flex; flex-direction: column; align-items: center;
  gap: var(--spacing-size04, 4px);
  padding-top: var(--spacing-size08); padding-bottom: var(--spacing-size08);
  background: transparent; border: 0; cursor: pointer;
}

.cnc-bottomnav__icon {
  position: relative;
  display: inline-flex; align-items: center; justify-content: center;
  color: var(--c-temp-bottomnav-icondefault);
}
.cnc-bottomnav__tab.is-active .cnc-bottomnav__icon { color: var(--c-temp-bottomnav-iconactive); }

.cnc-bottomnav__dot { position: absolute; top: 0; right: 0; }

.cnc-bottomnav__label {
  font-family: var(--type-style13-family);
  font-size: var(--type-style13-size);
  line-height: var(--type-style13-line);
  font-weight: var(--type-style13-weight);
  letter-spacing: var(--type-style13-tracking);
  text-align: center;
  color: var(--c-temp-bottomnav-textdefault);
}
.cnc-bottomnav__tab.is-active .cnc-bottomnav__label { color: var(--c-temp-bottomnav-textactive); }

/* Active indicator — a single bar pinned to the TOP edge that slides to the active tab. */
.cnc-bottomnav__indicator {
  position: absolute; top: 0; left: 0; height: 4px;
  background: var(--c-temp-bottomnav-underline);
  transition: transform .25s ease, opacity .15s ease;
  pointer-events: none;
}
@media (prefers-reduced-motion: reduce) {
  .cnc-bottomnav__indicator { transition: none; }
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
const DEFAULT_TABS = [{
  label: "Home",
  icon: "Navigation/Home/Home"
}, {
  label: "Apply",
  icon: "Finance/Loans/Loans"
}, {
  label: "Payments",
  icon: "Finance/Payment/Payment"
}, {
  label: "Search",
  icon: "Navigation/Search/Search"
}, {
  label: "Cards",
  icon: "Cards/Credit Card/Credit Card"
}];
function BottomNav({
  tabs = DEFAULT_TABS,
  active = -1,
  // active tab index; -1 / null = None
  onChange,
  homeIndicator = null,
  // OS Home Indicator slot (component unbuilt — see FLAGS)
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const Dot = fromNS("NotificationDot", __ds_scope.NotificationDot);
  const items = Array.isArray(tabs) && tabs.length ? tabs : DEFAULT_TABS;
  const activeIdx = active == null ? -1 : Number(active);
  const hasActive = activeIdx >= 0 && activeIdx < items.length;
  const select = i => {
    if (typeof onChange === "function") onChange(i);
  };
  const cls = ["cnc-bottomnav"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  return /*#__PURE__*/React.createElement("nav", _extends({
    className: cls.join(" "),
    style: rootStyle,
    "aria-label": "Primary"
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-bottomnav__indicator",
    "aria-hidden": "true",
    style: {
      width: `calc(100% / ${items.length})`,
      transform: `translateX(${(hasActive ? activeIdx : 0) * 100}%)`,
      opacity: hasActive ? 1 : 0
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-bottomnav__row",
    role: "tablist"
  }, items.map((t, i) => {
    const on = i === activeIdx;
    const glyph = on && t.activeIcon ? t.activeIcon : t.icon;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "tab",
      "aria-selected": on,
      className: "cnc-bottomnav__tab" + (on ? " is-active" : ""),
      onClick: () => select(i)
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-bottomnav__icon"
    }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
      name: glyph,
      src: t.src,
      size: 24,
      padding: true,
      basePath: basePath
    }) : null, t.dot && Dot ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-bottomnav__dot"
    }, /*#__PURE__*/React.createElement(Dot, {
      label: t.dotLabel || "New"
    })) : null), /*#__PURE__*/React.createElement("span", {
      className: "cnc-bottomnav__label"
    }, t.label));
  })), homeIndicator);
}
Object.assign(__ds_scope, { BottomNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Bottom_Nav/BottomNav/BottomNav.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Notification_Hint/NotificationHint/NotificationHint.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Notification Hint
 * An inline hint row: a leading Icon + a short message inside a rounded panel. Matches the Figma
 * "Notification Hint" set exactly:
 *   Type: Default | Secondary   (the only axis — changes the panel fill)
 *
 * Anatomy (token-driven):
 *   Root (V) — 375 FILL width / HUG height, padding L/R --spacing-size16, no fill.
 *   Frame (H row) — fill --background-panel-default (Default) | --background-panel-secondary
 *     (Secondary), radius --radius-default, padding --spacing-size12, gap --spacing-size12,
 *     items top-aligned (Figma MIN/MIN). Holds the 24px Icon (Padding=True) + the message
 *     (Style 8 / --text-sentiment-critical).
 *
 *   Glyph colour is token-driven (CSS mask): the row anchors color: --text-sentiment-critical so the
 *   leading Icon (mask filled by currentColor) tracks the message token and maps light/dark.
 *
 * No additions beyond the extraction — no extra variants, states, motion, or elevation.
 *
 * Source notes (flagged): the Frame corner radius is value 12 with token:null in Figma; it equals
 *   --radius-default exactly, used here to keep the file fully token-driven (no literal radii).
 *   The Icon is an instance swap with no resolvable colour token, so it follows the message colour
 *   (rule: glyph matches its adjacent label) — pin a dedicated icon token if the source intends one.
 */

const STYLE_ID = "cnc-hint-styles";
const CSS = `
.cnc-hint {
  box-sizing: border-box;
  display: flex; flex-direction: column;
  width: 100%; max-width: 375px;     /* Figma FILL frame, 375 reference (token:null) */
  padding-left: var(--spacing-size16);
  padding-right: var(--spacing-size16);
}
.cnc-hint__frame {
  box-sizing: border-box;
  display: flex; flex-direction: row; align-items: flex-start;
  gap: var(--spacing-size12);
  width: 100%;
  padding: var(--spacing-size12);
  border-radius: var(--radius-default);   /* Figma value 12, token:null — equals --radius-default */
  color: var(--text-sentiment-critical);  /* anchor: leading Icon follows the message colour */
}
.cnc-hint__frame--default { background: var(--background-panel-default); }
.cnc-hint__frame--secondary { background: var(--background-panel-secondary); }
.cnc-hint__text {
  flex: 1 1 auto; min-width: 0;
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-sentiment-critical);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function NotificationHint({
  type = "default",
  // "default" | "secondary"
  text = "This payment should be received within two hours.",
  icon = "Sentiment system/Critical/Critical",
  // leading glyph (instance swap in source)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const cls = ["cnc-hint"];
  if (className) cls.push(className);
  const frameCls = ["cnc-hint__frame", type === "secondary" ? "cnc-hint__frame--secondary" : "cnc-hint__frame--default"];
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: frameCls.join(" ")
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-hint__text"
  }, text)));
}
Object.assign(__ds_scope, { NotificationHint });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Notification_Hint/NotificationHint/NotificationHint.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Notification_Panel/NotificationPanel/NotificationPanel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Notification Panel
 * An inline sentiment panel: leading sentiment Icon + Title/Message, optional trailing dismiss Icon.
 * Matches the Figma "Notification Panel" set exactly. Axes:
 *   Type:        Primary | Secondary        (border treatment)
 *   Sentiment:   Critical | Warning | Success | Info   (fill / border / text / icon tokens)
 *   Layout:      Title + Message | Title Only | Message Only
 *   Dismiss Icon: True | False              (shows the trailing Action/close Icon)
 *   State:       Default | Pressed          (fill alt + border weight)
 *
 * Anatomy (token-driven):
 *   Container (H) — FILL width / HUG height, 375 reference (token:null). padding 12/16 (Size 12 T/B,
 *     Size 16 L/R), gap --spacing-size24, radius --radius-default (Figma value 12, token:null).
 *     Fill + border by Type × State (see the matrix below) — every value a sentiment token.
 *   Container (H) — counterAxis=MIN, gap --spacing-size24. Holds the H Stack and, as a TOP-aligned
 *     sibling, the 16px Action Icon (Padding=True) when Dismiss Icon=True (Figma: the close is a child
 *     of the Container, not the centred H Stack — so it sits at the top, level with the Title).
 *   H Stack (row) — gap --spacing-size12, items centred. Holds the 16px sentiment Icon (in a
 *     HUG/FILL container, glyph top-aligned) and the V Stack (Title Style 7 + Message Style 8).
 *
 *   Fill / border matrix (sentiment = <S>):
 *     Primary   Default : bg --background-sentiment-<S>      · border 1px --border-sentiment-<S>
 *     Primary   Pressed : bg --background-sentiment-<S>alt01 · border 2px --border-sentiment-<S>
 *     Secondary Default : bg --background-sentiment-<S>alt01 · border 1px --border-sentiment-<S>alt01
 *                                                              (transparent in light, coloured in dark)
 *     Secondary Pressed : bg --background-sentiment-<S>alt01 · border 2px --border-sentiment-<S>
 *
 *   Glyph colour is token-driven (CSS mask) and PINNED to the dedicated Figma icon token
 *   --icon-sentiment-<S> on both the sentiment and dismiss glyphs (carried via the --np-icon var on the
 *   root, read by each Icon's currentColor mask) — maps light/dark automatically.
 *
 * No additions beyond the extraction — no extra variants, states, motion, or elevation. The hidden
 * "Arrow Position" instance present on every Figma variant is visible:false throughout and is omitted.
 */

const STYLE_ID = "cnc-npanel-styles";
const CSS = `
.cnc-npanel {
  box-sizing: border-box;
  display: flex; flex-direction: row;
  justify-content: flex-end; align-items: flex-start;
  width: 100%; max-width: 375px;            /* Figma FILL frame, 375 reference (token:null) */
  padding: var(--spacing-size12) var(--spacing-size16);
  gap: var(--spacing-size24);
  border-radius: var(--radius-default);     /* Figma value 12, token:null — equals --radius-default */
  border-style: solid;
}
.cnc-npanel--critical {
  --np-bg: var(--background-sentiment-critical);
  --np-bg-alt: var(--background-sentiment-criticalalt01);
  --np-border: var(--border-sentiment-critical);
  --np-border-alt: var(--border-sentiment-criticalalt01);
  --np-text: var(--text-sentiment-critical);
  --np-icon: var(--icon-sentiment-critical);
}
.cnc-npanel--warning {
  --np-bg: var(--background-sentiment-warning);
  --np-bg-alt: var(--background-sentiment-warningalt01);
  --np-border: var(--border-sentiment-warning);
  --np-border-alt: var(--border-sentiment-warningalt01);
  --np-text: var(--text-sentiment-warning);
  --np-icon: var(--icon-sentiment-warning);
}
.cnc-npanel--success {
  --np-bg: var(--background-sentiment-success);
  --np-bg-alt: var(--background-sentiment-successalt01);
  --np-border: var(--border-sentiment-success);
  --np-border-alt: var(--border-sentiment-successalt01);
  --np-text: var(--text-sentiment-success);
  --np-icon: var(--icon-sentiment-success);
}
.cnc-npanel--info {
  --np-bg: var(--background-sentiment-info);
  --np-bg-alt: var(--background-sentiment-infoalt01);
  --np-border: var(--border-sentiment-info);
  --np-border-alt: var(--border-sentiment-infoalt01);
  --np-text: var(--text-sentiment-info);
  --np-icon: var(--icon-sentiment-info);
}
/* Fill + border by Type × State */
.cnc-npanel--primary.cnc-npanel--default   { background: var(--np-bg);     border-width: var(--border-width-01); border-color: var(--np-border); }
.cnc-npanel--primary.cnc-npanel--pressed   { background: var(--np-bg-alt); border-width: var(--border-width-02); border-color: var(--np-border); }
.cnc-npanel--secondary.cnc-npanel--default { background: var(--np-bg-alt); border-width: var(--border-width-01); border-color: var(--np-border-alt); }
.cnc-npanel--secondary.cnc-npanel--pressed { background: var(--np-bg-alt); border-width: var(--border-width-02); border-color: var(--np-border); }

.cnc-npanel__hstack {
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size12);
  flex: 1 1 auto; min-width: 0;
}
.cnc-npanel__iconwrap {
  flex: 0 0 auto; align-self: stretch;
  display: flex; align-items: flex-start; justify-content: center;
}
.cnc-npanel__vstack {
  display: flex; flex-direction: column; align-items: flex-start;
  flex: 1 1 auto; min-width: 0;
  color: var(--np-text);
}
.cnc-npanel__title {
  width: 100%;
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--np-text);
}
.cnc-npanel__message {
  width: 100%;
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--np-text);
}
.cnc-npanel__action {
  flex: 0 0 auto; align-self: flex-start;   /* Container counterAxis=MIN — close sits at the top */
  display: inline-flex; align-items: center; justify-content: center;
  padding: 0; border: none; background: transparent;
  -webkit-appearance: none; appearance: none;
}
.cnc-npanel__action--btn { cursor: pointer; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
const SENTIMENT_ICON = {
  critical: "Sentiment system/Critical/Critical",
  warning: "Sentiment system/Warning/Warning",
  success: "Sentiment system/Success/Success",
  info: "Sentiment system/Info/Info"
};
function NotificationPanel({
  type = "primary",
  // "primary" | "secondary"
  sentiment = "critical",
  // "critical" | "warning" | "success" | "info"
  layout = "title-message",
  // "title-message" | "title-only" | "message-only"
  dismiss = false,
  // Figma "Dismiss Icon" — show the trailing Action/close Icon
  state = "default",
  // "default" | "pressed"
  title = "Title",
  message = "Message",
  closeIcon = "Navigation/Close/Close",
  onDismiss,
  // dismiss handler (omit → static close affordance)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const ICON_COLOR = "var(--np-icon)";
  const cls = ["cnc-npanel", `cnc-npanel--${sentiment}`, `cnc-npanel--${type}`, `cnc-npanel--${state}`];
  if (className) cls.push(className);
  const showTitle = layout === "title-message" || layout === "title-only";
  const showMessage = layout === "title-message" || layout === "message-only";
  const actionCls = ["cnc-npanel__action"];
  if (onDismiss) actionCls.push("cnc-npanel__action--btn");
  const closeGlyph = IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: closeIcon,
    size: 16,
    padding: true,
    color: ICON_COLOR,
    basePath: basePath
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-npanel__hstack"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-npanel__iconwrap"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: SENTIMENT_ICON[sentiment],
    size: 16,
    color: ICON_COLOR,
    basePath: basePath
  }) : null), /*#__PURE__*/React.createElement("div", {
    className: "cnc-npanel__vstack"
  }, showTitle ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-npanel__title"
  }, title) : null, showMessage ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-npanel__message"
  }, message) : null)), dismiss ? onDismiss ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: actionCls.join(" "),
    "aria-label": "Dismiss",
    onClick: onDismiss
  }, closeGlyph) : /*#__PURE__*/React.createElement("span", {
    className: actionCls.join(" "),
    "aria-hidden": "true"
  }, closeGlyph) : null);
}
Object.assign(__ds_scope, { NotificationPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Notification_Panel/NotificationPanel/NotificationPanel.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Notification_Tag/NotificationTag/NotificationTag.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — Notification Tag
 * A small status pill. Matches the Figma "Notification Tag" set exactly:
 *   Type: NEW | Neutral | Critical | Warning | Success | Info
 *
 * Geometry (token-driven):
 *   Radius --radius-full (Figma 200), label --type-style12-* centred, vertical padding
 *   --spacing-size04. NEW uses --spacing-size08 horizontal padding; all others --spacing-size12.
 *   Border --border-width-01 (NEW has none).
 *   NEW      → bg --c-temp-notification-tag-new-backgrounddefault, text --c-temp-notification-tag-new-textdefault
 *   Neutral  → bg --background-panel-secondary, border --border-generic-defaultalt01, text --text-generic-default
 *   Critical → bg --background-sentiment-critical, border --border-sentiment-critical, text --text-sentiment-critical
 *   Warning  → bg --background-sentiment-warning, border --border-sentiment-warning, text --text-sentiment-warning
 *   Success  → bg --background-sentiment-success, border --border-sentiment-success, text --text-sentiment-success
 *   Info     → bg --background-sentiment-info, border --border-sentiment-info, text --text-sentiment-info
 * (min-width 51 on the non-NEW variants is un-tokenised in Figma and intentionally omitted; the tag hugs content.)
 */

const STYLE_ID = "cnc-notification-tag-styles";
const CSS = `
.cnc-notiftag {
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-size04) var(--spacing-size12);
  border-radius: var(--radius-full);
  border: var(--border-width-01) solid transparent;
  font-family: var(--type-style12-family);
  font-size: var(--type-style12-size);
  line-height: var(--type-style12-line);
  font-weight: var(--type-style12-weight);
  letter-spacing: var(--type-style12-tracking);
  text-align: center;
}
.cnc-notiftag--new { padding-left: var(--spacing-size08); padding-right: var(--spacing-size08); background: var(--c-temp-notification-tag-new-backgrounddefault); color: var(--c-temp-notification-tag-new-textdefault); border-color: transparent; }
.cnc-notiftag--neutral { background: var(--background-panel-secondary); border-color: var(--border-generic-defaultalt01); color: var(--text-generic-default); }
.cnc-notiftag--critical { background: var(--background-sentiment-critical); border-color: var(--border-sentiment-critical); color: var(--text-sentiment-critical); }
.cnc-notiftag--warning { background: var(--background-sentiment-warning); border-color: var(--border-sentiment-warning); color: var(--text-sentiment-warning); }
.cnc-notiftag--success { background: var(--background-sentiment-success); border-color: var(--border-sentiment-success); color: var(--text-sentiment-success); }
.cnc-notiftag--info { background: var(--background-sentiment-info); border-color: var(--border-sentiment-info); color: var(--text-sentiment-info); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function NotificationTag({
  type = "neutral",
  className = "",
  children = "Label",
  ...rest
}) {
  ensureStyles();
  const cls = ["cnc-notiftag", `cnc-notiftag--${type}`];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), children);
}
Object.assign(__ds_scope, { NotificationTag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Notification_Tag/NotificationTag/NotificationTag.jsx", error: String((e && e.message) || e) }); }

// src/components/action/Quick_Action_Button/QuickActionButton/QuickActionButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Quick Action Button
 * A compact vertical action: a 48px tap shape with a centred glyph (optional notification
 * badge / tag), and a two-line label beneath. Matches the Figma "Quick Action Button" set:
 *   State  : Default | Pressed (:active) | Disabled
 *   Preset : Account Quick Links — rounded-rect shape + Notification Badge (count)
 *          | Action Menu        — rounded-rect shape, glyph only
 *          | Default            — circular shape + Notification Tag ("NEW")
 *
 * Shape (project-owner direction): a 48×48 (--os-hittarget-default) shape with the
 *   --c-temp-quickaction-background* fill and a --c-temp-quickaction-borderwidth (1px)
 *   --c-temp-quickaction-border* stroke, contents centred. The "Account Quick Links" preset
 *   uses the rounded-rect (--c-temp-quickaction-cornerradiusdefault, 12px) from the Figma
 *   RECTANGLE; "Action Menu" / "Default" use the circle (--radius-circle) standing in for the
 *   bespoke "Shape Alt 02" (a Figma POLYGON with no path in the extraction). `shape` overrides.
 *
 * Tokens: shape fill/stroke → --c-temp-quickaction-background* / --c-temp-quickaction-border*
 *   (default / pressed / disabled). Label → --type-style9-*, centred, two lines; colour
 *   --c-temp-quickaction-text* (Account Quick Links / Action Menu) or --text-generic-default
 *   (Default preset). The 80px frame width is the Figma FIXED frame size (token:null in source),
 *   a documented literal exactly as Icon/Pictogram carry their un-tokenised sizes.
 *
 * Composes Icon (24px; Padding=True except the Default preset's bare glyph), Notification Badge
 * and Notification Tag. The glyph COLOUR is token-driven: the glyph is a mask filled by
 * currentColor, and the shape sets `color` to the Figma --c-temp-quickaction-icon* token
 * (default / pressed / disabled), so the glyph tracks state and maps light/dark automatically.
 */

const DEFAULT_GLYPH = "Action/Plus/Plus";
const SHAPE_BY_PRESET = {
  "account-quick-links": "rounded",
  "action-menu": "rounded",
  "default": "circle"
};
const STYLE_ID = "cnc-qab-styles";
const CSS = `
.cnc-qab {
  box-sizing: border-box;
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  gap: var(--spacing-size04);
  width: 80px;
  padding: 0;
  border: none;
  background: transparent;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  font-family: var(--type-style9-family);
}
.cnc-qab:disabled { cursor: not-allowed; }
.cnc-qab__shape {
  position: relative;
  box-sizing: border-box;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: var(--os-hittarget-default);
  height: var(--os-hittarget-default);
  background: var(--c-temp-quickaction-backgrounddefault);
  color: var(--c-temp-quickaction-icondefault);
  border: var(--c-temp-quickaction-borderwidth) solid var(--c-temp-quickaction-borderdefault);
}
.cnc-qab__shape--rounded { border-radius: var(--c-temp-quickaction-cornerradiusdefault); }
.cnc-qab__shape--circle { border-radius: var(--radius-circle); }
.cnc-qab:active:not(:disabled) .cnc-qab__shape, .cnc-qab--pressed .cnc-qab__shape { background: var(--c-temp-quickaction-backgroundpressed); border-color: var(--c-temp-quickaction-borderpressed); color: var(--c-temp-quickaction-iconpressed); }
.cnc-qab:disabled .cnc-qab__shape, .cnc-qab--disabled .cnc-qab__shape { background: var(--c-temp-quickaction-backgrounddisabled); border-color: var(--c-temp-quickaction-borderdisabled); color: var(--c-temp-quickaction-icondisabled); }

.cnc-qab__label {
  font-family: var(--type-style9-family);
  font-size: var(--type-style9-size);
  line-height: var(--type-style9-line);
  font-weight: var(--type-style9-weight);
  letter-spacing: var(--type-style9-tracking);
  text-align: center;
  white-space: pre-line;
  width: 80px;
  color: var(--c-temp-quickaction-textdefault);
}
.cnc-qab--preset-default .cnc-qab__label { color: var(--text-generic-default); }
.cnc-qab:active:not(:disabled) .cnc-qab__label, .cnc-qab--pressed .cnc-qab__label { color: var(--c-temp-quickaction-textpressed); }
.cnc-qab:disabled .cnc-qab__label, .cnc-qab--disabled .cnc-qab__label { color: var(--c-temp-quickaction-textdisabled); }

.cnc-qab__overlay { position: absolute; top: 0; right: 0; transform: translate(35%, -35%); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function QuickActionButton({
  preset = "account-quick-links",
  shape,
  state = "default",
  disabled = false,
  icon = DEFAULT_GLYPH,
  iconSrc,
  iconTitle,
  label = "Quick action\nlabel",
  badgeCount = "12",
  tagLabel = "NEW",
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const BadgeCmp = fromNS("NotificationBadge", __ds_scope.NotificationBadge);
  const TagCmp = fromNS("NotificationTag", __ds_scope.NotificationTag);
  const isDisabled = disabled || state === "disabled";
  const resolvedShape = shape || SHAPE_BY_PRESET[preset] || "rounded";
  // Default preset uses the bare glyph (Figma Padding=False); the others inset it (Padding=True).
  const iconPadded = preset !== "default";
  const cls = ["cnc-qab", `cnc-qab--preset-${preset}`];
  if (state === "pressed") cls.push("cnc-qab--pressed");
  if (isDisabled) cls.push("cnc-qab--disabled");
  if (className) cls.push(className);
  let overlay = null;
  if (preset === "account-quick-links" && BadgeCmp) {
    overlay = /*#__PURE__*/React.createElement("span", {
      className: "cnc-qab__overlay"
    }, /*#__PURE__*/React.createElement(BadgeCmp, {
      type: "default"
    }, badgeCount));
  } else if (preset === "default" && TagCmp) {
    overlay = /*#__PURE__*/React.createElement("span", {
      className: "cnc-qab__overlay"
    }, /*#__PURE__*/React.createElement(TagCmp, {
      type: "new"
    }, tagLabel));
  }
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    className: cls.join(" "),
    disabled: isDisabled
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: `cnc-qab__shape cnc-qab__shape--${resolvedShape}`
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: icon,
    src: iconSrc,
    size: 24,
    padding: iconPadded,
    basePath: basePath,
    title: iconTitle
  }) : null, overlay), /*#__PURE__*/React.createElement("span", {
    className: "cnc-qab__label"
  }, label));
}
Object.assign(__ds_scope, { QuickActionButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/action/Quick_Action_Button/QuickActionButton/QuickActionButton.jsx", error: String((e && e.message) || e) }); }

// src/components/content/List/ListItemAction/ListItemAction.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _List Item Action  (INTERNAL/private; Figma `_` prefix — composed by List Action Group)
 * A tappable list row. Matches the Figma "List Item Action" set exactly:
 *   Asset   : None | Icon (24px leading glyph, Padding=False) | Illustration (64px Pictogram)
 *           | Pictogram (40px Pictogram)
 *   Width   : Compact (16px screen gutter, rounded ends) | Full Width (edge to edge, square)
 *   Usage   : Group (one row in a stack, with a section Heading on Top) | Single (standalone)
 *   Position: Top | Middle | Bottom (where the row sits in a Group)
 *   State   : Default | Pressed (:active)
 *
 * Anatomy (token-driven):
 *   Outer (V stack) — Compact pads L/R --spacing-size16; Full Width is edge to edge.
 *   Group Heading (Usage=Group, Position=Top only) — Style 7 (--type-style7-*) / --text-generic-default,
 *     --spacing-size16 below; in Full Width it keeps its own --spacing-size16 L/R inset.
 *   Row (H stack) — --background-panel-default fill (Pressed → --background-panel-pressed),
 *     --spacing-size16 padding, --spacing-size12 gap, items centred. Optional leading asset, then a
 *     V stack (--spacing-size04 gap): Title --type-style6-* / --text-action-default + optional
 *     Description --type-style8-* / --text-generic-subdued. Optional Notification Tag (NEW). Trailing
 *     16px Action Icon (Padding=True) — Chevron Right by default.
 *   Corner radius (--radius-default = 12) — Compact only: top corners when it is the top edge
 *     (Single or Top); bottom corners when it is the bottom edge (Single or Bottom). Full Width square.
 *   Divider (Width=Full width, Type=Default) below every Group row except the last (Bottom); never
 *     for Single. The Figma instance zeroes the Divider's vertical padding (Spacing/None) — represented
 *     via the passed `.cnc-lia__rule` class (the approved Divider has no flush prop yet).
 *
 *   Glyph colour is token-driven (CSS mask): the row sets color: --text-action-default, so the
 *   trailing chevron and the leading Icon (masks filled by currentColor) track that token and map
 *   light/dark automatically; the leading Pictogram is coloured by its own --c-pictogram-icon token.
 *   (Figma's per-instance icons are swappable with no colour token; the row's action colour is the
 *   faithful anchor — it matches the Title, the dominant element. Flagged for owner confirmation.)
 */

const STYLE_ID = "cnc-lia-styles";
const CSS = `
.cnc-lia { box-sizing: border-box; width: 100%; font-family: var(--type-style6-family); }
.cnc-lia--compact { padding-left: var(--spacing-size16); padding-right: var(--spacing-size16); }

.cnc-lia__heading {
  padding-bottom: var(--spacing-size16);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--text-generic-default);
}
.cnc-lia--full .cnc-lia__heading { padding-left: var(--spacing-size16); padding-right: var(--spacing-size16); }

.cnc-lia__row {
  box-sizing: border-box;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size12);
  width: 100%;
  padding: var(--spacing-size16);
  background: var(--background-panel-default);
  border: none;
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none; appearance: none;
  font-family: var(--type-style6-family);
  color: var(--text-action-default);
}
.cnc-lia__row:active:not(:disabled), .cnc-lia__row--pressed { background: var(--background-panel-pressed); }
.cnc-lia__row--rt { border-top-left-radius: var(--radius-default); border-top-right-radius: var(--radius-default); }
.cnc-lia__row--rb { border-bottom-left-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }

.cnc-lia__lead, .cnc-lia__action, .cnc-lia__tag { flex: 0 0 auto; display: inline-flex; }
.cnc-lia__text { display: flex; flex-direction: column; gap: var(--spacing-size04); flex: 1 1 auto; min-width: 0; }
.cnc-lia__title {
  font-family: var(--type-style6-family); font-size: var(--type-style6-size);
  line-height: var(--type-style6-line); font-weight: var(--type-style6-weight);
  letter-spacing: var(--type-style6-tracking); color: var(--text-action-default);
}
.cnc-lia__desc {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-subdued);
}

/* Figma instance override: the in-row Divider has its vertical padding set to 0 (Spacing/None). */
.cnc-lia .cnc-lia__rule { padding-top: 0; padding-bottom: 0; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ListItemAction({
  asset = "none",
  // "none" | "icon" | "illustration" | "pictogram"
  width = "compact",
  // "compact" | "full"
  usage = "group",
  // "group" | "single"
  position = "top",
  // "top" | "middle" | "bottom"
  state = "default",
  // "default" | "pressed"
  heading = "Heading",
  // group section heading (Usage=Group, Position=Top)
  title = "Title",
  description,
  // optional second line
  showDescription = false,
  // Figma Description node is hidden by default
  tag = false,
  // show the NEW Notification Tag
  tagLabel = "NEW",
  icon,
  // asset="icon": 24px leading glyph name
  pictogramIcon,
  // asset="pictogram": glyph for the 40px Pictogram
  pictogramStyle = "default",
  illustration,
  // asset="illustration": illustration path for the 64px Pictogram
  actionIcon = "Arrows/Chevron Right/Chevron Right",
  onClick,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const PictogramCmp = fromNS("Pictogram", __ds_scope.Pictogram);
  const DividerCmp = fromNS("Divider", __ds_scope.Divider);
  const TagCmp = fromNS("NotificationTag", __ds_scope.NotificationTag);
  const isFull = width === "full";
  const isSingle = usage === "single";
  const isTop = position === "top";
  const isBottom = position === "bottom";
  const rowTop = !isFull && (isSingle || isTop);
  const rowBottom = !isFull && (isSingle || isBottom);
  // Divider sits below every Group row except the last (Bottom); never for Single.
  const showDivider = !isSingle && !isBottom;
  // Section heading only on a Group's Top row.
  const showHeading = !isSingle && isTop;
  const outerCls = ["cnc-lia", isFull ? "cnc-lia--full" : "cnc-lia--compact"];
  if (className) outerCls.push(className);
  const rowCls = ["cnc-lia__row"];
  if (state === "pressed") rowCls.push("cnc-lia__row--pressed");
  if (rowTop) rowCls.push("cnc-lia__row--rt");
  if (rowBottom) rowCls.push("cnc-lia__row--rb");
  let lead = null;
  if (asset === "icon" && IconCmp) {
    lead = /*#__PURE__*/React.createElement("span", {
      className: "cnc-lia__lead"
    }, /*#__PURE__*/React.createElement(IconCmp, {
      name: icon,
      size: 24,
      basePath: basePath
    }));
  } else if (asset === "pictogram" && PictogramCmp) {
    lead = /*#__PURE__*/React.createElement("span", {
      className: "cnc-lia__lead"
    }, /*#__PURE__*/React.createElement(PictogramCmp, {
      asset: "pictogram",
      style: pictogramStyle,
      icon: pictogramIcon,
      basePath: basePath
    }));
  } else if (asset === "illustration" && PictogramCmp) {
    lead = /*#__PURE__*/React.createElement("span", {
      className: "cnc-lia__lead"
    }, /*#__PURE__*/React.createElement(PictogramCmp, {
      asset: "illustration",
      style: pictogramStyle,
      illustration: illustration,
      basePath: basePath
    }));
  }
  const rule = DividerCmp ? /*#__PURE__*/React.createElement(DividerCmp, {
    width: "full",
    type: "default",
    className: "cnc-lia__rule"
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: outerCls.join(" ")
  }, rest), showHeading ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-lia__heading"
  }, heading) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: rowCls.join(" "),
    onClick: onClick
  }, lead, /*#__PURE__*/React.createElement("span", {
    className: "cnc-lia__text"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-lia__title"
  }, title), showDescription && description != null ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-lia__desc"
  }, description) : null), tag && TagCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-lia__tag"
  }, /*#__PURE__*/React.createElement(TagCmp, {
    type: "new"
  }, tagLabel)) : null, IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-lia__action"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: actionIcon,
    size: 16,
    padding: true,
    basePath: basePath
  })) : null), showDivider ? rule : null);
}
Object.assign(__ds_scope, { ListItemAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/List/ListItemAction/ListItemAction.jsx", error: String((e && e.message) || e) }); }

// src/components/content/List/ListActionGroup/ListActionGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — List Action Group  (composes _List Item Action)
 * A vertical stack of tappable Action List rows. Matches the Figma "List Action Group" set exactly:
 *   Width: Compact | Full Width   (the only variant axis — forwarded to every row)
 *
 * Geometry (token-driven):
 *   Container — VERTICAL auto-layout, MIN/MIN, 375 FIXED width (Figma min=max=375), HUG height,
 *     padding 0, itemSpacing 0 (rows abut; the row's own Divider separates them). fills:[] —
 *     no background of its own; each row carries --background-panel-default.
 *   Rows — every child is a _List Item Action with Usage=Group. Position is assigned by index:
 *     first → Top (rounds top corners in Compact, renders the section Heading), last → Bottom
 *     (rounds bottom corners, no trailing Divider), all others → Middle. Width is forwarded so
 *     Compact rows keep their 16px gutter + rounded ends and Full Width rows go edge-to-edge square.
 *   The 375 width carries token:null in Figma — a documented frame literal, exactly as the row and
 *     its nested Icon/Pictogram carry their un-tokenised sizes.
 *
 * Faithful mapping note: a one-row group is rendered Usage=Single (all corners rounded, no Divider) —
 *   the only sensible reduction of Top+Bottom onto a single row, since Position is a single value.
 *
 * Adds layout only — no fills, strokes, radii, behaviour, motion or props beyond the extraction.
 */

const STYLE_ID = "cnc-lag-styles";
const CSS = `
.cnc-lag {
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  width: 100%;
  max-width: 375px;            /* Figma FIXED frame width (token:null) */
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ListActionGroup({
  width = "compact",
  // "compact" | "full" — forwarded to every row
  items,
  // array of _List Item Action prop objects (one per row)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const ItemCmp = fromNS("ListItemAction", __ds_scope.ListItemAction);
  const data = Array.isArray(items) ? items : [];
  const n = data.length;
  const cls = ["cnc-lag"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), ItemCmp ? data.map((it, i) => {
    const single = n === 1;
    const position = i === 0 ? "top" : i === n - 1 ? "bottom" : "middle";
    return /*#__PURE__*/React.createElement(ItemCmp, _extends({
      key: i
    }, it, {
      usage: single ? "single" : "group",
      position: single ? "top" : position,
      width: width,
      basePath: basePath
    }));
  }) : null);
}
Object.assign(__ds_scope, { ListActionGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/List/ListActionGroup/ListActionGroup.jsx", error: String((e && e.message) || e) }); }

// src/components/content/List/ListItemDataPlayback/ListItemDataPlayback.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — _List Item Data Playback  (INTERNAL/private; Figma `_` prefix — composed by
 * List Data Playback Group). A tappable list row that plays back one to three Title/Data pairs.
 * Matches the Figma "List Item Data Playback" set exactly:
 *   Data Set: 01 | 02 | 03   (how many Title/Data pairs stack vertically)
 *   Width   : Compact (16px screen gutter, rounded ends) | Full Width (edge to edge, square)
 *   Usage   : Group (one row in a stack, Heading on Top) | Single (standalone)
 *   Position: Top | Middle | Bottom
 *   State   : Default | Pressed (:active)
 *
 * Anatomy (token-driven):
 *   Outer (V stack) — Compact pads L/R --spacing-size16; Full Width is edge to edge.
 *   Heading (Usage=Group, Position=Top only) — Style 7 (--type-style7-*) / --text-generic-default,
 *     --spacing-size16 below.
 *   Row "H Stack" — --background-panel-default fill (Pressed → --background-panel-pressed),
 *     --spacing-size16 padding, --spacing-size12 gap, items centred. A V Stack (--spacing-size12 gap)
 *     of 1–3 pair frames; each pair (--spacing-size04 gap) is an Item Title (--type-style8-* /
 *     --text-generic-subdued) over a Data value (--type-style7-* / --text-action-default). Then an
 *     optional NEW Notification Tag and a trailing 16px Action Icon (Padding=True) — Chevron Right.
 *   Corner radius (--radius-default = 12) — Compact only. Group: top corners on Top, bottom on
 *     Bottom, none on Middle. Single: top corners on Top; all corners on Middle/Bottom. Full = square.
 *   Divider (Full width, Default) below every Group row except the last (Bottom); never for Single.
 *
 *   Glyph colour is token-driven (CSS mask): the row anchors color: --text-action-default, so the
 *   trailing chevron (mask filled by currentColor) tracks the Data value's token and maps light/dark.
 */

const STYLE_ID = "cnc-lidp-styles";
const CSS = `
.cnc-lidp { box-sizing: border-box; width: 100%; font-family: var(--type-style7-family); }
.cnc-lidp--compact { padding-left: var(--spacing-size16); padding-right: var(--spacing-size16); }

.cnc-lidp__heading {
  padding-bottom: var(--spacing-size16);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--text-generic-default);
}

.cnc-lidp__row {
  box-sizing: border-box;
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size12);
  width: 100%;
  padding: var(--spacing-size16);
  background: var(--background-panel-default);
  border: none;
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none; appearance: none;
  font-family: var(--type-style7-family);
  color: var(--text-action-default);
}
.cnc-lidp__row:active:not(:disabled), .cnc-lidp__row--pressed { background: var(--background-panel-pressed); }
.cnc-lidp__row--rt { border-top-left-radius: var(--radius-default); border-top-right-radius: var(--radius-default); }
.cnc-lidp__row--rb { border-bottom-left-radius: var(--radius-default); border-bottom-right-radius: var(--radius-default); }

.cnc-lidp__sets { display: flex; flex-direction: column; gap: var(--spacing-size12); flex: 1 1 auto; min-width: 0; }
.cnc-lidp__pair { display: flex; flex-direction: column; gap: var(--spacing-size04); }
.cnc-lidp__title {
  font-family: var(--type-style8-family); font-size: var(--type-style8-size);
  line-height: var(--type-style8-line); font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking); color: var(--text-generic-subdued);
}
.cnc-lidp__data {
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
  letter-spacing: var(--type-style7-tracking); color: var(--text-action-default);
}
.cnc-lidp__tag, .cnc-lidp__action { flex: 0 0 auto; display: inline-flex; }

/* Figma instance override: the in-row Divider has its vertical padding set to 0 (Spacing/None). */
.cnc-lidp .cnc-lidp__rule { padding-top: 0; padding-bottom: 0; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ListItemDataPlayback({
  dataSet = 1,
  // 1 | 2 | 3 — number of Title/Data pairs
  width = "compact",
  // "compact" | "full"
  usage = "group",
  // "group" | "single"
  position = "top",
  // "top" | "middle" | "bottom"
  state = "default",
  // "default" | "pressed"
  heading = "Heading",
  // group section heading (Usage=Group, Position=Top)
  pairs,
  // [{ title, data }] — one per data set
  tag = false,
  // show the NEW Notification Tag
  tagLabel = "NEW",
  actionIcon = "Arrows/Chevron Right/Chevron Right",
  onClick,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const DividerCmp = fromNS("Divider", __ds_scope.Divider);
  const TagCmp = fromNS("NotificationTag", __ds_scope.NotificationTag);
  const isFull = width === "full";
  const isSingle = usage === "single";
  const count = Math.max(1, Math.min(3, dataSet));

  // Rounding (Compact only): faithful to the extraction matrix.
  let roundTop = false;
  let roundBottom = false;
  if (!isFull) {
    if (isSingle) {
      roundTop = true; // Single Top → top; Single Middle/Bottom → all
      if (position !== "top") roundBottom = true;
    } else {
      if (position === "top") roundTop = true;
      if (position === "bottom") roundBottom = true;
    }
  }
  const showDivider = !isSingle && position !== "bottom";
  const showHeading = !isSingle && position === "top";
  const data = Array.isArray(pairs) && pairs.length ? pairs : [{
    title: "Title",
    data: "Data"
  }];
  const rows = [];
  for (let i = 0; i < count; i += 1) {
    const p = data[i] || {
      title: "Title",
      data: "Data"
    };
    rows.push(/*#__PURE__*/React.createElement("span", {
      className: "cnc-lidp__pair",
      key: i
    }, /*#__PURE__*/React.createElement("span", {
      className: "cnc-lidp__title"
    }, p.title), /*#__PURE__*/React.createElement("span", {
      className: "cnc-lidp__data"
    }, p.data)));
  }
  const outerCls = ["cnc-lidp", isFull ? "cnc-lidp--full" : "cnc-lidp--compact"];
  if (className) outerCls.push(className);
  const rowCls = ["cnc-lidp__row"];
  if (state === "pressed") rowCls.push("cnc-lidp__row--pressed");
  if (roundTop) rowCls.push("cnc-lidp__row--rt");
  if (roundBottom) rowCls.push("cnc-lidp__row--rb");
  const rule = DividerCmp ? /*#__PURE__*/React.createElement(DividerCmp, {
    width: "full",
    type: "default",
    className: "cnc-lidp__rule"
  }) : null;
  return /*#__PURE__*/React.createElement("div", _extends({
    className: outerCls.join(" ")
  }, rest), showHeading ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-lidp__heading"
  }, heading) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: rowCls.join(" "),
    onClick: onClick
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-lidp__sets"
  }, rows), tag && TagCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-lidp__tag"
  }, /*#__PURE__*/React.createElement(TagCmp, {
    type: "new"
  }, tagLabel)) : null, IconCmp ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-lidp__action"
  }, /*#__PURE__*/React.createElement(IconCmp, {
    name: actionIcon,
    size: 16,
    padding: true,
    basePath: basePath
  })) : null), showDivider ? rule : null);
}
Object.assign(__ds_scope, { ListItemDataPlayback });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/List/ListItemDataPlayback/ListItemDataPlayback.jsx", error: String((e && e.message) || e) }); }

// src/components/content/List/ListDataPlaybackGroup/ListDataPlaybackGroup.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — List Data Playback Group  (composes _List Item Data Playback)
 * A vertical stack of Data Playback rows. Matches the Figma "List Data Playback Group" set exactly:
 *   Width: Compact | Full Width   (the only variant axis — forwarded to every row)
 *
 * Geometry (token-driven):
 *   Container — VERTICAL auto-layout, MIN/MIN, 375 FIXED width (Figma min=max=375), HUG height,
 *     padding 0, itemSpacing 0 (rows abut; the row's own Divider separates them). fills:[] —
 *     no background of its own; each row carries --background-panel-default.
 *   Rows — every child is a _List Item Data Playback with Usage=Group. Position is assigned by index:
 *     first → Top (rounds top corners in Compact, renders the section Heading), last → Bottom
 *     (rounds bottom corners, no trailing Divider), all others → Middle. Width is forwarded.
 *   The 375 width carries token:null in Figma — a documented frame literal.
 *
 * Faithful mapping note: a one-row group is rendered Usage=Single (per the row's own Single/Top
 *   rounding rule), the only sensible reduction of Top+Bottom onto a single Position value.
 *
 * Adds layout only — no fills, strokes, radii, behaviour, motion or props beyond the extraction.
 */

const STYLE_ID = "cnc-ldpg-styles";
const CSS = `
.cnc-ldpg {
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  width: 100%;
  max-width: 375px;            /* Figma FIXED frame width (token:null) */
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ListDataPlaybackGroup({
  width = "compact",
  // "compact" | "full" — forwarded to every row
  items,
  // array of _List Item Data Playback prop objects (one per row)
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const ItemCmp = fromNS("ListItemDataPlayback", __ds_scope.ListItemDataPlayback);
  const data = Array.isArray(items) ? items : [];
  const n = data.length;
  const cls = ["cnc-ldpg"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" ")
  }, rest), ItemCmp ? data.map((it, i) => {
    const single = n === 1;
    const position = i === 0 ? "top" : i === n - 1 ? "bottom" : "middle";
    return /*#__PURE__*/React.createElement(ItemCmp, _extends({
      key: i
    }, it, {
      usage: single ? "single" : "group",
      position: single ? "top" : position,
      width: width,
      basePath: basePath
    }));
  }) : null);
}
Object.assign(__ds_scope, { ListDataPlaybackGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/content/List/ListDataPlaybackGroup/ListDataPlaybackGroup.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Pop-up_Modal/CustomPopupModal/CustomPopupModal.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Custom Pop-up Modal
 * The flexible centre-screen alert: a dimmed overlay (Background/Overlay/Default) holding a 343-wide
 * white Pop-Up card. Matches the Figma "Custom Pop-up Modal" set (375×812) exactly:
 *   Link Stack : 2 Horizontal | Single | 2 Vertical   → forwarded to the nested Link Button Stack
 *   Sentiment  : Default | Success | Critical | Warning → recolours the title header + title text
 *
 * Anatomy (token-driven):
 *   Overlay  — fills the frame with --background-overlay-default, centring the card.
 *   Pop-Up   — column, --background-panel-default, radius --c-temp-popup-cornerradius, clipped.
 *     Title    — header bar, fill = sentiment header token, padding --spacing-size16, 0px bottom border
 *                (--c-temp-popup-headerbottomborderwidth · --border-generic-defaultalt01). Centred
 *                Style 3 title in the sentiment title token.
 *     Container— padding 16/16/32, gap --spacing-size16, centred. Style 8 message (--text-generic-default).
 *                The Figma Compact Button instance here is visible:false — surfaced as the optional
 *                `bodyExtra` slot (renders nothing by default, matching the extraction).
 *     Panel    — the Figma Notification Panel instance is visible:false — surfaced as the optional
 *                `panel` slot, rendered between Container and the Link Button Stack (off by default).
 *     Footer   — the composed Link Button Stack (rounded bottom corners).
 *
 * Composes Link Button Stack. No additions beyond the extraction — no extra variants, states,
 * motion, or elevation; the two hidden slots mirror Figma's visible:false instances.
 */

const STYLE_ID = "cnc-custom-popup-modal-styles";
const CSS = `
.cnc-cpm {
  box-sizing: border-box;
  width: 375px; height: 812px;
  display: flex; align-items: center; justify-content: center;
  background: var(--background-overlay-default);
}
.cnc-cpm__popup {
  box-sizing: border-box;
  width: 343px; max-width: 343px;
  display: flex; flex-direction: column;
  background: var(--background-panel-default);
  border-radius: var(--c-temp-popup-cornerradius);
  overflow: hidden;
}
.cnc-cpm__header {
  box-sizing: border-box;
  display: flex; align-items: flex-start;
  padding: var(--spacing-size16);
  background: var(--cpm-header-bg);
  border-bottom: var(--c-temp-popup-headerbottomborderwidth) solid var(--border-generic-defaultalt01);
}
.cnc-cpm__title {
  flex: 1 1 0; min-width: 0; margin: 0;
  text-align: center; text-wrap: pretty;
  color: var(--cpm-title);
  font-family: var(--type-style3-family);
  font-size: var(--type-style3-size);
  line-height: var(--type-style3-line);
  font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
}
.cnc-cpm__container {
  box-sizing: border-box;
  display: flex; flex-direction: column; align-items: center;
  gap: var(--spacing-size16);
  padding: var(--spacing-size16) var(--spacing-size16) var(--spacing-size32);
}
.cnc-cpm__message {
  margin: 0; align-self: stretch;
  text-align: center; text-wrap: pretty;
  color: var(--text-generic-default);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// Sentiment → header background + title colour tokens (Figma Component/Temp/Popup/*).
const SENTIMENT = {
  default: {
    header: "--c-temp-popup-headerdefaultbackground",
    title: "--c-temp-popup-titledefault"
  },
  success: {
    header: "--c-temp-popup-headerpositivebackground",
    title: "--c-temp-popup-titlepositive"
  },
  critical: {
    header: "--c-temp-popup-headernegativebackground",
    title: "--c-temp-popup-titlenegative"
  },
  warning: {
    header: "--c-temp-popup-headerfraudbackground",
    title: "--c-temp-popup-titlefraud"
  }
};

// Link Stack variant → nested Link Button Stack props.
const LINK_STACK = {
  "2-horizontal": {
    count: 2,
    direction: "horizontal"
  },
  "single": {
    count: 1,
    direction: "horizontal"
  },
  "2-vertical": {
    count: 2,
    direction: "vertical"
  }
};
function CustomPopupModal({
  sentiment = "default",
  linkStack = "2-horizontal",
  title = "Title",
  message = "Duis quis tempus dolor. Vivamus non pretium tortor, cursus pharetra turpis.",
  // Link Button Stack content (forwarded)
  primaryLabel = "Link",
  secondaryLabel = "Link",
  onPrimary,
  onSecondary,
  links,
  // Optional hidden-instance slots from the Figma tree (visible:false by default)
  bodyExtra = null,
  panel = null,
  basePath = ".",
  className = "",
  ...rest
}) {
  ensureStyles();
  const LBS = fromNS("LinkButtonStack", __ds_scope.LinkButtonStack);
  const sent = SENTIMENT[sentiment] || SENTIMENT.default;
  const stack = LINK_STACK[linkStack] || LINK_STACK["2-horizontal"];
  const cls = ["cnc-cpm"];
  if (className) cls.push(className);
  const style = {
    "--cpm-header-bg": `var(${sent.header})`,
    "--cpm-title": `var(${sent.title})`
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: style
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cpm__popup",
    role: "alertdialog",
    "aria-modal": "true"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-cpm__header"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "cnc-cpm__title"
  }, title)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-cpm__container"
  }, /*#__PURE__*/React.createElement("p", {
    className: "cnc-cpm__message"
  }, message), bodyExtra), panel, LBS ? /*#__PURE__*/React.createElement(LBS, {
    direction: stack.direction,
    count: stack.count,
    links: links,
    primaryLabel: primaryLabel,
    secondaryLabel: secondaryLabel,
    onPrimary: onPrimary,
    onSecondary: onSecondary
  }) : null));
}
Object.assign(__ds_scope, { CustomPopupModal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Pop-up_Modal/CustomPopupModal/CustomPopupModal.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Home_indicator/Handle/Handle.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — _Handle  (private `_` — composed by Home Indicator, not for direct use)
 * The grabber bar inside the OS home-indicator region. Matches the Figma "_Handle" set:
 *   OS : iOS | iPadOS | Android
 *
 * Geometry (per OS; all dims token:null in the source → documented literals, exactly as
 * Icon/Pictogram carry their un-tokenised sizes):
 *   iOS     — 139 × 5,   radius 100 (pill)
 *   iPadOS  — 315 × 5.5, radius 100 (pill)
 *   Android — 108 × 4,   radius 12
 * Fill — the single bar is filled with the OS home-indicator token (--os-homeindicator),
 * which maps light/dark automatically (black on light, white on dark). The outer Figma frame
 * is a transparent (visible:false) wrapper that hugs the bar, so only the bar is rendered.
 */

const STYLE_ID = "cnc-handle-styles";
const CSS = `
.cnc-handle { box-sizing: border-box; display: inline-flex; align-items: center; justify-content: center; flex: 0 0 auto; }
.cnc-handle__bar { background: var(--os-homeindicator); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
const DIMS = {
  iOS: {
    width: 139,
    height: 5,
    radius: 100
  },
  iPadOS: {
    width: 315,
    height: 5.5,
    radius: 100
  },
  Android: {
    width: 108,
    height: 4,
    radius: 12
  }
};
function Handle({
  os = "iOS",
  className = "",
  ...rest
}) {
  ensureStyles();
  const d = DIMS[os] || DIMS.iOS;
  const cls = ["cnc-handle"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" "),
    "aria-hidden": "true"
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-handle__bar",
    style: {
      width: `${d.width}px`,
      height: `${d.height}px`,
      borderRadius: `${d.radius}px`
    }
  }));
}
Object.assign(__ds_scope, { Handle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Home_indicator/Handle/Handle.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Home_indicator/HomeIndicator/HomeIndicator.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Home Indicator
 * The OS home-indicator strip that sits at the bottom of a screen. Matches the Figma
 * "Home indicator" component: a 375-wide, VERTICAL frame that centres a _Handle, with
 * 8px top + bottom padding.
 *
 * Anatomy (faithful to the extraction):
 *   Frame  — width 375 (Figma FIXED), height HUG (= 21 with the iOS handle). VERTICAL,
 *            primary + counter axis CENTER. Padding top/bottom 8 (Figma token
 *            "Home Indicator/Padding"), left/right 0.
 *   Handle — composes the _Handle child (Figma default OS=iOS). `os` is forwarded so the
 *            same indicator can render the iPadOS / Android handle.
 *
 * FLAGS:
 *   • The Figma padding token "Home Indicator/Padding" (8px) is not emitted as a dedicated CSS
 *     var in the token set; bound to the matching `--spacing-size08` primitive (identical 8px),
 *     consistent with the rest of the system. Flagged — not a guess (same value, spacing primitive).
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-homeindicator-styles";
const CSS = `
.cnc-homeindicator {
  box-sizing: border-box;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  padding-top: var(--spacing-size08); padding-bottom: var(--spacing-size08);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function HomeIndicator({
  os = "iOS",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const HandleCmp = fromNS("Handle", __ds_scope.Handle);
  const cls = ["cnc-homeindicator"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: {
      width: `${width}px`
    }
  }, rest), /*#__PURE__*/React.createElement(HandleCmp, {
    os: os
  }));
}
Object.assign(__ds_scope, { HomeIndicator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Home_indicator/HomeIndicator/HomeIndicator.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Spinner/SpinnerSet/SpinnerSet.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — _Spinner Set  (private `_` — composed by Spinner / Native Spinner Configuration)
 * The animated loader artwork. Figma ships FOUR sets (`_Spinner Set 01`–`04`), each a
 * COMPONENT_SET whose `State` variants are the keyframes of one rotation. They are structurally
 * identical — a per-frame artwork animation differing only in frame count, frame size, and the
 * artwork folder — so they are implemented here as ONE parametrised component, `SpinnerSet`,
 * selected by `set` (1–4). (See FLAGS.)
 *
 *   Set 1 — 8 frames (01–08), 64px   — the visible artwork in the public Spinner
 *   Set 2 — 4 frames (01–04), 64px   — present-but-hidden in the public Spinner
 *   Set 3 — 4 frames (1–4),   24px   — the iOS / iPadOS Native Spinner Configuration
 *   Set 4 — 4 frames (01–04), 44px   — the Android Native Spinner Configuration
 *
 * Artwork & colour:
 *   Each keyframe ships as a PAIRED light/dark SVG under
 *   assets/components/Spinner/spinner-set-<n>/{light,dark}/<frame>.svg. The frames are
 *   multi-colour (ring + highlight + padlock glyph), so they are rendered AS-AUTHORED via <img>
 *   and swapped on [data-theme="dark"] — an artwork swap, never a mask/recolour. The SVG fills are
 *   the Figma "Component/Temp/Spinner/*" token values verbatim (light #000 ring / #0BE248 highlight;
 *   dark #C7FFC6 ring / #0BE248) — source artwork, so the literal hex lives only in assets/.
 *
 * Animation: the frames cycle on a fixed interval (full rotation = `duration`, default 1000ms),
 * respecting prefers-reduced-motion (holds the first frame). All frames are pre-rendered (hidden)
 * so the swap never flashes.
 *
 * FLAGS:
 *   • Four Figma sets → one parametrised component. The sets are identical per-frame animations;
 *     collapsing them to one `set` prop avoids four duplicate files. Flagged for sign-off (split
 *     into four files on request).
 *   • `onColour` (Figma "on Colour") — the extraction captures NO visual difference between the
 *     False/True variants (same child, same state), and only light/dark artwork exists. Mapped:
 *     False = theme-following light/dark swap; True = force the dark-folder artwork (the light-green
 *     ring that reads on a brand-colour surface). Inferred — flagged.
 *   • Frame sizes (64/24/44) and counts are the Figma FIXED values (token:null) → documented literals.
 */

const STYLE_ID = "cnc-spinnerset-styles";
const CSS = `
.cnc-spinnerset { box-sizing: border-box; position: relative; display: inline-block; flex: 0 0 auto; }
.cnc-spinnerset__frame { position: absolute; inset: 0; display: none; }
.cnc-spinnerset__frame.is-current { display: block; }
.cnc-spinnerset__img { display: block; width: 100%; height: 100%; }
.cnc-spinnerset__img--dark { display: none; }
[data-theme="dark"] .cnc-spinnerset__img--light { display: none; }
[data-theme="dark"] .cnc-spinnerset__img--dark { display: block; }
.cnc-spinnerset--oncolour .cnc-spinnerset__img--light { display: none; }
.cnc-spinnerset--oncolour .cnc-spinnerset__img--dark { display: block; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}

// folder, frame names (faithful to the on-disk assets), default frame size.
const SETS = {
  1: {
    folder: "spinner-set-1",
    frames: ["01", "02", "03", "04", "05", "06", "07", "08"],
    size: 64
  },
  2: {
    folder: "spinner-set-2",
    frames: ["01", "02", "03", "04"],
    size: 64
  },
  3: {
    folder: "spinner-set-3",
    frames: ["1", "2", "3", "4"],
    size: 24
  },
  4: {
    folder: "spinner-set-4",
    frames: ["01", "02", "03", "04"],
    size: 44
  }
};
function SpinnerSet({
  set = 1,
  size,
  onColour = false,
  duration = 1000,
  basePath = ".",
  title,
  className = "",
  ...rest
}) {
  ensureStyles();
  const cfg = SETS[set] || SETS[1];
  const px = typeof size === "number" ? size : cfg.size;
  const count = cfg.frames.length;
  const [frame, setFrame] = React.useState(0);
  React.useEffect(() => {
    if (typeof window === "undefined") return undefined;
    const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduce || count < 2) return undefined;
    const id = window.setInterval(() => setFrame(f => (f + 1) % count), Math.max(60, duration / count));
    return () => window.clearInterval(id);
  }, [count, duration]);
  const base = `${basePath}/assets/components/Spinner/${cfg.folder}`;
  const cls = ["cnc-spinnerset"];
  if (onColour) cls.push("cnc-spinnerset--oncolour");
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" "),
    role: title ? "img" : "presentation",
    "aria-label": title || undefined,
    "aria-hidden": title ? undefined : true,
    style: {
      width: `${px}px`,
      height: `${px}px`
    }
  }, rest), cfg.frames.map((name, i) => /*#__PURE__*/React.createElement("span", {
    key: name,
    className: "cnc-spinnerset__frame" + (i === frame ? " is-current" : "")
  }, /*#__PURE__*/React.createElement("img", {
    className: "cnc-spinnerset__img cnc-spinnerset__img--light",
    src: `${base}/light/${name}.svg`,
    alt: "",
    draggable: "false"
  }), /*#__PURE__*/React.createElement("img", {
    className: "cnc-spinnerset__img cnc-spinnerset__img--dark",
    src: `${base}/dark/${name}.svg`,
    alt: "",
    draggable: "false"
  }))));
}
Object.assign(__ds_scope, { SpinnerSet });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Spinner/SpinnerSet/SpinnerSet.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Spinner/NativeSpinnerConfiguration/NativeSpinnerConfiguration.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Native Spinner Configuration
 * The platform-native loading spinner. Matches the Figma "Native Spinner Configuration" set:
 *   OS        : iOS | iPadOS | Android
 *   On Colour : False | True
 *
 * Composition (faithful to the extraction):
 *   iOS / iPadOS — composes _Spinner Set 03 (24 × 24)
 *   Android      — composes _Spinner Set 04 (44 × 44)
 *   `onColour` is forwarded to the set.
 *
 * No chrome of its own — it is purely a per-OS selection of the right spinner set/size, exactly
 * as the Figma component (a single instance child per variant).
 */

const STYLE_ID = "cnc-nativespinner-styles";
const CSS = `
.cnc-nativespinner { box-sizing: border-box; display: inline-flex; flex: 0 0 auto; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// Per-OS set selection (Figma): iOS/iPadOS → set 3 (24); Android → set 4 (44).
const OS_SET = {
  iOS: {
    set: 3,
    size: 24
  },
  iPadOS: {
    set: 3,
    size: 24
  },
  Android: {
    set: 4,
    size: 44
  }
};
function NativeSpinnerConfiguration({
  os = "iOS",
  onColour = false,
  duration = 1000,
  basePath = ".",
  title,
  className = "",
  ...rest
}) {
  ensureStyles();
  const Set = fromNS("SpinnerSet", __ds_scope.SpinnerSet);
  const cfg = OS_SET[os] || OS_SET.iOS;
  const cls = ["cnc-nativespinner"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement(Set, {
    set: cfg.set,
    size: cfg.size,
    onColour: onColour,
    duration: duration,
    basePath: basePath,
    title: title
  }));
}
Object.assign(__ds_scope, { NativeSpinnerConfiguration });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Spinner/NativeSpinnerConfiguration/NativeSpinnerConfiguration.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Spinner/Spinner/Spinner.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Spinner
 * The brand loading spinner (the large padlock-ring loader used in modals / splash screens).
 * Matches the Figma "Spinner" set:
 *   on Colour : False | True
 *
 * Composition (faithful to the extraction):
 *   The set composes _Spinner Set 02 (visible:false) + _Spinner Set 01 (visible:true) at 64 × 64.
 *   Only Set 01 is rendered — the hidden Set 02 duplicate is omitted (same precedent as the hidden
 *   "Shape 02" polygon omitted in MI Field). `onColour` is forwarded to the set.
 *
 * FLAGS:
 *   • Hidden _Spinner Set 02 child omitted (visible:false in Figma) — flagged.
 *   • `onColour` semantics are the SpinnerSet's (see that component): False = theme-following
 *     light/dark; True = force dark-folder artwork. Inferred (extraction shows no visual diff).
 */

const STYLE_ID = "cnc-spinner-styles";
const CSS = `
.cnc-spinner { box-sizing: border-box; display: inline-flex; flex: 0 0 auto; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function Spinner({
  onColour = false,
  size = 64,
  duration = 1000,
  basePath = ".",
  title,
  className = "",
  ...rest
}) {
  ensureStyles();
  const Set = fromNS("SpinnerSet", __ds_scope.SpinnerSet);
  const cls = ["cnc-spinner"];
  if (className) cls.push(className);
  return /*#__PURE__*/React.createElement("span", _extends({
    className: cls.join(" ")
  }, rest), /*#__PURE__*/React.createElement(Set, {
    set: 1,
    size: size,
    onColour: onColour,
    duration: duration,
    basePath: basePath,
    title: title
  }));
}
Object.assign(__ds_scope, { Spinner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Spinner/Spinner/Spinner.jsx", error: String((e && e.message) || e) }); }

// src/components/notification/Pop-up_Modal/PopupModalPresets/PopupModalPresets.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render via fromNS() (cross-directory safe).
// Imported for standalone use / type-resolution.

/**
 * Cancara — Pop-up Modal Presets
 * The ready-made centre-screen alerts (Figma "Pop-up Modal Presets" set, 375×812): a dimmed overlay
 * (Background/Overlay/Default) holding a 343-wide white Pop-Up card. Each preset is a fixed recipe of
 * the shared anatomy (Title header → Container → optional Link Button Stack footer):
 *
 *   Preset = Password        → POSITIVE header ("Enter password"), body copy, a label-less Password
 *                              field (caret on, masked), a Tertiary "Forgotten your password?"
 *                              Compact Button, and a 2-link horizontal footer.
 *   Preset = Password Error  → NEGATIVE (red) header, same body/field/button but the Password field
 *                              in its Error state, and a 2-link horizontal footer.
 *   Preset = Success         → DEFAULT (grey) header ("Title"), a centred success Icon + body copy,
 *                              and a single-link footer.
 *   Preset = Loader          → DEFAULT header, a centred 64px Spinner, no footer.
 *
 *   (Preset = "Payment check" — DROPPED per owner decision 2026-06-25: it composed an excluded
 *    brand, Barclays. Not built.)
 *
 * Composes Password · Compact Button · Spinner · Icon · Link Button Stack. 100% token-driven;
 * light/dark maps automatically. No additions beyond the extraction.
 *
 * FLAGS:
 *   • Payment check preset DROPPED (excluded brand). The 4 remaining presets are built.
 *   • Password gained two ADDITIVE, backward-compatible props to render faithfully here:
 *       `showFieldLabel={false}` — the popup instance is label-less (56px box only; the modal title is
 *         the label). Mirrors the Country Picker `showLabel` precedent.
 *       `showCursor` — the Figma Password instance sets "Show cursor=true" while State=Default; my
 *         state-derived caret couldn't express that, so an explicit override was added.
 *   • Success Icon is an INSTANCE_SWAP with NO captured fill → defaulted to "Sentiment system/
 *     Success/Success" and pinned to --icon-sentiment-success (the success sentiment token). Both
 *     overridable via `successIcon` / `successIconColor`.
 *   • Container itemSpacing — 16 (Password presets, tokenised --spacing-size16) and 10 (Success /
 *     Loader, token:null in Figma → literal 10px, flagged). Paddings all tokenised.
 *   • Pop-Up corner radius 16 token:null in Figma → bound to --c-temp-popup-cornerradius (16).
 *   • interactive forwards to the Password field (additive, off by default).
 */

const STYLE_ID = "cnc-popup-modal-presets-styles";
const CSS = `
.cnc-pmp { box-sizing: border-box; }
.cnc-pmp *, .cnc-pmp *::before, .cnc-pmp *::after { box-sizing: border-box; }

.cnc-pmp {
  width: 375px; height: 812px;
  display: flex; align-items: center; justify-content: center;
  background: var(--background-overlay-default);
}
.cnc-pmp__popup {
  width: 343px; max-width: 343px;
  display: flex; flex-direction: column;
  background: var(--background-panel-default);
  border-radius: var(--c-temp-popup-cornerradius);
  overflow: hidden;
}

/* Title header — fill + title colour set per preset via CSS vars. */
.cnc-pmp__header {
  display: flex; align-items: flex-start;
  padding: var(--spacing-size16);
  background: var(--pmp-header-bg);
  border-bottom: var(--c-temp-popup-headerbottomborderwidth) solid var(--border-generic-defaultalt01);
}
.cnc-pmp__title {
  flex: 1 1 0; min-width: 0; margin: 0;
  text-align: center; text-wrap: pretty;
  color: var(--pmp-title);
  font-family: var(--type-style3-family);
  font-size: var(--type-style3-size);
  line-height: var(--type-style3-line);
  font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
}

/* Container — padding + gap per preset. */
.cnc-pmp__container {
  display: flex; flex-direction: column; align-items: center;
}
.cnc-pmp__container--password {
  gap: var(--spacing-size16);
  padding: var(--spacing-size16) var(--spacing-size16) var(--spacing-size32);
}
/* itemSpacing 10 is token:null in Figma → literal 10px (flagged). */
.cnc-pmp__container--success {
  gap: 10px;
  padding: var(--spacing-size16) var(--spacing-size16) var(--spacing-size32);
}
.cnc-pmp__container--loader {
  gap: 10px;
  padding: var(--spacing-size32) var(--spacing-size16);
}

.cnc-pmp__message {
  margin: 0; align-self: stretch;
  text-align: center; text-wrap: pretty;
  color: var(--text-generic-default);
  font-family: var(--type-style8-family);
  font-size: var(--type-style8-size);
  line-height: var(--type-style8-line);
  font-weight: var(--type-style8-weight);
  letter-spacing: var(--type-style8-tracking);
}

/* The Password field instance fills the container width (Figma 343 FIXED inside 343 container). */
.cnc-pmp__password { align-self: stretch; }
.cnc-pmp__password > * { width: 100% !important; }

/* Forgotten-password Compact Button row — hugs + centres. */
.cnc-pmp__forgot { display: inline-flex; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}

// Preset → header token pair + which body/footer recipe to render.
const PRESETS = {
  "password": {
    header: "--c-temp-popup-headerpositivebackground",
    title: "--c-temp-popup-titlepositive",
    titleText: "Enter password",
    message: "Please enter your password to confirm this request.",
    passwordState: "default",
    footer: {
      count: 2,
      direction: "horizontal"
    }
  },
  "password-error": {
    header: "--c-temp-popup-headernegativebackground",
    title: "--c-temp-popup-titlenegative",
    titleText: "Enter password",
    message: "Please enter your password to confirm this request.",
    passwordState: "error",
    footer: {
      count: 2,
      direction: "horizontal"
    }
  },
  "success": {
    header: "--c-temp-popup-headerdefaultbackground",
    title: "--c-temp-popup-titledefault",
    titleText: "Title",
    message: "Duis quis tempus dolor. Vivamus non pretium tortor, cursus pharetra turpis.",
    footer: {
      count: 1,
      direction: "horizontal"
    }
  },
  "loader": {
    header: "--c-temp-popup-headerdefaultbackground",
    title: "--c-temp-popup-titledefault",
    titleText: "Title",
    footer: null
  }
};
function PopupModalPresets({
  preset = "password",
  // Content overrides (defaults follow the Figma sample strings) —
  title,
  message,
  forgotLabel = "Forgotten your password?",
  passwordPlaceholder = "Password",
  // Footer Link Button Stack —
  primaryLabel = "Link",
  secondaryLabel = "Link",
  links,
  onPrimary,
  onSecondary,
  // Success icon (INSTANCE_SWAP — no captured fill; see FLAGS) —
  successIcon = "Sentiment system/Success/Success",
  successIconColor = "var(--icon-sentiment-success)",
  // Loader —
  spinnerOnColour = false,
  // Handlers / behaviour —
  onForgot,
  onChangePassword,
  interactive = false,
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const LBS = fromNS("LinkButtonStack", __ds_scope.LinkButtonStack);
  const PW = fromNS("Password", __ds_scope.Password);
  const CompactBtn = fromNS("CompactButton", __ds_scope.CompactButton);
  const Spin = fromNS("Spinner", __ds_scope.Spinner);
  const Ic = fromNS("Icon", __ds_scope.Icon);
  const cfg = PRESETS[preset] || PRESETS.password;
  const isPassword = preset === "password" || preset === "password-error";
  const isSuccess = preset === "success";
  const isLoader = preset === "loader";
  const cls = ["cnc-pmp"];
  if (className) cls.push(className);
  const style = {
    "--pmp-header-bg": `var(${cfg.header})`,
    "--pmp-title": `var(${cfg.title})`
  };
  if (width != null) style.width = typeof width === "number" ? `${width}px` : width;
  const titleText = title != null ? title : cfg.titleText;
  const messageText = message != null ? message : cfg.message;

  // ---- Container body per preset ----
  let containerCls = "cnc-pmp__container";
  let body = null;
  if (isPassword) {
    containerCls += " cnc-pmp__container--password";
    body = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("p", {
      className: "cnc-pmp__message"
    }, messageText), PW ? /*#__PURE__*/React.createElement("div", {
      className: "cnc-pmp__password"
    }, /*#__PURE__*/React.createElement(PW, {
      state: cfg.passwordState,
      dataFormat: "none",
      masked: true,
      showFieldLabel: false,
      showCursor: true,
      placeholder: passwordPlaceholder,
      interactive: interactive,
      onChange: onChangePassword,
      basePath: basePath
    })) : null, CompactBtn ? /*#__PURE__*/React.createElement("span", {
      className: "cnc-pmp__forgot"
    }, /*#__PURE__*/React.createElement(CompactBtn, {
      variant: "tertiary",
      size: "default",
      iconPosition: "none",
      basePath: basePath,
      onClick: onForgot
    }, forgotLabel)) : null);
  } else if (isSuccess) {
    containerCls += " cnc-pmp__container--success";
    body = /*#__PURE__*/React.createElement(React.Fragment, null, Ic ? /*#__PURE__*/React.createElement(Ic, {
      name: successIcon,
      size: 32,
      color: successIconColor,
      basePath: basePath,
      title: "Success"
    }) : null, /*#__PURE__*/React.createElement("p", {
      className: "cnc-pmp__message"
    }, messageText));
  } else if (isLoader) {
    containerCls += " cnc-pmp__container--loader";
    body = Spin ? /*#__PURE__*/React.createElement(Spin, {
      onColour: spinnerOnColour,
      basePath: basePath,
      title: "Loading"
    }) : null;
  }
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: style
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-pmp__popup",
    role: isLoader ? "status" : "alertdialog",
    "aria-modal": "true",
    "aria-busy": isLoader || undefined
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-pmp__header"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "cnc-pmp__title"
  }, titleText)), /*#__PURE__*/React.createElement("div", {
    className: containerCls
  }, body), cfg.footer && LBS ? /*#__PURE__*/React.createElement(LBS, {
    direction: cfg.footer.direction,
    count: cfg.footer.count,
    links: links,
    primaryLabel: primaryLabel,
    secondaryLabel: secondaryLabel,
    onPrimary: onPrimary,
    onSecondary: onSecondary
  }) : null));
}
Object.assign(__ds_scope, { PopupModalPresets });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/notification/Pop-up_Modal/PopupModalPresets/PopupModalPresets.jsx", error: String((e && e.message) || e) }); }

// src/components/os/Status_Bar/StatusBar/StatusBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Cancara — _Status Bar  (private `_` — Internal: composed by Custom Header / Preset Header /
 * Modal Stack Header, not for direct use). Figma "_Status Bar" + "_Modal Stack Status Bar".
 *
 * The OS status strip: the time on the left and the signal / Wi-Fi / battery levels on the right,
 * 54px tall (FILL width). The supplied `_Status_Bar.json` was the empty INSTANCE shell (the glyph
 * nodes weren't captured); the real content + its colour tokens are built here from the owner's
 * Figma visual and `lloyds_reimagined_tokens.json`.
 *
 * TOKEN-DRIVEN (no hardcoded colour/size):
 *   • variant "default"  (Custom / Preset Header)  → time + glyphs `--c-statusbar-timepluslevels`,
 *     transparent background. Light/dark map automatically (black ↔ white).
 *   • variant "modal-ios"     (Modal Stack, iOS)     → `--os--modalstack-statusbarios` on
 *     `--os--modalstack-statusbariosbackground` (white on black).
 *   • variant "modal-android" (Modal Stack, Android) → `--os--modalstack-statusbarandroid` on
 *     `--os--modalstack-statusbarandroidbackground` (themed).
 *   • time type → `--type-style4-*` (GT Ultra 17/700 — the iOS time style; see FLAGS).
 *
 * ICON COLOUR: the glyphs are single-colour SVGs in `assets/os/statusbar/` used as CSS masks; the
 * row pins `color` to the variant's status-bar token and each glyph paints with `currentColor`, so
 * time + levels always share one token and map light/dark with no hand-written override.
 *
 * FLAGS:
 *   • Glyph artwork (signal / Wi-Fi / battery) and the time type had no structured nodes/dedicated
 *     type token in the extraction → authored single-colour SVGs + `--type-style4` (closest: GT Ultra
 *     17/700). Recolour is fully tokenised.
 *   • Height 54 and the internal time-inset / glyph gaps were token:null in Figma → mapped to the
 *     nearest spacing tokens (`--spacing-size16` inset, `--spacing-size04` gap); documented.
 *   • Android (modal-android) reuses the iOS glyph set (no separate Android glyph artwork supplied).
 */

const STATUS_BAR_HEIGHT = 54; // Figma height, token:null — documented literal.

const STYLE_ID = "cnc-statusbar-styles";
const CSS = `
.cnc-statusbar {
  box-sizing: border-box; position: relative; width: 100%; height: ${STATUS_BAR_HEIGHT}px;
  display: flex; align-items: center; justify-content: space-between;
  padding: var(--spacing-size08) var(--spacing-size16) 0;
}
.cnc-statusbar *, .cnc-statusbar *::before, .cnc-statusbar *::after { box-sizing: border-box; }
.cnc-statusbar--default       { background: transparent; color: var(--c-statusbar-timepluslevels); }
.cnc-statusbar--modal-ios     { background: var(--os--modalstack-statusbariosbackground); color: var(--os--modalstack-statusbarios); }
.cnc-statusbar--modal-android { background: var(--os--modalstack-statusbarandroidbackground); color: var(--os--modalstack-statusbarandroid); }

.cnc-statusbar__time {
  display: inline-flex; align-items: baseline; gap: var(--spacing-size08);
  font-family: var(--type-style4-family); font-size: var(--type-style4-size);
  line-height: var(--type-style4-line); font-weight: var(--type-style4-weight);
  letter-spacing: var(--type-style4-tracking); color: currentColor;
}
.cnc-statusbar__date {
  font-family: var(--type-style5-family); font-size: var(--type-style5-size);
  line-height: var(--type-style5-line); font-weight: var(--type-style5-weight);
}
.cnc-statusbar__levels { display: inline-flex; align-items: center; gap: var(--spacing-size04); }
.cnc-statusbar__pct {
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: 1; font-weight: var(--type-style7-weight); color: currentColor;
  margin-right: var(--spacing-size02, 2px);
}
.cnc-statusbar__glyph {
  display: inline-block; background-color: currentColor;
  -webkit-mask-repeat: no-repeat; mask-repeat: no-repeat;
  -webkit-mask-position: center; mask-position: center;
  -webkit-mask-size: contain; mask-size: contain;
}
.cnc-statusbar__glyph--signal  { width: 18px; height: 12px; }
.cnc-statusbar__glyph--wifi    { width: 17px; height: 12px; }
.cnc-statusbar__glyph--battery { width: 25px; height: 12px; }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function StatusBar({
  variant = "default",
  // "default" | "modal-ios" | "modal-android"
  time = "9:41",
  date = null,
  // e.g. "Mon Jun 6" — shown after the time
  batteryPercent = null,
  // e.g. "100%" — shown before the battery glyph
  basePath = ".",
  className = "",
  style,
  ...rest
}) {
  ensureStyles();
  const v = variant === "modal-ios" || variant === "modal-android" ? variant : "default";
  const cls = ["cnc-statusbar", `cnc-statusbar--${v}`];
  if (className) cls.push(className);
  const sb = `${basePath}/assets/os/statusbar`;
  const glyph = (name, w) => ({
    WebkitMaskImage: `url("${sb}/${name}.svg")`,
    maskImage: `url("${sb}/${name}.svg")`
  });
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: style,
    "data-variant": v
  }, rest), /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__time"
  }, /*#__PURE__*/React.createElement("span", null, time), date ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__date"
  }, date) : null), /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__levels"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__glyph cnc-statusbar__glyph--signal",
    style: glyph("signal"),
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__glyph cnc-statusbar__glyph--wifi",
    style: glyph("wifi"),
    "aria-hidden": "true"
  }), batteryPercent ? /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__pct"
  }, batteryPercent) : null, /*#__PURE__*/React.createElement("span", {
    className: "cnc-statusbar__glyph cnc-statusbar__glyph--battery",
    style: glyph("battery"),
    "aria-hidden": "true"
  })));
}
Object.assign(__ds_scope, { StatusBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/os/Status_Bar/StatusBar/StatusBar.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Header/CustomHeader/CustomHeader.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

// OS _Status Bar — resolved from the namespace at render (the 54px iOS status-bar reservation).

/**
 * Cancara — Custom Header
 * The app's top navigation header (Figma "Custom Header").
 *   Leading icon : True | False  (driven by `leadingIcon`)
 *
 * Anatomy (375 wide — Figma FIXED — token-driven, faithful to the extraction):
 *   Container — VERTICAL stack on --background-page-default:
 *     • _Status Bar (54px) — OS component, now built; mounted by default as the `statusBar`
 *       (opt out with statusBar={null}). See FLAGS.
 *     • H Stack — HORIZONTAL, FILL width, align CENTER. paddingRight --spacing-size04;
 *       paddingLeft --spacing-size04 (Leading=True) / --spacing-size16 (Leading=False).
 *       iOS centred Title (type Style 3, --text-generic-default) overlaid; Leading hit-target
 *       (44×44, 24px Icon, padding) at the left when Leading=True; Trailing cluster at the right
 *       (one or two 44×44 hit-targets, 24px Icons, 8px gap).
 *     • Progress indicator (4px) — hidden in Figma (visible:false). Shown only when `progress`
 *       is a number 0–1; track fill --c-temp-linearprogressindicator-track.
 *
 * ICON COLOUR: header glyphs follow the adjacent title text, so each Icon stays at its default
 * `currentColor` and the hit-target container anchors `color: var(--text-generic-default)` — the
 * glyph inherits it and maps light/dark + pressed automatically (per the icon-colour rule).
 *
 * FLAGS:
 *   • Status bar — the Figma composes the OS "_Status Bar" component (54px), now built and mounted
 *     by default. Its extraction is a shell (no clock/battery nodes), so it reserves 54px but draws
 *     no content; pass `statusBar` to override or `statusBar={null}` to hide.
 *   • Icon identities — Figma supplies the leading/trailing glyphs via INSTANCE_SWAP node IDs
 *     (29521:37157 / 22985:6751), not resolvable to icon names from the extraction. Sensible
 *     defaults chosen (Chevron Left back · Search); all are prop-driven. Inferred — flagged.
 *   • iOS centred title is absolutely centred over the bar (the iOS pattern); long titles ellipsis
 *     between the 44px edge insets. The hidden Android (left-aligned) title is omitted.
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-customheader-styles";
const CSS = `
.cnc-header { box-sizing: border-box; }
.cnc-header *, .cnc-header *::before, .cnc-header *::after { box-sizing: border-box; }

.cnc-header {
  display: flex; flex-direction: column; align-items: stretch;
  background: var(--background-page-default);
}

.cnc-header__hstack {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  min-height: 44px;
  padding-right: var(--spacing-size04);
}
.cnc-header__hstack--lead { padding-left: var(--spacing-size04); }
.cnc-header__hstack--nolead { padding-left: var(--spacing-size16); }

/* iOS centred title — overlaid, centred, ellipsised between the edge insets. */
.cnc-header__title {
  position: absolute; left: 48px; right: 48px; top: 0; bottom: 0;
  display: flex; align-items: center; justify-content: center;
  pointer-events: none;
  font-family: var(--type-style3-family);
  font-size: var(--type-style3-size);
  line-height: var(--type-style3-line);
  font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
  color: var(--text-generic-default);
}
.cnc-header__title span {
  max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
}

.cnc-header__leading { display: flex; flex-direction: row; align-items: center; }
.cnc-header__spacer { flex: 1 1 auto; }
.cnc-header__trailing {
  display: flex; flex-direction: row; align-items: center;
  gap: var(--spacing-size08);
}

/* 44×44 hit target — glyph inherits the title text colour via the color anchor. */
.cnc-header__hit {
  width: 44px; height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  background: transparent; border: 0; padding: 0; cursor: pointer;
  color: var(--text-generic-default);
}

/* Progress indicator — hidden unless a progress value is supplied. */
.cnc-header__progress { height: 4px; width: 100%; background: transparent; }
.cnc-header__progress-bar { height: 4px; background: var(--c-temp-linearprogressindicator-track); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function CustomHeader({
  title = "Title",
  leadingIcon = true,
  leadingIconName = "Arrows/Chevron Left/Chevron Left",
  onLeading,
  trailing = ["Navigation/Search/Search"],
  // array of icon names (1–2); Figma default has two hit-targets
  onTrailing,
  // (index) => void
  progress = null,
  // 0–1 to show the progress indicator; null = hidden (Figma default)
  statusBar,
  // OS _Status Bar slot: omit → default 54px bar; pass null/false → hide; pass a node → custom
  statusTime = "9:41",
  // time shown in the default _Status Bar
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const StatusBarCmp = fromNS("StatusBar", __ds_scope.StatusBar);
  // Default: mount the OS _Status Bar (now built). Opt out with statusBar={null} / {false}.
  const statusBarEl = statusBar === undefined ? StatusBarCmp ? /*#__PURE__*/React.createElement(StatusBarCmp, {
    variant: "default",
    time: statusTime,
    basePath: basePath
  }) : null : statusBar;
  const items = Array.isArray(trailing) ? trailing.filter(Boolean) : [];
  const cls = ["cnc-header"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const showProgress = typeof progress === "number";
  const pct = Math.max(0, Math.min(1, progress || 0)) * 100;
  return /*#__PURE__*/React.createElement("header", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), statusBarEl, /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__hstack " + (leadingIcon ? "cnc-header__hstack--lead" : "cnc-header__hstack--nolead")
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__title"
  }, /*#__PURE__*/React.createElement("span", null, title)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__leading"
  }, leadingIcon ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-header__hit",
    onClick: onLeading,
    "aria-label": "Back"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: leadingIconName,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null) : null), /*#__PURE__*/React.createElement("span", {
    className: "cnc-header__spacer"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__trailing"
  }, items.map((name, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    className: "cnc-header__hit",
    onClick: () => typeof onTrailing === "function" && onTrailing(i)
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: name,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null)))), showProgress ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__progress"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-header__progress-bar",
    style: {
      width: `${pct}%`
    }
  })) : null);
}
Object.assign(__ds_scope, { CustomHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Header/CustomHeader/CustomHeader.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Header/ModalStackHeader/ModalStackHeader.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Modal Stack Header
 * The header for a stacked modal sheet (Figma "Modal Stack Header").
 *   Layout       : Default | Co-Serve   (`layout`)
 *   Leading icon : True | False         (`leadingIcon`)
 *
 * Anatomy (375 wide — Figma FIXED — token-driven, faithful to the extraction):
 *   Container — VERTICAL stack on --background-panel-default, 1px inside border
 *     --border-generic-defaultalt01, paddingBottom --spacing-size16:
 *     • _Modal Stack Status Bar (modal-iOS: white time + levels on a black bar) — OS component,
 *       now built; mounted by default as the `statusBar` (opt out with statusBar={null}).
 *     • H Stack — HORIZONTAL, FILL, align CENTER, height 44, bg --background-panel-default.
 *       paddingRight 8 · paddingLeft 8 (Leading=True / Co-Serve) or 16 (Default + Leading=False).
 *         – Layout=Default  → iOS centred Title (type Style 3, --text-generic-default).
 *         – Layout=Co-Serve → centred Lloyds mark (44×44), masked to --c-header-logo-brandalt01
 *           (the heritage green #006A4D → white in dark); no title text.
 *       Leading 44×44 hit-target (24px Icon) at the left when Leading=True; Trailing 44×44
 *       hit-target with a 16×16 close Icon at the right.
 *
 * ICON COLOUR: the leading/close glyphs follow the title text — each Icon stays at default
 * `currentColor` and the hit-target anchors `color: var(--text-generic-default)`. The Lloyds mark
 * is single-colour, so it is PINNED to the dedicated logo token --c-header-logo-brandalt01 via the
 * Icon `color` prop (masked recolour, maps light/dark).
 *
 * FLAGS:
 *   • Status bar — composes the OS "_Modal Stack Status Bar" (modal-iOS variant: white time + signal/
 *     Wi-Fi/battery on a black bar), now built and mounted by default. Figma's 82px instance includes
 *     the rounded sheet-top transition (part of the panel); the glyph bar itself is the 54px black
 *     strip rendered here. Pass `statusBar` to override or `statusBar={null}` to hide.
 *   • Header left/right padding (8px) — Figma references token "Component/Header/Left & Right
 *     Padding" (value 8), which is ABSENT from the generated token set. Rendered with the
 *     same-value token --spacing-size08 (8px) and flagged (owner to confirm / supply the token).
 *   • Icon identities — leading/close glyphs come from INSTANCE_SWAP node IDs (29521:37157 /
 *     29194:30035), not resolvable to names from the extraction. Defaults chosen (Chevron Left ·
 *     Close); prop-driven. Inferred — flagged.
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-modalstackheader-styles";
const CSS = `
.cnc-msheader { box-sizing: border-box; }
.cnc-msheader *, .cnc-msheader *::before, .cnc-msheader *::after { box-sizing: border-box; }

.cnc-msheader {
  display: flex; flex-direction: column; align-items: stretch;
  background: var(--background-panel-default);
  border: var(--border-width-01, 1px) solid var(--border-generic-defaultalt01);
  padding-bottom: var(--spacing-size16);
}

.cnc-msheader__hstack {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  height: 44px;
  background: var(--background-panel-default);
  /* Header L/R padding token absent from the set → closest same-value token (8px). Flagged. */
  padding-right: var(--spacing-size08);
}
.cnc-msheader__hstack--lead { padding-left: var(--spacing-size08); }
.cnc-msheader__hstack--nolead { padding-left: var(--spacing-size16); }

/* Centred content — iOS title or the Co-Serve logo. */
.cnc-msheader__center {
  position: absolute; left: 48px; right: 48px; top: 0; bottom: 0;
  display: flex; align-items: center; justify-content: center;
  pointer-events: none;
}
.cnc-msheader__title {
  max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style3-family);
  font-size: var(--type-style3-size);
  line-height: var(--type-style3-line);
  font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
  color: var(--text-generic-default);
}

.cnc-msheader__leading { display: flex; flex-direction: row; align-items: center; }
.cnc-msheader__spacer { flex: 1 1 auto; }
.cnc-msheader__trailing { display: flex; flex-direction: row; align-items: center; }

.cnc-msheader__hit {
  width: 44px; height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  background: transparent; border: 0; padding: 0; cursor: pointer;
  color: var(--text-generic-default);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ModalStackHeader({
  layout = "default",
  // "default" | "co-serve"
  title = "Title",
  leadingIcon = true,
  leadingIconName = "Arrows/Chevron Left/Chevron Left",
  onLeading,
  trailingIconName = "Navigation/Close/Close",
  onTrailing,
  logoSrc = "components/Bank Logo/Lloyds/Lloyds.svg",
  // Co-Serve Lloyds mark (under /assets)
  statusBar,
  // OS _Modal Stack Status Bar: omit → default modal-iOS bar; null/false → hide; node → custom
  statusVariant = "modal-ios",
  // "modal-ios" | "modal-android" for the default status bar
  statusTime = "9:41",
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const StatusBarCmp = fromNS("StatusBar", __ds_scope.StatusBar);
  const statusBarEl = statusBar === undefined ? StatusBarCmp ? /*#__PURE__*/React.createElement(StatusBarCmp, {
    variant: statusVariant,
    time: statusTime,
    basePath: basePath
  }) : null : statusBar;
  const isCoServe = layout === "co-serve";
  const cls = ["cnc-msheader"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const logoUrl = encodeURI(`${basePath}/assets/${logoSrc}`);
  return /*#__PURE__*/React.createElement("header", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), statusBarEl, /*#__PURE__*/React.createElement("div", {
    className: "cnc-msheader__hstack " + (leadingIcon ? "cnc-msheader__hstack--lead" : "cnc-msheader__hstack--nolead")
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-msheader__center"
  }, isCoServe ?
  // Single-colour Lloyds mark — pinned to the dedicated logo token (masked recolour).
  IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    src: logoUrl,
    size: 44,
    color: "var(--c-header-logo-brandalt01)",
    title: "Lloyds"
  }) : null : /*#__PURE__*/React.createElement("span", {
    className: "cnc-msheader__title"
  }, title)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-msheader__leading"
  }, leadingIcon ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-msheader__hit",
    onClick: onLeading,
    "aria-label": "Back"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: leadingIconName,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null) : null), /*#__PURE__*/React.createElement("span", {
    className: "cnc-msheader__spacer"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-msheader__trailing"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-msheader__hit",
    onClick: onTrailing,
    "aria-label": "Close"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: trailingIconName,
    size: 16,
    basePath: basePath
  }) : null))));
}
Object.assign(__ds_scope, { ModalStackHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Header/ModalStackHeader/ModalStackHeader.jsx", error: String((e && e.message) || e) }); }

// src/components/navigation/Header/PresetHeader/PresetHeader.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

// OS _Status Bar — resolved from the namespace at render (the 54px iOS status-bar reservation).

/**
 * Cancara — Preset Header
 * The app's preset top-navigation header (Figma "Preset Header" → composes the
 * "Preset Header Configurations" component set, node 2694:4972).
 *   OS     : iOS | iPadOS | Android   (`os`)
 *   Preset : AOV Retail | AOV Commercial | Digital Transactions   (`preset`)
 *
 * Anatomy (375 wide — Figma FIXED — token-driven, faithful to the extraction):
 *   Container — VERTICAL stack on --background-page-default:
 *     • _Status Bar (54px) — OS component, NOT yet built → exposed as a `statusBar` slot
 *       (ReactNode, renders nothing by default). See FLAGS.
 *     • H Stack — HORIZONTAL, FILL width, align CENTER.
 *         iOS / iPadOS  : height 48, paddingLeft/Right 8 (Figma token:null → literal 8px).
 *         Android       : height 44, paddingLeft --spacing-size16, paddingRight --spacing-size04.
 *       iOS / iPadOS layout : Leading [44×44 hit-target → 24px Icon (+ Email Notification badge
 *         on AOV presets) · 8px Gap · 44px Empty-Space balancer]  |  Wrapper (FILL, centred
 *         Title, Style 3)  |  Trailing (one or two 44×44 icon hit-targets, 8px gap).
 *       Android layout      : NO leading — Wrapper (FILL, LEFT-aligned Title)  |  Trailing
 *         (icon hit-targets; the Email Notification badge rides the FIRST trailing icon on
 *         AOV presets).
 *     • Progress indicator (4px) — hidden in Figma (visible:false). Shown only when `progress`
 *       is a number 0–1; bar fill --c-temp-linearprogressindicator-track.
 *
 *   Email Notification — composes the built NotificationBadge (Type=Default, default count "12",
 *   --c-temp-notification-badge-background). Present on AOV Retail / AOV Commercial; absent on
 *   Digital Transactions (default keyed off `preset`, override with `emailNotification`).
 *
 * ICON COLOUR: header glyphs follow the adjacent title text, so each Icon stays at its default
 * `currentColor` and the hit-target container anchors `color: var(--text-generic-default)` — the
 * glyph inherits it and maps light/dark automatically (per the icon-colour rule).
 *
 * FLAGS:
 *   • Status bar — the Figma composes the OS "_Status Bar" component (54px), NOT yet built (OS
 *     category). Exposed as a `statusBar` slot; renders nothing by default. No placeholder invented.
 *   • Icon identities — Figma supplies the leading/trailing glyphs via INSTANCE_SWAP node IDs
 *     (e.g. 29521:37733 / 37157 / 36576 / 37916), not resolvable to icon names from the extraction.
 *     Sensible Navigation defaults chosen; all are prop-driven. Inferred — flagged.
 *   • Null-token spacing — the iOS H Stack padding (8) and the leading gap / empty-space / trailing
 *     gap carry token:null in Figma; written as literal px (precedent: Pagination Dots / Toggle).
 *   • Width — Figma frame is FIXED 375; kept as the `width` prop default for reuse.
 */

const STYLE_ID = "cnc-presetheader-styles";
const CSS = `
.cnc-presetheader { box-sizing: border-box; }
.cnc-presetheader *, .cnc-presetheader *::before, .cnc-presetheader *::after { box-sizing: border-box; }

.cnc-presetheader {
  display: flex; flex-direction: column; align-items: stretch;
  background: var(--background-page-default);
}

.cnc-presetheader__hstack {
  display: flex; flex-direction: row; align-items: center;
}
.cnc-presetheader__hstack--ios { min-height: 48px; padding: 0 8px; }
.cnc-presetheader__hstack--android {
  min-height: 44px;
  padding: 0 var(--spacing-size04) 0 var(--spacing-size16);
}

/* Leading cluster (iOS / iPadOS only) */
.cnc-presetheader__leading { display: flex; flex-direction: row; align-items: flex-start; }
.cnc-presetheader__gap { flex: 0 0 8px; width: 8px; align-self: stretch; }
.cnc-presetheader__empty { flex: 0 0 44px; width: 44px; }

/* Title wrapper — FILL, sits between leading and trailing */
.cnc-presetheader__wrapper {
  flex: 1 1 auto; min-width: 0;
  display: flex; flex-direction: row; align-items: center; justify-content: space-between;
  gap: 10px;
}
.cnc-presetheader__title {
  flex: 1 1 auto; min-width: 0;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  font-family: var(--type-style3-family);
  font-size: var(--type-style3-size);
  line-height: var(--type-style3-line);
  font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
  color: var(--text-generic-default);
}
.cnc-presetheader__title--center { text-align: center; }
.cnc-presetheader__title--left { text-align: left; }

/* Trailing cluster */
.cnc-presetheader__trailing { display: flex; flex-direction: row; align-items: flex-start; gap: 8px; }

/* 44×44 hit target — glyph inherits the title text colour via the color anchor. */
.cnc-presetheader__hit {
  min-width: 44px; height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  background: transparent; border: 0; padding: 0; cursor: pointer;
  color: var(--text-generic-default);
}
/* When an Email badge rides the icon, the hit target hugs the icon + badge row. */
.cnc-presetheader__iconrow { display: inline-flex; flex-direction: row; align-items: center; }

/* Progress indicator — hidden unless a progress value is supplied. */
.cnc-presetheader__progress { height: 4px; width: 100%; background: transparent; }
.cnc-presetheader__progress-bar { height: 4px; background: var(--c-temp-linearprogressindicator-track); }
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function PresetHeader({
  os = "iOS",
  // "iOS" | "iPadOS" | "Android"
  preset = "AOV Retail",
  // "AOV Retail" | "AOV Commercial" | "Digital Transactions"
  title = "Title",
  leadingIconName = "Navigation/Burger Menu/Burger Menu",
  onLeading,
  trailing,
  // array of icon names; defaults below keyed off os
  onTrailing,
  // (index) => void
  emailNotification,
  // bool; default = preset !== "Digital Transactions"
  emailCount = "12",
  progress = null,
  // 0–1 to show the progress indicator; null = hidden (Figma default)
  statusBar,
  // OS _Status Bar slot: omit → default 54px bar; null/false → hide; node → custom
  basePath = ".",
  width = 375,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const BadgeCmp = fromNS("NotificationBadge", __ds_scope.NotificationBadge);
  const StatusBarCmp = fromNS("StatusBar", __ds_scope.StatusBar);
  const statusBarEl = statusBar === undefined ? StatusBarCmp ? /*#__PURE__*/React.createElement(StatusBarCmp, {
    variant: "default",
    basePath: basePath
  }) : null : statusBar;
  const isAndroid = os === "Android";
  const showEmail = typeof emailNotification === "boolean" ? emailNotification : preset !== "Digital Transactions";
  const defaultTrailing = isAndroid ? ["Navigation/Search/Search", "Navigation/Notifications/Notifications", "Navigation/Kebab Menu/Kebab Menu"] : ["Navigation/Search/Search", "Navigation/Notifications/Notifications"];
  const items = Array.isArray(trailing) ? trailing.filter(Boolean) : defaultTrailing;
  const cls = ["cnc-presetheader"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width
  };
  const showProgress = typeof progress === "number";
  const pct = Math.max(0, Math.min(1, progress || 0)) * 100;
  const renderIcon = name => IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: name,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null;
  const emailBadge = showEmail && BadgeCmp ? /*#__PURE__*/React.createElement(BadgeCmp, {
    type: "default"
  }, emailCount) : null;
  return /*#__PURE__*/React.createElement("header", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), statusBarEl, /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__hstack " + (isAndroid ? "cnc-presetheader__hstack--android" : "cnc-presetheader__hstack--ios")
  }, !isAndroid ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__leading"
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-presetheader__hit",
    onClick: onLeading,
    "aria-label": "Menu"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-presetheader__iconrow"
  }, renderIcon(leadingIconName), emailBadge)), /*#__PURE__*/React.createElement("span", {
    className: "cnc-presetheader__gap"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cnc-presetheader__empty"
  })) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__wrapper"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-presetheader__title " + (isAndroid ? "cnc-presetheader__title--left" : "cnc-presetheader__title--center")
  }, title)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__trailing"
  }, items.map((name, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    className: "cnc-presetheader__hit",
    onClick: () => typeof onTrailing === "function" && onTrailing(i)
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-presetheader__iconrow"
  }, renderIcon(name), isAndroid && i === 0 ? emailBadge : null))))), showProgress ? /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__progress"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-presetheader__progress-bar",
    style: {
      width: `${pct}%`
    }
  })) : null);
}
Object.assign(__ds_scope, { PresetHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/navigation/Header/PresetHeader/PresetHeader.jsx", error: String((e && e.message) || e) }); }

// src/components/templates/ActionMenuTemplate/ActionMenuTemplate.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Action Menu Template
 * A bottom action-menu sheet over a scrimmed page (Figma "Action Menu Template", 375×812).
 * Layers, bottom of the stack to top:
 *   • Page content (`pageContent`) — the underlying screen (Figma "_Swap me out", a placeholder
 *     NOT built; left as an exposed swap-in slot). Absolute fill.
 *   • Overlay — --background-overlay-default scrim. Absolute fill.
 *   • Sheet — anchored to the bottom, leaving an 82px transparent scrim gap at the top
 *     (Figma "Apple Transparent Gap"):
 *       – Header — --background-section-brand, padding --spacing-size16, centred title
 *         (type Style 2, --text-generic-brand). (Android left-aligned title hidden in Figma — omitted.)
 *       – Container (body) — --background-section-defaultalt01, paddingTop/gap --spacing-size16; the
 *         menu's content region → exposed as `children` (a plain Figma layout frame).
 *       – Container (action) — --background-panel-default, paddingTop/gap --spacing-size16:
 *         a full-width primary Action Button + the OS Home Indicator.
 * Everything token-driven, light/dark automatic. Composes Action Button + Home Indicator.
 *
 * FLAGS:
 *   • `_Swap me out` (page content + menu body) is a Figma placeholder, NOT built — exposed as the
 *     `pageContent` / `children` swap-in slots (dashed placeholder by default), per the owner brief.
 *   • The 82px scrim gap above the sheet reproduces the transparent "Apple Transparent Gap" frame.
 *   • Header is the iOS (centred) title; the hidden Android (left) title is omitted (faithful).
 *   • Width/height — Figma FIXED 375×812; kept as `width`/`height` defaults.
 */

const SCRIM_GAP = 82; // Figma "Apple Transparent Gap" — token:null, documented literal.

const STYLE_ID = "cnc-actionmenu-styles";
const CSS = `
.cnc-tpl-menu { box-sizing: border-box; position: relative; overflow: hidden; background: var(--background-page-default); }
.cnc-tpl-menu *, .cnc-tpl-menu *::before, .cnc-tpl-menu *::after { box-sizing: border-box; }

@keyframes cnc-tpl-menu-fade { from { opacity: 0; } to { opacity: 1; } }
@keyframes cnc-tpl-menu-rise { from { transform: translateY(100%); } to { transform: translateY(0); } }

.cnc-tpl-menu__page { position: absolute; inset: 0; display: flex; flex-direction: column; }
.cnc-tpl-menu__scrim { position: absolute; inset: 0; background: var(--background-overlay-default); }

.cnc-tpl-menu__sheet {
  position: absolute; left: 0; right: 0; bottom: 0; top: ${SCRIM_GAP}px;
  display: flex; flex-direction: column;
}

/* Entrance: scrim fades in, sheet slides up. Honoured only when motion is allowed. */
@media (prefers-reduced-motion: no-preference) {
  .cnc-tpl-menu[data-animate="true"] .cnc-tpl-menu__scrim { animation: cnc-tpl-menu-fade 240ms ease; }
  .cnc-tpl-menu[data-animate="true"] .cnc-tpl-menu__sheet { animation: cnc-tpl-menu-rise 320ms cubic-bezier(0.22, 1, 0.36, 1); }
}

.cnc-tpl-menu__header {
  background: var(--background-section-brand);
  padding: var(--spacing-size16);
  text-align: center;
}
.cnc-tpl-menu__header-title {
  margin: 0;
  font-family: var(--type-style2-family); font-size: var(--type-style2-size);
  line-height: var(--type-style2-line); font-weight: var(--type-style2-weight);
  letter-spacing: var(--type-style2-tracking);
  color: var(--text-generic-brand);
}

.cnc-tpl-menu__body {
  flex: 1 1 auto; min-height: 0; overflow: auto;
  background: var(--background-section-defaultalt01);
  display: flex; flex-direction: column;
  padding-top: var(--spacing-size16);
  gap: var(--spacing-size16);
}

.cnc-tpl-menu__action {
  background: var(--background-panel-default);
  display: flex; flex-direction: column;
  padding-top: var(--spacing-size16);
  gap: var(--spacing-size16);
}
.cnc-tpl-menu__action-btn { padding: 0 var(--spacing-size16); }

.cnc-tpl-menu__placeholder {
  flex: 1 1 auto; margin: 0 var(--spacing-size16) var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
.cnc-tpl-menu__page-ph {
  flex: 1 1 auto; margin: var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ActionMenuTemplate({
  title = "Title",
  buttonLabel = "Button",
  buttonIcon = "Arrows/Arrow Right/Arrow Right",
  onButton,
  pageContent = null,
  children,
  homeIndicator,
  animateIn = false,
  basePath = ".",
  width = 375,
  height = 812,
  className = "",
  ...rest
}) {
  ensureStyles();
  const Btn = fromNS("ActionButton", __ds_scope.ActionButton);
  const Home = fromNS("HomeIndicator", __ds_scope.HomeIndicator);
  const cls = ["cnc-tpl-menu"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width,
    height: typeof height === "number" ? `${height}px` : height
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle,
    "data-animate": animateIn ? "true" : "false"
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__page"
  }, pageContent != null ? pageContent : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__page-ph"
  }, "Page content")), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__scrim"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__sheet"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__header"
  }, /*#__PURE__*/React.createElement("h2", {
    className: "cnc-tpl-menu__header-title"
  }, title)), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__body"
  }, children != null ? children : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__placeholder"
  }, "Menu content")), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__action"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-menu__action-btn"
  }, Btn ? /*#__PURE__*/React.createElement(Btn, {
    variant: "primary",
    fullWidth: true,
    icon: buttonIcon,
    onClick: onButton,
    basePath: basePath
  }, buttonLabel) : null), homeIndicator != null ? homeIndicator : Home ? /*#__PURE__*/React.createElement(Home, null) : null)));
}
Object.assign(__ds_scope, { ActionMenuTemplate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/templates/ActionMenuTemplate/ActionMenuTemplate.jsx", error: String((e && e.message) || e) }); }

// src/components/templates/BaseTemplate/BaseTemplate.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested refs — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Base Template
 * The standard app page scaffold (Figma "Base Template", 375×812). A VERTICAL stack:
 *   • Custom Header  — Leading icon True · two trailing icons · progress hidden (Figma instance
 *     "Leading icon=True", Trailing icon=True, 2nd Trailing icon=True, Progress indicator=False).
 *   • Container      — a plain Figma layout frame (NOT a component): VERTICAL, paddingTop
 *     --spacing-size16, itemSpacing --spacing-size16, transparent fill. This is the page's
 *     content region → exposed as `children` (empty placeholder by default).
 *   • Bottom Nav     — Active=None (Figma instance).
 * Page background --background-page-default; everything token-driven, light/dark automatic.
 *
 * Composes Custom Header + Bottom Nav from the namespace; the Container is a layout frame only.
 *
 * FLAGS:
 *   • The Container is a plain Figma box (layout frame), per the owner brief — no tokens beyond its
 *     padding/gap, no variants. It is the swap-in slot for real page content (`children`).
 *   • Width/height — Figma frame is FIXED 375×812; kept as `width`/`height` prop defaults.
 *   • OS status bar — Custom Header mounts the OS _Status Bar (54px) by default; Base Template only
 *     forwards `statusBar` when the caller overrides it (pass `statusBar={null}` to hide).
 */

const STYLE_ID = "cnc-basetemplate-styles";
const CSS = `
.cnc-tpl-base { box-sizing: border-box; position: relative; overflow: hidden; display: flex; flex-direction: column; background: var(--background-page-default); }
.cnc-tpl-base *, .cnc-tpl-base *::before, .cnc-tpl-base *::after { box-sizing: border-box; }

/* Container — plain Figma layout frame: the page content region. */
.cnc-tpl-base__container {
  flex: 1 1 auto; min-height: 0; overflow: auto;
  display: flex; flex-direction: column;
  padding-top: var(--spacing-size16);
  gap: var(--spacing-size16);
}

/* Empty-slot affordance (only when no children are supplied). */
.cnc-tpl-base__placeholder {
  flex: 1 1 auto; margin: 0 var(--spacing-size16) var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function BaseTemplate({
  title = "Title",
  leadingIcon = true,
  leadingIconName = "Arrows/Chevron Left/Chevron Left",
  onLeading,
  trailing = ["Navigation/Search/Search", "Navigation/Notifications/Notifications"],
  onTrailing,
  statusBar,
  os = "iOS",
  tabs,
  active = null,
  onTabChange,
  homeIndicator = null,
  children,
  basePath = ".",
  width = 375,
  height = 812,
  className = "",
  ...rest
}) {
  ensureStyles();
  const Header = fromNS("CustomHeader", __ds_scope.CustomHeader);
  const Nav = fromNS("BottomNav", __ds_scope.BottomNav);
  const cls = ["cnc-tpl-base"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width,
    height: typeof height === "number" ? `${height}px` : height
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), Header ? /*#__PURE__*/React.createElement(Header, _extends({
    title: title,
    leadingIcon: leadingIcon,
    leadingIconName: leadingIconName,
    onLeading: onLeading,
    trailing: trailing,
    onTrailing: onTrailing
  }, statusBar !== undefined ? {
    statusBar
  } : {}, {
    os: os,
    basePath: basePath,
    width: width
  })) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-base__container"
  }, children != null ? children : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-base__placeholder"
  }, "Content")), Nav ? /*#__PURE__*/React.createElement(Nav, {
    tabs: tabs,
    active: active,
    onChange: onTabChange,
    homeIndicator: homeIndicator,
    basePath: basePath,
    width: width
  }) : null);
}
Object.assign(__ds_scope, { BaseTemplate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/templates/BaseTemplate/BaseTemplate.jsx", error: String((e && e.message) || e) }); }

// src/components/templates/ModalStackTemplate/ModalStackTemplate.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Modal Stack Template
 * A stacked-modal scaffold (Figma "Modal Stack Template", 375×840). A VERTICAL stack:
 *   • Modal Stack Header (back) — Layout=Default, Leading icon=True. Represents the modal beneath.
 *   • Drop Shadow frame — visible:false in Figma → omitted (flagged).
 *   • Modal Stack Header (front) — Layout=Default, Leading icon=True. The active modal's header.
 *   • Container — --background-panel-default, paddingTop/gap --spacing-size16; the active modal's
 *     content region → exposed as `children` (a plain Figma layout frame).
 * Page background --background-page-default; everything token-driven, light/dark automatic.
 * Composes Modal Stack Header (×2) from the namespace.
 *
 * FLAGS:
 *   • The "Drop Shadow" frame between the two headers is visible:false in the extraction → omitted
 *     (no shadow rendered), faithful. Both headers carry their own 1px --border-generic-defaultalt01.
 *   • Container is a plain Figma layout frame (the swap-in content slot, `children`).
 *   • Width/height — Figma FIXED 375×840; kept as `width`/`height` defaults.
 */

const STYLE_ID = "cnc-modalstack-styles";
const CSS = `
.cnc-tpl-modal { box-sizing: border-box; position: relative; overflow: hidden; display: flex; flex-direction: column; background: var(--background-page-default); }
.cnc-tpl-modal *, .cnc-tpl-modal *::before, .cnc-tpl-modal *::after { box-sizing: border-box; }

.cnc-tpl-modal__container {
  flex: 1 1 auto; min-height: 0; overflow: auto;
  background: var(--background-panel-default);
  display: flex; flex-direction: column;
  padding-top: var(--spacing-size16);
  gap: var(--spacing-size16);
}
.cnc-tpl-modal__placeholder {
  flex: 1 1 auto; margin: 0 var(--spacing-size16) var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function ModalStackTemplate({
  title = "Title",
  backTitle = "Title",
  leadingIcon = true,
  onLeading,
  onBackLeading,
  onClose,
  onBackClose,
  children,
  basePath = ".",
  width = 375,
  height = 840,
  className = "",
  ...rest
}) {
  ensureStyles();
  const Header = fromNS("ModalStackHeader", __ds_scope.ModalStackHeader);
  const cls = ["cnc-tpl-modal"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width,
    height: typeof height === "number" ? `${height}px` : height
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle
  }, rest), Header ? /*#__PURE__*/React.createElement(Header, {
    layout: "default",
    title: backTitle,
    leadingIcon: leadingIcon,
    onLeading: onBackLeading,
    onTrailing: onBackClose,
    basePath: basePath,
    width: width
  }) : null, Header ? /*#__PURE__*/React.createElement(Header, {
    layout: "default",
    title: title,
    leadingIcon: leadingIcon,
    onLeading: onLeading,
    onTrailing: onClose,
    basePath: basePath,
    width: width
  }) : null, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-modal__container"
  }, children != null ? children : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-modal__placeholder"
  }, "Content")));
}
Object.assign(__ds_scope, { ModalStackTemplate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/templates/ModalStackTemplate/ModalStackTemplate.jsx", error: String((e && e.message) || e) }); }

// src/components/templates/TrayTemplate/TrayTemplate.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Nested ref — resolved from the DS namespace at render (cross-directory safe).

/**
 * Cancara — Tray Template
 * A bottom-sheet tray over a scrimmed page (Figma "Tray Template", 375×840). Layers, bottom to top:
 *   • Page content (`pageContent`) — the underlying screen (Figma "_Swap me out", a placeholder NOT
 *     built; left as an exposed swap-in slot). Absolute fill.
 *   • Overlay — --background-overlay-default scrim. Absolute fill.
 *   • Tray — anchored to the bottom, --background-panel-default, top corners --radius-medium. Its
 *     grabber / header / content sit inside Figma's "Content Goes Inside Here" wrapper at itemSpacing 0
 *     (no gaps between them):
 *       – Handle bar — a centred 36×5 grabber (radius 2.5, --c-temp-tray-handle).
 *       – Header — HORIZONTAL, padding L/R --spacing-size08: a 44×44 leading hit-target (24px Icon),
 *         an iOS-centred title (type Style 3, --text-generic-default), a 44×44 trailing hit-target
 *         (24px Icon). (Android left-aligned title hidden in Figma — omitted.)
 *       – Content (`children`) — the tray's content region (Figma "_Swap me out", swap-in slot).
 *         A --spacing-size16 top padding gives the content breathing room below the header
 *         (owner-requested; the grabber→header gap stays 0 per Figma).
 * Everything token-driven, light/dark automatic. Composes Icon from the namespace.
 *
 * MOTION: opt-in entrance (default on) — scrim fades, tray slides up; reduced-motion shows the
 * resting state. No motion exists in the Figma extraction, so it is additive and prop-gated.
 *
 * ICON COLOUR: the header glyphs follow the title text, so each Icon stays at its default
 * `currentColor` and the 44×44 hit-target anchors `color: var(--text-generic-default)`.
 *
 * FLAGS:
 *   • `_Swap me out` (page content + tray content) is a Figma placeholder, NOT built — exposed as the
 *     `pageContent` / `children` swap-in slots (dashed placeholder by default), per the owner brief.
 *   • Header L/R padding — Figma token "Component/Header/Left & Right Padding" (8px) has no dedicated
 *     CSS var → bound to --spacing-size08 (identical 8px). Flagged.
 *   • Grabber 36×5 / radius 2.5 are token:null Figma sizing → documented literals; fill is the
 *     --c-temp-tray-handle token.
 *   • Header is the iOS (centred) title; the hidden Android (left) title is omitted (faithful).
 *   • Icon identities (leading/trailing) are INSTANCE_SWAP node IDs in Figma → sensible defaults
 *     (Chevron Left back · Close); both prop-driven.
 *   • Width/height — Figma FIXED 375×840; kept as `width`/`height` defaults.
 *   • Content top padding (--spacing-size16) and the entrance animation are additive owner requests,
 *     not in the Figma extraction — both flagged; animation is reduced-motion-safe and prop-gated.
 */

const STYLE_ID = "cnc-traytemplate-styles";
const CSS = `
.cnc-tpl-tray { box-sizing: border-box; position: relative; overflow: hidden; background: var(--background-page-default); }
.cnc-tpl-tray *, .cnc-tpl-tray *::before, .cnc-tpl-tray *::after { box-sizing: border-box; }

@keyframes cnc-tpl-tray-fade { from { opacity: 0; } to { opacity: 1; } }
@keyframes cnc-tpl-tray-rise { from { transform: translateY(100%); } to { transform: translateY(0); } }

.cnc-tpl-tray__page { position: absolute; inset: 0; display: flex; flex-direction: column; }
.cnc-tpl-tray__scrim { position: absolute; inset: 0; background: var(--background-overlay-default); }

.cnc-tpl-tray__tray {
  position: absolute; left: 0; right: 0; bottom: 0;
  display: flex; flex-direction: column;
  background: var(--background-panel-default);
  border-top-left-radius: var(--radius-medium);
  border-top-right-radius: var(--radius-medium);
}

/* Entrance: scrim fades in, tray slides up. Honoured only when motion is allowed. */
@media (prefers-reduced-motion: no-preference) {
  .cnc-tpl-tray[data-animate="true"] .cnc-tpl-tray__scrim { animation: cnc-tpl-tray-fade 240ms ease; }
  .cnc-tpl-tray[data-animate="true"] .cnc-tpl-tray__tray { animation: cnc-tpl-tray-rise 320ms cubic-bezier(0.22, 1, 0.36, 1); }
}

.cnc-tpl-tray__handlebar { height: 14px; display: flex; align-items: center; justify-content: center; }
.cnc-tpl-tray__grabber { width: 36px; height: 5px; border-radius: 2.5px; background: var(--c-temp-tray-handle); }

.cnc-tpl-tray__header {
  position: relative;
  display: flex; flex-direction: row; align-items: center;
  min-height: 44px;
  padding-left: var(--spacing-size08); padding-right: var(--spacing-size08);
}
.cnc-tpl-tray__title {
  position: absolute; left: 52px; right: 52px; top: 0; bottom: 0;
  display: flex; align-items: center; justify-content: center;
  pointer-events: none;
  font-family: var(--type-style3-family); font-size: var(--type-style3-size);
  line-height: var(--type-style3-line); font-weight: var(--type-style3-weight);
  letter-spacing: var(--type-style3-tracking);
  color: var(--text-generic-default);
}
.cnc-tpl-tray__title span { max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

.cnc-tpl-tray__spacer { flex: 1 1 auto; }
.cnc-tpl-tray__hit {
  width: 44px; height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  background: transparent; border: 0; padding: 0; cursor: pointer;
  color: var(--text-generic-default);
}

.cnc-tpl-tray__content { flex: 1 1 auto; min-height: 0; overflow: auto; display: flex; flex-direction: column; padding-top: var(--spacing-size16); }
.cnc-tpl-tray__placeholder {
  flex: 1 1 auto; margin: 0 var(--spacing-size16) var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
.cnc-tpl-tray__page-ph {
  flex: 1 1 auto; margin: var(--spacing-size16);
  display: flex; align-items: center; justify-content: center;
  border: var(--border-width-01) dashed var(--border-generic-defaultalt01);
  color: var(--text-generic-subdued);
  font-family: var(--type-style7-family); font-size: var(--type-style7-size);
  line-height: var(--type-style7-line); font-weight: var(--type-style7-weight);
}
`;
function ensureStyles() {
  if (typeof document === "undefined") return;
  if (document.getElementById(STYLE_ID)) return;
  const el = document.createElement("style");
  el.id = STYLE_ID;
  el.textContent = CSS;
  document.head.appendChild(el);
}
function fromNS(name, fallback) {
  const ns = typeof window !== "undefined" ? window.CancaraDesignSystem_4b0109 : null;
  if (ns && ns[name]) return ns[name];
  return typeof fallback !== "undefined" ? fallback : null;
}
function TrayTemplate({
  title = "Title",
  leadingIcon = true,
  leadingIconName = "Arrows/Chevron Left/Chevron Left",
  onLeading,
  trailingIcon = true,
  trailingIconName = "Navigation/Close/Close",
  onTrailing,
  pageContent = null,
  children,
  animateIn = false,
  basePath = ".",
  width = 375,
  height = 840,
  className = "",
  ...rest
}) {
  ensureStyles();
  const IconCmp = fromNS("Icon", __ds_scope.Icon);
  const cls = ["cnc-tpl-tray"];
  if (className) cls.push(className);
  const rootStyle = {
    width: typeof width === "number" ? `${width}px` : width,
    height: typeof height === "number" ? `${height}px` : height
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    className: cls.join(" "),
    style: rootStyle,
    "data-animate": animateIn ? "true" : "false"
  }, rest), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__page"
  }, pageContent != null ? pageContent : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__page-ph"
  }, "Page content")), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__scrim"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__tray"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__handlebar"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cnc-tpl-tray__grabber"
  })), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__header"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__title"
  }, /*#__PURE__*/React.createElement("span", null, title)), leadingIcon ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-tpl-tray__hit",
    onClick: onLeading,
    "aria-label": "Back"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: leadingIconName,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null) : /*#__PURE__*/React.createElement("span", {
    className: "cnc-tpl-tray__hit",
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cnc-tpl-tray__spacer"
  }), trailingIcon ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "cnc-tpl-tray__hit",
    onClick: onTrailing,
    "aria-label": "Close"
  }, IconCmp ? /*#__PURE__*/React.createElement(IconCmp, {
    name: trailingIconName,
    size: 24,
    padding: true,
    basePath: basePath
  }) : null) : /*#__PURE__*/React.createElement("span", {
    className: "cnc-tpl-tray__hit",
    "aria-hidden": "true"
  })), /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__content"
  }, children != null ? children : /*#__PURE__*/React.createElement("div", {
    className: "cnc-tpl-tray__placeholder"
  }, "Tray content"))));
}
Object.assign(__ds_scope, { TrayTemplate });
})(); } catch (e) { __ds_ns.__errors.push({ path: "src/components/templates/TrayTemplate/TrayTemplate.jsx", error: String((e && e.message) || e) }); }

__ds_ns.ActionButton = __ds_scope.ActionButton;

__ds_ns.CompactButton = __ds_scope.CompactButton;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.LinkButton = __ds_scope.LinkButton;

__ds_ns.ActionButtonStack = __ds_scope.ActionButtonStack;

__ds_ns.ChatButtonStack = __ds_scope.ChatButtonStack;

__ds_ns.LinkButtonStack = __ds_scope.LinkButtonStack;

__ds_ns.GhostPanel = __ds_scope.GhostPanel;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.QuickActionButton = __ds_scope.QuickActionButton;

__ds_ns.Tile = __ds_scope.Tile;

__ds_ns.TileGrid = __ds_scope.TileGrid;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.SelectCategoryGrid = __ds_scope.SelectCategoryGrid;

__ds_ns.SelectCategoryItem = __ds_scope.SelectCategoryItem;

__ds_ns.Divider = __ds_scope.Divider;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.Pictogram = __ds_scope.Pictogram;

__ds_ns.SpotIllustration = __ds_scope.SpotIllustration;

__ds_ns.ListActionGroup = __ds_scope.ListActionGroup;

__ds_ns.ListDataPlaybackGroup = __ds_scope.ListDataPlaybackGroup;

__ds_ns.ListItemAction = __ds_scope.ListItemAction;

__ds_ns.ListItemDataPlayback = __ds_scope.ListItemDataPlayback;

__ds_ns.Calendar = __ds_scope.Calendar;

__ds_ns.CalendarDate = __ds_scope.CalendarDate;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.CheckboxGroup = __ds_scope.CheckboxGroup;

__ds_ns.ConfirmationBox = __ds_scope.ConfirmationBox;

__ds_ns.RadioButton = __ds_scope.RadioButton;

__ds_ns.RadioButtonGroup = __ds_scope.RadioButtonGroup;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.SwitchConfiguration = __ds_scope.SwitchConfiguration;

__ds_ns.CalendarPicker = __ds_scope.CalendarPicker;

__ds_ns.CountryPicker = __ds_scope.CountryPicker;

__ds_ns.DropdownPicker = __ds_scope.DropdownPicker;

__ds_ns.GlobalSearch = __ds_scope.GlobalSearch;

__ds_ns.MIField = __ds_scope.MIField;

__ds_ns.Password = __ds_scope.Password;

__ds_ns.PhoneNumber = __ds_scope.PhoneNumber;

__ds_ns.PhoneNumberExtension = __ds_scope.PhoneNumberExtension;

__ds_ns.PhoneNumberField = __ds_scope.PhoneNumberField;

__ds_ns.SortCode = __ds_scope.SortCode;

__ds_ns.TextField = __ds_scope.TextField;

__ds_ns.BottomNav = __ds_scope.BottomNav;

__ds_ns.CustomHeader = __ds_scope.CustomHeader;

__ds_ns.ModalStackHeader = __ds_scope.ModalStackHeader;

__ds_ns.PresetHeader = __ds_scope.PresetHeader;

__ds_ns.Pagination = __ds_scope.Pagination;

__ds_ns.PaginationDots = __ds_scope.PaginationDots;

__ds_ns.TabCollectionScroll = __ds_scope.TabCollectionScroll;

__ds_ns.ThreeTabsFixed = __ds_scope.ThreeTabsFixed;

__ds_ns.TwoTabsFixed = __ds_scope.TwoTabsFixed;

__ds_ns.Toggle = __ds_scope.Toggle;

__ds_ns.Coachmark = __ds_scope.Coachmark;

__ds_ns.CoachmarkPopup = __ds_scope.CoachmarkPopup;

__ds_ns.ErrorBanner = __ds_scope.ErrorBanner;

__ds_ns.NotificationBadge = __ds_scope.NotificationBadge;

__ds_ns.NotificationDot = __ds_scope.NotificationDot;

__ds_ns.NotificationHint = __ds_scope.NotificationHint;

__ds_ns.NotificationPanel = __ds_scope.NotificationPanel;

__ds_ns.NotificationTag = __ds_scope.NotificationTag;

__ds_ns.CustomPopupModal = __ds_scope.CustomPopupModal;

__ds_ns.PopupModalPresets = __ds_scope.PopupModalPresets;

__ds_ns.Handle = __ds_scope.Handle;

__ds_ns.HomeIndicator = __ds_scope.HomeIndicator;

__ds_ns.NativeSpinnerConfiguration = __ds_scope.NativeSpinnerConfiguration;

__ds_ns.Spinner = __ds_scope.Spinner;

__ds_ns.SpinnerSet = __ds_scope.SpinnerSet;

__ds_ns.StatusBar = __ds_scope.StatusBar;

__ds_ns.ActionMenuTemplate = __ds_scope.ActionMenuTemplate;

__ds_ns.BaseTemplate = __ds_scope.BaseTemplate;

__ds_ns.ModalStackTemplate = __ds_scope.ModalStackTemplate;

__ds_ns.TrayTemplate = __ds_scope.TrayTemplate;

})();
